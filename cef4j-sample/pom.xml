<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>net.kurobako.cef4j</groupId>
        <artifactId>cef4j-parent</artifactId>
        <version>146.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>cef4j-sample</artifactId>
    <packaging>jar</packaging>
    <name>cef4j-sample</name>
    <description>Sample Swing and JavaFX browsers using cef4j</description>

    <properties>
        <javafx.version>19.0.2.1</javafx.version>
        <exec.skip>false</exec.skip>
        <exec.mainClass>net.kurobako.cef4j.sample.SwingBrowserApp</exec.mainClass>
        <!-- macOS Swing/AWT requires the JVM to run on Thread 0 for AppKit compatibility.
             Set via -Dexec.extraJvmArg=-XstartOnFirstThread or use the macos profile below. -->
        <exec.extraJvmArg>-Dignore=true</exec.extraJvmArg>
    </properties>

    <dependencies>
        <dependency>
            <groupId>net.kurobako.cef4j</groupId>
            <artifactId>cef4j-api</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>net.kurobako.cef4j</groupId>
            <artifactId>cef4j-osr-swing</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>net.kurobako.cef4j</groupId>
            <artifactId>cef4j-osr-jfx</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>net.kurobako.cef4j</groupId>
            <artifactId>cef4j-platform</artifactId>
            <version>${project.version}</version>
            <classifier>${cef.platform}</classifier>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-base</artifactId>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-graphics</artifactId>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-controls</artifactId>
        </dependency>
        <dependency>
            <groupId>com.formdev</groupId>
            <artifactId>flatlaf</artifactId>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-simple</artifactId>
            <scope>runtime</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <release>11</release>
                </configuration>
            </plugin>
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <configuration>
                    <!-- exec:exec launches a separate JVM process. This is required on macOS
                         because exec:java runs main() on a worker thread, not Thread 0 (the
                         AppKit main thread), which deadlocks both JavaFX and CEF. -->
                    <executable>java</executable>
                    <arguments>
                        <argument>${exec.extraJvmArg}</argument>
                        <argument>--enable-native-access=ALL-UNNAMED</argument>
                        <argument>-classpath</argument>
                        <classpath/>
                        <argument>${exec.mainClass}</argument>
                    </arguments>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>macos</id>
            <activation>
                <os><family>mac</family></os>
            </activation>
            <properties>
                <exec.extraJvmArg>-XstartOnFirstThread</exec.extraJvmArg>
            </properties>
        </profile>
    </profiles>
</project>
