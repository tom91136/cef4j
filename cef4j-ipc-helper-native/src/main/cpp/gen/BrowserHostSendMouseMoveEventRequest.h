// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
#ifndef NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_BROWSER_HOST_SEND_MOUSE_MOVE_EVENT_REQUEST_H_
#define NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_BROWSER_HOST_SEND_MOUSE_MOVE_EVENT_REQUEST_H_

#include <cstdint>
#include <cstring>
#include <string>
#include <vector>
#include "MouseEvent.h"

namespace net_kurobako_cef4j_ipc_protocol_gen {

struct BrowserHostSendMouseMoveEventRequest {
    static constexpr int32_t kMessageId = 747572728;

    std::int32_t self = 0;
    MouseEvent event;
    std::int32_t mouseLeave = 0;

    /** Number of payload bytes that {@link encodeInto} writes for this instance. */
    std::size_t encodedSize() const noexcept {
        return 8 + event.encodedSize();
    }

    /** Writes exactly {@link encodedSize} bytes starting at {@code dst}. */
    void encodeInto(std::uint8_t* dst) const noexcept {
        std::size_t pos = 0;
        {
            std::uint32_t v = static_cast<std::uint32_t>(self);
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
        event.encodeInto(dst + pos);
        pos += event.encodedSize();
        {
            std::uint32_t v = static_cast<std::uint32_t>(mouseLeave);
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
    }

    /** Reads a BrowserHostSendMouseMoveEventRequest from a payload starting at {@code src}; {@code len} bounds the buffer. */
    static BrowserHostSendMouseMoveEventRequest decode(const std::uint8_t* src, std::size_t len) noexcept {
        BrowserHostSendMouseMoveEventRequest out;
        std::size_t pos = 0;
        (void)len;
        out.self = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
        pos += 4;
        out.event = MouseEvent::decode(src + pos, len - pos);
        pos += out.event.encodedSize();
        out.mouseLeave = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
        pos += 4;
        return out;
    }
};

} // namespace net_kurobako_cef4j_ipc_protocol_gen

#endif // NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_BROWSER_HOST_SEND_MOUSE_MOVE_EVENT_REQUEST_H_
