package net.kurobako.cef4j.ipc.frame;

/** Pixel format produced by the runtime server's OSR paint pipeline. */
public enum PixelFormat {
    /** 32-bit packed BGRA (stride = width * 4), CEF's default OSR format. */
    BGRA
}
