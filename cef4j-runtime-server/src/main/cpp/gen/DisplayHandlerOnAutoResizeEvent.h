// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
#ifndef NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_DISPLAY_HANDLER_ON_AUTO_RESIZE_EVENT_H_
#define NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_DISPLAY_HANDLER_ON_AUTO_RESIZE_EVENT_H_

#include <cstdint>
#include <cstring>
#include <stdexcept>
#include <string>
#include <vector>
#include "Size.h"

namespace net_kurobako_cef4j_ipc_protocol_gen {

struct DisplayHandlerOnAutoResizeEvent {
    static constexpr int32_t kMessageId = 1745044920;
    static constexpr std::size_t kMaxFieldBytes = 64U * 1024U * 1024U;
    static constexpr std::size_t kMaxCollectionItems = 1000000U;

    std::int32_t browser = 0;
    Size newSize;

    /** Number of payload bytes that {@link encodeInto} writes for this instance. */
    std::size_t encodedSize() const noexcept {
        return 4 + newSize.encodedSize();
    }

    /** Writes exactly {@link encodedSize} bytes starting at {@code dst}. */
    void encodeInto(std::uint8_t* dst) const noexcept {
        std::size_t pos = 0;
        {
            std::uint32_t v = static_cast<std::uint32_t>(browser);
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
        newSize.encodeInto(dst + pos);
        pos += newSize.encodedSize();
    }

    /** Reads a DisplayHandlerOnAutoResizeEvent from a payload starting at {@code src}; {@code len} bounds the buffer. */
    static DisplayHandlerOnAutoResizeEvent decode(const std::uint8_t* src, std::size_t len) {
        DisplayHandlerOnAutoResizeEvent out;
        std::size_t pos = 0;
        [[maybe_unused]] auto requireAvailable = [&](std::size_t count) {
            if (pos > len || count > len - pos)
                throw std::invalid_argument("truncated DisplayHandlerOnAutoResizeEvent payload");
        };
        requireAvailable(4);
        out.browser = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
        pos += 4;
        out.newSize = Size::decode(src + pos, len - pos);
        requireAvailable(out.newSize.encodedSize());
        pos += out.newSize.encodedSize();
        if (pos != len)
            throw std::invalid_argument("trailing DisplayHandlerOnAutoResizeEvent payload");
        return out;
    }
};

} // namespace net_kurobako_cef4j_ipc_protocol_gen

#endif // NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_DISPLAY_HANDLER_ON_AUTO_RESIZE_EVENT_H_
