// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
#ifndef NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_CONTEXT_MENU_HANDLER_RUN_QUICK_MENU_EVENT_H_
#define NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_CONTEXT_MENU_HANDLER_RUN_QUICK_MENU_EVENT_H_

#include <cstdint>
#include <cstring>
#include <stdexcept>
#include <string>
#include <vector>
#include "Point.h"
#include "Size.h"

namespace net_kurobako_cef4j_ipc_protocol_gen {

struct ContextMenuHandlerRunQuickMenuEvent {
    static constexpr int32_t kMessageId = 2062326684;
    static constexpr std::size_t kMaxFieldBytes = 64U * 1024U * 1024U;
    static constexpr std::size_t kMaxCollectionItems = 1000000U;

    std::int32_t browser = 0;
    std::int32_t frame = 0;
    Point location;
    Size size;
    std::int32_t editStateFlags = 0;
    std::int32_t callback = 0;

    /** Number of payload bytes that {@link encodeInto} writes for this instance. */
    std::size_t encodedSize() const noexcept {
        return 16 + location.encodedSize() + size.encodedSize();
    }

    /** Writes exactly {@link encodedSize} bytes starting at {@code dst}. */
    void encodeInto(std::uint8_t* dst) const noexcept {
        std::size_t pos = 0;
        {
            std::uint32_t v = static_cast<std::uint32_t>(browser);
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
        {
            std::uint32_t v = static_cast<std::uint32_t>(frame);
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
        location.encodeInto(dst + pos);
        pos += location.encodedSize();
        size.encodeInto(dst + pos);
        pos += size.encodedSize();
        {
            std::uint32_t v = static_cast<std::uint32_t>(editStateFlags);
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
        {
            std::uint32_t v = static_cast<std::uint32_t>(callback);
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
    }

    /** Reads a ContextMenuHandlerRunQuickMenuEvent from a payload starting at {@code src}; {@code len} bounds the buffer. */
    static ContextMenuHandlerRunQuickMenuEvent decode(const std::uint8_t* src, std::size_t len) {
        ContextMenuHandlerRunQuickMenuEvent out;
        std::size_t pos = 0;
        [[maybe_unused]] auto requireAvailable = [&](std::size_t count) {
            if (pos > len || count > len - pos)
                throw std::invalid_argument("truncated ContextMenuHandlerRunQuickMenuEvent payload");
        };
        requireAvailable(4);
        out.browser = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
        pos += 4;
        requireAvailable(4);
        out.frame = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
        pos += 4;
        out.location = Point::decode(src + pos, len - pos);
        requireAvailable(out.location.encodedSize());
        pos += out.location.encodedSize();
        out.size = Size::decode(src + pos, len - pos);
        requireAvailable(out.size.encodedSize());
        pos += out.size.encodedSize();
        requireAvailable(4);
        out.editStateFlags = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
        pos += 4;
        requireAvailable(4);
        out.callback = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
        pos += 4;
        if (pos != len)
            throw std::invalid_argument("trailing ContextMenuHandlerRunQuickMenuEvent payload");
        return out;
    }
};

} // namespace net_kurobako_cef4j_ipc_protocol_gen

#endif // NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_CONTEXT_MENU_HANDLER_RUN_QUICK_MENU_EVENT_H_
