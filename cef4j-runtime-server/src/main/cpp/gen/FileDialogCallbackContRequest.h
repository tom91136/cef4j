// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
#ifndef NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_FILE_DIALOG_CALLBACK_CONT_REQUEST_H_
#define NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_FILE_DIALOG_CALLBACK_CONT_REQUEST_H_

#include <cstdint>
#include <cstring>
#include <stdexcept>
#include <string>
#include <vector>


namespace net_kurobako_cef4j_ipc_protocol_gen {

struct FileDialogCallbackContRequest {
    static constexpr int32_t kMessageId = 963636576;

    std::int32_t self = 0;
    std::vector<std::string> filePaths;

    /** Number of payload bytes that {@link encodeInto} writes for this instance. */
    std::size_t encodedSize() const noexcept {
        return 8 + [&]() { std::size_t n = 0; for (const auto& __s : filePaths) n += 4 + __s.size(); return n; }();
    }

    /** Writes exactly {@link encodedSize} bytes starting at {@code dst}. */
    void encodeInto(std::uint8_t* dst) const noexcept {
        std::size_t pos = 0;
        {
            std::uint32_t v = static_cast<std::uint32_t>(self);
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
        {
            std::uint32_t v = static_cast<std::uint32_t>(filePaths.size());
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
        for (const auto& __s : filePaths) {
        {
            std::uint32_t v = static_cast<std::uint32_t>(__s.size());
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
            std::memcpy(dst + pos, __s.data(), __s.size());
            pos += __s.size();
        }
    }

    /** Reads a FileDialogCallbackContRequest from a payload starting at {@code src}; {@code len} bounds the buffer. */
    static FileDialogCallbackContRequest decode(const std::uint8_t* src, std::size_t len) {
        FileDialogCallbackContRequest out;
        std::size_t pos = 0;
        [[maybe_unused]] auto requireAvailable = [&](std::size_t count) {
            if (pos > len || count > len - pos)
                throw std::invalid_argument("truncated FileDialogCallbackContRequest payload");
        };
        requireAvailable(4);
        out.self = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
        pos += 4;
        {
            requireAvailable(4);
            std::int32_t cnt = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
            pos += 4;
            if (cnt < 0) throw std::invalid_argument("negative count for filePaths");
            if (static_cast<std::size_t>(cnt) > (len - pos) / 4) throw std::invalid_argument("invalid count for filePaths");
            out.filePaths.reserve(static_cast<std::size_t>(cnt));
            for (std::int32_t __i = 0; __i < cnt; ++__i) {
                requireAvailable(4);
                std::int32_t slen = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
                pos += 4;
                if (slen < 0) throw std::invalid_argument("negative string length for filePaths");
                requireAvailable(static_cast<std::size_t>(slen));
                out.filePaths.emplace_back(reinterpret_cast<const char*>(src + pos), static_cast<std::size_t>(slen));
                pos += static_cast<std::size_t>(slen);
            }
        }
        return out;
    }
};

} // namespace net_kurobako_cef4j_ipc_protocol_gen

#endif // NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_FILE_DIALOG_CALLBACK_CONT_REQUEST_H_
