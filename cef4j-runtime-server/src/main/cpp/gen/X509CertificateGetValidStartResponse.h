// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
#ifndef NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_X509_CERTIFICATE_GET_VALID_START_RESPONSE_H_
#define NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_X509_CERTIFICATE_GET_VALID_START_RESPONSE_H_

#include <cstdint>
#include <cstring>
#include <stdexcept>
#include <string>
#include <vector>
#include "Basetime.h"

namespace net_kurobako_cef4j_ipc_protocol_gen {

struct X509CertificateGetValidStartResponse {
    static constexpr int32_t kMessageId = 1463118402;
    static constexpr std::size_t kMaxFieldBytes = 64U * 1024U * 1024U;
    static constexpr std::size_t kMaxCollectionItems = 1000000U;

    Basetime result;

    /** Number of payload bytes that {@link encodeInto} writes for this instance. */
    std::size_t encodedSize() const noexcept {
        return 0 + result.encodedSize();
    }

    /** Writes exactly {@link encodedSize} bytes starting at {@code dst}. */
    void encodeInto(std::uint8_t* dst) const noexcept {
        std::size_t pos = 0;
        result.encodeInto(dst + pos);
        pos += result.encodedSize();
    }

    /** Reads a X509CertificateGetValidStartResponse from a payload starting at {@code src}; {@code len} bounds the buffer. */
    static X509CertificateGetValidStartResponse decode(const std::uint8_t* src, std::size_t len) {
        X509CertificateGetValidStartResponse out;
        std::size_t pos = 0;
        [[maybe_unused]] auto requireAvailable = [&](std::size_t count) {
            if (pos > len || count > len - pos)
                throw std::invalid_argument("truncated X509CertificateGetValidStartResponse payload");
        };
        out.result = Basetime::decode(src + pos, len - pos);
        requireAvailable(out.result.encodedSize());
        pos += out.result.encodedSize();
        if (pos != len)
            throw std::invalid_argument("trailing X509CertificateGetValidStartResponse payload");
        return out;
    }
};

} // namespace net_kurobako_cef4j_ipc_protocol_gen

#endif // NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_X509_CERTIFICATE_GET_VALID_START_RESPONSE_H_
