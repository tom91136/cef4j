// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
#ifndef NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_DIALOG_HANDLER_ON_FILE_DIALOG_EVENT_H_
#define NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_DIALOG_HANDLER_ON_FILE_DIALOG_EVENT_H_

#include <cstdint>
#include <cstring>
#include <stdexcept>
#include <string>
#include <vector>


namespace net_kurobako_cef4j_ipc_protocol_gen {

struct DialogHandlerOnFileDialogEvent {
    static constexpr int32_t kMessageId = 1340471147;

    std::int32_t browser = 0;
    std::int32_t mode = 0;
    std::string title;
    std::string defaultFilePath;
    std::vector<std::string> acceptFilters;
    std::vector<std::string> acceptExtensions;
    std::vector<std::string> acceptDescriptions;
    std::int32_t callback = 0;

    /** Number of payload bytes that {@link encodeInto} writes for this instance. */
    std::size_t encodedSize() const noexcept {
        return 32 + title.size() + defaultFilePath.size() + [&]() { std::size_t n = 0; for (const auto& __s : acceptFilters) n += 4 + __s.size(); return n; }() + [&]() { std::size_t n = 0; for (const auto& __s : acceptExtensions) n += 4 + __s.size(); return n; }() + [&]() { std::size_t n = 0; for (const auto& __s : acceptDescriptions) n += 4 + __s.size(); return n; }();
    }

    /** Writes exactly {@link encodedSize} bytes starting at {@code dst}. */
    void encodeInto(std::uint8_t* dst) const noexcept {
        std::size_t pos = 0;
        {
            std::uint32_t v = static_cast<std::uint32_t>(browser);
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
        {
            std::uint32_t v = static_cast<std::uint32_t>(mode);
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
        {
            std::uint32_t v = static_cast<std::uint32_t>(title.size());
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
        std::memcpy(dst + pos, title.data(), title.size());
        pos += title.size();
        {
            std::uint32_t v = static_cast<std::uint32_t>(defaultFilePath.size());
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
        std::memcpy(dst + pos, defaultFilePath.data(), defaultFilePath.size());
        pos += defaultFilePath.size();
        {
            std::uint32_t v = static_cast<std::uint32_t>(acceptFilters.size());
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
        for (const auto& __s : acceptFilters) {
        {
            std::uint32_t v = static_cast<std::uint32_t>(__s.size());
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
            std::memcpy(dst + pos, __s.data(), __s.size());
            pos += __s.size();
        }
        {
            std::uint32_t v = static_cast<std::uint32_t>(acceptExtensions.size());
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
        for (const auto& __s : acceptExtensions) {
        {
            std::uint32_t v = static_cast<std::uint32_t>(__s.size());
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
            std::memcpy(dst + pos, __s.data(), __s.size());
            pos += __s.size();
        }
        {
            std::uint32_t v = static_cast<std::uint32_t>(acceptDescriptions.size());
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
        for (const auto& __s : acceptDescriptions) {
        {
            std::uint32_t v = static_cast<std::uint32_t>(__s.size());
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
            std::memcpy(dst + pos, __s.data(), __s.size());
            pos += __s.size();
        }
        {
            std::uint32_t v = static_cast<std::uint32_t>(callback);
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
    }

    /** Reads a DialogHandlerOnFileDialogEvent from a payload starting at {@code src}; {@code len} bounds the buffer. */
    static DialogHandlerOnFileDialogEvent decode(const std::uint8_t* src, std::size_t len) {
        DialogHandlerOnFileDialogEvent out;
        std::size_t pos = 0;
        [[maybe_unused]] auto requireAvailable = [&](std::size_t count) {
            if (pos > len || count > len - pos)
                throw std::invalid_argument("truncated DialogHandlerOnFileDialogEvent payload");
        };
        requireAvailable(4);
        out.browser = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
        pos += 4;
        requireAvailable(4);
        out.mode = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
        pos += 4;
        {
            requireAvailable(4);
            std::int32_t n = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
            pos += 4;
            if (n < 0) throw std::invalid_argument("negative length for title");
            requireAvailable(static_cast<std::size_t>(n));
            out.title.assign(reinterpret_cast<const char*>(src + pos), static_cast<std::size_t>(n));
            pos += static_cast<std::size_t>(n);
        }
        {
            requireAvailable(4);
            std::int32_t n = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
            pos += 4;
            if (n < 0) throw std::invalid_argument("negative length for defaultFilePath");
            requireAvailable(static_cast<std::size_t>(n));
            out.defaultFilePath.assign(reinterpret_cast<const char*>(src + pos), static_cast<std::size_t>(n));
            pos += static_cast<std::size_t>(n);
        }
        {
            requireAvailable(4);
            std::int32_t cnt = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
            pos += 4;
            if (cnt < 0) throw std::invalid_argument("negative count for acceptFilters");
            if (static_cast<std::size_t>(cnt) > (len - pos) / 4) throw std::invalid_argument("invalid count for acceptFilters");
            out.acceptFilters.reserve(static_cast<std::size_t>(cnt));
            for (std::int32_t __i = 0; __i < cnt; ++__i) {
                requireAvailable(4);
                std::int32_t slen = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
                pos += 4;
                if (slen < 0) throw std::invalid_argument("negative string length for acceptFilters");
                requireAvailable(static_cast<std::size_t>(slen));
                out.acceptFilters.emplace_back(reinterpret_cast<const char*>(src + pos), static_cast<std::size_t>(slen));
                pos += static_cast<std::size_t>(slen);
            }
        }
        {
            requireAvailable(4);
            std::int32_t cnt = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
            pos += 4;
            if (cnt < 0) throw std::invalid_argument("negative count for acceptExtensions");
            if (static_cast<std::size_t>(cnt) > (len - pos) / 4) throw std::invalid_argument("invalid count for acceptExtensions");
            out.acceptExtensions.reserve(static_cast<std::size_t>(cnt));
            for (std::int32_t __i = 0; __i < cnt; ++__i) {
                requireAvailable(4);
                std::int32_t slen = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
                pos += 4;
                if (slen < 0) throw std::invalid_argument("negative string length for acceptExtensions");
                requireAvailable(static_cast<std::size_t>(slen));
                out.acceptExtensions.emplace_back(reinterpret_cast<const char*>(src + pos), static_cast<std::size_t>(slen));
                pos += static_cast<std::size_t>(slen);
            }
        }
        {
            requireAvailable(4);
            std::int32_t cnt = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
            pos += 4;
            if (cnt < 0) throw std::invalid_argument("negative count for acceptDescriptions");
            if (static_cast<std::size_t>(cnt) > (len - pos) / 4) throw std::invalid_argument("invalid count for acceptDescriptions");
            out.acceptDescriptions.reserve(static_cast<std::size_t>(cnt));
            for (std::int32_t __i = 0; __i < cnt; ++__i) {
                requireAvailable(4);
                std::int32_t slen = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
                pos += 4;
                if (slen < 0) throw std::invalid_argument("negative string length for acceptDescriptions");
                requireAvailable(static_cast<std::size_t>(slen));
                out.acceptDescriptions.emplace_back(reinterpret_cast<const char*>(src + pos), static_cast<std::size_t>(slen));
                pos += static_cast<std::size_t>(slen);
            }
        }
        requireAvailable(4);
        out.callback = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
        pos += 4;
        return out;
    }
};

} // namespace net_kurobako_cef4j_ipc_protocol_gen

#endif // NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_DIALOG_HANDLER_ON_FILE_DIALOG_EVENT_H_
