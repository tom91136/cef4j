// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
#ifndef NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_TEXTFIELD_SELECT_RANGE_REQUEST_H_
#define NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_TEXTFIELD_SELECT_RANGE_REQUEST_H_

#include <cstdint>
#include <cstring>
#include <stdexcept>
#include <string>
#include <vector>
#include "Range.h"

namespace net_kurobako_cef4j_ipc_protocol_gen {

struct TextfieldSelectRangeRequest {
    static constexpr int32_t kMessageId = 1140162383;
    static constexpr std::size_t kMaxFieldBytes = 64U * 1024U * 1024U;
    static constexpr std::size_t kMaxCollectionItems = 1000000U;

    std::int32_t self = 0;
    Range range;

    /** Number of payload bytes that {@link encodeInto} writes for this instance. */
    std::size_t encodedSize() const noexcept {
        return 4 + range.encodedSize();
    }

    /** Writes exactly {@link encodedSize} bytes starting at {@code dst}. */
    void encodeInto(std::uint8_t* dst) const noexcept {
        std::size_t pos = 0;
        {
            std::uint32_t v = static_cast<std::uint32_t>(self);
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
        range.encodeInto(dst + pos);
        pos += range.encodedSize();
    }

    /** Reads a TextfieldSelectRangeRequest from a payload starting at {@code src}; {@code len} bounds the buffer. */
    static TextfieldSelectRangeRequest decode(const std::uint8_t* src, std::size_t len) {
        TextfieldSelectRangeRequest out;
        std::size_t pos = 0;
        [[maybe_unused]] auto requireAvailable = [&](std::size_t count) {
            if (pos > len || count > len - pos)
                throw std::invalid_argument("truncated TextfieldSelectRangeRequest payload");
        };
        requireAvailable(4);
        out.self = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
        pos += 4;
        out.range = Range::decode(src + pos, len - pos);
        requireAvailable(out.range.encodedSize());
        pos += out.range.encodedSize();
        if (pos != len)
            throw std::invalid_argument("trailing TextfieldSelectRangeRequest payload");
        return out;
    }
};

} // namespace net_kurobako_cef4j_ipc_protocol_gen

#endif // NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_TEXTFIELD_SELECT_RANGE_REQUEST_H_
