// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
#ifndef NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_DISPLAY_HANDLER_ON_CONSOLE_MESSAGE_EVENT_H_
#define NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_DISPLAY_HANDLER_ON_CONSOLE_MESSAGE_EVENT_H_

#include <cstdint>
#include <cstring>
#include <stdexcept>
#include <string>
#include <vector>


namespace net_kurobako_cef4j_ipc_protocol_gen {

struct DisplayHandlerOnConsoleMessageEvent {
    static constexpr int32_t kMessageId = 1245975857;
    static constexpr std::size_t kMaxFieldBytes = 64U * 1024U * 1024U;
    static constexpr std::size_t kMaxCollectionItems = 1000000U;

    std::int32_t browser = 0;
    std::int32_t level = 0;
    std::string message;
    std::string source;
    std::int32_t line = 0;

    /** Number of payload bytes that {@link encodeInto} writes for this instance. */
    std::size_t encodedSize() const noexcept {
        return 20 + message.size() + source.size();
    }

    /** Writes exactly {@link encodedSize} bytes starting at {@code dst}. */
    void encodeInto(std::uint8_t* dst) const noexcept {
        std::size_t pos = 0;
        {
            std::uint32_t v = static_cast<std::uint32_t>(browser);
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
        {
            std::uint32_t v = static_cast<std::uint32_t>(level);
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
        {
            std::uint32_t v = static_cast<std::uint32_t>(message.size());
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
        std::memcpy(dst + pos, message.data(), message.size());
        pos += message.size();
        {
            std::uint32_t v = static_cast<std::uint32_t>(source.size());
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
        std::memcpy(dst + pos, source.data(), source.size());
        pos += source.size();
        {
            std::uint32_t v = static_cast<std::uint32_t>(line);
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
    }

    /** Reads a DisplayHandlerOnConsoleMessageEvent from a payload starting at {@code src}; {@code len} bounds the buffer. */
    static DisplayHandlerOnConsoleMessageEvent decode(const std::uint8_t* src, std::size_t len) {
        DisplayHandlerOnConsoleMessageEvent out;
        std::size_t pos = 0;
        [[maybe_unused]] auto requireAvailable = [&](std::size_t count) {
            if (pos > len || count > len - pos)
                throw std::invalid_argument("truncated DisplayHandlerOnConsoleMessageEvent payload");
        };
        requireAvailable(4);
        out.browser = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
        pos += 4;
        requireAvailable(4);
        out.level = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
        pos += 4;
        {
            requireAvailable(4);
            std::int32_t n = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
            pos += 4;
            if (n < 0) throw std::invalid_argument("negative length for message");
            if (static_cast<std::size_t>(n) > kMaxFieldBytes) throw std::invalid_argument("oversized length for message");
            requireAvailable(static_cast<std::size_t>(n));
            out.message.assign(reinterpret_cast<const char*>(src + pos), static_cast<std::size_t>(n));
            pos += static_cast<std::size_t>(n);
        }
        {
            requireAvailable(4);
            std::int32_t n = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
            pos += 4;
            if (n < 0) throw std::invalid_argument("negative length for source");
            if (static_cast<std::size_t>(n) > kMaxFieldBytes) throw std::invalid_argument("oversized length for source");
            requireAvailable(static_cast<std::size_t>(n));
            out.source.assign(reinterpret_cast<const char*>(src + pos), static_cast<std::size_t>(n));
            pos += static_cast<std::size_t>(n);
        }
        requireAvailable(4);
        out.line = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
        pos += 4;
        if (pos != len)
            throw std::invalid_argument("trailing DisplayHandlerOnConsoleMessageEvent payload");
        return out;
    }
};

} // namespace net_kurobako_cef4j_ipc_protocol_gen

#endif // NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_DISPLAY_HANDLER_ON_CONSOLE_MESSAGE_EVENT_H_
