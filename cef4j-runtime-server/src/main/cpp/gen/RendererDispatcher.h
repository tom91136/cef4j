// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
//
// Renderer-affinity dispatchable methods covered: 109
// Renderer-affinity methods skipped (unsupported shape): 7
#ifndef CEF4J_IPC_GEN_RENDERER_DISPATCHER_H_
#define CEF4J_IPC_GEN_RENDERER_DISPATCHER_H_

#include <cstdint>
#include <cstring>
#include <string>
#include <vector>

#include "include/capi/cef_base_capi.h"
#include "include/capi/cef_frame_capi.h"
#include "include/capi/cef_process_message_capi.h"
#include "include/capi/cef_values_capi.h"
#include "include/internal/cef_string.h"
#include "include/internal/cef_string_list.h"
#include "include/capi/cef_accessibility_handler_capi.h"
#include "include/capi/cef_app_capi.h"
#include "include/capi/cef_audio_handler_capi.h"
#include "include/capi/cef_auth_callback_capi.h"
#include "include/capi/cef_base_capi.h"
#include "include/capi/cef_browser_capi.h"
#include "include/capi/cef_browser_process_handler_capi.h"
#include "include/capi/cef_callback_capi.h"
#include "include/capi/cef_client_capi.h"
#include "include/capi/cef_command_handler_capi.h"
#include "include/capi/cef_command_line_capi.h"
#include "include/capi/cef_component_updater_capi.h"
#include "include/capi/cef_context_menu_handler_capi.h"
#include "include/capi/cef_cookie_capi.h"
#include "include/capi/cef_crash_util_capi.h"
#include "include/capi/cef_devtools_message_observer_capi.h"
#include "include/capi/cef_dialog_handler_capi.h"
#include "include/capi/cef_display_handler_capi.h"
#include "include/capi/cef_dom_capi.h"
#include "include/capi/cef_download_handler_capi.h"
#include "include/capi/cef_download_item_capi.h"
#include "include/capi/cef_drag_data_capi.h"
#include "include/capi/cef_drag_handler_capi.h"
#include "include/capi/cef_file_util_capi.h"
#include "include/capi/cef_find_handler_capi.h"
#include "include/capi/cef_focus_handler_capi.h"
#include "include/capi/cef_frame_capi.h"
#include "include/capi/cef_frame_handler_capi.h"
#include "include/capi/cef_i18n_util_capi.h"
#include "include/capi/cef_image_capi.h"
#include "include/capi/cef_jsdialog_handler_capi.h"
#include "include/capi/cef_keyboard_handler_capi.h"
#include "include/capi/cef_life_span_handler_capi.h"
#include "include/capi/cef_load_handler_capi.h"
#include "include/capi/cef_media_router_capi.h"
#include "include/capi/cef_menu_model_capi.h"
#include "include/capi/cef_menu_model_delegate_capi.h"
#include "include/capi/cef_navigation_entry_capi.h"
#include "include/capi/cef_origin_whitelist_capi.h"
#include "include/capi/cef_parser_capi.h"
#include "include/capi/cef_path_util_capi.h"
#include "include/capi/cef_permission_handler_capi.h"
#include "include/capi/cef_preference_capi.h"
#include "include/capi/cef_print_handler_capi.h"
#include "include/capi/cef_print_settings_capi.h"
#include "include/capi/cef_process_message_capi.h"
#include "include/capi/cef_process_util_capi.h"
#include "include/capi/cef_registration_capi.h"
#include "include/capi/cef_render_handler_capi.h"
#include "include/capi/cef_render_process_handler_capi.h"
#include "include/capi/cef_request_capi.h"
#include "include/capi/cef_request_context_capi.h"
#include "include/capi/cef_request_context_handler_capi.h"
#include "include/capi/cef_request_handler_capi.h"
#include "include/capi/cef_resource_bundle_capi.h"
#include "include/capi/cef_resource_bundle_handler_capi.h"
#include "include/capi/cef_resource_handler_capi.h"
#include "include/capi/cef_resource_request_handler_capi.h"
#include "include/capi/cef_response_capi.h"
#include "include/capi/cef_response_filter_capi.h"
#include "include/capi/cef_scheme_capi.h"
#include "include/capi/cef_server_capi.h"
#include "include/capi/cef_shared_memory_region_capi.h"
#include "include/capi/cef_shared_process_message_builder_capi.h"
#include "include/capi/cef_ssl_info_capi.h"
#include "include/capi/cef_ssl_status_capi.h"
#include "include/capi/cef_stream_capi.h"
#include "include/capi/cef_string_visitor_capi.h"
#include "include/capi/cef_task_capi.h"
#include "include/capi/cef_task_manager_capi.h"
#include "include/capi/cef_thread_capi.h"
#include "include/capi/cef_trace_capi.h"
#include "include/capi/cef_unresponsive_process_callback_capi.h"
#include "include/capi/cef_urlrequest_capi.h"
#include "include/capi/cef_v8_capi.h"
#include "include/capi/cef_values_capi.h"
#include "include/capi/cef_waitable_event_capi.h"
#include "include/capi/cef_x509_certificate_capi.h"
#include "include/capi/cef_xml_reader_capi.h"
#include "include/capi/cef_zip_reader_capi.h"
#include "include/capi/views/cef_box_layout_capi.h"
#include "include/capi/views/cef_browser_view_capi.h"
#include "include/capi/views/cef_browser_view_delegate_capi.h"
#include "include/capi/views/cef_button_capi.h"
#include "include/capi/views/cef_button_delegate_capi.h"
#include "include/capi/views/cef_display_capi.h"
#include "include/capi/views/cef_fill_layout_capi.h"
#include "include/capi/views/cef_label_button_capi.h"
#include "include/capi/views/cef_layout_capi.h"
#include "include/capi/views/cef_menu_button_capi.h"
#include "include/capi/views/cef_menu_button_delegate_capi.h"
#include "include/capi/views/cef_overlay_controller_capi.h"
#include "include/capi/views/cef_panel_capi.h"
#include "include/capi/views/cef_panel_delegate_capi.h"
#include "include/capi/views/cef_scroll_view_capi.h"
#include "include/capi/views/cef_textfield_capi.h"
#include "include/capi/views/cef_textfield_delegate_capi.h"
#include "include/capi/views/cef_view_capi.h"
#include "include/capi/views/cef_view_delegate_capi.h"
#include "include/capi/views/cef_window_capi.h"
#include "include/capi/views/cef_window_delegate_capi.h"

#include "CefCompat.h"
#include "Dispatcher.h"

#include "DomDocumentGetBaseUrlRequest.h"
#include "DomDocumentGetBaseUrlResponse.h"
#include "DomDocumentGetBodyRequest.h"
#include "DomDocumentGetBodyResponse.h"
#include "DomDocumentGetCompleteUrlRequest.h"
#include "DomDocumentGetCompleteUrlResponse.h"
#include "DomDocumentGetDocumentRequest.h"
#include "DomDocumentGetDocumentResponse.h"
#include "DomDocumentGetElementByIdRequest.h"
#include "DomDocumentGetElementByIdResponse.h"
#include "DomDocumentGetFocusedNodeRequest.h"
#include "DomDocumentGetFocusedNodeResponse.h"
#include "DomDocumentGetHeadRequest.h"
#include "DomDocumentGetHeadResponse.h"
#include "DomDocumentGetSelectionAsMarkupRequest.h"
#include "DomDocumentGetSelectionAsMarkupResponse.h"
#include "DomDocumentGetSelectionAsTextRequest.h"
#include "DomDocumentGetSelectionAsTextResponse.h"
#include "DomDocumentGetSelectionEndOffsetRequest.h"
#include "DomDocumentGetSelectionEndOffsetResponse.h"
#include "DomDocumentGetSelectionStartOffsetRequest.h"
#include "DomDocumentGetSelectionStartOffsetResponse.h"
#include "DomDocumentGetTitleRequest.h"
#include "DomDocumentGetTitleResponse.h"
#include "DomDocumentGetTypeRequest.h"
#include "DomDocumentGetTypeResponse.h"
#include "DomDocumentHasSelectionRequest.h"
#include "DomDocumentHasSelectionResponse.h"
#include "DomNodeGetAsMarkupRequest.h"
#include "DomNodeGetAsMarkupResponse.h"
#include "DomNodeGetDocumentRequest.h"
#include "DomNodeGetDocumentResponse.h"
#include "DomNodeGetElementAttributeRequest.h"
#include "DomNodeGetElementAttributeResponse.h"
#include "DomNodeGetElementInnerTextRequest.h"
#include "DomNodeGetElementInnerTextResponse.h"
#include "DomNodeGetElementTagNameRequest.h"
#include "DomNodeGetElementTagNameResponse.h"
#include "DomNodeGetFirstChildRequest.h"
#include "DomNodeGetFirstChildResponse.h"
#include "DomNodeGetFormControlElementTypeRequest.h"
#include "DomNodeGetFormControlElementTypeResponse.h"
#include "DomNodeGetLastChildRequest.h"
#include "DomNodeGetLastChildResponse.h"
#include "DomNodeGetNameRequest.h"
#include "DomNodeGetNameResponse.h"
#include "DomNodeGetNextSiblingRequest.h"
#include "DomNodeGetNextSiblingResponse.h"
#include "DomNodeGetParentRequest.h"
#include "DomNodeGetParentResponse.h"
#include "DomNodeGetPreviousSiblingRequest.h"
#include "DomNodeGetPreviousSiblingResponse.h"
#include "DomNodeGetTypeRequest.h"
#include "DomNodeGetTypeResponse.h"
#include "DomNodeGetValueRequest.h"
#include "DomNodeGetValueResponse.h"
#include "DomNodeHasChildrenRequest.h"
#include "DomNodeHasChildrenResponse.h"
#include "DomNodeHasElementAttributeRequest.h"
#include "DomNodeHasElementAttributeResponse.h"
#include "DomNodeHasElementAttributesRequest.h"
#include "DomNodeHasElementAttributesResponse.h"
#include "DomNodeIsEditableRequest.h"
#include "DomNodeIsEditableResponse.h"
#include "DomNodeIsElementRequest.h"
#include "DomNodeIsElementResponse.h"
#include "DomNodeIsFormControlElementRequest.h"
#include "DomNodeIsFormControlElementResponse.h"
#include "DomNodeIsSameRequest.h"
#include "DomNodeIsSameResponse.h"
#include "DomNodeIsTextRequest.h"
#include "DomNodeIsTextResponse.h"
#include "DomNodeSetElementAttributeRequest.h"
#include "DomNodeSetElementAttributeResponse.h"
#include "DomNodeSetValueRequest.h"
#include "DomNodeSetValueResponse.h"
#include "V8BackingStoreByteLengthRequest.h"
#include "V8BackingStoreByteLengthResponse.h"
#include "V8BackingStoreIsValidRequest.h"
#include "V8BackingStoreIsValidResponse.h"
#include "V8ContextEnterRequest.h"
#include "V8ContextEnterResponse.h"
#include "V8ContextExitRequest.h"
#include "V8ContextExitResponse.h"
#include "V8ContextGetBrowserRequest.h"
#include "V8ContextGetBrowserResponse.h"
#include "V8ContextGetFrameRequest.h"
#include "V8ContextGetFrameResponse.h"
#include "V8ContextGetGlobalRequest.h"
#include "V8ContextGetGlobalResponse.h"
#include "V8ContextGetTaskRunnerRequest.h"
#include "V8ContextGetTaskRunnerResponse.h"
#include "V8ContextIsSameRequest.h"
#include "V8ContextIsSameResponse.h"
#include "V8ContextIsValidRequest.h"
#include "V8ContextIsValidResponse.h"
#include "V8ExceptionGetEndColumnRequest.h"
#include "V8ExceptionGetEndColumnResponse.h"
#include "V8ExceptionGetEndPositionRequest.h"
#include "V8ExceptionGetEndPositionResponse.h"
#include "V8ExceptionGetLineNumberRequest.h"
#include "V8ExceptionGetLineNumberResponse.h"
#include "V8ExceptionGetMessageRequest.h"
#include "V8ExceptionGetMessageResponse.h"
#include "V8ExceptionGetScriptResourceNameRequest.h"
#include "V8ExceptionGetScriptResourceNameResponse.h"
#include "V8ExceptionGetSourceLineRequest.h"
#include "V8ExceptionGetSourceLineResponse.h"
#include "V8ExceptionGetStartColumnRequest.h"
#include "V8ExceptionGetStartColumnResponse.h"
#include "V8ExceptionGetStartPositionRequest.h"
#include "V8ExceptionGetStartPositionResponse.h"
#include "V8StackFrameGetColumnRequest.h"
#include "V8StackFrameGetColumnResponse.h"
#include "V8StackFrameGetFunctionNameRequest.h"
#include "V8StackFrameGetFunctionNameResponse.h"
#include "V8StackFrameGetLineNumberRequest.h"
#include "V8StackFrameGetLineNumberResponse.h"
#include "V8StackFrameGetScriptNameOrSourceUrlRequest.h"
#include "V8StackFrameGetScriptNameOrSourceUrlResponse.h"
#include "V8StackFrameGetScriptNameRequest.h"
#include "V8StackFrameGetScriptNameResponse.h"
#include "V8StackFrameIsConstructorRequest.h"
#include "V8StackFrameIsConstructorResponse.h"
#include "V8StackFrameIsEvalRequest.h"
#include "V8StackFrameIsEvalResponse.h"
#include "V8StackFrameIsValidRequest.h"
#include "V8StackFrameIsValidResponse.h"
#include "V8StackTraceGetFrameCountRequest.h"
#include "V8StackTraceGetFrameCountResponse.h"
#include "V8StackTraceGetFrameRequest.h"
#include "V8StackTraceGetFrameResponse.h"
#include "V8StackTraceIsValidRequest.h"
#include "V8StackTraceIsValidResponse.h"
#include "V8ValueAdjustExternallyAllocatedMemoryRequest.h"
#include "V8ValueAdjustExternallyAllocatedMemoryResponse.h"
#include "V8ValueClearExceptionRequest.h"
#include "V8ValueClearExceptionResponse.h"
#include "V8ValueDeleteValueByindexRequest.h"
#include "V8ValueDeleteValueByindexResponse.h"
#include "V8ValueDeleteValueBykeyRequest.h"
#include "V8ValueDeleteValueBykeyResponse.h"
#include "V8ValueGetArrayBufferByteLengthRequest.h"
#include "V8ValueGetArrayBufferByteLengthResponse.h"
#include "V8ValueGetArrayLengthRequest.h"
#include "V8ValueGetArrayLengthResponse.h"
#include "V8ValueGetBoolValueRequest.h"
#include "V8ValueGetBoolValueResponse.h"
#include "V8ValueGetExceptionRequest.h"
#include "V8ValueGetExceptionResponse.h"
#include "V8ValueGetExternallyAllocatedMemoryRequest.h"
#include "V8ValueGetExternallyAllocatedMemoryResponse.h"
#include "V8ValueGetFunctionNameRequest.h"
#include "V8ValueGetFunctionNameResponse.h"
#include "V8ValueGetIntValueRequest.h"
#include "V8ValueGetIntValueResponse.h"
#include "V8ValueGetStringValueRequest.h"
#include "V8ValueGetStringValueResponse.h"
#include "V8ValueGetUintValueRequest.h"
#include "V8ValueGetUintValueResponse.h"
#include "V8ValueGetValueByindexRequest.h"
#include "V8ValueGetValueByindexResponse.h"
#include "V8ValueGetValueBykeyRequest.h"
#include "V8ValueGetValueBykeyResponse.h"
#include "V8ValueHasExceptionRequest.h"
#include "V8ValueHasExceptionResponse.h"
#include "V8ValueHasValueByindexRequest.h"
#include "V8ValueHasValueByindexResponse.h"
#include "V8ValueHasValueBykeyRequest.h"
#include "V8ValueHasValueBykeyResponse.h"
#include "V8ValueIsArrayBufferRequest.h"
#include "V8ValueIsArrayBufferResponse.h"
#include "V8ValueIsArrayRequest.h"
#include "V8ValueIsArrayResponse.h"
#include "V8ValueIsBoolRequest.h"
#include "V8ValueIsBoolResponse.h"
#include "V8ValueIsDateRequest.h"
#include "V8ValueIsDateResponse.h"
#include "V8ValueIsDoubleRequest.h"
#include "V8ValueIsDoubleResponse.h"
#include "V8ValueIsFunctionRequest.h"
#include "V8ValueIsFunctionResponse.h"
#include "V8ValueIsIntRequest.h"
#include "V8ValueIsIntResponse.h"
#include "V8ValueIsNullRequest.h"
#include "V8ValueIsNullResponse.h"
#include "V8ValueIsObjectRequest.h"
#include "V8ValueIsObjectResponse.h"
#include "V8ValueIsPromiseRequest.h"
#include "V8ValueIsPromiseResponse.h"
#include "V8ValueIsSameRequest.h"
#include "V8ValueIsSameResponse.h"
#include "V8ValueIsStringRequest.h"
#include "V8ValueIsStringResponse.h"
#include "V8ValueIsUintRequest.h"
#include "V8ValueIsUintResponse.h"
#include "V8ValueIsUndefinedRequest.h"
#include "V8ValueIsUndefinedResponse.h"
#include "V8ValueIsUserCreatedRequest.h"
#include "V8ValueIsUserCreatedResponse.h"
#include "V8ValueIsValidRequest.h"
#include "V8ValueIsValidResponse.h"
#include "V8ValueNeuterArrayBufferRequest.h"
#include "V8ValueNeuterArrayBufferResponse.h"
#include "V8ValueRejectPromiseRequest.h"
#include "V8ValueRejectPromiseResponse.h"
#include "V8ValueResolvePromiseRequest.h"
#include "V8ValueResolvePromiseResponse.h"
#include "V8ValueSetRethrowExceptionsRequest.h"
#include "V8ValueSetRethrowExceptionsResponse.h"
#include "V8ValueSetValueByaccessorRequest.h"
#include "V8ValueSetValueByaccessorResponse.h"
#include "V8ValueSetValueByindexRequest.h"
#include "V8ValueSetValueByindexResponse.h"
#include "V8ValueSetValueBykeyRequest.h"
#include "V8ValueSetValueBykeyResponse.h"
#include "V8ValueWillRethrowExceptionsRequest.h"
#include "V8ValueWillRethrowExceptionsResponse.h"

namespace net_kurobako_cef4j_ipc_protocol_gen_renderer_dispatcher {

namespace tables = net_kurobako_cef4j_ipc_protocol_gen_dispatcher::tables;
using ScopedCefString = net_kurobako_cef4j_ipc_protocol_gen_dispatcher::ScopedCefString;
using net_kurobako_cef4j_ipc_protocol_gen_dispatcher::insertOrRelease;

// Pairs V8 context enter/exit and releases the retained context.
class ScopedV8Context {
public:
    explicit ScopedV8Context(cef_v8_context_t* ctx) : ctx_(ctx), entered_(false) {
        if (ctx_) entered_ = ctx_->enter(ctx_) != 0;
    }
    ~ScopedV8Context() {
        if (ctx_) {
            if (entered_) ctx_->exit(ctx_);
            auto* base = reinterpret_cast<cef_base_ref_counted_t*>(ctx_);
            base->release(base);
        }
    }
    ScopedV8Context(const ScopedV8Context&) = delete;
    ScopedV8Context& operator=(const ScopedV8Context&) = delete;
    bool entered() const { return entered_; }
private:
    cef_v8_context_t* ctx_;
    bool entered_;
};

// Sends [corrId, messageId, payload] and transfers message ownership to CEF.
inline void sendResponseEnvelope(cef_frame_t* frame, const char* envelopeName, std::int32_t corrId,
                                 std::int32_t messageId, const std::uint8_t* payload, std::size_t size) {
    cef_string_t name{};
    cef_string_utf8_to_utf16(envelopeName, std::strlen(envelopeName), &name);
    cef_process_message_t* msg = cef_process_message_create(&name);
    cef_string_clear(&name);
    cef_list_value_t* args = msg->get_argument_list(msg);
    if (args) {
        args->set_int(args, 0, corrId);
        args->set_int(args, 1, messageId);
        cef_binary_value_t* binary = cef_binary_value_create(payload, size);
        if (binary) {
            // set_binary adopts the retained value.
            args->set_binary(args, 2, binary);
        }
        auto* ab = reinterpret_cast<cef_base_ref_counted_t*>(args);
        ab->release(ab);
    }
    frame->send_process_message(frame, PID_BROWSER, msg);
}

// Sends ReceiverGone through the renderer error envelope.
inline void sendReceiverGone(cef_frame_t* frame, std::int32_t corrId, std::int32_t messageId) {
    static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
    sendResponseEnvelope(frame, "cef4j_renderer_err", corrId, messageId,
                          kReceiverGonePayload, sizeof(kReceiverGonePayload));
}

// Returns whether the message was dispatched and answered.
inline bool dispatch(cef_frame_t* frame, std::int32_t corrId, std::int32_t messageId,
                     const std::vector<std::uint8_t>& payload) {
    namespace gen = net_kurobako_cef4j_ipc_protocol_gen;
    switch (messageId) {
        case gen::DomDocumentGetTypeRequest::kMessageId: {
            auto req = gen::DomDocumentGetTypeRequest::decode(payload.data(), payload.size());
            cef_domdocument_t* receiver = tables::domdocument.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->get_type(receiver);

            gen::DomDocumentGetTypeResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomDocumentGetDocumentRequest::kMessageId: {
            auto req = gen::DomDocumentGetDocumentRequest::decode(payload.data(), payload.size());
            cef_domdocument_t* receiver = tables::domdocument.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_domnode_t* rawResult = receiver->get_document(receiver);

            gen::DomDocumentGetDocumentResponse resp;
            resp.result = insertOrRelease(&tables::domnode, rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomDocumentGetBodyRequest::kMessageId: {
            auto req = gen::DomDocumentGetBodyRequest::decode(payload.data(), payload.size());
            cef_domdocument_t* receiver = tables::domdocument.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_domnode_t* rawResult = receiver->get_body(receiver);

            gen::DomDocumentGetBodyResponse resp;
            resp.result = insertOrRelease(&tables::domnode, rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomDocumentGetHeadRequest::kMessageId: {
            auto req = gen::DomDocumentGetHeadRequest::decode(payload.data(), payload.size());
            cef_domdocument_t* receiver = tables::domdocument.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_domnode_t* rawResult = receiver->get_head(receiver);

            gen::DomDocumentGetHeadResponse resp;
            resp.result = insertOrRelease(&tables::domnode, rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomDocumentGetTitleRequest::kMessageId: {
            auto req = gen::DomDocumentGetTitleRequest::decode(payload.data(), payload.size());
            cef_domdocument_t* receiver = tables::domdocument.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_string_userfree_t rawResult = receiver->get_title(receiver);

            gen::DomDocumentGetTitleResponse resp;
            resp.result = ScopedCefString::take(rawResult).toUtf8();
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomDocumentGetElementByIdRequest::kMessageId: {
            auto req = gen::DomDocumentGetElementByIdRequest::decode(payload.data(), payload.size());
            cef_domdocument_t* receiver = tables::domdocument.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            ScopedCefString id_cef(req.id);
                        cef_domnode_t* rawResult = receiver->get_element_by_id(receiver, id_cef.get());

            gen::DomDocumentGetElementByIdResponse resp;
            resp.result = insertOrRelease(&tables::domnode, rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomDocumentGetFocusedNodeRequest::kMessageId: {
            auto req = gen::DomDocumentGetFocusedNodeRequest::decode(payload.data(), payload.size());
            cef_domdocument_t* receiver = tables::domdocument.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_domnode_t* rawResult = receiver->get_focused_node(receiver);

            gen::DomDocumentGetFocusedNodeResponse resp;
            resp.result = insertOrRelease(&tables::domnode, rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomDocumentHasSelectionRequest::kMessageId: {
            auto req = gen::DomDocumentHasSelectionRequest::decode(payload.data(), payload.size());
            cef_domdocument_t* receiver = tables::domdocument.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->has_selection(receiver);

            gen::DomDocumentHasSelectionResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomDocumentGetSelectionStartOffsetRequest::kMessageId: {
            auto req = gen::DomDocumentGetSelectionStartOffsetRequest::decode(payload.data(), payload.size());
            cef_domdocument_t* receiver = tables::domdocument.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->get_selection_start_offset(receiver);

            gen::DomDocumentGetSelectionStartOffsetResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomDocumentGetSelectionEndOffsetRequest::kMessageId: {
            auto req = gen::DomDocumentGetSelectionEndOffsetRequest::decode(payload.data(), payload.size());
            cef_domdocument_t* receiver = tables::domdocument.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->get_selection_end_offset(receiver);

            gen::DomDocumentGetSelectionEndOffsetResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomDocumentGetSelectionAsMarkupRequest::kMessageId: {
            auto req = gen::DomDocumentGetSelectionAsMarkupRequest::decode(payload.data(), payload.size());
            cef_domdocument_t* receiver = tables::domdocument.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_string_userfree_t rawResult = receiver->get_selection_as_markup(receiver);

            gen::DomDocumentGetSelectionAsMarkupResponse resp;
            resp.result = ScopedCefString::take(rawResult).toUtf8();
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomDocumentGetSelectionAsTextRequest::kMessageId: {
            auto req = gen::DomDocumentGetSelectionAsTextRequest::decode(payload.data(), payload.size());
            cef_domdocument_t* receiver = tables::domdocument.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_string_userfree_t rawResult = receiver->get_selection_as_text(receiver);

            gen::DomDocumentGetSelectionAsTextResponse resp;
            resp.result = ScopedCefString::take(rawResult).toUtf8();
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomDocumentGetBaseUrlRequest::kMessageId: {
            auto req = gen::DomDocumentGetBaseUrlRequest::decode(payload.data(), payload.size());
            cef_domdocument_t* receiver = tables::domdocument.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_string_userfree_t rawResult = receiver->get_base_url(receiver);

            gen::DomDocumentGetBaseUrlResponse resp;
            resp.result = ScopedCefString::take(rawResult).toUtf8();
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomDocumentGetCompleteUrlRequest::kMessageId: {
            auto req = gen::DomDocumentGetCompleteUrlRequest::decode(payload.data(), payload.size());
            cef_domdocument_t* receiver = tables::domdocument.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            ScopedCefString partialURL_cef(req.partialURL);
                        cef_string_userfree_t rawResult = receiver->get_complete_url(receiver, partialURL_cef.get());

            gen::DomDocumentGetCompleteUrlResponse resp;
            resp.result = ScopedCefString::take(rawResult).toUtf8();
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomNodeGetTypeRequest::kMessageId: {
            auto req = gen::DomNodeGetTypeRequest::decode(payload.data(), payload.size());
            cef_domnode_t* receiver = tables::domnode.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->get_type(receiver);

            gen::DomNodeGetTypeResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomNodeIsTextRequest::kMessageId: {
            auto req = gen::DomNodeIsTextRequest::decode(payload.data(), payload.size());
            cef_domnode_t* receiver = tables::domnode.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->is_text(receiver);

            gen::DomNodeIsTextResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomNodeIsElementRequest::kMessageId: {
            auto req = gen::DomNodeIsElementRequest::decode(payload.data(), payload.size());
            cef_domnode_t* receiver = tables::domnode.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->is_element(receiver);

            gen::DomNodeIsElementResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomNodeIsEditableRequest::kMessageId: {
            auto req = gen::DomNodeIsEditableRequest::decode(payload.data(), payload.size());
            cef_domnode_t* receiver = tables::domnode.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->is_editable(receiver);

            gen::DomNodeIsEditableResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomNodeIsFormControlElementRequest::kMessageId: {
            auto req = gen::DomNodeIsFormControlElementRequest::decode(payload.data(), payload.size());
            cef_domnode_t* receiver = tables::domnode.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->is_form_control_element(receiver);

            gen::DomNodeIsFormControlElementResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomNodeGetFormControlElementTypeRequest::kMessageId: {
            auto req = gen::DomNodeGetFormControlElementTypeRequest::decode(payload.data(), payload.size());
            cef_domnode_t* receiver = tables::domnode.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->get_form_control_element_type(receiver);

            gen::DomNodeGetFormControlElementTypeResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomNodeIsSameRequest::kMessageId: {
            auto req = gen::DomNodeIsSameRequest::decode(payload.data(), payload.size());
            cef_domnode_t* receiver = tables::domnode.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_domnode_t* that_ptr = req.that != 0 ? tables::domnode.retain(req.that) : nullptr;
                        int rawResult = receiver->is_same(receiver, that_ptr);
            if (that_ptr) {
                auto* that_base = reinterpret_cast<cef_base_ref_counted_t*>(that_ptr);
                that_base->release(that_base);
            }
            gen::DomNodeIsSameResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomNodeGetNameRequest::kMessageId: {
            auto req = gen::DomNodeGetNameRequest::decode(payload.data(), payload.size());
            cef_domnode_t* receiver = tables::domnode.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_string_userfree_t rawResult = receiver->get_name(receiver);

            gen::DomNodeGetNameResponse resp;
            resp.result = ScopedCefString::take(rawResult).toUtf8();
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomNodeGetValueRequest::kMessageId: {
            auto req = gen::DomNodeGetValueRequest::decode(payload.data(), payload.size());
            cef_domnode_t* receiver = tables::domnode.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_string_userfree_t rawResult = receiver->get_value(receiver);

            gen::DomNodeGetValueResponse resp;
            resp.result = ScopedCefString::take(rawResult).toUtf8();
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomNodeSetValueRequest::kMessageId: {
            auto req = gen::DomNodeSetValueRequest::decode(payload.data(), payload.size());
            cef_domnode_t* receiver = tables::domnode.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            ScopedCefString value_cef(req.value);
                        int rawResult = receiver->set_value(receiver, value_cef.get());

            gen::DomNodeSetValueResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomNodeGetAsMarkupRequest::kMessageId: {
            auto req = gen::DomNodeGetAsMarkupRequest::decode(payload.data(), payload.size());
            cef_domnode_t* receiver = tables::domnode.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_string_userfree_t rawResult = receiver->get_as_markup(receiver);

            gen::DomNodeGetAsMarkupResponse resp;
            resp.result = ScopedCefString::take(rawResult).toUtf8();
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomNodeGetDocumentRequest::kMessageId: {
            auto req = gen::DomNodeGetDocumentRequest::decode(payload.data(), payload.size());
            cef_domnode_t* receiver = tables::domnode.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_domdocument_t* rawResult = receiver->get_document(receiver);

            gen::DomNodeGetDocumentResponse resp;
            resp.result = insertOrRelease(&tables::domdocument, rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomNodeGetParentRequest::kMessageId: {
            auto req = gen::DomNodeGetParentRequest::decode(payload.data(), payload.size());
            cef_domnode_t* receiver = tables::domnode.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_domnode_t* rawResult = receiver->get_parent(receiver);

            gen::DomNodeGetParentResponse resp;
            resp.result = insertOrRelease(&tables::domnode, rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomNodeGetPreviousSiblingRequest::kMessageId: {
            auto req = gen::DomNodeGetPreviousSiblingRequest::decode(payload.data(), payload.size());
            cef_domnode_t* receiver = tables::domnode.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_domnode_t* rawResult = receiver->get_previous_sibling(receiver);

            gen::DomNodeGetPreviousSiblingResponse resp;
            resp.result = insertOrRelease(&tables::domnode, rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomNodeGetNextSiblingRequest::kMessageId: {
            auto req = gen::DomNodeGetNextSiblingRequest::decode(payload.data(), payload.size());
            cef_domnode_t* receiver = tables::domnode.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_domnode_t* rawResult = receiver->get_next_sibling(receiver);

            gen::DomNodeGetNextSiblingResponse resp;
            resp.result = insertOrRelease(&tables::domnode, rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomNodeHasChildrenRequest::kMessageId: {
            auto req = gen::DomNodeHasChildrenRequest::decode(payload.data(), payload.size());
            cef_domnode_t* receiver = tables::domnode.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->has_children(receiver);

            gen::DomNodeHasChildrenResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomNodeGetFirstChildRequest::kMessageId: {
            auto req = gen::DomNodeGetFirstChildRequest::decode(payload.data(), payload.size());
            cef_domnode_t* receiver = tables::domnode.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_domnode_t* rawResult = receiver->get_first_child(receiver);

            gen::DomNodeGetFirstChildResponse resp;
            resp.result = insertOrRelease(&tables::domnode, rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomNodeGetLastChildRequest::kMessageId: {
            auto req = gen::DomNodeGetLastChildRequest::decode(payload.data(), payload.size());
            cef_domnode_t* receiver = tables::domnode.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_domnode_t* rawResult = receiver->get_last_child(receiver);

            gen::DomNodeGetLastChildResponse resp;
            resp.result = insertOrRelease(&tables::domnode, rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomNodeGetElementTagNameRequest::kMessageId: {
            auto req = gen::DomNodeGetElementTagNameRequest::decode(payload.data(), payload.size());
            cef_domnode_t* receiver = tables::domnode.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_string_userfree_t rawResult = receiver->get_element_tag_name(receiver);

            gen::DomNodeGetElementTagNameResponse resp;
            resp.result = ScopedCefString::take(rawResult).toUtf8();
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomNodeHasElementAttributesRequest::kMessageId: {
            auto req = gen::DomNodeHasElementAttributesRequest::decode(payload.data(), payload.size());
            cef_domnode_t* receiver = tables::domnode.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->has_element_attributes(receiver);

            gen::DomNodeHasElementAttributesResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomNodeHasElementAttributeRequest::kMessageId: {
            auto req = gen::DomNodeHasElementAttributeRequest::decode(payload.data(), payload.size());
            cef_domnode_t* receiver = tables::domnode.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            ScopedCefString attrName_cef(req.attrName);
                        int rawResult = receiver->has_element_attribute(receiver, attrName_cef.get());

            gen::DomNodeHasElementAttributeResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomNodeGetElementAttributeRequest::kMessageId: {
            auto req = gen::DomNodeGetElementAttributeRequest::decode(payload.data(), payload.size());
            cef_domnode_t* receiver = tables::domnode.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            ScopedCefString attrName_cef(req.attrName);
                        cef_string_userfree_t rawResult = receiver->get_element_attribute(receiver, attrName_cef.get());

            gen::DomNodeGetElementAttributeResponse resp;
            resp.result = ScopedCefString::take(rawResult).toUtf8();
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomNodeSetElementAttributeRequest::kMessageId: {
            auto req = gen::DomNodeSetElementAttributeRequest::decode(payload.data(), payload.size());
            cef_domnode_t* receiver = tables::domnode.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            ScopedCefString attrName_cef(req.attrName);
            ScopedCefString value_cef(req.value);
                        int rawResult = receiver->set_element_attribute(receiver, attrName_cef.get(), value_cef.get());

            gen::DomNodeSetElementAttributeResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::DomNodeGetElementInnerTextRequest::kMessageId: {
            auto req = gen::DomNodeGetElementInnerTextRequest::decode(payload.data(), payload.size());
            cef_domnode_t* receiver = tables::domnode.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_string_userfree_t rawResult = receiver->get_element_inner_text(receiver);

            gen::DomNodeGetElementInnerTextResponse resp;
            resp.result = ScopedCefString::take(rawResult).toUtf8();
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ContextGetTaskRunnerRequest::kMessageId: {
            auto req = gen::V8ContextGetTaskRunnerRequest::decode(payload.data(), payload.size());
            cef_v8_context_t* receiver = tables::v8Context.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_task_runner_t* rawResult = receiver->get_task_runner(receiver);

            gen::V8ContextGetTaskRunnerResponse resp;
            resp.result = insertOrRelease(&tables::taskRunner, rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ContextIsValidRequest::kMessageId: {
            auto req = gen::V8ContextIsValidRequest::decode(payload.data(), payload.size());
            cef_v8_context_t* receiver = tables::v8Context.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->is_valid(receiver);

            gen::V8ContextIsValidResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ContextGetBrowserRequest::kMessageId: {
            auto req = gen::V8ContextGetBrowserRequest::decode(payload.data(), payload.size());
            cef_v8_context_t* receiver = tables::v8Context.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_browser_t* rawResult = receiver->get_browser(receiver);

            gen::V8ContextGetBrowserResponse resp;
            resp.result = insertOrRelease(&tables::browser, rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ContextGetFrameRequest::kMessageId: {
            auto req = gen::V8ContextGetFrameRequest::decode(payload.data(), payload.size());
            cef_v8_context_t* receiver = tables::v8Context.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_frame_t* rawResult = receiver->get_frame(receiver);

            gen::V8ContextGetFrameResponse resp;
            resp.result = insertOrRelease(&tables::frame, rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ContextGetGlobalRequest::kMessageId: {
            auto req = gen::V8ContextGetGlobalRequest::decode(payload.data(), payload.size());
            cef_v8_context_t* receiver = tables::v8Context.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_v8_value_t* rawResult = receiver->get_global(receiver);

            gen::V8ContextGetGlobalResponse resp;
            resp.result = insertOrRelease(&tables::v8Value, rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ContextEnterRequest::kMessageId: {
            auto req = gen::V8ContextEnterRequest::decode(payload.data(), payload.size());
            cef_v8_context_t* receiver = tables::v8Context.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->enter(receiver);

            gen::V8ContextEnterResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ContextExitRequest::kMessageId: {
            auto req = gen::V8ContextExitRequest::decode(payload.data(), payload.size());
            cef_v8_context_t* receiver = tables::v8Context.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->exit(receiver);

            gen::V8ContextExitResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ContextIsSameRequest::kMessageId: {
            auto req = gen::V8ContextIsSameRequest::decode(payload.data(), payload.size());
            cef_v8_context_t* receiver = tables::v8Context.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* that_ptr = req.that != 0 ? tables::v8Context.retain(req.that) : nullptr;
                        int rawResult = receiver->is_same(receiver, that_ptr);
            if (that_ptr) {
                auto* that_base = reinterpret_cast<cef_base_ref_counted_t*>(that_ptr);
                that_base->release(that_base);
            }
            gen::V8ContextIsSameResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ExceptionGetMessageRequest::kMessageId: {
            auto req = gen::V8ExceptionGetMessageRequest::decode(payload.data(), payload.size());
            cef_v8_exception_t* receiver = tables::v8Exception.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_string_userfree_t rawResult = receiver->get_message(receiver);

            gen::V8ExceptionGetMessageResponse resp;
            resp.result = ScopedCefString::take(rawResult).toUtf8();
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ExceptionGetSourceLineRequest::kMessageId: {
            auto req = gen::V8ExceptionGetSourceLineRequest::decode(payload.data(), payload.size());
            cef_v8_exception_t* receiver = tables::v8Exception.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_string_userfree_t rawResult = receiver->get_source_line(receiver);

            gen::V8ExceptionGetSourceLineResponse resp;
            resp.result = ScopedCefString::take(rawResult).toUtf8();
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ExceptionGetScriptResourceNameRequest::kMessageId: {
            auto req = gen::V8ExceptionGetScriptResourceNameRequest::decode(payload.data(), payload.size());
            cef_v8_exception_t* receiver = tables::v8Exception.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_string_userfree_t rawResult = receiver->get_script_resource_name(receiver);

            gen::V8ExceptionGetScriptResourceNameResponse resp;
            resp.result = ScopedCefString::take(rawResult).toUtf8();
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ExceptionGetLineNumberRequest::kMessageId: {
            auto req = gen::V8ExceptionGetLineNumberRequest::decode(payload.data(), payload.size());
            cef_v8_exception_t* receiver = tables::v8Exception.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->get_line_number(receiver);

            gen::V8ExceptionGetLineNumberResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ExceptionGetStartPositionRequest::kMessageId: {
            auto req = gen::V8ExceptionGetStartPositionRequest::decode(payload.data(), payload.size());
            cef_v8_exception_t* receiver = tables::v8Exception.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->get_start_position(receiver);

            gen::V8ExceptionGetStartPositionResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ExceptionGetEndPositionRequest::kMessageId: {
            auto req = gen::V8ExceptionGetEndPositionRequest::decode(payload.data(), payload.size());
            cef_v8_exception_t* receiver = tables::v8Exception.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->get_end_position(receiver);

            gen::V8ExceptionGetEndPositionResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ExceptionGetStartColumnRequest::kMessageId: {
            auto req = gen::V8ExceptionGetStartColumnRequest::decode(payload.data(), payload.size());
            cef_v8_exception_t* receiver = tables::v8Exception.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->get_start_column(receiver);

            gen::V8ExceptionGetStartColumnResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ExceptionGetEndColumnRequest::kMessageId: {
            auto req = gen::V8ExceptionGetEndColumnRequest::decode(payload.data(), payload.size());
            cef_v8_exception_t* receiver = tables::v8Exception.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->get_end_column(receiver);

            gen::V8ExceptionGetEndColumnResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8BackingStoreByteLengthRequest::kMessageId: {
            auto req = gen::V8BackingStoreByteLengthRequest::decode(payload.data(), payload.size());
            cef_v8_backing_store_t* receiver = tables::v8BackingStore.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int64_t rawResult = receiver->byte_length(receiver);

            gen::V8BackingStoreByteLengthResponse resp;
            resp.result = static_cast<std::int64_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8BackingStoreIsValidRequest::kMessageId: {
            auto req = gen::V8BackingStoreIsValidRequest::decode(payload.data(), payload.size());
            cef_v8_backing_store_t* receiver = tables::v8BackingStore.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->is_valid(receiver);

            gen::V8BackingStoreIsValidResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueIsValidRequest::kMessageId: {
            auto req = gen::V8ValueIsValidRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->is_valid(receiver);

            gen::V8ValueIsValidResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueIsUndefinedRequest::kMessageId: {
            auto req = gen::V8ValueIsUndefinedRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->is_undefined(receiver);

            gen::V8ValueIsUndefinedResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueIsNullRequest::kMessageId: {
            auto req = gen::V8ValueIsNullRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->is_null(receiver);

            gen::V8ValueIsNullResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueIsBoolRequest::kMessageId: {
            auto req = gen::V8ValueIsBoolRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->is_bool(receiver);

            gen::V8ValueIsBoolResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueIsIntRequest::kMessageId: {
            auto req = gen::V8ValueIsIntRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->is_int(receiver);

            gen::V8ValueIsIntResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueIsUintRequest::kMessageId: {
            auto req = gen::V8ValueIsUintRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->is_uint(receiver);

            gen::V8ValueIsUintResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueIsDoubleRequest::kMessageId: {
            auto req = gen::V8ValueIsDoubleRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->is_double(receiver);

            gen::V8ValueIsDoubleResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueIsDateRequest::kMessageId: {
            auto req = gen::V8ValueIsDateRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->is_date(receiver);

            gen::V8ValueIsDateResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueIsStringRequest::kMessageId: {
            auto req = gen::V8ValueIsStringRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->is_string(receiver);

            gen::V8ValueIsStringResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueIsObjectRequest::kMessageId: {
            auto req = gen::V8ValueIsObjectRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->is_object(receiver);

            gen::V8ValueIsObjectResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueIsArrayRequest::kMessageId: {
            auto req = gen::V8ValueIsArrayRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->is_array(receiver);

            gen::V8ValueIsArrayResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueIsArrayBufferRequest::kMessageId: {
            auto req = gen::V8ValueIsArrayBufferRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->is_array_buffer(receiver);

            gen::V8ValueIsArrayBufferResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueIsFunctionRequest::kMessageId: {
            auto req = gen::V8ValueIsFunctionRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->is_function(receiver);

            gen::V8ValueIsFunctionResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueIsPromiseRequest::kMessageId: {
            auto req = gen::V8ValueIsPromiseRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->is_promise(receiver);

            gen::V8ValueIsPromiseResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueIsSameRequest::kMessageId: {
            auto req = gen::V8ValueIsSameRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_value_t* that_ptr = req.that != 0 ? tables::v8Value.retain(req.that) : nullptr;
                        int rawResult = receiver->is_same(receiver, that_ptr);
            if (that_ptr) {
                auto* that_base = reinterpret_cast<cef_base_ref_counted_t*>(that_ptr);
                that_base->release(that_base);
            }
            gen::V8ValueIsSameResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueGetBoolValueRequest::kMessageId: {
            auto req = gen::V8ValueGetBoolValueRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->get_bool_value(receiver);

            gen::V8ValueGetBoolValueResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueGetIntValueRequest::kMessageId: {
            auto req = gen::V8ValueGetIntValueRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->get_int_value(receiver);

            gen::V8ValueGetIntValueResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueGetUintValueRequest::kMessageId: {
            auto req = gen::V8ValueGetUintValueRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->get_uint_value(receiver);

            gen::V8ValueGetUintValueResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueGetStringValueRequest::kMessageId: {
            auto req = gen::V8ValueGetStringValueRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_string_userfree_t rawResult = receiver->get_string_value(receiver);

            gen::V8ValueGetStringValueResponse resp;
            resp.result = ScopedCefString::take(rawResult).toUtf8();
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueIsUserCreatedRequest::kMessageId: {
            auto req = gen::V8ValueIsUserCreatedRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->is_user_created(receiver);

            gen::V8ValueIsUserCreatedResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueHasExceptionRequest::kMessageId: {
            auto req = gen::V8ValueHasExceptionRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->has_exception(receiver);

            gen::V8ValueHasExceptionResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueGetExceptionRequest::kMessageId: {
            auto req = gen::V8ValueGetExceptionRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_v8_exception_t* rawResult = receiver->get_exception(receiver);

            gen::V8ValueGetExceptionResponse resp;
            resp.result = insertOrRelease(&tables::v8Exception, rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueClearExceptionRequest::kMessageId: {
            auto req = gen::V8ValueClearExceptionRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->clear_exception(receiver);

            gen::V8ValueClearExceptionResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueWillRethrowExceptionsRequest::kMessageId: {
            auto req = gen::V8ValueWillRethrowExceptionsRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->will_rethrow_exceptions(receiver);

            gen::V8ValueWillRethrowExceptionsResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueSetRethrowExceptionsRequest::kMessageId: {
            auto req = gen::V8ValueSetRethrowExceptionsRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->set_rethrow_exceptions(receiver, net_kurobako_cef4j_ipc_protocol_gen_dispatcher::cefArg<decltype(receiver->set_rethrow_exceptions), 1>(req.rethrow));

            gen::V8ValueSetRethrowExceptionsResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueHasValueBykeyRequest::kMessageId: {
            auto req = gen::V8ValueHasValueBykeyRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            ScopedCefString key_cef(req.key);
                        int rawResult = receiver->has_value_bykey(receiver, key_cef.get());

            gen::V8ValueHasValueBykeyResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueHasValueByindexRequest::kMessageId: {
            auto req = gen::V8ValueHasValueByindexRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->has_value_byindex(receiver, net_kurobako_cef4j_ipc_protocol_gen_dispatcher::cefArg<decltype(receiver->has_value_byindex), 1>(req.index));

            gen::V8ValueHasValueByindexResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueDeleteValueBykeyRequest::kMessageId: {
            auto req = gen::V8ValueDeleteValueBykeyRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            ScopedCefString key_cef(req.key);
                        int rawResult = receiver->delete_value_bykey(receiver, key_cef.get());

            gen::V8ValueDeleteValueBykeyResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueDeleteValueByindexRequest::kMessageId: {
            auto req = gen::V8ValueDeleteValueByindexRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->delete_value_byindex(receiver, net_kurobako_cef4j_ipc_protocol_gen_dispatcher::cefArg<decltype(receiver->delete_value_byindex), 1>(req.index));

            gen::V8ValueDeleteValueByindexResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueGetValueBykeyRequest::kMessageId: {
            auto req = gen::V8ValueGetValueBykeyRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            ScopedCefString key_cef(req.key);
                        cef_v8_value_t* rawResult = receiver->get_value_bykey(receiver, key_cef.get());

            gen::V8ValueGetValueBykeyResponse resp;
            resp.result = insertOrRelease(&tables::v8Value, rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueGetValueByindexRequest::kMessageId: {
            auto req = gen::V8ValueGetValueByindexRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_v8_value_t* rawResult = receiver->get_value_byindex(receiver, net_kurobako_cef4j_ipc_protocol_gen_dispatcher::cefArg<decltype(receiver->get_value_byindex), 1>(req.index));

            gen::V8ValueGetValueByindexResponse resp;
            resp.result = insertOrRelease(&tables::v8Value, rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueSetValueBykeyRequest::kMessageId: {
            auto req = gen::V8ValueSetValueBykeyRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            ScopedCefString key_cef(req.key);
            cef_v8_value_t* value_ptr = req.value != 0 ? tables::v8Value.retain(req.value) : nullptr;
                        int rawResult = receiver->set_value_bykey(receiver, key_cef.get(), value_ptr, net_kurobako_cef4j_ipc_protocol_gen_dispatcher::cefArg<decltype(receiver->set_value_bykey), 3>(req.attribute));
            if (value_ptr) {
                auto* value_base = reinterpret_cast<cef_base_ref_counted_t*>(value_ptr);
                value_base->release(value_base);
            }
            gen::V8ValueSetValueBykeyResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueSetValueByindexRequest::kMessageId: {
            auto req = gen::V8ValueSetValueByindexRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_value_t* value_ptr = req.value != 0 ? tables::v8Value.retain(req.value) : nullptr;
                        int rawResult = receiver->set_value_byindex(receiver, net_kurobako_cef4j_ipc_protocol_gen_dispatcher::cefArg<decltype(receiver->set_value_byindex), 1>(req.index), value_ptr);
            if (value_ptr) {
                auto* value_base = reinterpret_cast<cef_base_ref_counted_t*>(value_ptr);
                value_base->release(value_base);
            }
            gen::V8ValueSetValueByindexResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueSetValueByaccessorRequest::kMessageId: {
            auto req = gen::V8ValueSetValueByaccessorRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            ScopedCefString key_cef(req.key);
                        int rawResult = receiver->set_value_byaccessor(receiver, key_cef.get(), net_kurobako_cef4j_ipc_protocol_gen_dispatcher::cefArg<decltype(receiver->set_value_byaccessor), 2>(req.attribute));

            gen::V8ValueSetValueByaccessorResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueGetExternallyAllocatedMemoryRequest::kMessageId: {
            auto req = gen::V8ValueGetExternallyAllocatedMemoryRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->get_externally_allocated_memory(receiver);

            gen::V8ValueGetExternallyAllocatedMemoryResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueAdjustExternallyAllocatedMemoryRequest::kMessageId: {
            auto req = gen::V8ValueAdjustExternallyAllocatedMemoryRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->adjust_externally_allocated_memory(receiver, net_kurobako_cef4j_ipc_protocol_gen_dispatcher::cefArg<decltype(receiver->adjust_externally_allocated_memory), 1>(req.changeInBytes));

            gen::V8ValueAdjustExternallyAllocatedMemoryResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueGetArrayLengthRequest::kMessageId: {
            auto req = gen::V8ValueGetArrayLengthRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->get_array_length(receiver);

            gen::V8ValueGetArrayLengthResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueNeuterArrayBufferRequest::kMessageId: {
            auto req = gen::V8ValueNeuterArrayBufferRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->neuter_array_buffer(receiver);

            gen::V8ValueNeuterArrayBufferResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueGetArrayBufferByteLengthRequest::kMessageId: {
            auto req = gen::V8ValueGetArrayBufferByteLengthRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int64_t rawResult = receiver->get_array_buffer_byte_length(receiver);

            gen::V8ValueGetArrayBufferByteLengthResponse resp;
            resp.result = static_cast<std::int64_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueGetFunctionNameRequest::kMessageId: {
            auto req = gen::V8ValueGetFunctionNameRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_string_userfree_t rawResult = receiver->get_function_name(receiver);

            gen::V8ValueGetFunctionNameResponse resp;
            resp.result = ScopedCefString::take(rawResult).toUtf8();
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueResolvePromiseRequest::kMessageId: {
            auto req = gen::V8ValueResolvePromiseRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_value_t* arg_ptr = req.arg != 0 ? tables::v8Value.retain(req.arg) : nullptr;
                        int rawResult = receiver->resolve_promise(receiver, arg_ptr);
            if (arg_ptr) {
                auto* arg_base = reinterpret_cast<cef_base_ref_counted_t*>(arg_ptr);
                arg_base->release(arg_base);
            }
            gen::V8ValueResolvePromiseResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8ValueRejectPromiseRequest::kMessageId: {
            auto req = gen::V8ValueRejectPromiseRequest::decode(payload.data(), payload.size());
            cef_v8_value_t* receiver = tables::v8Value.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            ScopedCefString errorMsg_cef(req.errorMsg);
                        int rawResult = receiver->reject_promise(receiver, errorMsg_cef.get());

            gen::V8ValueRejectPromiseResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8StackTraceIsValidRequest::kMessageId: {
            auto req = gen::V8StackTraceIsValidRequest::decode(payload.data(), payload.size());
            cef_v8_stack_trace_t* receiver = tables::v8StackTrace.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->is_valid(receiver);

            gen::V8StackTraceIsValidResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8StackTraceGetFrameCountRequest::kMessageId: {
            auto req = gen::V8StackTraceGetFrameCountRequest::decode(payload.data(), payload.size());
            cef_v8_stack_trace_t* receiver = tables::v8StackTrace.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->get_frame_count(receiver);

            gen::V8StackTraceGetFrameCountResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8StackTraceGetFrameRequest::kMessageId: {
            auto req = gen::V8StackTraceGetFrameRequest::decode(payload.data(), payload.size());
            cef_v8_stack_trace_t* receiver = tables::v8StackTrace.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_v8_stack_frame_t* rawResult = receiver->get_frame(receiver, net_kurobako_cef4j_ipc_protocol_gen_dispatcher::cefArg<decltype(receiver->get_frame), 1>(req.index));

            gen::V8StackTraceGetFrameResponse resp;
            resp.result = insertOrRelease(&tables::v8StackFrame, rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8StackFrameIsValidRequest::kMessageId: {
            auto req = gen::V8StackFrameIsValidRequest::decode(payload.data(), payload.size());
            cef_v8_stack_frame_t* receiver = tables::v8StackFrame.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->is_valid(receiver);

            gen::V8StackFrameIsValidResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8StackFrameGetScriptNameRequest::kMessageId: {
            auto req = gen::V8StackFrameGetScriptNameRequest::decode(payload.data(), payload.size());
            cef_v8_stack_frame_t* receiver = tables::v8StackFrame.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_string_userfree_t rawResult = receiver->get_script_name(receiver);

            gen::V8StackFrameGetScriptNameResponse resp;
            resp.result = ScopedCefString::take(rawResult).toUtf8();
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8StackFrameGetScriptNameOrSourceUrlRequest::kMessageId: {
            auto req = gen::V8StackFrameGetScriptNameOrSourceUrlRequest::decode(payload.data(), payload.size());
            cef_v8_stack_frame_t* receiver = tables::v8StackFrame.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_string_userfree_t rawResult = receiver->get_script_name_or_source_url(receiver);

            gen::V8StackFrameGetScriptNameOrSourceUrlResponse resp;
            resp.result = ScopedCefString::take(rawResult).toUtf8();
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8StackFrameGetFunctionNameRequest::kMessageId: {
            auto req = gen::V8StackFrameGetFunctionNameRequest::decode(payload.data(), payload.size());
            cef_v8_stack_frame_t* receiver = tables::v8StackFrame.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        cef_string_userfree_t rawResult = receiver->get_function_name(receiver);

            gen::V8StackFrameGetFunctionNameResponse resp;
            resp.result = ScopedCefString::take(rawResult).toUtf8();
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8StackFrameGetLineNumberRequest::kMessageId: {
            auto req = gen::V8StackFrameGetLineNumberRequest::decode(payload.data(), payload.size());
            cef_v8_stack_frame_t* receiver = tables::v8StackFrame.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->get_line_number(receiver);

            gen::V8StackFrameGetLineNumberResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8StackFrameGetColumnRequest::kMessageId: {
            auto req = gen::V8StackFrameGetColumnRequest::decode(payload.data(), payload.size());
            cef_v8_stack_frame_t* receiver = tables::v8StackFrame.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->get_column(receiver);

            gen::V8StackFrameGetColumnResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8StackFrameIsEvalRequest::kMessageId: {
            auto req = gen::V8StackFrameIsEvalRequest::decode(payload.data(), payload.size());
            cef_v8_stack_frame_t* receiver = tables::v8StackFrame.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->is_eval(receiver);

            gen::V8StackFrameIsEvalResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        case gen::V8StackFrameIsConstructorRequest::kMessageId: {
            auto req = gen::V8StackFrameIsConstructorRequest::decode(payload.data(), payload.size());
            cef_v8_stack_frame_t* receiver = tables::v8StackFrame.retain(req.self);
            if (!receiver) {
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
            cef_v8_context_t* rawCtx = frame->get_v8_context(frame);
            ScopedV8Context v8ctx(rawCtx);
            if (!v8ctx.entered()) {
                auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                recvBase->release(recvBase);
                sendReceiverGone(frame, corrId, messageId);
                return true;
            }
                        int rawResult = receiver->is_constructor(receiver);

            gen::V8StackFrameIsConstructorResponse resp;
            resp.result = static_cast<std::int32_t>(rawResult);
            std::vector<std::uint8_t> respPayload(resp.encodedSize());
            resp.encodeInto(respPayload.data());
            sendResponseEnvelope(frame, "cef4j_renderer_resp", corrId, messageId,
                                  respPayload.data(), respPayload.size());
            auto* recvBase = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
            recvBase->release(recvBase);
            return true;
        }
        default:
            return false;
    }
}

} // namespace net_kurobako_cef4j_ipc_protocol_gen_renderer_dispatcher

#endif // CEF4J_IPC_GEN_RENDERER_DISPATCHER_H_
