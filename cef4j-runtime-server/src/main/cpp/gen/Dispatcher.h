// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
//
// Dispatchable AST methods covered: 815
// Methods skipped (RemoteHandle params/return, byte arrays, etc.): 25
// Renderer-affinity methods relayed to renderer subprocess: 116
#ifndef CEF4J_IPC_GEN_DISPATCHER_H_
#define CEF4J_IPC_GEN_DISPATCHER_H_

#include <atomic>
#include <cstdint>
#include <cstring>
#include <functional>
#include <string>
#include <tuple>
#include <utility>
#include <vector>

#include "include/capi/cef_base_capi.h"
#include "include/capi/cef_frame_capi.h"
#include "include/capi/cef_process_message_capi.h"
#include "include/capi/cef_task_capi.h"
#include "include/internal/cef_string.h"
#include "include/internal/cef_string_list.h"
#include "include/capi/cef_accessibility_handler_capi.h"
#include "include/capi/cef_app_capi.h"
#include "include/capi/cef_audio_handler_capi.h"
#include "include/capi/cef_auth_callback_capi.h"
#include "include/capi/cef_base_capi.h"
#include "include/capi/cef_browser_capi.h"
#include "include/capi/cef_browser_process_handler_capi.h"
#include "include/capi/cef_callback_capi.h"
#include "include/capi/cef_client_capi.h"
#include "include/capi/cef_command_handler_capi.h"
#include "include/capi/cef_command_line_capi.h"
#include "include/capi/cef_component_updater_capi.h"
#include "include/capi/cef_context_menu_handler_capi.h"
#include "include/capi/cef_cookie_capi.h"
#include "include/capi/cef_crash_util_capi.h"
#include "include/capi/cef_devtools_message_observer_capi.h"
#include "include/capi/cef_dialog_handler_capi.h"
#include "include/capi/cef_display_handler_capi.h"
#include "include/capi/cef_dom_capi.h"
#include "include/capi/cef_download_handler_capi.h"
#include "include/capi/cef_download_item_capi.h"
#include "include/capi/cef_drag_data_capi.h"
#include "include/capi/cef_drag_handler_capi.h"
#include "include/capi/cef_file_util_capi.h"
#include "include/capi/cef_find_handler_capi.h"
#include "include/capi/cef_focus_handler_capi.h"
#include "include/capi/cef_frame_capi.h"
#include "include/capi/cef_frame_handler_capi.h"
#include "include/capi/cef_i18n_util_capi.h"
#include "include/capi/cef_image_capi.h"
#include "include/capi/cef_jsdialog_handler_capi.h"
#include "include/capi/cef_keyboard_handler_capi.h"
#include "include/capi/cef_life_span_handler_capi.h"
#include "include/capi/cef_load_handler_capi.h"
#include "include/capi/cef_media_router_capi.h"
#include "include/capi/cef_menu_model_capi.h"
#include "include/capi/cef_menu_model_delegate_capi.h"
#include "include/capi/cef_navigation_entry_capi.h"
#include "include/capi/cef_origin_whitelist_capi.h"
#include "include/capi/cef_parser_capi.h"
#include "include/capi/cef_path_util_capi.h"
#include "include/capi/cef_permission_handler_capi.h"
#include "include/capi/cef_preference_capi.h"
#include "include/capi/cef_print_handler_capi.h"
#include "include/capi/cef_print_settings_capi.h"
#include "include/capi/cef_process_message_capi.h"
#include "include/capi/cef_process_util_capi.h"
#include "include/capi/cef_registration_capi.h"
#include "include/capi/cef_render_handler_capi.h"
#include "include/capi/cef_render_process_handler_capi.h"
#include "include/capi/cef_request_capi.h"
#include "include/capi/cef_request_context_capi.h"
#include "include/capi/cef_request_context_handler_capi.h"
#include "include/capi/cef_request_handler_capi.h"
#include "include/capi/cef_resource_bundle_capi.h"
#include "include/capi/cef_resource_bundle_handler_capi.h"
#include "include/capi/cef_resource_handler_capi.h"
#include "include/capi/cef_resource_request_handler_capi.h"
#include "include/capi/cef_response_capi.h"
#include "include/capi/cef_response_filter_capi.h"
#include "include/capi/cef_scheme_capi.h"
#include "include/capi/cef_server_capi.h"
#include "include/capi/cef_shared_memory_region_capi.h"
#include "include/capi/cef_shared_process_message_builder_capi.h"
#include "include/capi/cef_ssl_info_capi.h"
#include "include/capi/cef_ssl_status_capi.h"
#include "include/capi/cef_stream_capi.h"
#include "include/capi/cef_string_visitor_capi.h"
#include "include/capi/cef_task_capi.h"
#include "include/capi/cef_task_manager_capi.h"
#include "include/capi/cef_thread_capi.h"
#include "include/capi/cef_trace_capi.h"
#include "include/capi/cef_unresponsive_process_callback_capi.h"
#include "include/capi/cef_urlrequest_capi.h"
#include "include/capi/cef_v8_capi.h"
#include "include/capi/cef_values_capi.h"
#include "include/capi/cef_waitable_event_capi.h"
#include "include/capi/cef_x509_certificate_capi.h"
#include "include/capi/cef_xml_reader_capi.h"
#include "include/capi/cef_zip_reader_capi.h"
#include "include/capi/views/cef_box_layout_capi.h"
#include "include/capi/views/cef_browser_view_capi.h"
#include "include/capi/views/cef_browser_view_delegate_capi.h"
#include "include/capi/views/cef_button_capi.h"
#include "include/capi/views/cef_button_delegate_capi.h"
#include "include/capi/views/cef_display_capi.h"
#include "include/capi/views/cef_fill_layout_capi.h"
#include "include/capi/views/cef_label_button_capi.h"
#include "include/capi/views/cef_layout_capi.h"
#include "include/capi/views/cef_menu_button_capi.h"
#include "include/capi/views/cef_menu_button_delegate_capi.h"
#include "include/capi/views/cef_overlay_controller_capi.h"
#include "include/capi/views/cef_panel_capi.h"
#include "include/capi/views/cef_panel_delegate_capi.h"
#include "include/capi/views/cef_scroll_view_capi.h"
#include "include/capi/views/cef_textfield_capi.h"
#include "include/capi/views/cef_textfield_delegate_capi.h"
#include "include/capi/views/cef_view_capi.h"
#include "include/capi/views/cef_view_delegate_capi.h"
#include "include/capi/views/cef_window_capi.h"
#include "include/capi/views/cef_window_delegate_capi.h"

#include "Envelope.h"
#include "HandleTable.h"
#include "IpcServer.h"

#include "JvmVisitors.h"

#include "AuthCallbackCancelRequest.h"
#include "AuthCallbackCancelResponse.h"
#include "AuthCallbackContRequest.h"
#include "AuthCallbackContResponse.h"
#include "BeforeDownloadCallbackContRequest.h"
#include "BeforeDownloadCallbackContResponse.h"
#include "BinaryValueCopyRequest.h"
#include "BinaryValueCopyResponse.h"
#include "BinaryValueGetSizeRequest.h"
#include "BinaryValueGetSizeResponse.h"
#include "BinaryValueIsEqualRequest.h"
#include "BinaryValueIsEqualResponse.h"
#include "BinaryValueIsOwnedRequest.h"
#include "BinaryValueIsOwnedResponse.h"
#include "BinaryValueIsSameRequest.h"
#include "BinaryValueIsSameResponse.h"
#include "BinaryValueIsValidRequest.h"
#include "BinaryValueIsValidResponse.h"
#include "BoxLayoutClearFlexForViewRequest.h"
#include "BoxLayoutClearFlexForViewResponse.h"
#include "BoxLayoutSetFlexForViewRequest.h"
#include "BoxLayoutSetFlexForViewResponse.h"
#include "BrowserCanGoBackRequest.h"
#include "BrowserCanGoBackResponse.h"
#include "BrowserCanGoForwardRequest.h"
#include "BrowserCanGoForwardResponse.h"
#include "BrowserGetFocusedFrameRequest.h"
#include "BrowserGetFocusedFrameResponse.h"
#include "BrowserGetFrameByIdentifierRequest.h"
#include "BrowserGetFrameByIdentifierResponse.h"
#include "BrowserGetFrameByNameRequest.h"
#include "BrowserGetFrameByNameResponse.h"
#include "BrowserGetFrameCountRequest.h"
#include "BrowserGetFrameCountResponse.h"
#include "BrowserGetFrameIdentifiersRequest.h"
#include "BrowserGetFrameIdentifiersResponse.h"
#include "BrowserGetFrameNamesRequest.h"
#include "BrowserGetFrameNamesResponse.h"
#include "BrowserGetHostRequest.h"
#include "BrowserGetHostResponse.h"
#include "BrowserGetIdentifierRequest.h"
#include "BrowserGetIdentifierResponse.h"
#include "BrowserGetMainFrameRequest.h"
#include "BrowserGetMainFrameResponse.h"
#include "BrowserGoBackRequest.h"
#include "BrowserGoBackResponse.h"
#include "BrowserGoForwardRequest.h"
#include "BrowserGoForwardResponse.h"
#include "BrowserHasDocumentRequest.h"
#include "BrowserHasDocumentResponse.h"
#include "BrowserHostAddWordToDictionaryRequest.h"
#include "BrowserHostAddWordToDictionaryResponse.h"
#include "BrowserHostCanExecuteChromeCommandRequest.h"
#include "BrowserHostCanExecuteChromeCommandResponse.h"
#include "BrowserHostCanZoomRequest.h"
#include "BrowserHostCanZoomResponse.h"
#include "BrowserHostCloseBrowserRequest.h"
#include "BrowserHostCloseBrowserResponse.h"
#include "BrowserHostCloseDevToolsRequest.h"
#include "BrowserHostCloseDevToolsResponse.h"
#include "BrowserHostDragSourceEndedAtRequest.h"
#include "BrowserHostDragSourceEndedAtResponse.h"
#include "BrowserHostDragSourceSystemDragEndedRequest.h"
#include "BrowserHostDragSourceSystemDragEndedResponse.h"
#include "BrowserHostDragTargetDragEnterRequest.h"
#include "BrowserHostDragTargetDragEnterResponse.h"
#include "BrowserHostDragTargetDragLeaveRequest.h"
#include "BrowserHostDragTargetDragLeaveResponse.h"
#include "BrowserHostDragTargetDragOverRequest.h"
#include "BrowserHostDragTargetDragOverResponse.h"
#include "BrowserHostDragTargetDropRequest.h"
#include "BrowserHostDragTargetDropResponse.h"
#include "BrowserHostExecuteChromeCommandRequest.h"
#include "BrowserHostExecuteChromeCommandResponse.h"
#include "BrowserHostExecuteDevToolsMethodRequest.h"
#include "BrowserHostExecuteDevToolsMethodResponse.h"
#include "BrowserHostExitFullscreenRequest.h"
#include "BrowserHostExitFullscreenResponse.h"
#include "BrowserHostFindRequest.h"
#include "BrowserHostFindResponse.h"
#include "BrowserHostGetBrowserRequest.h"
#include "BrowserHostGetBrowserResponse.h"
#include "BrowserHostGetOpenerIdentifierRequest.h"
#include "BrowserHostGetOpenerIdentifierResponse.h"
#include "BrowserHostGetOpenerWindowHandleRequest.h"
#include "BrowserHostGetOpenerWindowHandleResponse.h"
#include "BrowserHostGetRequestContextRequest.h"
#include "BrowserHostGetRequestContextResponse.h"
#include "BrowserHostGetRuntimeStyleRequest.h"
#include "BrowserHostGetRuntimeStyleResponse.h"
#include "BrowserHostGetVisibleNavigationEntryRequest.h"
#include "BrowserHostGetVisibleNavigationEntryResponse.h"
#include "BrowserHostGetWindowHandleRequest.h"
#include "BrowserHostGetWindowHandleResponse.h"
#include "BrowserHostGetWindowlessFrameRateRequest.h"
#include "BrowserHostGetWindowlessFrameRateResponse.h"
#include "BrowserHostHasDevToolsRequest.h"
#include "BrowserHostHasDevToolsResponse.h"
#include "BrowserHostHasViewRequest.h"
#include "BrowserHostHasViewResponse.h"
#include "BrowserHostImeCancelCompositionRequest.h"
#include "BrowserHostImeCancelCompositionResponse.h"
#include "BrowserHostImeCommitTextRequest.h"
#include "BrowserHostImeCommitTextResponse.h"
#include "BrowserHostImeFinishComposingTextRequest.h"
#include "BrowserHostImeFinishComposingTextResponse.h"
#include "BrowserHostInvalidateRequest.h"
#include "BrowserHostInvalidateResponse.h"
#include "BrowserHostIsAudioMutedRequest.h"
#include "BrowserHostIsAudioMutedResponse.h"
#include "BrowserHostIsFullscreenRequest.h"
#include "BrowserHostIsFullscreenResponse.h"
#include "BrowserHostIsReadyToBeClosedRequest.h"
#include "BrowserHostIsReadyToBeClosedResponse.h"
#include "BrowserHostIsRenderProcessUnresponsiveRequest.h"
#include "BrowserHostIsRenderProcessUnresponsiveResponse.h"
#include "BrowserHostIsWindowRenderingDisabledRequest.h"
#include "BrowserHostIsWindowRenderingDisabledResponse.h"
#include "BrowserHostNotifyMoveOrResizeStartedRequest.h"
#include "BrowserHostNotifyMoveOrResizeStartedResponse.h"
#include "BrowserHostNotifyScreenInfoChangedRequest.h"
#include "BrowserHostNotifyScreenInfoChangedResponse.h"
#include "BrowserHostPrintRequest.h"
#include "BrowserHostPrintResponse.h"
#include "BrowserHostReplaceMisspellingRequest.h"
#include "BrowserHostReplaceMisspellingResponse.h"
#include "BrowserHostSendCaptureLostEventRequest.h"
#include "BrowserHostSendCaptureLostEventResponse.h"
#include "BrowserHostSendDevToolsMessageRequest.h"
#include "BrowserHostSendDevToolsMessageResponse.h"
#include "BrowserHostSendExternalBeginFrameRequest.h"
#include "BrowserHostSendExternalBeginFrameResponse.h"
#include "BrowserHostSendKeyEventRequest.h"
#include "BrowserHostSendKeyEventResponse.h"
#include "BrowserHostSendMouseClickEventRequest.h"
#include "BrowserHostSendMouseClickEventResponse.h"
#include "BrowserHostSendMouseMoveEventRequest.h"
#include "BrowserHostSendMouseMoveEventResponse.h"
#include "BrowserHostSendMouseWheelEventRequest.h"
#include "BrowserHostSendMouseWheelEventResponse.h"
#include "BrowserHostSetAccessibilityStateRequest.h"
#include "BrowserHostSetAccessibilityStateResponse.h"
#include "BrowserHostSetAudioMutedRequest.h"
#include "BrowserHostSetAudioMutedResponse.h"
#include "BrowserHostSetAutoResizeEnabledRequest.h"
#include "BrowserHostSetAutoResizeEnabledResponse.h"
#include "BrowserHostSetFocusRequest.h"
#include "BrowserHostSetFocusResponse.h"
#include "BrowserHostSetWindowlessFrameRateRequest.h"
#include "BrowserHostSetWindowlessFrameRateResponse.h"
#include "BrowserHostStartDownloadRequest.h"
#include "BrowserHostStartDownloadResponse.h"
#include "BrowserHostStopFindingRequest.h"
#include "BrowserHostStopFindingResponse.h"
#include "BrowserHostTryCloseBrowserRequest.h"
#include "BrowserHostTryCloseBrowserResponse.h"
#include "BrowserHostWasHiddenRequest.h"
#include "BrowserHostWasHiddenResponse.h"
#include "BrowserHostWasResizedRequest.h"
#include "BrowserHostWasResizedResponse.h"
#include "BrowserHostZoomRequest.h"
#include "BrowserHostZoomResponse.h"
#include "BrowserIsLoadingRequest.h"
#include "BrowserIsLoadingResponse.h"
#include "BrowserIsPopupRequest.h"
#include "BrowserIsPopupResponse.h"
#include "BrowserIsSameRequest.h"
#include "BrowserIsSameResponse.h"
#include "BrowserIsValidRequest.h"
#include "BrowserIsValidResponse.h"
#include "BrowserReloadIgnoreCacheRequest.h"
#include "BrowserReloadIgnoreCacheResponse.h"
#include "BrowserReloadRequest.h"
#include "BrowserReloadResponse.h"
#include "BrowserStopLoadRequest.h"
#include "BrowserStopLoadResponse.h"
#include "BrowserViewDelegateAllowMoveForPictureInPictureRequest.h"
#include "BrowserViewDelegateAllowMoveForPictureInPictureResponse.h"
#include "BrowserViewDelegateAllowPictureInPictureWithoutUserActivationRequest.h"
#include "BrowserViewDelegateAllowPictureInPictureWithoutUserActivationResponse.h"
#include "BrowserViewDelegateGetBrowserRuntimeStyleRequest.h"
#include "BrowserViewDelegateGetBrowserRuntimeStyleResponse.h"
#include "BrowserViewDelegateGetChromeToolbarTypeRequest.h"
#include "BrowserViewDelegateGetChromeToolbarTypeResponse.h"
#include "BrowserViewDelegateOnBrowserCreatedRequest.h"
#include "BrowserViewDelegateOnBrowserCreatedResponse.h"
#include "BrowserViewDelegateOnBrowserDestroyedRequest.h"
#include "BrowserViewDelegateOnBrowserDestroyedResponse.h"
#include "BrowserViewDelegateOnGestureCommandRequest.h"
#include "BrowserViewDelegateOnGestureCommandResponse.h"
#include "BrowserViewDelegateOnPopupBrowserViewCreatedRequest.h"
#include "BrowserViewDelegateOnPopupBrowserViewCreatedResponse.h"
#include "BrowserViewDelegateUseFramelessWindowForPictureInPictureRequest.h"
#include "BrowserViewDelegateUseFramelessWindowForPictureInPictureResponse.h"
#include "BrowserViewGetBrowserRequest.h"
#include "BrowserViewGetBrowserResponse.h"
#include "BrowserViewGetChromeToolbarRequest.h"
#include "BrowserViewGetChromeToolbarResponse.h"
#include "BrowserViewGetRuntimeStyleRequest.h"
#include "BrowserViewGetRuntimeStyleResponse.h"
#include "BrowserViewSetPreferAcceleratorsRequest.h"
#include "BrowserViewSetPreferAcceleratorsResponse.h"
#include "ButtonAsLabelButtonRequest.h"
#include "ButtonAsLabelButtonResponse.h"
#include "ButtonDelegateOnButtonPressedRequest.h"
#include "ButtonDelegateOnButtonPressedResponse.h"
#include "ButtonDelegateOnButtonStateChangedRequest.h"
#include "ButtonDelegateOnButtonStateChangedResponse.h"
#include "ButtonGetStateRequest.h"
#include "ButtonGetStateResponse.h"
#include "ButtonSetAccessibleNameRequest.h"
#include "ButtonSetAccessibleNameResponse.h"
#include "ButtonSetInkDropEnabledRequest.h"
#include "ButtonSetInkDropEnabledResponse.h"
#include "ButtonSetStateRequest.h"
#include "ButtonSetStateResponse.h"
#include "ButtonSetTooltipTextRequest.h"
#include "ButtonSetTooltipTextResponse.h"
#include "CallbackCancelRequest.h"
#include "CallbackCancelResponse.h"
#include "CallbackContRequest.h"
#include "CallbackContResponse.h"
#include "CommandLineAppendArgumentRequest.h"
#include "CommandLineAppendArgumentResponse.h"
#include "CommandLineAppendSwitchRequest.h"
#include "CommandLineAppendSwitchResponse.h"
#include "CommandLineAppendSwitchWithValueRequest.h"
#include "CommandLineAppendSwitchWithValueResponse.h"
#include "CommandLineCopyRequest.h"
#include "CommandLineCopyResponse.h"
#include "CommandLineGetArgumentsRequest.h"
#include "CommandLineGetArgumentsResponse.h"
#include "CommandLineGetArgvRequest.h"
#include "CommandLineGetArgvResponse.h"
#include "CommandLineGetCommandLineStringRequest.h"
#include "CommandLineGetCommandLineStringResponse.h"
#include "CommandLineGetProgramRequest.h"
#include "CommandLineGetProgramResponse.h"
#include "CommandLineGetSwitchValueRequest.h"
#include "CommandLineGetSwitchValueResponse.h"
#include "CommandLineHasArgumentsRequest.h"
#include "CommandLineHasArgumentsResponse.h"
#include "CommandLineHasSwitchRequest.h"
#include "CommandLineHasSwitchResponse.h"
#include "CommandLineHasSwitchesRequest.h"
#include "CommandLineHasSwitchesResponse.h"
#include "CommandLineInitFromStringRequest.h"
#include "CommandLineInitFromStringResponse.h"
#include "CommandLineIsReadOnlyRequest.h"
#include "CommandLineIsReadOnlyResponse.h"
#include "CommandLineIsValidRequest.h"
#include "CommandLineIsValidResponse.h"
#include "CommandLinePrependWrapperRequest.h"
#include "CommandLinePrependWrapperResponse.h"
#include "CommandLineRemoveSwitchRequest.h"
#include "CommandLineRemoveSwitchResponse.h"
#include "CommandLineResetRequest.h"
#include "CommandLineResetResponse.h"
#include "CommandLineSetProgramRequest.h"
#include "CommandLineSetProgramResponse.h"
#include "ComponentGetIdRequest.h"
#include "ComponentGetIdResponse.h"
#include "ComponentGetNameRequest.h"
#include "ComponentGetNameResponse.h"
#include "ComponentGetStateRequest.h"
#include "ComponentGetStateResponse.h"
#include "ComponentGetVersionRequest.h"
#include "ComponentGetVersionResponse.h"
#include "ComponentUpdaterGetComponentByIdRequest.h"
#include "ComponentUpdaterGetComponentByIdResponse.h"
#include "ComponentUpdaterGetComponentCountRequest.h"
#include "ComponentUpdaterGetComponentCountResponse.h"
#include "ComponentUpdaterUpdateRequest.h"
#include "ComponentUpdaterUpdateResponse.h"
#include "ContextMenuParamsGetDictionarySuggestionsRequest.h"
#include "ContextMenuParamsGetDictionarySuggestionsResponse.h"
#include "ContextMenuParamsGetEditStateFlagsRequest.h"
#include "ContextMenuParamsGetEditStateFlagsResponse.h"
#include "ContextMenuParamsGetFrameCharsetRequest.h"
#include "ContextMenuParamsGetFrameCharsetResponse.h"
#include "ContextMenuParamsGetFrameUrlRequest.h"
#include "ContextMenuParamsGetFrameUrlResponse.h"
#include "ContextMenuParamsGetLinkUrlRequest.h"
#include "ContextMenuParamsGetLinkUrlResponse.h"
#include "ContextMenuParamsGetMediaStateFlagsRequest.h"
#include "ContextMenuParamsGetMediaStateFlagsResponse.h"
#include "ContextMenuParamsGetMediaTypeRequest.h"
#include "ContextMenuParamsGetMediaTypeResponse.h"
#include "ContextMenuParamsGetMisspelledWordRequest.h"
#include "ContextMenuParamsGetMisspelledWordResponse.h"
#include "ContextMenuParamsGetPageUrlRequest.h"
#include "ContextMenuParamsGetPageUrlResponse.h"
#include "ContextMenuParamsGetSelectionTextRequest.h"
#include "ContextMenuParamsGetSelectionTextResponse.h"
#include "ContextMenuParamsGetSourceUrlRequest.h"
#include "ContextMenuParamsGetSourceUrlResponse.h"
#include "ContextMenuParamsGetTitleTextRequest.h"
#include "ContextMenuParamsGetTitleTextResponse.h"
#include "ContextMenuParamsGetTypeFlagsRequest.h"
#include "ContextMenuParamsGetTypeFlagsResponse.h"
#include "ContextMenuParamsGetUnfilteredLinkUrlRequest.h"
#include "ContextMenuParamsGetUnfilteredLinkUrlResponse.h"
#include "ContextMenuParamsGetXcoordRequest.h"
#include "ContextMenuParamsGetXcoordResponse.h"
#include "ContextMenuParamsGetYcoordRequest.h"
#include "ContextMenuParamsGetYcoordResponse.h"
#include "ContextMenuParamsHasImageContentsRequest.h"
#include "ContextMenuParamsHasImageContentsResponse.h"
#include "ContextMenuParamsIsCustomMenuRequest.h"
#include "ContextMenuParamsIsCustomMenuResponse.h"
#include "ContextMenuParamsIsEditableRequest.h"
#include "ContextMenuParamsIsEditableResponse.h"
#include "ContextMenuParamsIsSpellCheckEnabledRequest.h"
#include "ContextMenuParamsIsSpellCheckEnabledResponse.h"
#include "CookieManagerDeleteCookiesRequest.h"
#include "CookieManagerDeleteCookiesResponse.h"
#include "CookieManagerFlushStoreRequest.h"
#include "CookieManagerFlushStoreResponse.h"
#include "DictionaryValueClearRequest.h"
#include "DictionaryValueClearResponse.h"
#include "DictionaryValueCopyRequest.h"
#include "DictionaryValueCopyResponse.h"
#include "DictionaryValueGetBinaryRequest.h"
#include "DictionaryValueGetBinaryResponse.h"
#include "DictionaryValueGetBoolRequest.h"
#include "DictionaryValueGetBoolResponse.h"
#include "DictionaryValueGetDictionaryRequest.h"
#include "DictionaryValueGetDictionaryResponse.h"
#include "DictionaryValueGetIntRequest.h"
#include "DictionaryValueGetIntResponse.h"
#include "DictionaryValueGetKeysRequest.h"
#include "DictionaryValueGetKeysResponse.h"
#include "DictionaryValueGetListRequest.h"
#include "DictionaryValueGetListResponse.h"
#include "DictionaryValueGetSizeRequest.h"
#include "DictionaryValueGetSizeResponse.h"
#include "DictionaryValueGetStringRequest.h"
#include "DictionaryValueGetStringResponse.h"
#include "DictionaryValueGetTypeRequest.h"
#include "DictionaryValueGetTypeResponse.h"
#include "DictionaryValueGetValueRequest.h"
#include "DictionaryValueGetValueResponse.h"
#include "DictionaryValueHasKeyRequest.h"
#include "DictionaryValueHasKeyResponse.h"
#include "DictionaryValueIsEqualRequest.h"
#include "DictionaryValueIsEqualResponse.h"
#include "DictionaryValueIsOwnedRequest.h"
#include "DictionaryValueIsOwnedResponse.h"
#include "DictionaryValueIsReadOnlyRequest.h"
#include "DictionaryValueIsReadOnlyResponse.h"
#include "DictionaryValueIsSameRequest.h"
#include "DictionaryValueIsSameResponse.h"
#include "DictionaryValueIsValidRequest.h"
#include "DictionaryValueIsValidResponse.h"
#include "DictionaryValueRemoveRequest.h"
#include "DictionaryValueRemoveResponse.h"
#include "DictionaryValueSetBinaryRequest.h"
#include "DictionaryValueSetBinaryResponse.h"
#include "DictionaryValueSetBoolRequest.h"
#include "DictionaryValueSetBoolResponse.h"
#include "DictionaryValueSetDictionaryRequest.h"
#include "DictionaryValueSetDictionaryResponse.h"
#include "DictionaryValueSetIntRequest.h"
#include "DictionaryValueSetIntResponse.h"
#include "DictionaryValueSetListRequest.h"
#include "DictionaryValueSetListResponse.h"
#include "DictionaryValueSetNullRequest.h"
#include "DictionaryValueSetNullResponse.h"
#include "DictionaryValueSetStringRequest.h"
#include "DictionaryValueSetStringResponse.h"
#include "DictionaryValueSetValueRequest.h"
#include "DictionaryValueSetValueResponse.h"
#include "DisplayGetBoundsRequest.h"
#include "DisplayGetBoundsResponse.h"
#include "DisplayGetIdRequest.h"
#include "DisplayGetIdResponse.h"
#include "DisplayGetRotationRequest.h"
#include "DisplayGetRotationResponse.h"
#include "DisplayGetWorkAreaRequest.h"
#include "DisplayGetWorkAreaResponse.h"
#include "DownloadItemCallbackCancelRequest.h"
#include "DownloadItemCallbackCancelResponse.h"
#include "DownloadItemCallbackPauseRequest.h"
#include "DownloadItemCallbackPauseResponse.h"
#include "DownloadItemCallbackResumeRequest.h"
#include "DownloadItemCallbackResumeResponse.h"
#include "DownloadItemGetContentDispositionRequest.h"
#include "DownloadItemGetContentDispositionResponse.h"
#include "DownloadItemGetCurrentSpeedRequest.h"
#include "DownloadItemGetCurrentSpeedResponse.h"
#include "DownloadItemGetEndTimeRequest.h"
#include "DownloadItemGetEndTimeResponse.h"
#include "DownloadItemGetFullPathRequest.h"
#include "DownloadItemGetFullPathResponse.h"
#include "DownloadItemGetIdRequest.h"
#include "DownloadItemGetIdResponse.h"
#include "DownloadItemGetInterruptReasonRequest.h"
#include "DownloadItemGetInterruptReasonResponse.h"
#include "DownloadItemGetMimeTypeRequest.h"
#include "DownloadItemGetMimeTypeResponse.h"
#include "DownloadItemGetOriginalUrlRequest.h"
#include "DownloadItemGetOriginalUrlResponse.h"
#include "DownloadItemGetPercentCompleteRequest.h"
#include "DownloadItemGetPercentCompleteResponse.h"
#include "DownloadItemGetReceivedBytesRequest.h"
#include "DownloadItemGetReceivedBytesResponse.h"
#include "DownloadItemGetStartTimeRequest.h"
#include "DownloadItemGetStartTimeResponse.h"
#include "DownloadItemGetSuggestedFileNameRequest.h"
#include "DownloadItemGetSuggestedFileNameResponse.h"
#include "DownloadItemGetTotalBytesRequest.h"
#include "DownloadItemGetTotalBytesResponse.h"
#include "DownloadItemGetUrlRequest.h"
#include "DownloadItemGetUrlResponse.h"
#include "DownloadItemIsCanceledRequest.h"
#include "DownloadItemIsCanceledResponse.h"
#include "DownloadItemIsCompleteRequest.h"
#include "DownloadItemIsCompleteResponse.h"
#include "DownloadItemIsInProgressRequest.h"
#include "DownloadItemIsInProgressResponse.h"
#include "DownloadItemIsInterruptedRequest.h"
#include "DownloadItemIsInterruptedResponse.h"
#include "DownloadItemIsPausedRequest.h"
#include "DownloadItemIsPausedResponse.h"
#include "DownloadItemIsValidRequest.h"
#include "DownloadItemIsValidResponse.h"
#include "DragDataAddFileRequest.h"
#include "DragDataAddFileResponse.h"
#include "DragDataClearFilenamesRequest.h"
#include "DragDataClearFilenamesResponse.h"
#include "DragDataCloneRequest.h"
#include "DragDataCloneResponse.h"
#include "DragDataGetFileContentsRequest.h"
#include "DragDataGetFileContentsResponse.h"
#include "DragDataGetFileNameRequest.h"
#include "DragDataGetFileNameResponse.h"
#include "DragDataGetFileNamesRequest.h"
#include "DragDataGetFileNamesResponse.h"
#include "DragDataGetFilePathsRequest.h"
#include "DragDataGetFilePathsResponse.h"
#include "DragDataGetFragmentBaseUrlRequest.h"
#include "DragDataGetFragmentBaseUrlResponse.h"
#include "DragDataGetFragmentHtmlRequest.h"
#include "DragDataGetFragmentHtmlResponse.h"
#include "DragDataGetFragmentTextRequest.h"
#include "DragDataGetFragmentTextResponse.h"
#include "DragDataGetImageHotspotRequest.h"
#include "DragDataGetImageHotspotResponse.h"
#include "DragDataGetImageRequest.h"
#include "DragDataGetImageResponse.h"
#include "DragDataGetLinkMetadataRequest.h"
#include "DragDataGetLinkMetadataResponse.h"
#include "DragDataGetLinkTitleRequest.h"
#include "DragDataGetLinkTitleResponse.h"
#include "DragDataGetLinkUrlRequest.h"
#include "DragDataGetLinkUrlResponse.h"
#include "DragDataHasImageRequest.h"
#include "DragDataHasImageResponse.h"
#include "DragDataIsFileRequest.h"
#include "DragDataIsFileResponse.h"
#include "DragDataIsFragmentRequest.h"
#include "DragDataIsFragmentResponse.h"
#include "DragDataIsLinkRequest.h"
#include "DragDataIsLinkResponse.h"
#include "DragDataIsReadOnlyRequest.h"
#include "DragDataIsReadOnlyResponse.h"
#include "DragDataResetFileContentsRequest.h"
#include "DragDataResetFileContentsResponse.h"
#include "DragDataSetFragmentBaseUrlRequest.h"
#include "DragDataSetFragmentBaseUrlResponse.h"
#include "DragDataSetFragmentHtmlRequest.h"
#include "DragDataSetFragmentHtmlResponse.h"
#include "DragDataSetFragmentTextRequest.h"
#include "DragDataSetFragmentTextResponse.h"
#include "DragDataSetLinkMetadataRequest.h"
#include "DragDataSetLinkMetadataResponse.h"
#include "DragDataSetLinkTitleRequest.h"
#include "DragDataSetLinkTitleResponse.h"
#include "DragDataSetLinkUrlRequest.h"
#include "DragDataSetLinkUrlResponse.h"
#include "FileDialogCallbackCancelRequest.h"
#include "FileDialogCallbackCancelResponse.h"
#include "FileDialogCallbackContRequest.h"
#include "FileDialogCallbackContResponse.h"
#include "FrameCopyRequest.h"
#include "FrameCopyResponse.h"
#include "FrameCutRequest.h"
#include "FrameCutResponse.h"
#include "FrameDelRequest.h"
#include "FrameDelResponse.h"
#include "FrameExecuteJavaScriptRequest.h"
#include "FrameExecuteJavaScriptResponse.h"
#include "FrameGetBrowserRequest.h"
#include "FrameGetBrowserResponse.h"
#include "FrameGetIdentifierRequest.h"
#include "FrameGetIdentifierResponse.h"
#include "FrameGetNameRequest.h"
#include "FrameGetNameResponse.h"
#include "FrameGetParentRequest.h"
#include "FrameGetParentResponse.h"
#include "FrameGetSourceRequest.h"
#include "FrameGetSourceResponse.h"
#include "FrameGetTextRequest.h"
#include "FrameGetTextResponse.h"
#include "FrameGetUrlRequest.h"
#include "FrameGetUrlResponse.h"
#include "FrameIsFocusedRequest.h"
#include "FrameIsFocusedResponse.h"
#include "FrameIsMainRequest.h"
#include "FrameIsMainResponse.h"
#include "FrameIsValidRequest.h"
#include "FrameIsValidResponse.h"
#include "FrameLoadRequestRequest.h"
#include "FrameLoadRequestResponse.h"
#include "FrameLoadUrlRequest.h"
#include "FrameLoadUrlResponse.h"
#include "FramePasteAndMatchStyleRequest.h"
#include "FramePasteAndMatchStyleResponse.h"
#include "FramePasteRequest.h"
#include "FramePasteResponse.h"
#include "FrameRedoRequest.h"
#include "FrameRedoResponse.h"
#include "FrameSelectAllRequest.h"
#include "FrameSelectAllResponse.h"
#include "FrameSendProcessMessageRequest.h"
#include "FrameSendProcessMessageResponse.h"
#include "FrameUndoRequest.h"
#include "FrameUndoResponse.h"
#include "FrameViewSourceRequest.h"
#include "FrameViewSourceResponse.h"
#include "ImageGetHeightRequest.h"
#include "ImageGetHeightResponse.h"
#include "ImageGetWidthRequest.h"
#include "ImageGetWidthResponse.h"
#include "ImageIsEmptyRequest.h"
#include "ImageIsEmptyResponse.h"
#include "ImageIsSameRequest.h"
#include "ImageIsSameResponse.h"
#include "JsDialogCallbackContRequest.h"
#include "JsDialogCallbackContResponse.h"
#include "LabelButtonAsMenuButtonRequest.h"
#include "LabelButtonAsMenuButtonResponse.h"
#include "LabelButtonGetImageRequest.h"
#include "LabelButtonGetImageResponse.h"
#include "LabelButtonGetTextRequest.h"
#include "LabelButtonGetTextResponse.h"
#include "LabelButtonSetEnabledTextColorsRequest.h"
#include "LabelButtonSetEnabledTextColorsResponse.h"
#include "LabelButtonSetFontListRequest.h"
#include "LabelButtonSetFontListResponse.h"
#include "LabelButtonSetHorizontalAlignmentRequest.h"
#include "LabelButtonSetHorizontalAlignmentResponse.h"
#include "LabelButtonSetImageRequest.h"
#include "LabelButtonSetImageResponse.h"
#include "LabelButtonSetMaximumSizeRequest.h"
#include "LabelButtonSetMaximumSizeResponse.h"
#include "LabelButtonSetMinimumSizeRequest.h"
#include "LabelButtonSetMinimumSizeResponse.h"
#include "LabelButtonSetTextColorRequest.h"
#include "LabelButtonSetTextColorResponse.h"
#include "LabelButtonSetTextRequest.h"
#include "LabelButtonSetTextResponse.h"
#include "LayoutAsBoxLayoutRequest.h"
#include "LayoutAsBoxLayoutResponse.h"
#include "LayoutIsValidRequest.h"
#include "LayoutIsValidResponse.h"
#include "ListValueClearRequest.h"
#include "ListValueClearResponse.h"
#include "ListValueCopyRequest.h"
#include "ListValueCopyResponse.h"
#include "ListValueGetBinaryRequest.h"
#include "ListValueGetBinaryResponse.h"
#include "ListValueGetBoolRequest.h"
#include "ListValueGetBoolResponse.h"
#include "ListValueGetDictionaryRequest.h"
#include "ListValueGetDictionaryResponse.h"
#include "ListValueGetIntRequest.h"
#include "ListValueGetIntResponse.h"
#include "ListValueGetListRequest.h"
#include "ListValueGetListResponse.h"
#include "ListValueGetSizeRequest.h"
#include "ListValueGetSizeResponse.h"
#include "ListValueGetTypeRequest.h"
#include "ListValueGetTypeResponse.h"
#include "ListValueGetValueRequest.h"
#include "ListValueGetValueResponse.h"
#include "ListValueIsEqualRequest.h"
#include "ListValueIsEqualResponse.h"
#include "ListValueIsOwnedRequest.h"
#include "ListValueIsOwnedResponse.h"
#include "ListValueIsReadOnlyRequest.h"
#include "ListValueIsReadOnlyResponse.h"
#include "ListValueIsSameRequest.h"
#include "ListValueIsSameResponse.h"
#include "ListValueIsValidRequest.h"
#include "ListValueIsValidResponse.h"
#include "ListValueRemoveRequest.h"
#include "ListValueRemoveResponse.h"
#include "ListValueSetBinaryRequest.h"
#include "ListValueSetBinaryResponse.h"
#include "ListValueSetBoolRequest.h"
#include "ListValueSetBoolResponse.h"
#include "ListValueSetDictionaryRequest.h"
#include "ListValueSetDictionaryResponse.h"
#include "ListValueSetIntRequest.h"
#include "ListValueSetIntResponse.h"
#include "ListValueSetListRequest.h"
#include "ListValueSetListResponse.h"
#include "ListValueSetNullRequest.h"
#include "ListValueSetNullResponse.h"
#include "ListValueSetSizeRequest.h"
#include "ListValueSetSizeResponse.h"
#include "ListValueSetStringRequest.h"
#include "ListValueSetStringResponse.h"
#include "ListValueSetValueRequest.h"
#include "ListValueSetValueResponse.h"
#include "MediaAccessCallbackCancelRequest.h"
#include "MediaAccessCallbackCancelResponse.h"
#include "MediaAccessCallbackContRequest.h"
#include "MediaAccessCallbackContResponse.h"
#include "MediaRouteGetIdRequest.h"
#include "MediaRouteGetIdResponse.h"
#include "MediaRouteGetSinkRequest.h"
#include "MediaRouteGetSinkResponse.h"
#include "MediaRouteGetSourceRequest.h"
#include "MediaRouteGetSourceResponse.h"
#include "MediaRouteSendRouteMessageRequest.h"
#include "MediaRouteSendRouteMessageResponse.h"
#include "MediaRouteTerminateRequest.h"
#include "MediaRouteTerminateResponse.h"
#include "MediaRouterGetSourceRequest.h"
#include "MediaRouterGetSourceResponse.h"
#include "MediaRouterNotifyCurrentRoutesRequest.h"
#include "MediaRouterNotifyCurrentRoutesResponse.h"
#include "MediaRouterNotifyCurrentSinksRequest.h"
#include "MediaRouterNotifyCurrentSinksResponse.h"
#include "MediaSinkGetIconTypeRequest.h"
#include "MediaSinkGetIconTypeResponse.h"
#include "MediaSinkGetIdRequest.h"
#include "MediaSinkGetIdResponse.h"
#include "MediaSinkGetNameRequest.h"
#include "MediaSinkGetNameResponse.h"
#include "MediaSinkIsCastSinkRequest.h"
#include "MediaSinkIsCastSinkResponse.h"
#include "MediaSinkIsCompatibleWithRequest.h"
#include "MediaSinkIsCompatibleWithResponse.h"
#include "MediaSinkIsDialSinkRequest.h"
#include "MediaSinkIsDialSinkResponse.h"
#include "MediaSourceGetIdRequest.h"
#include "MediaSourceGetIdResponse.h"
#include "MediaSourceIsCastSourceRequest.h"
#include "MediaSourceIsCastSourceResponse.h"
#include "MediaSourceIsDialSourceRequest.h"
#include "MediaSourceIsDialSourceResponse.h"
#include "MenuButtonShowMenuRequest.h"
#include "MenuButtonShowMenuResponse.h"
#include "MenuButtonTriggerMenuRequest.h"
#include "MenuButtonTriggerMenuResponse.h"
#include "MenuModelAddCheckItemRequest.h"
#include "MenuModelAddCheckItemResponse.h"
#include "MenuModelAddItemRequest.h"
#include "MenuModelAddItemResponse.h"
#include "MenuModelAddRadioItemRequest.h"
#include "MenuModelAddRadioItemResponse.h"
#include "MenuModelAddSeparatorRequest.h"
#include "MenuModelAddSeparatorResponse.h"
#include "MenuModelAddSubMenuRequest.h"
#include "MenuModelAddSubMenuResponse.h"
#include "MenuModelClearRequest.h"
#include "MenuModelClearResponse.h"
#include "MenuModelGetCommandIdAtRequest.h"
#include "MenuModelGetCommandIdAtResponse.h"
#include "MenuModelGetCountRequest.h"
#include "MenuModelGetCountResponse.h"
#include "MenuModelGetGroupIdAtRequest.h"
#include "MenuModelGetGroupIdAtResponse.h"
#include "MenuModelGetGroupIdRequest.h"
#include "MenuModelGetGroupIdResponse.h"
#include "MenuModelGetIndexOfRequest.h"
#include "MenuModelGetIndexOfResponse.h"
#include "MenuModelGetLabelRequest.h"
#include "MenuModelGetLabelResponse.h"
#include "MenuModelGetSubMenuAtRequest.h"
#include "MenuModelGetSubMenuAtResponse.h"
#include "MenuModelGetSubMenuRequest.h"
#include "MenuModelGetSubMenuResponse.h"
#include "MenuModelGetTypeRequest.h"
#include "MenuModelGetTypeResponse.h"
#include "MenuModelHasAcceleratorAtRequest.h"
#include "MenuModelHasAcceleratorAtResponse.h"
#include "MenuModelHasAcceleratorRequest.h"
#include "MenuModelHasAcceleratorResponse.h"
#include "MenuModelInsertCheckItemAtRequest.h"
#include "MenuModelInsertCheckItemAtResponse.h"
#include "MenuModelInsertItemAtRequest.h"
#include "MenuModelInsertItemAtResponse.h"
#include "MenuModelInsertRadioItemAtRequest.h"
#include "MenuModelInsertRadioItemAtResponse.h"
#include "MenuModelInsertSeparatorAtRequest.h"
#include "MenuModelInsertSeparatorAtResponse.h"
#include "MenuModelInsertSubMenuAtRequest.h"
#include "MenuModelInsertSubMenuAtResponse.h"
#include "MenuModelIsCheckedAtRequest.h"
#include "MenuModelIsCheckedAtResponse.h"
#include "MenuModelIsCheckedRequest.h"
#include "MenuModelIsCheckedResponse.h"
#include "MenuModelIsEnabledAtRequest.h"
#include "MenuModelIsEnabledAtResponse.h"
#include "MenuModelIsEnabledRequest.h"
#include "MenuModelIsEnabledResponse.h"
#include "MenuModelIsSubMenuRequest.h"
#include "MenuModelIsSubMenuResponse.h"
#include "MenuModelIsVisibleAtRequest.h"
#include "MenuModelIsVisibleAtResponse.h"
#include "MenuModelIsVisibleRequest.h"
#include "MenuModelIsVisibleResponse.h"
#include "MenuModelRemoveAcceleratorAtRequest.h"
#include "MenuModelRemoveAcceleratorAtResponse.h"
#include "MenuModelRemoveAcceleratorRequest.h"
#include "MenuModelRemoveAcceleratorResponse.h"
#include "MenuModelRemoveAtRequest.h"
#include "MenuModelRemoveAtResponse.h"
#include "MenuModelRemoveRequest.h"
#include "MenuModelRemoveResponse.h"
#include "MenuModelSetAcceleratorAtRequest.h"
#include "MenuModelSetAcceleratorAtResponse.h"
#include "MenuModelSetAcceleratorRequest.h"
#include "MenuModelSetAcceleratorResponse.h"
#include "MenuModelSetCheckedAtRequest.h"
#include "MenuModelSetCheckedAtResponse.h"
#include "MenuModelSetCheckedRequest.h"
#include "MenuModelSetCheckedResponse.h"
#include "MenuModelSetColorAtRequest.h"
#include "MenuModelSetColorAtResponse.h"
#include "MenuModelSetColorRequest.h"
#include "MenuModelSetColorResponse.h"
#include "MenuModelSetCommandIdAtRequest.h"
#include "MenuModelSetCommandIdAtResponse.h"
#include "MenuModelSetEnabledAtRequest.h"
#include "MenuModelSetEnabledAtResponse.h"
#include "MenuModelSetEnabledRequest.h"
#include "MenuModelSetEnabledResponse.h"
#include "MenuModelSetFontListAtRequest.h"
#include "MenuModelSetFontListAtResponse.h"
#include "MenuModelSetFontListRequest.h"
#include "MenuModelSetFontListResponse.h"
#include "MenuModelSetGroupIdAtRequest.h"
#include "MenuModelSetGroupIdAtResponse.h"
#include "MenuModelSetGroupIdRequest.h"
#include "MenuModelSetGroupIdResponse.h"
#include "MenuModelSetLabelAtRequest.h"
#include "MenuModelSetLabelAtResponse.h"
#include "MenuModelSetLabelRequest.h"
#include "MenuModelSetLabelResponse.h"
#include "MenuModelSetVisibleAtRequest.h"
#include "MenuModelSetVisibleAtResponse.h"
#include "MenuModelSetVisibleRequest.h"
#include "MenuModelSetVisibleResponse.h"
#include "NavigationEntryGetCompletionTimeRequest.h"
#include "NavigationEntryGetCompletionTimeResponse.h"
#include "NavigationEntryGetDisplayUrlRequest.h"
#include "NavigationEntryGetDisplayUrlResponse.h"
#include "NavigationEntryGetHttpStatusCodeRequest.h"
#include "NavigationEntryGetHttpStatusCodeResponse.h"
#include "NavigationEntryGetOriginalUrlRequest.h"
#include "NavigationEntryGetOriginalUrlResponse.h"
#include "NavigationEntryGetSslStatusRequest.h"
#include "NavigationEntryGetSslStatusResponse.h"
#include "NavigationEntryGetTitleRequest.h"
#include "NavigationEntryGetTitleResponse.h"
#include "NavigationEntryGetTransitionTypeRequest.h"
#include "NavigationEntryGetTransitionTypeResponse.h"
#include "NavigationEntryGetUrlRequest.h"
#include "NavigationEntryGetUrlResponse.h"
#include "NavigationEntryHasPostDataRequest.h"
#include "NavigationEntryHasPostDataResponse.h"
#include "NavigationEntryIsValidRequest.h"
#include "NavigationEntryIsValidResponse.h"
#include "OverlayControllerDestroyRequest.h"
#include "OverlayControllerDestroyResponse.h"
#include "OverlayControllerGetBoundsInScreenRequest.h"
#include "OverlayControllerGetBoundsInScreenResponse.h"
#include "OverlayControllerGetBoundsRequest.h"
#include "OverlayControllerGetBoundsResponse.h"
#include "OverlayControllerGetContentsViewRequest.h"
#include "OverlayControllerGetContentsViewResponse.h"
#include "OverlayControllerGetDockingModeRequest.h"
#include "OverlayControllerGetDockingModeResponse.h"
#include "OverlayControllerGetInsetsRequest.h"
#include "OverlayControllerGetInsetsResponse.h"
#include "OverlayControllerGetPositionRequest.h"
#include "OverlayControllerGetPositionResponse.h"
#include "OverlayControllerGetSizeRequest.h"
#include "OverlayControllerGetSizeResponse.h"
#include "OverlayControllerGetWindowRequest.h"
#include "OverlayControllerGetWindowResponse.h"
#include "OverlayControllerIsDrawnRequest.h"
#include "OverlayControllerIsDrawnResponse.h"
#include "OverlayControllerIsSameRequest.h"
#include "OverlayControllerIsSameResponse.h"
#include "OverlayControllerIsValidRequest.h"
#include "OverlayControllerIsValidResponse.h"
#include "OverlayControllerIsVisibleRequest.h"
#include "OverlayControllerIsVisibleResponse.h"
#include "OverlayControllerSetBoundsRequest.h"
#include "OverlayControllerSetBoundsResponse.h"
#include "OverlayControllerSetInsetsRequest.h"
#include "OverlayControllerSetInsetsResponse.h"
#include "OverlayControllerSetPositionRequest.h"
#include "OverlayControllerSetPositionResponse.h"
#include "OverlayControllerSetSizeRequest.h"
#include "OverlayControllerSetSizeResponse.h"
#include "OverlayControllerSetVisibleRequest.h"
#include "OverlayControllerSetVisibleResponse.h"
#include "OverlayControllerSizeToPreferredSizeRequest.h"
#include "OverlayControllerSizeToPreferredSizeResponse.h"
#include "PanelAddChildViewAtRequest.h"
#include "PanelAddChildViewAtResponse.h"
#include "PanelAddChildViewRequest.h"
#include "PanelAddChildViewResponse.h"
#include "PanelAsWindowRequest.h"
#include "PanelAsWindowResponse.h"
#include "PanelGetChildViewAtRequest.h"
#include "PanelGetChildViewAtResponse.h"
#include "PanelGetChildViewCountRequest.h"
#include "PanelGetChildViewCountResponse.h"
#include "PanelGetLayoutRequest.h"
#include "PanelGetLayoutResponse.h"
#include "PanelLayoutRequest.h"
#include "PanelLayoutResponse.h"
#include "PanelRemoveAllChildViewsRequest.h"
#include "PanelRemoveAllChildViewsResponse.h"
#include "PanelRemoveChildViewRequest.h"
#include "PanelRemoveChildViewResponse.h"
#include "PanelReorderChildViewRequest.h"
#include "PanelReorderChildViewResponse.h"
#include "PermissionPromptCallbackContRequest.h"
#include "PermissionPromptCallbackContResponse.h"
#include "PostDataAddElementRequest.h"
#include "PostDataAddElementResponse.h"
#include "PostDataElementGetBytesCountRequest.h"
#include "PostDataElementGetBytesCountResponse.h"
#include "PostDataElementGetFileRequest.h"
#include "PostDataElementGetFileResponse.h"
#include "PostDataElementGetTypeRequest.h"
#include "PostDataElementGetTypeResponse.h"
#include "PostDataElementIsReadOnlyRequest.h"
#include "PostDataElementIsReadOnlyResponse.h"
#include "PostDataElementSetToBytesRequest.h"
#include "PostDataElementSetToBytesResponse.h"
#include "PostDataElementSetToEmptyRequest.h"
#include "PostDataElementSetToEmptyResponse.h"
#include "PostDataElementSetToFileRequest.h"
#include "PostDataElementSetToFileResponse.h"
#include "PostDataGetElementCountRequest.h"
#include "PostDataGetElementCountResponse.h"
#include "PostDataHasExcludedElementsRequest.h"
#include "PostDataHasExcludedElementsResponse.h"
#include "PostDataIsReadOnlyRequest.h"
#include "PostDataIsReadOnlyResponse.h"
#include "PostDataRemoveElementRequest.h"
#include "PostDataRemoveElementResponse.h"
#include "PostDataRemoveElementsRequest.h"
#include "PostDataRemoveElementsResponse.h"
#include "PreferenceManagerCanSetPreferenceRequest.h"
#include "PreferenceManagerCanSetPreferenceResponse.h"
#include "PreferenceManagerGetAllPreferencesRequest.h"
#include "PreferenceManagerGetAllPreferencesResponse.h"
#include "PreferenceManagerGetPreferenceRequest.h"
#include "PreferenceManagerGetPreferenceResponse.h"
#include "PreferenceManagerHasPreferenceRequest.h"
#include "PreferenceManagerHasPreferenceResponse.h"
#include "PreferenceManagerSetPreferenceRequest.h"
#include "PreferenceManagerSetPreferenceResponse.h"
#include "PreferenceRegistrarAddPreferenceRequest.h"
#include "PreferenceRegistrarAddPreferenceResponse.h"
#include "PrintDialogCallbackCancelRequest.h"
#include "PrintDialogCallbackCancelResponse.h"
#include "PrintDialogCallbackContRequest.h"
#include "PrintDialogCallbackContResponse.h"
#include "PrintJobCallbackContRequest.h"
#include "PrintJobCallbackContResponse.h"
#include "PrintSettingsGetColorModelRequest.h"
#include "PrintSettingsGetColorModelResponse.h"
#include "PrintSettingsGetCopiesRequest.h"
#include "PrintSettingsGetCopiesResponse.h"
#include "PrintSettingsGetDeviceNameRequest.h"
#include "PrintSettingsGetDeviceNameResponse.h"
#include "PrintSettingsGetDpiRequest.h"
#include "PrintSettingsGetDpiResponse.h"
#include "PrintSettingsGetDuplexModeRequest.h"
#include "PrintSettingsGetDuplexModeResponse.h"
#include "PrintSettingsGetPageRangesCountRequest.h"
#include "PrintSettingsGetPageRangesCountResponse.h"
#include "PrintSettingsIsLandscapeRequest.h"
#include "PrintSettingsIsLandscapeResponse.h"
#include "PrintSettingsIsReadOnlyRequest.h"
#include "PrintSettingsIsReadOnlyResponse.h"
#include "PrintSettingsIsSelectionOnlyRequest.h"
#include "PrintSettingsIsSelectionOnlyResponse.h"
#include "PrintSettingsIsValidRequest.h"
#include "PrintSettingsIsValidResponse.h"
#include "PrintSettingsSetCollateRequest.h"
#include "PrintSettingsSetCollateResponse.h"
#include "PrintSettingsSetColorModelRequest.h"
#include "PrintSettingsSetColorModelResponse.h"
#include "PrintSettingsSetCopiesRequest.h"
#include "PrintSettingsSetCopiesResponse.h"
#include "PrintSettingsSetDeviceNameRequest.h"
#include "PrintSettingsSetDeviceNameResponse.h"
#include "PrintSettingsSetDpiRequest.h"
#include "PrintSettingsSetDpiResponse.h"
#include "PrintSettingsSetDuplexModeRequest.h"
#include "PrintSettingsSetDuplexModeResponse.h"
#include "PrintSettingsSetOrientationRequest.h"
#include "PrintSettingsSetOrientationResponse.h"
#include "PrintSettingsSetPrinterPrintableAreaRequest.h"
#include "PrintSettingsSetPrinterPrintableAreaResponse.h"
#include "PrintSettingsSetSelectionOnlyRequest.h"
#include "PrintSettingsSetSelectionOnlyResponse.h"
#include "PrintSettingsWillCollateRequest.h"
#include "PrintSettingsWillCollateResponse.h"
#include "ProcessMessageCopyRequest.h"
#include "ProcessMessageCopyResponse.h"
#include "ProcessMessageGetArgumentListRequest.h"
#include "ProcessMessageGetArgumentListResponse.h"
#include "ProcessMessageGetNameRequest.h"
#include "ProcessMessageGetNameResponse.h"
#include "ProcessMessageGetSharedMemoryRegionRequest.h"
#include "ProcessMessageGetSharedMemoryRegionResponse.h"
#include "ProcessMessageIsReadOnlyRequest.h"
#include "ProcessMessageIsReadOnlyResponse.h"
#include "ProcessMessageIsValidRequest.h"
#include "ProcessMessageIsValidResponse.h"
#include "RequestContextClearCertificateExceptionsRequest.h"
#include "RequestContextClearCertificateExceptionsResponse.h"
#include "RequestContextClearHttpAuthCredentialsRequest.h"
#include "RequestContextClearHttpAuthCredentialsResponse.h"
#include "RequestContextClearHttpCacheRequest.h"
#include "RequestContextClearHttpCacheResponse.h"
#include "RequestContextClearSchemeHandlerFactoriesRequest.h"
#include "RequestContextClearSchemeHandlerFactoriesResponse.h"
#include "RequestContextCloseAllConnectionsRequest.h"
#include "RequestContextCloseAllConnectionsResponse.h"
#include "RequestContextGetCachePathRequest.h"
#include "RequestContextGetCachePathResponse.h"
#include "RequestContextGetChromeColorSchemeColorRequest.h"
#include "RequestContextGetChromeColorSchemeColorResponse.h"
#include "RequestContextGetChromeColorSchemeModeRequest.h"
#include "RequestContextGetChromeColorSchemeModeResponse.h"
#include "RequestContextGetChromeColorSchemeVariantRequest.h"
#include "RequestContextGetChromeColorSchemeVariantResponse.h"
#include "RequestContextGetContentSettingRequest.h"
#include "RequestContextGetContentSettingResponse.h"
#include "RequestContextGetCookieManagerRequest.h"
#include "RequestContextGetCookieManagerResponse.h"
#include "RequestContextGetMediaRouterRequest.h"
#include "RequestContextGetMediaRouterResponse.h"
#include "RequestContextGetWebsiteSettingRequest.h"
#include "RequestContextGetWebsiteSettingResponse.h"
#include "RequestContextIsGlobalRequest.h"
#include "RequestContextIsGlobalResponse.h"
#include "RequestContextIsSameRequest.h"
#include "RequestContextIsSameResponse.h"
#include "RequestContextIsSharingWithRequest.h"
#include "RequestContextIsSharingWithResponse.h"
#include "RequestContextSetChromeColorSchemeRequest.h"
#include "RequestContextSetChromeColorSchemeResponse.h"
#include "RequestContextSetContentSettingRequest.h"
#include "RequestContextSetContentSettingResponse.h"
#include "RequestContextSetWebsiteSettingRequest.h"
#include "RequestContextSetWebsiteSettingResponse.h"
#include "RequestGetFirstPartyForCookiesRequest.h"
#include "RequestGetFirstPartyForCookiesResponse.h"
#include "RequestGetFlagsRequest.h"
#include "RequestGetFlagsResponse.h"
#include "RequestGetHeaderByNameRequest.h"
#include "RequestGetHeaderByNameResponse.h"
#include "RequestGetIdentifierRequest.h"
#include "RequestGetIdentifierResponse.h"
#include "RequestGetMethodRequest.h"
#include "RequestGetMethodResponse.h"
#include "RequestGetPostDataRequest.h"
#include "RequestGetPostDataResponse.h"
#include "RequestGetReferrerPolicyRequest.h"
#include "RequestGetReferrerPolicyResponse.h"
#include "RequestGetReferrerUrlRequest.h"
#include "RequestGetReferrerUrlResponse.h"
#include "RequestGetResourceTypeRequest.h"
#include "RequestGetResourceTypeResponse.h"
#include "RequestGetTransitionTypeRequest.h"
#include "RequestGetTransitionTypeResponse.h"
#include "RequestGetUrlRequest.h"
#include "RequestGetUrlResponse.h"
#include "RequestIsReadOnlyRequest.h"
#include "RequestIsReadOnlyResponse.h"
#include "RequestSetFirstPartyForCookiesRequest.h"
#include "RequestSetFirstPartyForCookiesResponse.h"
#include "RequestSetFlagsRequest.h"
#include "RequestSetFlagsResponse.h"
#include "RequestSetHeaderByNameRequest.h"
#include "RequestSetHeaderByNameResponse.h"
#include "RequestSetMethodRequest.h"
#include "RequestSetMethodResponse.h"
#include "RequestSetPostDataRequest.h"
#include "RequestSetPostDataResponse.h"
#include "RequestSetReferrerRequest.h"
#include "RequestSetReferrerResponse.h"
#include "RequestSetUrlRequest.h"
#include "RequestSetUrlResponse.h"
#include "ResourceBundleGetDataResourceForScaleRequest.h"
#include "ResourceBundleGetDataResourceForScaleResponse.h"
#include "ResourceBundleGetDataResourceRequest.h"
#include "ResourceBundleGetDataResourceResponse.h"
#include "ResourceBundleGetLocalizedStringRequest.h"
#include "ResourceBundleGetLocalizedStringResponse.h"
#include "ResourceReadCallbackContRequest.h"
#include "ResourceReadCallbackContResponse.h"
#include "ResourceSkipCallbackContRequest.h"
#include "ResourceSkipCallbackContResponse.h"
#include "ResponseGetCharsetRequest.h"
#include "ResponseGetCharsetResponse.h"
#include "ResponseGetErrorRequest.h"
#include "ResponseGetErrorResponse.h"
#include "ResponseGetHeaderByNameRequest.h"
#include "ResponseGetHeaderByNameResponse.h"
#include "ResponseGetMimeTypeRequest.h"
#include "ResponseGetMimeTypeResponse.h"
#include "ResponseGetStatusRequest.h"
#include "ResponseGetStatusResponse.h"
#include "ResponseGetStatusTextRequest.h"
#include "ResponseGetStatusTextResponse.h"
#include "ResponseGetUrlRequest.h"
#include "ResponseGetUrlResponse.h"
#include "ResponseIsReadOnlyRequest.h"
#include "ResponseIsReadOnlyResponse.h"
#include "ResponseSetCharsetRequest.h"
#include "ResponseSetCharsetResponse.h"
#include "ResponseSetErrorRequest.h"
#include "ResponseSetErrorResponse.h"
#include "ResponseSetHeaderByNameRequest.h"
#include "ResponseSetHeaderByNameResponse.h"
#include "ResponseSetMimeTypeRequest.h"
#include "ResponseSetMimeTypeResponse.h"
#include "ResponseSetStatusRequest.h"
#include "ResponseSetStatusResponse.h"
#include "ResponseSetStatusTextRequest.h"
#include "ResponseSetStatusTextResponse.h"
#include "ResponseSetUrlRequest.h"
#include "ResponseSetUrlResponse.h"
#include "RunContextMenuCallbackCancelRequest.h"
#include "RunContextMenuCallbackCancelResponse.h"
#include "RunContextMenuCallbackContRequest.h"
#include "RunContextMenuCallbackContResponse.h"
#include "RunQuickMenuCallbackCancelRequest.h"
#include "RunQuickMenuCallbackCancelResponse.h"
#include "RunQuickMenuCallbackContRequest.h"
#include "RunQuickMenuCallbackContResponse.h"
#include "SchemeRegistrarAddCustomSchemeRequest.h"
#include "SchemeRegistrarAddCustomSchemeResponse.h"
#include "ScrollViewGetContentViewRequest.h"
#include "ScrollViewGetContentViewResponse.h"
#include "ScrollViewGetHorizontalScrollbarHeightRequest.h"
#include "ScrollViewGetHorizontalScrollbarHeightResponse.h"
#include "ScrollViewGetVerticalScrollbarWidthRequest.h"
#include "ScrollViewGetVerticalScrollbarWidthResponse.h"
#include "ScrollViewGetVisibleContentRectRequest.h"
#include "ScrollViewGetVisibleContentRectResponse.h"
#include "ScrollViewHasHorizontalScrollbarRequest.h"
#include "ScrollViewHasHorizontalScrollbarResponse.h"
#include "ScrollViewHasVerticalScrollbarRequest.h"
#include "ScrollViewHasVerticalScrollbarResponse.h"
#include "ScrollViewSetContentViewRequest.h"
#include "ScrollViewSetContentViewResponse.h"
#include "SelectClientCertificateCallbackSelectRequest.h"
#include "SelectClientCertificateCallbackSelectResponse.h"
#include "ServerCloseConnectionRequest.h"
#include "ServerCloseConnectionResponse.h"
#include "ServerGetAddressRequest.h"
#include "ServerGetAddressResponse.h"
#include "ServerGetTaskRunnerRequest.h"
#include "ServerGetTaskRunnerResponse.h"
#include "ServerHasConnectionRequest.h"
#include "ServerHasConnectionResponse.h"
#include "ServerIsRunningRequest.h"
#include "ServerIsRunningResponse.h"
#include "ServerIsValidConnectionRequest.h"
#include "ServerIsValidConnectionResponse.h"
#include "ServerSendHttp200ResponseRequest.h"
#include "ServerSendHttp200ResponseResponse.h"
#include "ServerSendHttp404ResponseRequest.h"
#include "ServerSendHttp404ResponseResponse.h"
#include "ServerSendHttp500ResponseRequest.h"
#include "ServerSendHttp500ResponseResponse.h"
#include "ServerSendRawDataRequest.h"
#include "ServerSendRawDataResponse.h"
#include "ServerSendWebSocketMessageRequest.h"
#include "ServerSendWebSocketMessageResponse.h"
#include "ServerShutdownRequest.h"
#include "ServerShutdownResponse.h"
#include "SharedMemoryRegionIsValidRequest.h"
#include "SharedMemoryRegionIsValidResponse.h"
#include "SharedMemoryRegionSizeRequest.h"
#include "SharedMemoryRegionSizeResponse.h"
#include "SharedProcessMessageBuilderBuildRequest.h"
#include "SharedProcessMessageBuilderBuildResponse.h"
#include "SharedProcessMessageBuilderIsValidRequest.h"
#include "SharedProcessMessageBuilderIsValidResponse.h"
#include "SharedProcessMessageBuilderSizeRequest.h"
#include "SharedProcessMessageBuilderSizeResponse.h"
#include "SslInfoGetCertStatusRequest.h"
#include "SslInfoGetCertStatusResponse.h"
#include "SslInfoGetX509CertificateRequest.h"
#include "SslInfoGetX509CertificateResponse.h"
#include "SslStatusGetCertStatusRequest.h"
#include "SslStatusGetCertStatusResponse.h"
#include "SslStatusGetContentStatusRequest.h"
#include "SslStatusGetContentStatusResponse.h"
#include "SslStatusGetSslversionRequest.h"
#include "SslStatusGetSslversionResponse.h"
#include "SslStatusGetX509CertificateRequest.h"
#include "SslStatusGetX509CertificateResponse.h"
#include "SslStatusIsSecureConnectionRequest.h"
#include "SslStatusIsSecureConnectionResponse.h"
#include "StreamReaderEofRequest.h"
#include "StreamReaderEofResponse.h"
#include "StreamReaderMayBlockRequest.h"
#include "StreamReaderMayBlockResponse.h"
#include "StreamReaderSeekRequest.h"
#include "StreamReaderSeekResponse.h"
#include "StreamReaderTellRequest.h"
#include "StreamReaderTellResponse.h"
#include "StreamWriterFlushRequest.h"
#include "StreamWriterFlushResponse.h"
#include "StreamWriterMayBlockRequest.h"
#include "StreamWriterMayBlockResponse.h"
#include "StreamWriterSeekRequest.h"
#include "StreamWriterSeekResponse.h"
#include "StreamWriterTellRequest.h"
#include "StreamWriterTellResponse.h"
#include "StreamWriterWriteRequest.h"
#include "StreamWriterWriteResponse.h"
#include "TaskManagerGetTaskIdForBrowserIdRequest.h"
#include "TaskManagerGetTaskIdForBrowserIdResponse.h"
#include "TaskManagerGetTasksCountRequest.h"
#include "TaskManagerGetTasksCountResponse.h"
#include "TaskManagerKillTaskRequest.h"
#include "TaskManagerKillTaskResponse.h"
#include "TaskRunnerBelongsToCurrentThreadRequest.h"
#include "TaskRunnerBelongsToCurrentThreadResponse.h"
#include "TaskRunnerBelongsToThreadRequest.h"
#include "TaskRunnerBelongsToThreadResponse.h"
#include "TaskRunnerIsSameRequest.h"
#include "TaskRunnerIsSameResponse.h"
#include "TaskRunnerPostDelayedTaskRequest.h"
#include "TaskRunnerPostDelayedTaskResponse.h"
#include "TaskRunnerPostTaskRequest.h"
#include "TaskRunnerPostTaskResponse.h"
#include "TextFieldAppendTextRequest.h"
#include "TextFieldAppendTextResponse.h"
#include "TextFieldApplyTextColorRequest.h"
#include "TextFieldApplyTextColorResponse.h"
#include "TextFieldApplyTextStyleRequest.h"
#include "TextFieldApplyTextStyleResponse.h"
#include "TextFieldClearEditHistoryRequest.h"
#include "TextFieldClearEditHistoryResponse.h"
#include "TextFieldClearSelectionRequest.h"
#include "TextFieldClearSelectionResponse.h"
#include "TextFieldDelegateOnAfterUserActionRequest.h"
#include "TextFieldDelegateOnAfterUserActionResponse.h"
#include "TextFieldDelegateOnKeyEventRequest.h"
#include "TextFieldDelegateOnKeyEventResponse.h"
#include "TextFieldExecuteCommandRequest.h"
#include "TextFieldExecuteCommandResponse.h"
#include "TextFieldGetCursorPositionRequest.h"
#include "TextFieldGetCursorPositionResponse.h"
#include "TextFieldGetPlaceholderTextRequest.h"
#include "TextFieldGetPlaceholderTextResponse.h"
#include "TextFieldGetSelectedRangeRequest.h"
#include "TextFieldGetSelectedRangeResponse.h"
#include "TextFieldGetSelectedTextRequest.h"
#include "TextFieldGetSelectedTextResponse.h"
#include "TextFieldGetTextRequest.h"
#include "TextFieldGetTextResponse.h"
#include "TextFieldHasSelectionRequest.h"
#include "TextFieldHasSelectionResponse.h"
#include "TextFieldInsertOrReplaceTextRequest.h"
#include "TextFieldInsertOrReplaceTextResponse.h"
#include "TextFieldIsCommandEnabledRequest.h"
#include "TextFieldIsCommandEnabledResponse.h"
#include "TextFieldIsPasswordInputRequest.h"
#include "TextFieldIsPasswordInputResponse.h"
#include "TextFieldIsReadOnlyRequest.h"
#include "TextFieldIsReadOnlyResponse.h"
#include "TextFieldSelectAllRequest.h"
#include "TextFieldSelectAllResponse.h"
#include "TextFieldSelectRangeRequest.h"
#include "TextFieldSelectRangeResponse.h"
#include "TextFieldSetAccessibleNameRequest.h"
#include "TextFieldSetAccessibleNameResponse.h"
#include "TextFieldSetFontListRequest.h"
#include "TextFieldSetFontListResponse.h"
#include "TextFieldSetPasswordInputRequest.h"
#include "TextFieldSetPasswordInputResponse.h"
#include "TextFieldSetPlaceholderTextRequest.h"
#include "TextFieldSetPlaceholderTextResponse.h"
#include "TextFieldSetReadOnlyRequest.h"
#include "TextFieldSetReadOnlyResponse.h"
#include "TextFieldSetTextRequest.h"
#include "TextFieldSetTextResponse.h"
#include "ThreadGetPlatformThreadIdRequest.h"
#include "ThreadGetPlatformThreadIdResponse.h"
#include "ThreadGetTaskRunnerRequest.h"
#include "ThreadGetTaskRunnerResponse.h"
#include "ThreadIsRunningRequest.h"
#include "ThreadIsRunningResponse.h"
#include "ThreadStopRequest.h"
#include "ThreadStopResponse.h"
#include "UnresponsiveProcessCallbackTerminateRequest.h"
#include "UnresponsiveProcessCallbackTerminateResponse.h"
#include "UnresponsiveProcessCallbackWaitRequest.h"
#include "UnresponsiveProcessCallbackWaitResponse.h"
#include "UrlRequestCancelRequest.h"
#include "UrlRequestCancelResponse.h"
#include "UrlRequestGetRequestErrorRequest.h"
#include "UrlRequestGetRequestErrorResponse.h"
#include "UrlRequestGetRequestRequest.h"
#include "UrlRequestGetRequestResponse.h"
#include "UrlRequestGetRequestStatusRequest.h"
#include "UrlRequestGetRequestStatusResponse.h"
#include "UrlRequestGetResponseRequest.h"
#include "UrlRequestGetResponseResponse.h"
#include "UrlRequestResponseWasCachedRequest.h"
#include "UrlRequestResponseWasCachedResponse.h"
#include "ValueCopyRequest.h"
#include "ValueCopyResponse.h"
#include "ValueGetBinaryRequest.h"
#include "ValueGetBinaryResponse.h"
#include "ValueGetBoolRequest.h"
#include "ValueGetBoolResponse.h"
#include "ValueGetDictionaryRequest.h"
#include "ValueGetDictionaryResponse.h"
#include "ValueGetIntRequest.h"
#include "ValueGetIntResponse.h"
#include "ValueGetListRequest.h"
#include "ValueGetListResponse.h"
#include "ValueGetStringRequest.h"
#include "ValueGetStringResponse.h"
#include "ValueGetTypeRequest.h"
#include "ValueGetTypeResponse.h"
#include "ValueIsEqualRequest.h"
#include "ValueIsEqualResponse.h"
#include "ValueIsOwnedRequest.h"
#include "ValueIsOwnedResponse.h"
#include "ValueIsReadOnlyRequest.h"
#include "ValueIsReadOnlyResponse.h"
#include "ValueIsSameRequest.h"
#include "ValueIsSameResponse.h"
#include "ValueIsValidRequest.h"
#include "ValueIsValidResponse.h"
#include "ValueSetBinaryRequest.h"
#include "ValueSetBinaryResponse.h"
#include "ValueSetBoolRequest.h"
#include "ValueSetBoolResponse.h"
#include "ValueSetDictionaryRequest.h"
#include "ValueSetDictionaryResponse.h"
#include "ValueSetIntRequest.h"
#include "ValueSetIntResponse.h"
#include "ValueSetListRequest.h"
#include "ValueSetListResponse.h"
#include "ValueSetNullRequest.h"
#include "ValueSetNullResponse.h"
#include "ValueSetStringRequest.h"
#include "ValueSetStringResponse.h"
#include "ViewAsBrowserViewRequest.h"
#include "ViewAsBrowserViewResponse.h"
#include "ViewAsButtonRequest.h"
#include "ViewAsButtonResponse.h"
#include "ViewAsPanelRequest.h"
#include "ViewAsPanelResponse.h"
#include "ViewAsScrollViewRequest.h"
#include "ViewAsScrollViewResponse.h"
#include "ViewAsTextFieldRequest.h"
#include "ViewAsTextFieldResponse.h"
#include "ViewDelegateGetHeightForWidthRequest.h"
#include "ViewDelegateGetHeightForWidthResponse.h"
#include "ViewDelegateGetMaximumSizeRequest.h"
#include "ViewDelegateGetMaximumSizeResponse.h"
#include "ViewDelegateGetMinimumSizeRequest.h"
#include "ViewDelegateGetMinimumSizeResponse.h"
#include "ViewDelegateGetPreferredSizeRequest.h"
#include "ViewDelegateGetPreferredSizeResponse.h"
#include "ViewDelegateOnBlurRequest.h"
#include "ViewDelegateOnBlurResponse.h"
#include "ViewDelegateOnChildViewChangedRequest.h"
#include "ViewDelegateOnChildViewChangedResponse.h"
#include "ViewDelegateOnFocusRequest.h"
#include "ViewDelegateOnFocusResponse.h"
#include "ViewDelegateOnLayoutChangedRequest.h"
#include "ViewDelegateOnLayoutChangedResponse.h"
#include "ViewDelegateOnParentViewChangedRequest.h"
#include "ViewDelegateOnParentViewChangedResponse.h"
#include "ViewDelegateOnThemeChangedRequest.h"
#include "ViewDelegateOnThemeChangedResponse.h"
#include "ViewDelegateOnWindowChangedRequest.h"
#include "ViewDelegateOnWindowChangedResponse.h"
#include "ViewGetBackgroundColorRequest.h"
#include "ViewGetBackgroundColorResponse.h"
#include "ViewGetBoundsInScreenRequest.h"
#include "ViewGetBoundsInScreenResponse.h"
#include "ViewGetBoundsRequest.h"
#include "ViewGetBoundsResponse.h"
#include "ViewGetDelegateRequest.h"
#include "ViewGetDelegateResponse.h"
#include "ViewGetGroupIdRequest.h"
#include "ViewGetGroupIdResponse.h"
#include "ViewGetHeightForWidthRequest.h"
#include "ViewGetHeightForWidthResponse.h"
#include "ViewGetIdRequest.h"
#include "ViewGetIdResponse.h"
#include "ViewGetInsetsRequest.h"
#include "ViewGetInsetsResponse.h"
#include "ViewGetMaximumSizeRequest.h"
#include "ViewGetMaximumSizeResponse.h"
#include "ViewGetMinimumSizeRequest.h"
#include "ViewGetMinimumSizeResponse.h"
#include "ViewGetParentViewRequest.h"
#include "ViewGetParentViewResponse.h"
#include "ViewGetPositionRequest.h"
#include "ViewGetPositionResponse.h"
#include "ViewGetPreferredSizeRequest.h"
#include "ViewGetPreferredSizeResponse.h"
#include "ViewGetSizeRequest.h"
#include "ViewGetSizeResponse.h"
#include "ViewGetThemeColorRequest.h"
#include "ViewGetThemeColorResponse.h"
#include "ViewGetTypeStringRequest.h"
#include "ViewGetTypeStringResponse.h"
#include "ViewGetViewForIdRequest.h"
#include "ViewGetViewForIdResponse.h"
#include "ViewGetWindowRequest.h"
#include "ViewGetWindowResponse.h"
#include "ViewHasFocusRequest.h"
#include "ViewHasFocusResponse.h"
#include "ViewInvalidateLayoutRequest.h"
#include "ViewInvalidateLayoutResponse.h"
#include "ViewIsAccessibilityFocusableRequest.h"
#include "ViewIsAccessibilityFocusableResponse.h"
#include "ViewIsAttachedRequest.h"
#include "ViewIsAttachedResponse.h"
#include "ViewIsDrawnRequest.h"
#include "ViewIsDrawnResponse.h"
#include "ViewIsEnabledRequest.h"
#include "ViewIsEnabledResponse.h"
#include "ViewIsFocusableRequest.h"
#include "ViewIsFocusableResponse.h"
#include "ViewIsSameRequest.h"
#include "ViewIsSameResponse.h"
#include "ViewIsValidRequest.h"
#include "ViewIsValidResponse.h"
#include "ViewIsVisibleRequest.h"
#include "ViewIsVisibleResponse.h"
#include "ViewRequestFocusRequest.h"
#include "ViewRequestFocusResponse.h"
#include "ViewSetBackgroundColorRequest.h"
#include "ViewSetBackgroundColorResponse.h"
#include "ViewSetBoundsRequest.h"
#include "ViewSetBoundsResponse.h"
#include "ViewSetEnabledRequest.h"
#include "ViewSetEnabledResponse.h"
#include "ViewSetFocusableRequest.h"
#include "ViewSetFocusableResponse.h"
#include "ViewSetGroupIdRequest.h"
#include "ViewSetGroupIdResponse.h"
#include "ViewSetIdRequest.h"
#include "ViewSetIdResponse.h"
#include "ViewSetInsetsRequest.h"
#include "ViewSetInsetsResponse.h"
#include "ViewSetPositionRequest.h"
#include "ViewSetPositionResponse.h"
#include "ViewSetSizeRequest.h"
#include "ViewSetSizeResponse.h"
#include "ViewSetVisibleRequest.h"
#include "ViewSetVisibleResponse.h"
#include "ViewSizeToPreferredSizeRequest.h"
#include "ViewSizeToPreferredSizeResponse.h"
#include "ViewToStringRequest.h"
#include "ViewToStringResponse.h"
#include "WaitableEventIsSignaledRequest.h"
#include "WaitableEventIsSignaledResponse.h"
#include "WaitableEventResetRequest.h"
#include "WaitableEventResetResponse.h"
#include "WaitableEventSignalRequest.h"
#include "WaitableEventSignalResponse.h"
#include "WaitableEventTimedWaitRequest.h"
#include "WaitableEventTimedWaitResponse.h"
#include "WaitableEventWaitRequest.h"
#include "WaitableEventWaitResponse.h"
#include "WindowActivateRequest.h"
#include "WindowActivateResponse.h"
#include "WindowAddOverlayViewRequest.h"
#include "WindowAddOverlayViewResponse.h"
#include "WindowBringToTopRequest.h"
#include "WindowBringToTopResponse.h"
#include "WindowCancelMenuRequest.h"
#include "WindowCancelMenuResponse.h"
#include "WindowCenterWindowRequest.h"
#include "WindowCenterWindowResponse.h"
#include "WindowCloseRequest.h"
#include "WindowCloseResponse.h"
#include "WindowDeactivateRequest.h"
#include "WindowDeactivateResponse.h"
#include "WindowDelegateAcceptsFirstMouseRequest.h"
#include "WindowDelegateAcceptsFirstMouseResponse.h"
#include "WindowDelegateCanCloseRequest.h"
#include "WindowDelegateCanCloseResponse.h"
#include "WindowDelegateCanMaximizeRequest.h"
#include "WindowDelegateCanMaximizeResponse.h"
#include "WindowDelegateCanMinimizeRequest.h"
#include "WindowDelegateCanMinimizeResponse.h"
#include "WindowDelegateCanResizeRequest.h"
#include "WindowDelegateCanResizeResponse.h"
#include "WindowDelegateGetInitialBoundsRequest.h"
#include "WindowDelegateGetInitialBoundsResponse.h"
#include "WindowDelegateGetInitialShowStateRequest.h"
#include "WindowDelegateGetInitialShowStateResponse.h"
#include "WindowDelegateGetWindowRuntimeStyleRequest.h"
#include "WindowDelegateGetWindowRuntimeStyleResponse.h"
#include "WindowDelegateIsFramelessRequest.h"
#include "WindowDelegateIsFramelessResponse.h"
#include "WindowDelegateIsWindowModalDialogRequest.h"
#include "WindowDelegateIsWindowModalDialogResponse.h"
#include "WindowDelegateOnAcceleratorRequest.h"
#include "WindowDelegateOnAcceleratorResponse.h"
#include "WindowDelegateOnKeyEventRequest.h"
#include "WindowDelegateOnKeyEventResponse.h"
#include "WindowDelegateOnThemeColorsChangedRequest.h"
#include "WindowDelegateOnThemeColorsChangedResponse.h"
#include "WindowDelegateOnWindowActivationChangedRequest.h"
#include "WindowDelegateOnWindowActivationChangedResponse.h"
#include "WindowDelegateOnWindowBoundsChangedRequest.h"
#include "WindowDelegateOnWindowBoundsChangedResponse.h"
#include "WindowDelegateOnWindowClosingRequest.h"
#include "WindowDelegateOnWindowClosingResponse.h"
#include "WindowDelegateOnWindowCreatedRequest.h"
#include "WindowDelegateOnWindowCreatedResponse.h"
#include "WindowDelegateOnWindowDestroyedRequest.h"
#include "WindowDelegateOnWindowDestroyedResponse.h"
#include "WindowDelegateOnWindowFullscreenTransitionRequest.h"
#include "WindowDelegateOnWindowFullscreenTransitionResponse.h"
#include "WindowDelegateWithStandardWindowButtonsRequest.h"
#include "WindowDelegateWithStandardWindowButtonsResponse.h"
#include "WindowGetClientAreaBoundsInScreenRequest.h"
#include "WindowGetClientAreaBoundsInScreenResponse.h"
#include "WindowGetDisplayRequest.h"
#include "WindowGetDisplayResponse.h"
#include "WindowGetFocusedViewRequest.h"
#include "WindowGetFocusedViewResponse.h"
#include "WindowGetRuntimeStyleRequest.h"
#include "WindowGetRuntimeStyleResponse.h"
#include "WindowGetTitleRequest.h"
#include "WindowGetTitleResponse.h"
#include "WindowGetWindowAppIconRequest.h"
#include "WindowGetWindowAppIconResponse.h"
#include "WindowGetWindowHandleRequest.h"
#include "WindowGetWindowHandleResponse.h"
#include "WindowGetWindowIconRequest.h"
#include "WindowGetWindowIconResponse.h"
#include "WindowHideRequest.h"
#include "WindowHideResponse.h"
#include "WindowIsActiveRequest.h"
#include "WindowIsActiveResponse.h"
#include "WindowIsAlwaysOnTopRequest.h"
#include "WindowIsAlwaysOnTopResponse.h"
#include "WindowIsClosedRequest.h"
#include "WindowIsClosedResponse.h"
#include "WindowIsFullscreenRequest.h"
#include "WindowIsFullscreenResponse.h"
#include "WindowIsMaximizedRequest.h"
#include "WindowIsMaximizedResponse.h"
#include "WindowIsMinimizedRequest.h"
#include "WindowIsMinimizedResponse.h"
#include "WindowMaximizeRequest.h"
#include "WindowMaximizeResponse.h"
#include "WindowMinimizeRequest.h"
#include "WindowMinimizeResponse.h"
#include "WindowRemoveAcceleratorRequest.h"
#include "WindowRemoveAcceleratorResponse.h"
#include "WindowRemoveAllAcceleratorsRequest.h"
#include "WindowRemoveAllAcceleratorsResponse.h"
#include "WindowRestoreRequest.h"
#include "WindowRestoreResponse.h"
#include "WindowSendKeyPressRequest.h"
#include "WindowSendKeyPressResponse.h"
#include "WindowSendMouseEventsRequest.h"
#include "WindowSendMouseEventsResponse.h"
#include "WindowSendMouseMoveRequest.h"
#include "WindowSendMouseMoveResponse.h"
#include "WindowSetAcceleratorRequest.h"
#include "WindowSetAcceleratorResponse.h"
#include "WindowSetAlwaysOnTopRequest.h"
#include "WindowSetAlwaysOnTopResponse.h"
#include "WindowSetFullscreenRequest.h"
#include "WindowSetFullscreenResponse.h"
#include "WindowSetThemeColorRequest.h"
#include "WindowSetThemeColorResponse.h"
#include "WindowSetTitleRequest.h"
#include "WindowSetTitleResponse.h"
#include "WindowSetWindowAppIconRequest.h"
#include "WindowSetWindowAppIconResponse.h"
#include "WindowSetWindowIconRequest.h"
#include "WindowSetWindowIconResponse.h"
#include "WindowShowAsBrowserModalDialogRequest.h"
#include "WindowShowAsBrowserModalDialogResponse.h"
#include "WindowShowMenuRequest.h"
#include "WindowShowMenuResponse.h"
#include "WindowShowRequest.h"
#include "WindowShowResponse.h"
#include "WindowThemeChangedRequest.h"
#include "WindowThemeChangedResponse.h"
#include "X509CertPrincipalGetCommonNameRequest.h"
#include "X509CertPrincipalGetCommonNameResponse.h"
#include "X509CertPrincipalGetCountryNameRequest.h"
#include "X509CertPrincipalGetCountryNameResponse.h"
#include "X509CertPrincipalGetDisplayNameRequest.h"
#include "X509CertPrincipalGetDisplayNameResponse.h"
#include "X509CertPrincipalGetLocalityNameRequest.h"
#include "X509CertPrincipalGetLocalityNameResponse.h"
#include "X509CertPrincipalGetOrganizationNamesRequest.h"
#include "X509CertPrincipalGetOrganizationNamesResponse.h"
#include "X509CertPrincipalGetOrganizationUnitNamesRequest.h"
#include "X509CertPrincipalGetOrganizationUnitNamesResponse.h"
#include "X509CertPrincipalGetStateOrProvinceNameRequest.h"
#include "X509CertPrincipalGetStateOrProvinceNameResponse.h"
#include "X509CertificateGetDerencodedRequest.h"
#include "X509CertificateGetDerencodedResponse.h"
#include "X509CertificateGetIssuerChainSizeRequest.h"
#include "X509CertificateGetIssuerChainSizeResponse.h"
#include "X509CertificateGetIssuerRequest.h"
#include "X509CertificateGetIssuerResponse.h"
#include "X509CertificateGetPemencodedRequest.h"
#include "X509CertificateGetPemencodedResponse.h"
#include "X509CertificateGetSerialNumberRequest.h"
#include "X509CertificateGetSerialNumberResponse.h"
#include "X509CertificateGetSubjectRequest.h"
#include "X509CertificateGetSubjectResponse.h"
#include "X509CertificateGetValidExpiryRequest.h"
#include "X509CertificateGetValidExpiryResponse.h"
#include "X509CertificateGetValidStartRequest.h"
#include "X509CertificateGetValidStartResponse.h"
#include "XmlReaderCloseRequest.h"
#include "XmlReaderCloseResponse.h"
#include "XmlReaderGetAttributeByindexRequest.h"
#include "XmlReaderGetAttributeByindexResponse.h"
#include "XmlReaderGetAttributeBylnameRequest.h"
#include "XmlReaderGetAttributeBylnameResponse.h"
#include "XmlReaderGetAttributeByqnameRequest.h"
#include "XmlReaderGetAttributeByqnameResponse.h"
#include "XmlReaderGetAttributeCountRequest.h"
#include "XmlReaderGetAttributeCountResponse.h"
#include "XmlReaderGetBaseUriRequest.h"
#include "XmlReaderGetBaseUriResponse.h"
#include "XmlReaderGetDepthRequest.h"
#include "XmlReaderGetDepthResponse.h"
#include "XmlReaderGetErrorRequest.h"
#include "XmlReaderGetErrorResponse.h"
#include "XmlReaderGetInnerXmlRequest.h"
#include "XmlReaderGetInnerXmlResponse.h"
#include "XmlReaderGetLineNumberRequest.h"
#include "XmlReaderGetLineNumberResponse.h"
#include "XmlReaderGetLocalNameRequest.h"
#include "XmlReaderGetLocalNameResponse.h"
#include "XmlReaderGetNamespaceUriRequest.h"
#include "XmlReaderGetNamespaceUriResponse.h"
#include "XmlReaderGetOuterXmlRequest.h"
#include "XmlReaderGetOuterXmlResponse.h"
#include "XmlReaderGetPrefixRequest.h"
#include "XmlReaderGetPrefixResponse.h"
#include "XmlReaderGetQualifiedNameRequest.h"
#include "XmlReaderGetQualifiedNameResponse.h"
#include "XmlReaderGetTypeRequest.h"
#include "XmlReaderGetTypeResponse.h"
#include "XmlReaderGetValueRequest.h"
#include "XmlReaderGetValueResponse.h"
#include "XmlReaderGetXmlLangRequest.h"
#include "XmlReaderGetXmlLangResponse.h"
#include "XmlReaderHasAttributesRequest.h"
#include "XmlReaderHasAttributesResponse.h"
#include "XmlReaderHasErrorRequest.h"
#include "XmlReaderHasErrorResponse.h"
#include "XmlReaderHasValueRequest.h"
#include "XmlReaderHasValueResponse.h"
#include "XmlReaderIsEmptyElementRequest.h"
#include "XmlReaderIsEmptyElementResponse.h"
#include "XmlReaderMoveToAttributeByindexRequest.h"
#include "XmlReaderMoveToAttributeByindexResponse.h"
#include "XmlReaderMoveToAttributeBylnameRequest.h"
#include "XmlReaderMoveToAttributeBylnameResponse.h"
#include "XmlReaderMoveToAttributeByqnameRequest.h"
#include "XmlReaderMoveToAttributeByqnameResponse.h"
#include "XmlReaderMoveToCarryingElementRequest.h"
#include "XmlReaderMoveToCarryingElementResponse.h"
#include "XmlReaderMoveToFirstAttributeRequest.h"
#include "XmlReaderMoveToFirstAttributeResponse.h"
#include "XmlReaderMoveToNextAttributeRequest.h"
#include "XmlReaderMoveToNextAttributeResponse.h"
#include "XmlReaderMoveToNextNodeRequest.h"
#include "XmlReaderMoveToNextNodeResponse.h"
#include "ZipReaderCloseFileRequest.h"
#include "ZipReaderCloseFileResponse.h"
#include "ZipReaderCloseRequest.h"
#include "ZipReaderCloseResponse.h"
#include "ZipReaderEofRequest.h"
#include "ZipReaderEofResponse.h"
#include "ZipReaderGetFileLastModifiedRequest.h"
#include "ZipReaderGetFileLastModifiedResponse.h"
#include "ZipReaderGetFileNameRequest.h"
#include "ZipReaderGetFileNameResponse.h"
#include "ZipReaderGetFileSizeRequest.h"
#include "ZipReaderGetFileSizeResponse.h"
#include "ZipReaderMoveToFileRequest.h"
#include "ZipReaderMoveToFileResponse.h"
#include "ZipReaderMoveToFirstFileRequest.h"
#include "ZipReaderMoveToFirstFileResponse.h"
#include "ZipReaderMoveToNextFileRequest.h"
#include "ZipReaderMoveToNextFileResponse.h"
#include "ZipReaderOpenFileRequest.h"
#include "ZipReaderOpenFileResponse.h"
#include "ZipReaderTellRequest.h"
#include "ZipReaderTellResponse.h"

namespace net_kurobako_cef4j_ipc_protocol_gen_dispatcher {

namespace genvisitors = net_kurobako_cef4j_ipc_protocol_gen_visitors;

/** RAII shim around `cef_string_t` for params and `cef_string_userfree_t` for returns; mirrors the runtime server's
  * own ScopedCefString. Lives in the dispatcher namespace to avoid clashing with the runtime server's copy. */
class ScopedCefString {
public:
    ScopedCefString() : s_{} {}
    explicit ScopedCefString(const std::string& utf8) : s_{} {
        if (!utf8.empty()) cef_string_utf8_to_utf16(utf8.data(), utf8.size(), &s_);
    }
    static ScopedCefString take(cef_string_userfree_t uf) {
        ScopedCefString s;
        if (uf) {
            s.s_ = *uf;
            uf->str = nullptr;
            uf->length = 0;
            cef_string_userfree_free(uf);
        }
        return s;
    }
    ~ScopedCefString() { cef_string_clear(&s_); }
    ScopedCefString(const ScopedCefString&) = delete;
    ScopedCefString& operator=(const ScopedCefString&) = delete;
    ScopedCefString(ScopedCefString&& o) noexcept : s_(o.s_) { o.s_ = {}; }
    cef_string_t* get() { return &s_; }
    const cef_string_t* get() const { return &s_; }
    std::string toUtf8() const {
        if (s_.length == 0) return {};
        cef_string_utf8_t utf8{};
        cef_string_utf16_to_utf8(s_.str, s_.length, &utf8);
        std::string r(utf8.str, utf8.length);
        cef_string_utf8_clear(&utf8);
        return r;
    }
private:
    cef_string_t s_;
};

/** Extracts CEF C-API function-pointer parameter types so we can `static_cast` int32 wire values to the
  * native enum types CEF expects (cef_state_t, cef_color_model_t, etc.). The dispatcher's call sites use
  * `cefArg<decltype(receiver->method), N>(value)` to convert arg N to the right CEF enum/int. */
template <typename F> struct fn_args;
template <typename R, typename... Args> struct fn_args<R (*)(Args...)> {
    using result = R;
    template <std::size_t N> using arg = std::tuple_element_t<N, std::tuple<Args...>>;
};
template <typename F, std::size_t N, typename V>
constexpr typename fn_args<F>::template arg<N> cefArg(V v) {
    return static_cast<typename fn_args<F>::template arg<N>>(v);
}
template <typename F, typename V>
constexpr typename fn_args<F>::result cefRet(V v) {
    return static_cast<typename fn_args<F>::result>(v);
}

/** Reads a `cef_string_t` member out of a CEF return-by-value struct into UTF-8. The native cef_string_t
  * still owns its UTF-16 buffer until the caller clears it; this helper is read-only. */
inline std::string cef4j_dispatcher_utf16ToStdString(const cef_string_t& s) {
    if (s.length == 0) return {};
    cef_string_utf8_t utf8{};
    cef_string_utf16_to_utf8(s.str, s.length, &utf8);
    std::string r(utf8.str, utf8.length);
    cef_string_utf8_clear(&utf8);
    return r;
}

/** Inserts a freshly-returned, +1-retained CEF pointer into a HandleTable and rebalances the refcount.
  * `HandleTable::insert` retains again (+2 total), so we release once to drop back to +1 owned by the table.
  * Null pointers become id 0; a null table releases the input and returns 0. */
template <typename T>
std::int32_t insertOrRelease(cef4j::ipc::HandleTable<T>* table, T* ptr) {
    if (!ptr) return 0;
    if (!table) {
        auto* base = reinterpret_cast<cef_base_ref_counted_t*>(ptr);
        base->release(base);
        return 0;
    }
    std::int32_t id = table->insert(ptr);
    auto* base = reinterpret_cast<cef_base_ref_counted_t*>(ptr);
    base->release(base);
    return id;
}

/** Per-facade-struct HandleTable<T> instances. Lifetime: process. The runtime server accesses these directly to
  * register browsers etc. (e.g. `tables::browser.insert(b)` after `OnAfterCreated`); the dispatcher's
  * generated cases use them for retain/release/insert against handle params and returns. */
namespace tables {
    inline cef4j::ipc::HandleTable<cef_value_t> value;
    inline cef4j::ipc::HandleTable<cef_binary_value_t> binaryValue;
    inline cef4j::ipc::HandleTable<cef_dictionary_value_t> dictionaryValue;
    inline cef4j::ipc::HandleTable<cef_list_value_t> listValue;
    inline cef4j::ipc::HandleTable<cef_image_t> image;
    inline cef4j::ipc::HandleTable<cef_stream_reader_t> streamReader;
    inline cef4j::ipc::HandleTable<cef_stream_writer_t> streamWriter;
    inline cef4j::ipc::HandleTable<cef_drag_data_t> dragData;
    inline cef4j::ipc::HandleTable<cef_domdocument_t> domdocument;
    inline cef4j::ipc::HandleTable<cef_domnode_t> domnode;
    inline cef4j::ipc::HandleTable<cef_shared_memory_region_t> sharedMemoryRegion;
    inline cef4j::ipc::HandleTable<cef_process_message_t> processMessage;
    inline cef4j::ipc::HandleTable<cef_request_t> request;
    inline cef4j::ipc::HandleTable<cef_post_data_t> postData;
    inline cef4j::ipc::HandleTable<cef_post_data_element_t> postDataElement;
    inline cef4j::ipc::HandleTable<cef_frame_t> frame;
    inline cef4j::ipc::HandleTable<cef_x509_cert_principal_t> x509CertPrincipal;
    inline cef4j::ipc::HandleTable<cef_x509_certificate_t> x509Certificate;
    inline cef4j::ipc::HandleTable<cef_sslstatus_t> sslstatus;
    inline cef4j::ipc::HandleTable<cef_navigation_entry_t> navigationEntry;
    inline cef4j::ipc::HandleTable<cef_callback_t> callback;
    inline cef4j::ipc::HandleTable<cef_cookie_manager_t> cookieManager;
    inline cef4j::ipc::HandleTable<cef_media_router_t> mediaRouter;
    inline cef4j::ipc::HandleTable<cef_media_route_t> mediaRoute;
    inline cef4j::ipc::HandleTable<cef_media_sink_t> mediaSink;
    inline cef4j::ipc::HandleTable<cef_media_source_t> mediaSource;
    inline cef4j::ipc::HandleTable<cef_preference_registrar_t> preferenceRegistrar;
    inline cef4j::ipc::HandleTable<cef_preference_manager_t> preferenceManager;
    inline cef4j::ipc::HandleTable<cef_request_context_t> requestContext;
    inline cef4j::ipc::HandleTable<cef_browser_t> browser;
    inline cef4j::ipc::HandleTable<cef_browser_host_t> browserHost;
    inline cef4j::ipc::HandleTable<cef_menu_model_t> menuModel;
    inline cef4j::ipc::HandleTable<cef_run_context_menu_callback_t> runContextMenuCallback;
    inline cef4j::ipc::HandleTable<cef_run_quick_menu_callback_t> runQuickMenuCallback;
    inline cef4j::ipc::HandleTable<cef_context_menu_params_t> contextMenuParams;
    inline cef4j::ipc::HandleTable<cef_file_dialog_callback_t> fileDialogCallback;
    inline cef4j::ipc::HandleTable<cef_download_item_t> downloadItem;
    inline cef4j::ipc::HandleTable<cef_before_download_callback_t> beforeDownloadCallback;
    inline cef4j::ipc::HandleTable<cef_download_item_callback_t> downloadItemCallback;
    inline cef4j::ipc::HandleTable<cef_jsdialog_callback_t> jsDialogCallback;
    inline cef4j::ipc::HandleTable<cef_media_access_callback_t> mediaAccessCallback;
    inline cef4j::ipc::HandleTable<cef_permission_prompt_callback_t> permissionPromptCallback;
    inline cef4j::ipc::HandleTable<cef_print_settings_t> printSettings;
    inline cef4j::ipc::HandleTable<cef_print_dialog_callback_t> printDialogCallback;
    inline cef4j::ipc::HandleTable<cef_print_job_callback_t> printJobCallback;
    inline cef4j::ipc::HandleTable<cef_auth_callback_t> authCallback;
    inline cef4j::ipc::HandleTable<cef_response_t> response;
    inline cef4j::ipc::HandleTable<cef_resource_skip_callback_t> resourceSkipCallback;
    inline cef4j::ipc::HandleTable<cef_resource_read_callback_t> resourceReadCallback;
    inline cef4j::ipc::HandleTable<cef_sslinfo_t> sslinfo;
    inline cef4j::ipc::HandleTable<cef_unresponsive_process_callback_t> unresponsiveProcessCallback;
    inline cef4j::ipc::HandleTable<cef_select_client_certificate_callback_t> selectClientCertificateCallback;
    inline cef4j::ipc::HandleTable<cef_command_line_t> commandLine;
    inline cef4j::ipc::HandleTable<cef_task_runner_t> taskRunner;
    inline cef4j::ipc::HandleTable<cef_v8_context_t> v8Context;
    inline cef4j::ipc::HandleTable<cef_v8_exception_t> v8Exception;
    inline cef4j::ipc::HandleTable<cef_v8_backing_store_t> v8BackingStore;
    inline cef4j::ipc::HandleTable<cef_v8_value_t> v8Value;
    inline cef4j::ipc::HandleTable<cef_v8_stack_trace_t> v8StackTrace;
    inline cef4j::ipc::HandleTable<cef_v8_stack_frame_t> v8StackFrame;
    inline cef4j::ipc::HandleTable<cef_scheme_registrar_t> schemeRegistrar;
    inline cef4j::ipc::HandleTable<cef_component_t> component;
    inline cef4j::ipc::HandleTable<cef_component_updater_t> componentUpdater;
    inline cef4j::ipc::HandleTable<cef_resource_bundle_t> resourceBundle;
    inline cef4j::ipc::HandleTable<cef_server_t> server;
    inline cef4j::ipc::HandleTable<cef_shared_process_message_builder_t> sharedProcessMessageBuilder;
    inline cef4j::ipc::HandleTable<cef_task_manager_t> taskManager;
    inline cef4j::ipc::HandleTable<cef_thread_t> thread;
    inline cef4j::ipc::HandleTable<cef_urlrequest_t> urlrequest;
    inline cef4j::ipc::HandleTable<cef_waitable_event_t> waitableEvent;
    inline cef4j::ipc::HandleTable<cef_xml_reader_t> xmlReader;
    inline cef4j::ipc::HandleTable<cef_zip_reader_t> zipReader;
    inline cef4j::ipc::HandleTable<cef_layout_t> layout;
    inline cef4j::ipc::HandleTable<cef_box_layout_t> boxLayout;
    inline cef4j::ipc::HandleTable<cef_view_delegate_t> viewDelegate;
    inline cef4j::ipc::HandleTable<cef_browser_view_delegate_t> browserViewDelegate;
    inline cef4j::ipc::HandleTable<cef_view_t> view;
    inline cef4j::ipc::HandleTable<cef_browser_view_t> browserView;
    inline cef4j::ipc::HandleTable<cef_button_t> button;
    inline cef4j::ipc::HandleTable<cef_button_delegate_t> buttonDelegate;
    inline cef4j::ipc::HandleTable<cef_display_t> display;
    inline cef4j::ipc::HandleTable<cef_label_button_t> labelButton;
    inline cef4j::ipc::HandleTable<cef_menu_button_t> menuButton;
    inline cef4j::ipc::HandleTable<cef_overlay_controller_t> overlayController;
    inline cef4j::ipc::HandleTable<cef_panel_t> panel;
    inline cef4j::ipc::HandleTable<cef_scroll_view_t> scrollView;
    inline cef4j::ipc::HandleTable<cef_textfield_delegate_t> textFieldDelegate;
    inline cef4j::ipc::HandleTable<cef_textfield_t> textfield;
    inline cef4j::ipc::HandleTable<cef_window_delegate_t> windowDelegate;
    inline cef4j::ipc::HandleTable<cef_window_t> window;

} // namespace tables

/** Releases the table entry matching the given CEF struct name. Used by the runtime server to dispatch
  * `ReleaseHandleRequest{handle, kind}` to the right `HandleTable<T>::release`. Returns true if `kind`
  * was recognised (release attempted). Unknown kinds are silently ignored. */
inline bool dispatchRelease(const std::string& kind, std::int32_t id) {
    if (kind == "cef_value_t") { tables::value.release(id); return true; }
    if (kind == "cef_binary_value_t") { tables::binaryValue.release(id); return true; }
    if (kind == "cef_dictionary_value_t") { tables::dictionaryValue.release(id); return true; }
    if (kind == "cef_list_value_t") { tables::listValue.release(id); return true; }
    if (kind == "cef_image_t") { tables::image.release(id); return true; }
    if (kind == "cef_stream_reader_t") { tables::streamReader.release(id); return true; }
    if (kind == "cef_stream_writer_t") { tables::streamWriter.release(id); return true; }
    if (kind == "cef_drag_data_t") { tables::dragData.release(id); return true; }
    if (kind == "cef_domdocument_t") { tables::domdocument.release(id); return true; }
    if (kind == "cef_domnode_t") { tables::domnode.release(id); return true; }
    if (kind == "cef_shared_memory_region_t") { tables::sharedMemoryRegion.release(id); return true; }
    if (kind == "cef_process_message_t") { tables::processMessage.release(id); return true; }
    if (kind == "cef_request_t") { tables::request.release(id); return true; }
    if (kind == "cef_post_data_t") { tables::postData.release(id); return true; }
    if (kind == "cef_post_data_element_t") { tables::postDataElement.release(id); return true; }
    if (kind == "cef_frame_t") { tables::frame.release(id); return true; }
    if (kind == "cef_x509_cert_principal_t") { tables::x509CertPrincipal.release(id); return true; }
    if (kind == "cef_x509_certificate_t") { tables::x509Certificate.release(id); return true; }
    if (kind == "cef_sslstatus_t") { tables::sslstatus.release(id); return true; }
    if (kind == "cef_navigation_entry_t") { tables::navigationEntry.release(id); return true; }
    if (kind == "cef_callback_t") { tables::callback.release(id); return true; }
    if (kind == "cef_cookie_manager_t") { tables::cookieManager.release(id); return true; }
    if (kind == "cef_media_router_t") { tables::mediaRouter.release(id); return true; }
    if (kind == "cef_media_route_t") { tables::mediaRoute.release(id); return true; }
    if (kind == "cef_media_sink_t") { tables::mediaSink.release(id); return true; }
    if (kind == "cef_media_source_t") { tables::mediaSource.release(id); return true; }
    if (kind == "cef_preference_registrar_t") { tables::preferenceRegistrar.release(id); return true; }
    if (kind == "cef_preference_manager_t") { tables::preferenceManager.release(id); return true; }
    if (kind == "cef_request_context_t") { tables::requestContext.release(id); return true; }
    if (kind == "cef_browser_t") { tables::browser.release(id); return true; }
    if (kind == "cef_browser_host_t") { tables::browserHost.release(id); return true; }
    if (kind == "cef_menu_model_t") { tables::menuModel.release(id); return true; }
    if (kind == "cef_run_context_menu_callback_t") { tables::runContextMenuCallback.release(id); return true; }
    if (kind == "cef_run_quick_menu_callback_t") { tables::runQuickMenuCallback.release(id); return true; }
    if (kind == "cef_context_menu_params_t") { tables::contextMenuParams.release(id); return true; }
    if (kind == "cef_file_dialog_callback_t") { tables::fileDialogCallback.release(id); return true; }
    if (kind == "cef_download_item_t") { tables::downloadItem.release(id); return true; }
    if (kind == "cef_before_download_callback_t") { tables::beforeDownloadCallback.release(id); return true; }
    if (kind == "cef_download_item_callback_t") { tables::downloadItemCallback.release(id); return true; }
    if (kind == "cef_jsdialog_callback_t") { tables::jsDialogCallback.release(id); return true; }
    if (kind == "cef_media_access_callback_t") { tables::mediaAccessCallback.release(id); return true; }
    if (kind == "cef_permission_prompt_callback_t") { tables::permissionPromptCallback.release(id); return true; }
    if (kind == "cef_print_settings_t") { tables::printSettings.release(id); return true; }
    if (kind == "cef_print_dialog_callback_t") { tables::printDialogCallback.release(id); return true; }
    if (kind == "cef_print_job_callback_t") { tables::printJobCallback.release(id); return true; }
    if (kind == "cef_auth_callback_t") { tables::authCallback.release(id); return true; }
    if (kind == "cef_response_t") { tables::response.release(id); return true; }
    if (kind == "cef_resource_skip_callback_t") { tables::resourceSkipCallback.release(id); return true; }
    if (kind == "cef_resource_read_callback_t") { tables::resourceReadCallback.release(id); return true; }
    if (kind == "cef_sslinfo_t") { tables::sslinfo.release(id); return true; }
    if (kind == "cef_unresponsive_process_callback_t") { tables::unresponsiveProcessCallback.release(id); return true; }
    if (kind == "cef_select_client_certificate_callback_t") { tables::selectClientCertificateCallback.release(id); return true; }
    if (kind == "cef_command_line_t") { tables::commandLine.release(id); return true; }
    if (kind == "cef_task_runner_t") { tables::taskRunner.release(id); return true; }
    if (kind == "cef_v8_context_t") { tables::v8Context.release(id); return true; }
    if (kind == "cef_v8_exception_t") { tables::v8Exception.release(id); return true; }
    if (kind == "cef_v8_backing_store_t") { tables::v8BackingStore.release(id); return true; }
    if (kind == "cef_v8_value_t") { tables::v8Value.release(id); return true; }
    if (kind == "cef_v8_stack_trace_t") { tables::v8StackTrace.release(id); return true; }
    if (kind == "cef_v8_stack_frame_t") { tables::v8StackFrame.release(id); return true; }
    if (kind == "cef_scheme_registrar_t") { tables::schemeRegistrar.release(id); return true; }
    if (kind == "cef_component_t") { tables::component.release(id); return true; }
    if (kind == "cef_component_updater_t") { tables::componentUpdater.release(id); return true; }
    if (kind == "cef_resource_bundle_t") { tables::resourceBundle.release(id); return true; }
    if (kind == "cef_server_t") { tables::server.release(id); return true; }
    if (kind == "cef_shared_process_message_builder_t") { tables::sharedProcessMessageBuilder.release(id); return true; }
    if (kind == "cef_task_manager_t") { tables::taskManager.release(id); return true; }
    if (kind == "cef_thread_t") { tables::thread.release(id); return true; }
    if (kind == "cef_urlrequest_t") { tables::urlrequest.release(id); return true; }
    if (kind == "cef_waitable_event_t") { tables::waitableEvent.release(id); return true; }
    if (kind == "cef_xml_reader_t") { tables::xmlReader.release(id); return true; }
    if (kind == "cef_zip_reader_t") { tables::zipReader.release(id); return true; }
    if (kind == "cef_layout_t") { tables::layout.release(id); return true; }
    if (kind == "cef_box_layout_t") { tables::boxLayout.release(id); return true; }
    if (kind == "cef_view_delegate_t") { tables::viewDelegate.release(id); return true; }
    if (kind == "cef_browser_view_delegate_t") { tables::browserViewDelegate.release(id); return true; }
    if (kind == "cef_view_t") { tables::view.release(id); return true; }
    if (kind == "cef_browser_view_t") { tables::browserView.release(id); return true; }
    if (kind == "cef_button_t") { tables::button.release(id); return true; }
    if (kind == "cef_button_delegate_t") { tables::buttonDelegate.release(id); return true; }
    if (kind == "cef_display_t") { tables::display.release(id); return true; }
    if (kind == "cef_label_button_t") { tables::labelButton.release(id); return true; }
    if (kind == "cef_menu_button_t") { tables::menuButton.release(id); return true; }
    if (kind == "cef_overlay_controller_t") { tables::overlayController.release(id); return true; }
    if (kind == "cef_panel_t") { tables::panel.release(id); return true; }
    if (kind == "cef_scroll_view_t") { tables::scrollView.release(id); return true; }
    if (kind == "cef_textfield_delegate_t") { tables::textFieldDelegate.release(id); return true; }
    if (kind == "cef_textfield_t") { tables::textfield.release(id); return true; }
    if (kind == "cef_window_delegate_t") { tables::windowDelegate.release(id); return true; }
    if (kind == "cef_window_t") { tables::window.release(id); return true; }
    return false;
}

/** Held by the runtime server main and passed into {@link dispatch}. Owned by the runtime server; the dispatcher does not
  * free anything. Tables are static and live in `net_kurobako_cef4j_ipc_protocol_gen_dispatcher::tables`. */
struct DispatcherContext {
    cef4j::ipc::IpcServer* ipc = nullptr;
};

/** Runs a {@code std::function<void()>} as a CEF task. Self-deletes after one execute via the standard
  * ref-counted release path; refCount starts at 1 and the task pointer is handed off to {@code cef_post_task}. */
struct LambdaTask : cef_task_t {
    std::atomic<int> refCount{1};
    std::function<void()> fn;

    explicit LambdaTask(std::function<void()> f) : cef_task_t{}, fn(std::move(f)) {
        base.size = sizeof(cef_task_t);
        base.add_ref = [](cef_base_ref_counted_t* self) {
            reinterpret_cast<LambdaTask*>(self)->refCount.fetch_add(1, std::memory_order_relaxed);
        };
        base.release = [](cef_base_ref_counted_t* self) -> int {
            auto* t = reinterpret_cast<LambdaTask*>(self);
            if (t->refCount.fetch_sub(1, std::memory_order_acq_rel) == 1) {
                delete t;
                return 1;
            }
            return 0;
        };
        base.has_one_ref = [](cef_base_ref_counted_t* self) -> int {
            return reinterpret_cast<LambdaTask*>(self)->refCount.load(std::memory_order_acquire) == 1;
        };
        base.has_at_least_one_ref = [](cef_base_ref_counted_t* self) -> int {
            return reinterpret_cast<LambdaTask*>(self)->refCount.load(std::memory_order_acquire) >= 1;
        };
        execute = [](cef_task_t* self) { reinterpret_cast<LambdaTask*>(self)->fn(); };
    }
};

inline bool postUiTask(cef_task_t* task) {
    if (cef_post_task(TID_UI, task)) return true;
    auto* base = reinterpret_cast<cef_base_ref_counted_t*>(task);
    base->release(base);
    return false;
}

inline void sendTaskRejected(cef4j::ipc::IpcServer* ipc, std::int32_t corrId, std::int32_t messageId) {
    constexpr std::int32_t code = cef4j::ipc::ErrorCode::TaskRejected;
    static const std::uint8_t kTaskRejectedPayload[8] = {
            static_cast<std::uint8_t>(code), static_cast<std::uint8_t>(code >> 8),
            static_cast<std::uint8_t>(code >> 16), static_cast<std::uint8_t>(code >> 24),
            0x00, 0x00, 0x00, 0x00};
    if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, messageId,
                       kTaskRejectedPayload, sizeof(kTaskRejectedPayload));
}

/** Relays a renderer-affinity Request to the renderer subprocess via cef_process_message. Decodes the
  * leading int32 frame handle from the wire payload (renderer-affinity Requests have `frame: RemoteHandle`
  * as their first field, see SpecDeriver.deriveOne), looks the frame up in `tables::frame`, and ships the
  * raw payload over a "cef4j_renderer_req" process_message. The renderer-side dispatcher decodes the
  * Request from the same payload bytes and invokes the method inside the right V8 context.
  *
  * Reply path: renderer fires "cef4j_renderer_resp" carrying corrId/messageId/payload back; the runtime server's
  * Client::on_process_message_received turns that into a Kind::Response on the IPC wire.
  *
  * Posts the actual `frame->send_process_message` call onto the CEF UI thread (CEF API contract for
  * cef_frame_t methods); the dispatcher itself runs on the IPC worker thread.
  */
inline bool relayToRenderer(const DispatcherContext& ctx, const cef4j::ipc::Header& h,
                            const std::vector<std::uint8_t>& payload) {
    if (payload.size() < 4) return false;
    std::int32_t frameId = 0;
    std::memcpy(&frameId, payload.data(), sizeof(frameId));
    cef_frame_t* frame = tables::frame.retain(frameId);
    if (!frame) {
        static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
        if (ctx.ipc) ctx.ipc->send(cef4j::ipc::Kind::Error, 0, h.corrId, h.messageId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
        return true;
    }
    std::int32_t corrId    = h.corrId;
    std::int32_t messageId = h.messageId;
    std::vector<std::uint8_t> payloadCopy = payload;
    auto* uiTask = new LambdaTask([frame, corrId, messageId, payloadCopy]() {
        cef_string_t name{};
        cef_string_utf8_to_utf16("cef4j_renderer_req", 18, &name);
        cef_process_message_t* msg = cef_process_message_create(&name);
        cef_string_clear(&name);
        if (msg) {
            cef_list_value_t* args = msg->get_argument_list(msg);
            if (args) {
                args->set_int(args, 0, corrId);
                args->set_int(args, 1, messageId);
                cef_binary_value_t* binary = cef_binary_value_create(payloadCopy.data(), payloadCopy.size());
                if (binary) {
                    // `set_binary` ADOPTS the +1 from `cef_binary_value_create` — do NOT release `binary`
                    // again here, that's a double-decrement that crashes the runtime server.
                    args->set_binary(args, 2, binary);
                }
                auto* ab = reinterpret_cast<cef_base_ref_counted_t*>(args);
                ab->release(ab);
            }
            // CEF adopts the message and invalidates our reference — do NOT release it ourselves.
            frame->send_process_message(frame, PID_RENDERER, msg);
        }
        auto* base = reinterpret_cast<cef_base_ref_counted_t*>(frame);
        base->release(base);
    });
    if (!postUiTask(uiTask)) {
        auto* base = reinterpret_cast<cef_base_ref_counted_t*>(frame);
        base->release(base);
        sendTaskRejected(ctx.ipc, corrId, messageId);
    }
    return true;
}

/** Tries to handle one frame. Returns {@code true} iff the message id was recognised by codegen — the runtime server's
  * own switch should fall through to this for ids it doesn't claim. */
inline bool dispatch(const DispatcherContext& ctx, const cef4j::ipc::Header& h,
                     std::vector<std::uint8_t> payload) {
    if (h.kind != cef4j::ipc::Kind::Request) return false;
    namespace gen = net_kurobako_cef4j_ipc_protocol_gen;
    switch (h.messageId) {
        case gen::ValueIsValidRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ValueIsValidRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::value.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_valid(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ValueIsValidResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ValueIsOwnedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ValueIsOwnedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::value.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_owned(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ValueIsOwnedResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ValueIsReadOnlyRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ValueIsReadOnlyRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::value.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_read_only(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ValueIsReadOnlyResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ValueIsSameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ValueIsSameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::value.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t that = req.that;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, that]() {
                cef_value_t* that_ptr = that != 0 ? tables::value.retain(that) : nullptr;
                auto rawResult = receiver->is_same(receiver, that_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (that_ptr) {
                    auto* that_base = reinterpret_cast<cef_base_ref_counted_t*>(that_ptr);
                    that_base->release(that_base);
                }
                if (ipc) {
                    gen::ValueIsSameResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ValueIsEqualRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ValueIsEqualRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::value.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t that = req.that;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, that]() {
                cef_value_t* that_ptr = that != 0 ? tables::value.retain(that) : nullptr;
                auto rawResult = receiver->is_equal(receiver, that_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (that_ptr) {
                    auto* that_base = reinterpret_cast<cef_base_ref_counted_t*>(that_ptr);
                    that_base->release(that_base);
                }
                if (ipc) {
                    gen::ValueIsEqualResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ValueCopyRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ValueCopyRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::value.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->copy(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ValueCopyResponse resp;
                    resp.result = insertOrRelease(&tables::value, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ValueGetTypeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ValueGetTypeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::value.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_type(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ValueGetTypeResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ValueGetBoolRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ValueGetBoolRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::value.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_bool(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ValueGetBoolResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ValueGetIntRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ValueGetIntRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::value.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_int(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ValueGetIntResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ValueGetStringRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ValueGetStringRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::value.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_string(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ValueGetStringResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ValueGetBinaryRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ValueGetBinaryRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::value.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_binary(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ValueGetBinaryResponse resp;
                    resp.result = insertOrRelease(&tables::binaryValue, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ValueGetDictionaryRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ValueGetDictionaryRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::value.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_dictionary(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ValueGetDictionaryResponse resp;
                    resp.result = insertOrRelease(&tables::dictionaryValue, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ValueGetListRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ValueGetListRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::value.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_list(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ValueGetListResponse resp;
                    resp.result = insertOrRelease(&tables::listValue, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ValueSetNullRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ValueSetNullRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::value.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->set_null(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ValueSetNullResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ValueSetBoolRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ValueSetBoolRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::value.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t value = req.value;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, value]() {
                auto rawResult = receiver->set_bool(receiver, cefArg<decltype(receiver->set_bool), 1>(value));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ValueSetBoolResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ValueSetIntRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ValueSetIntRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::value.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t value = req.value;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, value]() {
                auto rawResult = receiver->set_int(receiver, cefArg<decltype(receiver->set_int), 1>(value));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ValueSetIntResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ValueSetStringRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ValueSetStringRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::value.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string value = req.value;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, value]() {
                ScopedCefString value_cef(value);
                auto rawResult = receiver->set_string(receiver, value_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ValueSetStringResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ValueSetBinaryRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ValueSetBinaryRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::value.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t value = req.value;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, value]() {
                cef_binary_value_t* value_ptr = value != 0 ? tables::binaryValue.retain(value) : nullptr;
                auto rawResult = receiver->set_binary(receiver, value_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (value_ptr) {
                    auto* value_base = reinterpret_cast<cef_base_ref_counted_t*>(value_ptr);
                    value_base->release(value_base);
                }
                if (ipc) {
                    gen::ValueSetBinaryResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ValueSetDictionaryRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ValueSetDictionaryRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::value.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t value = req.value;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, value]() {
                cef_dictionary_value_t* value_ptr = value != 0 ? tables::dictionaryValue.retain(value) : nullptr;
                auto rawResult = receiver->set_dictionary(receiver, value_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (value_ptr) {
                    auto* value_base = reinterpret_cast<cef_base_ref_counted_t*>(value_ptr);
                    value_base->release(value_base);
                }
                if (ipc) {
                    gen::ValueSetDictionaryResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ValueSetListRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ValueSetListRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::value.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t value = req.value;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, value]() {
                cef_list_value_t* value_ptr = value != 0 ? tables::listValue.retain(value) : nullptr;
                auto rawResult = receiver->set_list(receiver, value_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (value_ptr) {
                    auto* value_base = reinterpret_cast<cef_base_ref_counted_t*>(value_ptr);
                    value_base->release(value_base);
                }
                if (ipc) {
                    gen::ValueSetListResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BinaryValueIsValidRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BinaryValueIsValidRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::binaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_valid(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BinaryValueIsValidResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BinaryValueIsOwnedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BinaryValueIsOwnedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::binaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_owned(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BinaryValueIsOwnedResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BinaryValueIsSameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BinaryValueIsSameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::binaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t that = req.that;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, that]() {
                cef_binary_value_t* that_ptr = that != 0 ? tables::binaryValue.retain(that) : nullptr;
                auto rawResult = receiver->is_same(receiver, that_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (that_ptr) {
                    auto* that_base = reinterpret_cast<cef_base_ref_counted_t*>(that_ptr);
                    that_base->release(that_base);
                }
                if (ipc) {
                    gen::BinaryValueIsSameResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BinaryValueIsEqualRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BinaryValueIsEqualRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::binaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t that = req.that;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, that]() {
                cef_binary_value_t* that_ptr = that != 0 ? tables::binaryValue.retain(that) : nullptr;
                auto rawResult = receiver->is_equal(receiver, that_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (that_ptr) {
                    auto* that_base = reinterpret_cast<cef_base_ref_counted_t*>(that_ptr);
                    that_base->release(that_base);
                }
                if (ipc) {
                    gen::BinaryValueIsEqualResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BinaryValueCopyRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BinaryValueCopyRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::binaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->copy(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BinaryValueCopyResponse resp;
                    resp.result = insertOrRelease(&tables::binaryValue, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BinaryValueGetSizeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BinaryValueGetSizeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::binaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_size(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BinaryValueGetSizeResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DictionaryValueIsValidRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DictionaryValueIsValidRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dictionaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_valid(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DictionaryValueIsValidResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DictionaryValueIsOwnedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DictionaryValueIsOwnedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dictionaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_owned(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DictionaryValueIsOwnedResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DictionaryValueIsReadOnlyRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DictionaryValueIsReadOnlyRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dictionaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_read_only(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DictionaryValueIsReadOnlyResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DictionaryValueIsSameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DictionaryValueIsSameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dictionaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t that = req.that;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, that]() {
                cef_dictionary_value_t* that_ptr = that != 0 ? tables::dictionaryValue.retain(that) : nullptr;
                auto rawResult = receiver->is_same(receiver, that_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (that_ptr) {
                    auto* that_base = reinterpret_cast<cef_base_ref_counted_t*>(that_ptr);
                    that_base->release(that_base);
                }
                if (ipc) {
                    gen::DictionaryValueIsSameResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DictionaryValueIsEqualRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DictionaryValueIsEqualRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dictionaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t that = req.that;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, that]() {
                cef_dictionary_value_t* that_ptr = that != 0 ? tables::dictionaryValue.retain(that) : nullptr;
                auto rawResult = receiver->is_equal(receiver, that_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (that_ptr) {
                    auto* that_base = reinterpret_cast<cef_base_ref_counted_t*>(that_ptr);
                    that_base->release(that_base);
                }
                if (ipc) {
                    gen::DictionaryValueIsEqualResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DictionaryValueCopyRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DictionaryValueCopyRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dictionaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t excludeEmptyChildren = req.excludeEmptyChildren;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, excludeEmptyChildren]() {
                auto rawResult = receiver->copy(receiver, cefArg<decltype(receiver->copy), 1>(excludeEmptyChildren));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DictionaryValueCopyResponse resp;
                    resp.result = insertOrRelease(&tables::dictionaryValue, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DictionaryValueGetSizeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DictionaryValueGetSizeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dictionaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_size(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DictionaryValueGetSizeResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DictionaryValueClearRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DictionaryValueClearRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dictionaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->clear(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DictionaryValueClearResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DictionaryValueHasKeyRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DictionaryValueHasKeyRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dictionaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string key = req.key;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, key]() {
                ScopedCefString key_cef(key);
                auto rawResult = receiver->has_key(receiver, key_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DictionaryValueHasKeyResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DictionaryValueGetKeysRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DictionaryValueGetKeysRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dictionaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::vector<std::string> keys = std::move(req.keys);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, keys]() {
                cef_string_list_t keys_list = cef_string_list_alloc();
                for (const auto& __s : keys) {
                    cef_string_t __cs{};
                    if (!__s.empty()) cef_string_utf8_to_utf16(__s.data(), __s.size(), &__cs);
                    cef_string_list_append(keys_list, &__cs);
                    cef_string_clear(&__cs);
                }
                auto rawResult = receiver->get_keys(receiver, keys_list);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                cef_string_list_free(keys_list);
                if (ipc) {
                    gen::DictionaryValueGetKeysResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DictionaryValueRemoveRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DictionaryValueRemoveRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dictionaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string key = req.key;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, key]() {
                ScopedCefString key_cef(key);
                auto rawResult = receiver->remove(receiver, key_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DictionaryValueRemoveResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DictionaryValueGetTypeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DictionaryValueGetTypeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dictionaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string key = req.key;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, key]() {
                ScopedCefString key_cef(key);
                auto rawResult = receiver->get_type(receiver, key_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DictionaryValueGetTypeResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DictionaryValueGetValueRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DictionaryValueGetValueRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dictionaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string key = req.key;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, key]() {
                ScopedCefString key_cef(key);
                auto rawResult = receiver->get_value(receiver, key_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DictionaryValueGetValueResponse resp;
                    resp.result = insertOrRelease(&tables::value, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DictionaryValueGetBoolRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DictionaryValueGetBoolRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dictionaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string key = req.key;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, key]() {
                ScopedCefString key_cef(key);
                auto rawResult = receiver->get_bool(receiver, key_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DictionaryValueGetBoolResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DictionaryValueGetIntRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DictionaryValueGetIntRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dictionaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string key = req.key;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, key]() {
                ScopedCefString key_cef(key);
                auto rawResult = receiver->get_int(receiver, key_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DictionaryValueGetIntResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DictionaryValueGetStringRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DictionaryValueGetStringRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dictionaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string key = req.key;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, key]() {
                ScopedCefString key_cef(key);
                auto rawResult = receiver->get_string(receiver, key_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DictionaryValueGetStringResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DictionaryValueGetBinaryRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DictionaryValueGetBinaryRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dictionaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string key = req.key;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, key]() {
                ScopedCefString key_cef(key);
                auto rawResult = receiver->get_binary(receiver, key_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DictionaryValueGetBinaryResponse resp;
                    resp.result = insertOrRelease(&tables::binaryValue, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DictionaryValueGetDictionaryRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DictionaryValueGetDictionaryRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dictionaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string key = req.key;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, key]() {
                ScopedCefString key_cef(key);
                auto rawResult = receiver->get_dictionary(receiver, key_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DictionaryValueGetDictionaryResponse resp;
                    resp.result = insertOrRelease(&tables::dictionaryValue, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DictionaryValueGetListRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DictionaryValueGetListRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dictionaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string key = req.key;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, key]() {
                ScopedCefString key_cef(key);
                auto rawResult = receiver->get_list(receiver, key_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DictionaryValueGetListResponse resp;
                    resp.result = insertOrRelease(&tables::listValue, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DictionaryValueSetValueRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DictionaryValueSetValueRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dictionaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string key = req.key;
            std::int32_t value = req.value;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, key, value]() {
                ScopedCefString key_cef(key);
                cef_value_t* value_ptr = value != 0 ? tables::value.retain(value) : nullptr;
                auto rawResult = receiver->set_value(receiver, key_cef.get(), value_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (value_ptr) {
                    auto* value_base = reinterpret_cast<cef_base_ref_counted_t*>(value_ptr);
                    value_base->release(value_base);
                }
                if (ipc) {
                    gen::DictionaryValueSetValueResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DictionaryValueSetNullRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DictionaryValueSetNullRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dictionaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string key = req.key;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, key]() {
                ScopedCefString key_cef(key);
                auto rawResult = receiver->set_null(receiver, key_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DictionaryValueSetNullResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DictionaryValueSetBoolRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DictionaryValueSetBoolRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dictionaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string key = req.key;
            std::int32_t value = req.value;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, key, value]() {
                ScopedCefString key_cef(key);
                auto rawResult = receiver->set_bool(receiver, key_cef.get(), cefArg<decltype(receiver->set_bool), 2>(value));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DictionaryValueSetBoolResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DictionaryValueSetIntRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DictionaryValueSetIntRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dictionaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string key = req.key;
            std::int32_t value = req.value;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, key, value]() {
                ScopedCefString key_cef(key);
                auto rawResult = receiver->set_int(receiver, key_cef.get(), cefArg<decltype(receiver->set_int), 2>(value));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DictionaryValueSetIntResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DictionaryValueSetStringRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DictionaryValueSetStringRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dictionaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string key = req.key;
            std::string value = req.value;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, key, value]() {
                ScopedCefString key_cef(key);
                ScopedCefString value_cef(value);
                auto rawResult = receiver->set_string(receiver, key_cef.get(), value_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DictionaryValueSetStringResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DictionaryValueSetBinaryRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DictionaryValueSetBinaryRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dictionaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string key = req.key;
            std::int32_t value = req.value;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, key, value]() {
                ScopedCefString key_cef(key);
                cef_binary_value_t* value_ptr = value != 0 ? tables::binaryValue.retain(value) : nullptr;
                auto rawResult = receiver->set_binary(receiver, key_cef.get(), value_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (value_ptr) {
                    auto* value_base = reinterpret_cast<cef_base_ref_counted_t*>(value_ptr);
                    value_base->release(value_base);
                }
                if (ipc) {
                    gen::DictionaryValueSetBinaryResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DictionaryValueSetDictionaryRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DictionaryValueSetDictionaryRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dictionaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string key = req.key;
            std::int32_t value = req.value;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, key, value]() {
                ScopedCefString key_cef(key);
                cef_dictionary_value_t* value_ptr = value != 0 ? tables::dictionaryValue.retain(value) : nullptr;
                auto rawResult = receiver->set_dictionary(receiver, key_cef.get(), value_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (value_ptr) {
                    auto* value_base = reinterpret_cast<cef_base_ref_counted_t*>(value_ptr);
                    value_base->release(value_base);
                }
                if (ipc) {
                    gen::DictionaryValueSetDictionaryResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DictionaryValueSetListRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DictionaryValueSetListRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dictionaryValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string key = req.key;
            std::int32_t value = req.value;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, key, value]() {
                ScopedCefString key_cef(key);
                cef_list_value_t* value_ptr = value != 0 ? tables::listValue.retain(value) : nullptr;
                auto rawResult = receiver->set_list(receiver, key_cef.get(), value_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (value_ptr) {
                    auto* value_base = reinterpret_cast<cef_base_ref_counted_t*>(value_ptr);
                    value_base->release(value_base);
                }
                if (ipc) {
                    gen::DictionaryValueSetListResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ListValueIsValidRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ListValueIsValidRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::listValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_valid(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ListValueIsValidResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ListValueIsOwnedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ListValueIsOwnedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::listValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_owned(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ListValueIsOwnedResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ListValueIsReadOnlyRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ListValueIsReadOnlyRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::listValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_read_only(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ListValueIsReadOnlyResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ListValueIsSameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ListValueIsSameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::listValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t that = req.that;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, that]() {
                cef_list_value_t* that_ptr = that != 0 ? tables::listValue.retain(that) : nullptr;
                auto rawResult = receiver->is_same(receiver, that_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (that_ptr) {
                    auto* that_base = reinterpret_cast<cef_base_ref_counted_t*>(that_ptr);
                    that_base->release(that_base);
                }
                if (ipc) {
                    gen::ListValueIsSameResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ListValueIsEqualRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ListValueIsEqualRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::listValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t that = req.that;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, that]() {
                cef_list_value_t* that_ptr = that != 0 ? tables::listValue.retain(that) : nullptr;
                auto rawResult = receiver->is_equal(receiver, that_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (that_ptr) {
                    auto* that_base = reinterpret_cast<cef_base_ref_counted_t*>(that_ptr);
                    that_base->release(that_base);
                }
                if (ipc) {
                    gen::ListValueIsEqualResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ListValueCopyRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ListValueCopyRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::listValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->copy(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ListValueCopyResponse resp;
                    resp.result = insertOrRelease(&tables::listValue, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ListValueSetSizeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ListValueSetSizeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::listValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t size = req.size;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, size]() {
                auto rawResult = receiver->set_size(receiver, cefArg<decltype(receiver->set_size), 1>(size));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ListValueSetSizeResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ListValueGetSizeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ListValueGetSizeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::listValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_size(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ListValueGetSizeResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ListValueClearRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ListValueClearRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::listValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->clear(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ListValueClearResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ListValueRemoveRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ListValueRemoveRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::listValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index]() {
                auto rawResult = receiver->remove(receiver, cefArg<decltype(receiver->remove), 1>(index));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ListValueRemoveResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ListValueGetTypeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ListValueGetTypeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::listValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index]() {
                auto rawResult = receiver->get_type(receiver, cefArg<decltype(receiver->get_type), 1>(index));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ListValueGetTypeResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ListValueGetValueRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ListValueGetValueRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::listValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index]() {
                auto rawResult = receiver->get_value(receiver, cefArg<decltype(receiver->get_value), 1>(index));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ListValueGetValueResponse resp;
                    resp.result = insertOrRelease(&tables::value, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ListValueGetBoolRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ListValueGetBoolRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::listValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index]() {
                auto rawResult = receiver->get_bool(receiver, cefArg<decltype(receiver->get_bool), 1>(index));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ListValueGetBoolResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ListValueGetIntRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ListValueGetIntRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::listValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index]() {
                auto rawResult = receiver->get_int(receiver, cefArg<decltype(receiver->get_int), 1>(index));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ListValueGetIntResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ListValueGetBinaryRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ListValueGetBinaryRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::listValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index]() {
                auto rawResult = receiver->get_binary(receiver, cefArg<decltype(receiver->get_binary), 1>(index));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ListValueGetBinaryResponse resp;
                    resp.result = insertOrRelease(&tables::binaryValue, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ListValueGetDictionaryRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ListValueGetDictionaryRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::listValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index]() {
                auto rawResult = receiver->get_dictionary(receiver, cefArg<decltype(receiver->get_dictionary), 1>(index));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ListValueGetDictionaryResponse resp;
                    resp.result = insertOrRelease(&tables::dictionaryValue, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ListValueGetListRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ListValueGetListRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::listValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index]() {
                auto rawResult = receiver->get_list(receiver, cefArg<decltype(receiver->get_list), 1>(index));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ListValueGetListResponse resp;
                    resp.result = insertOrRelease(&tables::listValue, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ListValueSetValueRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ListValueSetValueRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::listValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            std::int32_t value = req.value;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index, value]() {
                cef_value_t* value_ptr = value != 0 ? tables::value.retain(value) : nullptr;
                auto rawResult = receiver->set_value(receiver, cefArg<decltype(receiver->set_value), 1>(index), value_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (value_ptr) {
                    auto* value_base = reinterpret_cast<cef_base_ref_counted_t*>(value_ptr);
                    value_base->release(value_base);
                }
                if (ipc) {
                    gen::ListValueSetValueResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ListValueSetNullRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ListValueSetNullRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::listValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index]() {
                auto rawResult = receiver->set_null(receiver, cefArg<decltype(receiver->set_null), 1>(index));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ListValueSetNullResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ListValueSetBoolRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ListValueSetBoolRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::listValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            std::int32_t value = req.value;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index, value]() {
                auto rawResult = receiver->set_bool(receiver, cefArg<decltype(receiver->set_bool), 1>(index), cefArg<decltype(receiver->set_bool), 2>(value));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ListValueSetBoolResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ListValueSetIntRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ListValueSetIntRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::listValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            std::int32_t value = req.value;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index, value]() {
                auto rawResult = receiver->set_int(receiver, cefArg<decltype(receiver->set_int), 1>(index), cefArg<decltype(receiver->set_int), 2>(value));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ListValueSetIntResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ListValueSetStringRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ListValueSetStringRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::listValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            std::string value = req.value;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index, value]() {
                ScopedCefString value_cef(value);
                auto rawResult = receiver->set_string(receiver, cefArg<decltype(receiver->set_string), 1>(index), value_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ListValueSetStringResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ListValueSetBinaryRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ListValueSetBinaryRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::listValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            std::int32_t value = req.value;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index, value]() {
                cef_binary_value_t* value_ptr = value != 0 ? tables::binaryValue.retain(value) : nullptr;
                auto rawResult = receiver->set_binary(receiver, cefArg<decltype(receiver->set_binary), 1>(index), value_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (value_ptr) {
                    auto* value_base = reinterpret_cast<cef_base_ref_counted_t*>(value_ptr);
                    value_base->release(value_base);
                }
                if (ipc) {
                    gen::ListValueSetBinaryResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ListValueSetDictionaryRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ListValueSetDictionaryRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::listValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            std::int32_t value = req.value;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index, value]() {
                cef_dictionary_value_t* value_ptr = value != 0 ? tables::dictionaryValue.retain(value) : nullptr;
                auto rawResult = receiver->set_dictionary(receiver, cefArg<decltype(receiver->set_dictionary), 1>(index), value_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (value_ptr) {
                    auto* value_base = reinterpret_cast<cef_base_ref_counted_t*>(value_ptr);
                    value_base->release(value_base);
                }
                if (ipc) {
                    gen::ListValueSetDictionaryResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ListValueSetListRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ListValueSetListRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::listValue.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            std::int32_t value = req.value;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index, value]() {
                cef_list_value_t* value_ptr = value != 0 ? tables::listValue.retain(value) : nullptr;
                auto rawResult = receiver->set_list(receiver, cefArg<decltype(receiver->set_list), 1>(index), value_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (value_ptr) {
                    auto* value_base = reinterpret_cast<cef_base_ref_counted_t*>(value_ptr);
                    value_base->release(value_base);
                }
                if (ipc) {
                    gen::ListValueSetListResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ImageIsEmptyRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ImageIsEmptyRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::image.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_empty(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ImageIsEmptyResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ImageIsSameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ImageIsSameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::image.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t that = req.that;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, that]() {
                cef_image_t* that_ptr = that != 0 ? tables::image.retain(that) : nullptr;
                auto rawResult = receiver->is_same(receiver, that_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (that_ptr) {
                    auto* that_base = reinterpret_cast<cef_base_ref_counted_t*>(that_ptr);
                    that_base->release(that_base);
                }
                if (ipc) {
                    gen::ImageIsSameResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ImageGetWidthRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ImageGetWidthRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::image.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_width(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ImageGetWidthResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ImageGetHeightRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ImageGetHeightRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::image.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_height(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ImageGetHeightResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::StreamReaderSeekRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::StreamReaderSeekRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::streamReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t offset = req.offset;
            std::int32_t whence = req.whence;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, offset, whence]() {
                auto rawResult = receiver->seek(receiver, cefArg<decltype(receiver->seek), 1>(offset), cefArg<decltype(receiver->seek), 2>(whence));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::StreamReaderSeekResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::StreamReaderTellRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::StreamReaderTellRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::streamReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->tell(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::StreamReaderTellResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::StreamReaderEofRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::StreamReaderEofRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::streamReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->eof(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::StreamReaderEofResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::StreamReaderMayBlockRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::StreamReaderMayBlockRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::streamReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->may_block(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::StreamReaderMayBlockResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::StreamWriterWriteRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::StreamWriterWriteRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::streamWriter.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::vector<std::uint8_t> ptr = std::move(req.ptr);
            std::int64_t n = req.n;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, ptr, n]() {
                auto rawResult = receiver->write(receiver, ptr.data(), static_cast<size_t>(ptr.size()), cefArg<decltype(receiver->write), 3>(n));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::StreamWriterWriteResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::StreamWriterSeekRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::StreamWriterSeekRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::streamWriter.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t offset = req.offset;
            std::int32_t whence = req.whence;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, offset, whence]() {
                auto rawResult = receiver->seek(receiver, cefArg<decltype(receiver->seek), 1>(offset), cefArg<decltype(receiver->seek), 2>(whence));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::StreamWriterSeekResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::StreamWriterTellRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::StreamWriterTellRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::streamWriter.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->tell(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::StreamWriterTellResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::StreamWriterFlushRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::StreamWriterFlushRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::streamWriter.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->flush(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::StreamWriterFlushResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::StreamWriterMayBlockRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::StreamWriterMayBlockRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::streamWriter.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->may_block(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::StreamWriterMayBlockResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DragDataCloneRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DragDataCloneRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dragData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->clone(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DragDataCloneResponse resp;
                    resp.result = insertOrRelease(&tables::dragData, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DragDataIsReadOnlyRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DragDataIsReadOnlyRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dragData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_read_only(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DragDataIsReadOnlyResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DragDataIsLinkRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DragDataIsLinkRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dragData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_link(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DragDataIsLinkResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DragDataIsFragmentRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DragDataIsFragmentRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dragData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_fragment(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DragDataIsFragmentResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DragDataIsFileRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DragDataIsFileRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dragData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_file(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DragDataIsFileResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DragDataGetLinkUrlRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DragDataGetLinkUrlRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dragData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_link_url(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DragDataGetLinkUrlResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DragDataGetLinkTitleRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DragDataGetLinkTitleRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dragData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_link_title(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DragDataGetLinkTitleResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DragDataGetLinkMetadataRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DragDataGetLinkMetadataRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dragData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_link_metadata(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DragDataGetLinkMetadataResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DragDataGetFragmentTextRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DragDataGetFragmentTextRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dragData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_fragment_text(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DragDataGetFragmentTextResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DragDataGetFragmentHtmlRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DragDataGetFragmentHtmlRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dragData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_fragment_html(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DragDataGetFragmentHtmlResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DragDataGetFragmentBaseUrlRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DragDataGetFragmentBaseUrlRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dragData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_fragment_base_url(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DragDataGetFragmentBaseUrlResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DragDataGetFileNameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DragDataGetFileNameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dragData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_file_name(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DragDataGetFileNameResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DragDataGetFileContentsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DragDataGetFileContentsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dragData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t writer = req.writer;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, writer]() {
                cef_stream_writer_t* writer_ptr = writer != 0 ? tables::streamWriter.retain(writer) : nullptr;
                auto rawResult = receiver->get_file_contents(receiver, writer_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (writer_ptr) {
                    auto* writer_base = reinterpret_cast<cef_base_ref_counted_t*>(writer_ptr);
                    writer_base->release(writer_base);
                }
                if (ipc) {
                    gen::DragDataGetFileContentsResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DragDataGetFileNamesRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DragDataGetFileNamesRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dragData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::vector<std::string> names = std::move(req.names);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, names]() {
                cef_string_list_t names_list = cef_string_list_alloc();
                for (const auto& __s : names) {
                    cef_string_t __cs{};
                    if (!__s.empty()) cef_string_utf8_to_utf16(__s.data(), __s.size(), &__cs);
                    cef_string_list_append(names_list, &__cs);
                    cef_string_clear(&__cs);
                }
                auto rawResult = receiver->get_file_names(receiver, names_list);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                cef_string_list_free(names_list);
                if (ipc) {
                    gen::DragDataGetFileNamesResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DragDataGetFilePathsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DragDataGetFilePathsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dragData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::vector<std::string> paths = std::move(req.paths);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, paths]() {
                cef_string_list_t paths_list = cef_string_list_alloc();
                for (const auto& __s : paths) {
                    cef_string_t __cs{};
                    if (!__s.empty()) cef_string_utf8_to_utf16(__s.data(), __s.size(), &__cs);
                    cef_string_list_append(paths_list, &__cs);
                    cef_string_clear(&__cs);
                }
                auto rawResult = receiver->get_file_paths(receiver, paths_list);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                cef_string_list_free(paths_list);
                if (ipc) {
                    gen::DragDataGetFilePathsResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DragDataSetLinkUrlRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DragDataSetLinkUrlRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dragData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string url = req.url;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, url]() {
                ScopedCefString url_cef(url);
                receiver->set_link_url(receiver, url_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DragDataSetLinkTitleRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DragDataSetLinkTitleRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dragData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string title = req.title;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, title]() {
                ScopedCefString title_cef(title);
                receiver->set_link_title(receiver, title_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DragDataSetLinkMetadataRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DragDataSetLinkMetadataRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dragData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string data = req.data;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, data]() {
                ScopedCefString data_cef(data);
                receiver->set_link_metadata(receiver, data_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DragDataSetFragmentTextRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DragDataSetFragmentTextRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dragData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string text = req.text;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, text]() {
                ScopedCefString text_cef(text);
                receiver->set_fragment_text(receiver, text_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DragDataSetFragmentHtmlRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DragDataSetFragmentHtmlRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dragData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string html = req.html;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, html]() {
                ScopedCefString html_cef(html);
                receiver->set_fragment_html(receiver, html_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DragDataSetFragmentBaseUrlRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DragDataSetFragmentBaseUrlRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dragData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string baseUrl = req.baseUrl;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, baseUrl]() {
                ScopedCefString baseUrl_cef(baseUrl);
                receiver->set_fragment_base_url(receiver, baseUrl_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DragDataResetFileContentsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DragDataResetFileContentsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dragData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->reset_file_contents(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DragDataAddFileRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DragDataAddFileRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dragData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string path = req.path;
            std::string displayName = req.displayName;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, path, displayName]() {
                ScopedCefString path_cef(path);
                ScopedCefString displayName_cef(displayName);
                receiver->add_file(receiver, path_cef.get(), displayName_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DragDataClearFilenamesRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DragDataClearFilenamesRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dragData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->clear_filenames(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DragDataGetImageRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DragDataGetImageRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dragData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_image(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DragDataGetImageResponse resp;
                    resp.result = insertOrRelease(&tables::image, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DragDataGetImageHotspotRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DragDataGetImageHotspotRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dragData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                cef_point_t rawResult = receiver->get_image_hotspot(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DragDataGetImageHotspotResponse resp;
                resp.result.x = static_cast<decltype(resp.result.x)>(rawResult.x);
                resp.result.y = static_cast<decltype(resp.result.y)>(rawResult.y);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DragDataHasImageRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DragDataHasImageRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::dragData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->has_image(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DragDataHasImageResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::SharedMemoryRegionIsValidRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::SharedMemoryRegionIsValidRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::sharedMemoryRegion.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_valid(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::SharedMemoryRegionIsValidResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::SharedMemoryRegionSizeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::SharedMemoryRegionSizeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::sharedMemoryRegion.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->size(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::SharedMemoryRegionSizeResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ProcessMessageIsValidRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ProcessMessageIsValidRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::processMessage.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_valid(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ProcessMessageIsValidResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ProcessMessageIsReadOnlyRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ProcessMessageIsReadOnlyRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::processMessage.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_read_only(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ProcessMessageIsReadOnlyResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ProcessMessageCopyRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ProcessMessageCopyRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::processMessage.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->copy(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ProcessMessageCopyResponse resp;
                    resp.result = insertOrRelease(&tables::processMessage, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ProcessMessageGetNameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ProcessMessageGetNameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::processMessage.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_name(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ProcessMessageGetNameResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ProcessMessageGetArgumentListRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ProcessMessageGetArgumentListRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::processMessage.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_argument_list(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ProcessMessageGetArgumentListResponse resp;
                    resp.result = insertOrRelease(&tables::listValue, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ProcessMessageGetSharedMemoryRegionRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ProcessMessageGetSharedMemoryRegionRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::processMessage.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_shared_memory_region(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ProcessMessageGetSharedMemoryRegionResponse resp;
                    resp.result = insertOrRelease(&tables::sharedMemoryRegion, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestIsReadOnlyRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestIsReadOnlyRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::request.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_read_only(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::RequestIsReadOnlyResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestGetUrlRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestGetUrlRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::request.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_url(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::RequestGetUrlResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestSetUrlRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestSetUrlRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::request.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string url = req.url;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, url]() {
                ScopedCefString url_cef(url);
                receiver->set_url(receiver, url_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestGetMethodRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestGetMethodRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::request.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_method(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::RequestGetMethodResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestSetMethodRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestSetMethodRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::request.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string method = req.method;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, method]() {
                ScopedCefString method_cef(method);
                receiver->set_method(receiver, method_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestSetReferrerRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestSetReferrerRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::request.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string referrerUrl = req.referrerUrl;
            std::int32_t policy = req.policy;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, referrerUrl, policy]() {
                ScopedCefString referrerUrl_cef(referrerUrl);
                receiver->set_referrer(receiver, referrerUrl_cef.get(), cefArg<decltype(receiver->set_referrer), 2>(policy));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestGetReferrerUrlRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestGetReferrerUrlRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::request.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_referrer_url(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::RequestGetReferrerUrlResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestGetReferrerPolicyRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestGetReferrerPolicyRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::request.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_referrer_policy(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::RequestGetReferrerPolicyResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestGetPostDataRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestGetPostDataRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::request.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_post_data(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::RequestGetPostDataResponse resp;
                    resp.result = insertOrRelease(&tables::postData, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestSetPostDataRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestSetPostDataRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::request.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t postData = req.postData;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, postData]() {
                cef_post_data_t* postData_ptr = postData != 0 ? tables::postData.retain(postData) : nullptr;
                receiver->set_post_data(receiver, postData_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (postData_ptr) {
                    auto* postData_base = reinterpret_cast<cef_base_ref_counted_t*>(postData_ptr);
                    postData_base->release(postData_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestGetHeaderByNameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestGetHeaderByNameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::request.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string name = req.name;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, name]() {
                ScopedCefString name_cef(name);
                auto rawResult = receiver->get_header_by_name(receiver, name_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::RequestGetHeaderByNameResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestSetHeaderByNameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestSetHeaderByNameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::request.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string name = req.name;
            std::string value = req.value;
            std::int32_t overwrite = req.overwrite;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, name, value, overwrite]() {
                ScopedCefString name_cef(name);
                ScopedCefString value_cef(value);
                receiver->set_header_by_name(receiver, name_cef.get(), value_cef.get(), cefArg<decltype(receiver->set_header_by_name), 3>(overwrite));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestGetFlagsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestGetFlagsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::request.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_flags(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::RequestGetFlagsResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestSetFlagsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestSetFlagsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::request.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t flags = req.flags;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, flags]() {
                receiver->set_flags(receiver, cefArg<decltype(receiver->set_flags), 1>(flags));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestGetFirstPartyForCookiesRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestGetFirstPartyForCookiesRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::request.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_first_party_for_cookies(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::RequestGetFirstPartyForCookiesResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestSetFirstPartyForCookiesRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestSetFirstPartyForCookiesRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::request.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string url = req.url;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, url]() {
                ScopedCefString url_cef(url);
                receiver->set_first_party_for_cookies(receiver, url_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestGetResourceTypeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestGetResourceTypeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::request.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_resource_type(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::RequestGetResourceTypeResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestGetTransitionTypeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestGetTransitionTypeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::request.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_transition_type(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::RequestGetTransitionTypeResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestGetIdentifierRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestGetIdentifierRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::request.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_identifier(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::RequestGetIdentifierResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PostDataIsReadOnlyRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PostDataIsReadOnlyRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::postData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_read_only(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::PostDataIsReadOnlyResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PostDataHasExcludedElementsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PostDataHasExcludedElementsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::postData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->has_excluded_elements(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::PostDataHasExcludedElementsResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PostDataGetElementCountRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PostDataGetElementCountRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::postData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_element_count(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::PostDataGetElementCountResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PostDataRemoveElementRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PostDataRemoveElementRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::postData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t element = req.element;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, element]() {
                cef_post_data_element_t* element_ptr = element != 0 ? tables::postDataElement.retain(element) : nullptr;
                auto rawResult = receiver->remove_element(receiver, element_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (element_ptr) {
                    auto* element_base = reinterpret_cast<cef_base_ref_counted_t*>(element_ptr);
                    element_base->release(element_base);
                }
                if (ipc) {
                    gen::PostDataRemoveElementResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PostDataAddElementRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PostDataAddElementRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::postData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t element = req.element;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, element]() {
                cef_post_data_element_t* element_ptr = element != 0 ? tables::postDataElement.retain(element) : nullptr;
                auto rawResult = receiver->add_element(receiver, element_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (element_ptr) {
                    auto* element_base = reinterpret_cast<cef_base_ref_counted_t*>(element_ptr);
                    element_base->release(element_base);
                }
                if (ipc) {
                    gen::PostDataAddElementResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PostDataRemoveElementsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PostDataRemoveElementsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::postData.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->remove_elements(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PostDataElementIsReadOnlyRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PostDataElementIsReadOnlyRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::postDataElement.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_read_only(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::PostDataElementIsReadOnlyResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PostDataElementSetToEmptyRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PostDataElementSetToEmptyRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::postDataElement.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->set_to_empty(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PostDataElementSetToFileRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PostDataElementSetToFileRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::postDataElement.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string fileName = req.fileName;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, fileName]() {
                ScopedCefString fileName_cef(fileName);
                receiver->set_to_file(receiver, fileName_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PostDataElementSetToBytesRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PostDataElementSetToBytesRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::postDataElement.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::vector<std::uint8_t> bytes = std::move(req.bytes);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, bytes]() {
                receiver->set_to_bytes(receiver, static_cast<size_t>(bytes.size()), bytes.data());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PostDataElementGetTypeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PostDataElementGetTypeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::postDataElement.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_type(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::PostDataElementGetTypeResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PostDataElementGetFileRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PostDataElementGetFileRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::postDataElement.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_file(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::PostDataElementGetFileResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PostDataElementGetBytesCountRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PostDataElementGetBytesCountRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::postDataElement.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_bytes_count(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::PostDataElementGetBytesCountResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::FrameIsValidRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::FrameIsValidRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::frame.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_valid(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::FrameIsValidResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::FrameUndoRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::FrameUndoRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::frame.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->undo(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::FrameRedoRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::FrameRedoRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::frame.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->redo(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::FrameCutRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::FrameCutRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::frame.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->cut(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::FrameCopyRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::FrameCopyRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::frame.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->copy(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::FramePasteRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::FramePasteRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::frame.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->paste(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::FramePasteAndMatchStyleRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::FramePasteAndMatchStyleRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::frame.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->paste_and_match_style(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::FrameDelRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::FrameDelRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::frame.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->del(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::FrameSelectAllRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::FrameSelectAllRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::frame.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->select_all(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::FrameViewSourceRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::FrameViewSourceRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::frame.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->view_source(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::FrameGetSourceRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::FrameGetSourceRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::frame.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t visitor = req.visitor;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, visitor]() {
                cef_string_visitor_t* visitor_ptr = visitor != 0 ?
                        reinterpret_cast<cef_string_visitor_t*>(new genvisitors::JvmStringVisitor(visitor, ipc)) :
                        nullptr;
                receiver->get_source(receiver, visitor_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::FrameGetTextRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::FrameGetTextRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::frame.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t visitor = req.visitor;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, visitor]() {
                cef_string_visitor_t* visitor_ptr = visitor != 0 ?
                        reinterpret_cast<cef_string_visitor_t*>(new genvisitors::JvmStringVisitor(visitor, ipc)) :
                        nullptr;
                receiver->get_text(receiver, visitor_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::FrameLoadRequestRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::FrameLoadRequestRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::frame.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t request = req.request;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, request]() {
                cef_request_t* request_ptr = request != 0 ? tables::request.retain(request) : nullptr;
                receiver->load_request(receiver, request_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (request_ptr) {
                    auto* request_base = reinterpret_cast<cef_base_ref_counted_t*>(request_ptr);
                    request_base->release(request_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::FrameLoadUrlRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::FrameLoadUrlRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::frame.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string url = req.url;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, url]() {
                ScopedCefString url_cef(url);
                receiver->load_url(receiver, url_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::FrameExecuteJavaScriptRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::FrameExecuteJavaScriptRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::frame.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string code = req.code;
            std::string scriptUrl = req.scriptUrl;
            std::int32_t startLine = req.startLine;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, code, scriptUrl, startLine]() {
                ScopedCefString code_cef(code);
                ScopedCefString scriptUrl_cef(scriptUrl);
                receiver->execute_java_script(receiver, code_cef.get(), scriptUrl_cef.get(), cefArg<decltype(receiver->execute_java_script), 3>(startLine));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::FrameIsMainRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::FrameIsMainRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::frame.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_main(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::FrameIsMainResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::FrameIsFocusedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::FrameIsFocusedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::frame.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_focused(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::FrameIsFocusedResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::FrameGetNameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::FrameGetNameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::frame.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_name(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::FrameGetNameResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::FrameGetIdentifierRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::FrameGetIdentifierRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::frame.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_identifier(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::FrameGetIdentifierResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::FrameGetParentRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::FrameGetParentRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::frame.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_parent(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::FrameGetParentResponse resp;
                    resp.result = insertOrRelease(&tables::frame, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::FrameGetUrlRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::FrameGetUrlRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::frame.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_url(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::FrameGetUrlResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::FrameGetBrowserRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::FrameGetBrowserRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::frame.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_browser(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::FrameGetBrowserResponse resp;
                    resp.result = insertOrRelease(&tables::browser, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::FrameSendProcessMessageRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::FrameSendProcessMessageRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::frame.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t targetProcess = req.targetProcess;
            std::int32_t message = req.message;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, targetProcess, message]() {
                cef_process_message_t* message_ptr = message != 0 ? tables::processMessage.retain(message) : nullptr;
                receiver->send_process_message(receiver, cefArg<decltype(receiver->send_process_message), 1>(targetProcess), message_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (message_ptr) {
                    auto* message_base = reinterpret_cast<cef_base_ref_counted_t*>(message_ptr);
                    message_base->release(message_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::X509CertPrincipalGetDisplayNameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::X509CertPrincipalGetDisplayNameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::x509CertPrincipal.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_display_name(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::X509CertPrincipalGetDisplayNameResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::X509CertPrincipalGetCommonNameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::X509CertPrincipalGetCommonNameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::x509CertPrincipal.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_common_name(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::X509CertPrincipalGetCommonNameResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::X509CertPrincipalGetLocalityNameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::X509CertPrincipalGetLocalityNameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::x509CertPrincipal.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_locality_name(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::X509CertPrincipalGetLocalityNameResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::X509CertPrincipalGetStateOrProvinceNameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::X509CertPrincipalGetStateOrProvinceNameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::x509CertPrincipal.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_state_or_province_name(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::X509CertPrincipalGetStateOrProvinceNameResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::X509CertPrincipalGetCountryNameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::X509CertPrincipalGetCountryNameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::x509CertPrincipal.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_country_name(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::X509CertPrincipalGetCountryNameResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::X509CertPrincipalGetOrganizationNamesRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::X509CertPrincipalGetOrganizationNamesRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::x509CertPrincipal.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::vector<std::string> names = std::move(req.names);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, names]() {
                cef_string_list_t names_list = cef_string_list_alloc();
                for (const auto& __s : names) {
                    cef_string_t __cs{};
                    if (!__s.empty()) cef_string_utf8_to_utf16(__s.data(), __s.size(), &__cs);
                    cef_string_list_append(names_list, &__cs);
                    cef_string_clear(&__cs);
                }
                receiver->get_organization_names(receiver, names_list);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                cef_string_list_free(names_list);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::X509CertPrincipalGetOrganizationUnitNamesRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::X509CertPrincipalGetOrganizationUnitNamesRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::x509CertPrincipal.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::vector<std::string> names = std::move(req.names);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, names]() {
                cef_string_list_t names_list = cef_string_list_alloc();
                for (const auto& __s : names) {
                    cef_string_t __cs{};
                    if (!__s.empty()) cef_string_utf8_to_utf16(__s.data(), __s.size(), &__cs);
                    cef_string_list_append(names_list, &__cs);
                    cef_string_clear(&__cs);
                }
                receiver->get_organization_unit_names(receiver, names_list);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                cef_string_list_free(names_list);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::X509CertificateGetSubjectRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::X509CertificateGetSubjectRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::x509Certificate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_subject(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::X509CertificateGetSubjectResponse resp;
                    resp.result = insertOrRelease(&tables::x509CertPrincipal, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::X509CertificateGetIssuerRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::X509CertificateGetIssuerRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::x509Certificate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_issuer(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::X509CertificateGetIssuerResponse resp;
                    resp.result = insertOrRelease(&tables::x509CertPrincipal, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::X509CertificateGetSerialNumberRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::X509CertificateGetSerialNumberRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::x509Certificate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_serial_number(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::X509CertificateGetSerialNumberResponse resp;
                    resp.result = insertOrRelease(&tables::binaryValue, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::X509CertificateGetValidStartRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::X509CertificateGetValidStartRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::x509Certificate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                cef_basetime_t rawResult = receiver->get_valid_start(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::X509CertificateGetValidStartResponse resp;
                resp.result.val = static_cast<decltype(resp.result.val)>(rawResult.val);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::X509CertificateGetValidExpiryRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::X509CertificateGetValidExpiryRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::x509Certificate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                cef_basetime_t rawResult = receiver->get_valid_expiry(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::X509CertificateGetValidExpiryResponse resp;
                resp.result.val = static_cast<decltype(resp.result.val)>(rawResult.val);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::X509CertificateGetDerencodedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::X509CertificateGetDerencodedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::x509Certificate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_derencoded(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::X509CertificateGetDerencodedResponse resp;
                    resp.result = insertOrRelease(&tables::binaryValue, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::X509CertificateGetPemencodedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::X509CertificateGetPemencodedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::x509Certificate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_pemencoded(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::X509CertificateGetPemencodedResponse resp;
                    resp.result = insertOrRelease(&tables::binaryValue, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::X509CertificateGetIssuerChainSizeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::X509CertificateGetIssuerChainSizeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::x509Certificate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_issuer_chain_size(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::X509CertificateGetIssuerChainSizeResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::SslStatusIsSecureConnectionRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::SslStatusIsSecureConnectionRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::sslstatus.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_secure_connection(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::SslStatusIsSecureConnectionResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::SslStatusGetCertStatusRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::SslStatusGetCertStatusRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::sslstatus.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_cert_status(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::SslStatusGetCertStatusResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::SslStatusGetSslversionRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::SslStatusGetSslversionRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::sslstatus.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_sslversion(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::SslStatusGetSslversionResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::SslStatusGetContentStatusRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::SslStatusGetContentStatusRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::sslstatus.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_content_status(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::SslStatusGetContentStatusResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::SslStatusGetX509CertificateRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::SslStatusGetX509CertificateRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::sslstatus.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_x509_certificate(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::SslStatusGetX509CertificateResponse resp;
                    resp.result = insertOrRelease(&tables::x509Certificate, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::NavigationEntryIsValidRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::NavigationEntryIsValidRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::navigationEntry.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_valid(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::NavigationEntryIsValidResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::NavigationEntryGetUrlRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::NavigationEntryGetUrlRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::navigationEntry.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_url(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::NavigationEntryGetUrlResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::NavigationEntryGetDisplayUrlRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::NavigationEntryGetDisplayUrlRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::navigationEntry.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_display_url(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::NavigationEntryGetDisplayUrlResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::NavigationEntryGetOriginalUrlRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::NavigationEntryGetOriginalUrlRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::navigationEntry.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_original_url(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::NavigationEntryGetOriginalUrlResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::NavigationEntryGetTitleRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::NavigationEntryGetTitleRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::navigationEntry.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_title(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::NavigationEntryGetTitleResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::NavigationEntryGetTransitionTypeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::NavigationEntryGetTransitionTypeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::navigationEntry.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_transition_type(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::NavigationEntryGetTransitionTypeResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::NavigationEntryHasPostDataRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::NavigationEntryHasPostDataRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::navigationEntry.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->has_post_data(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::NavigationEntryHasPostDataResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::NavigationEntryGetCompletionTimeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::NavigationEntryGetCompletionTimeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::navigationEntry.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                cef_basetime_t rawResult = receiver->get_completion_time(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::NavigationEntryGetCompletionTimeResponse resp;
                resp.result.val = static_cast<decltype(resp.result.val)>(rawResult.val);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::NavigationEntryGetHttpStatusCodeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::NavigationEntryGetHttpStatusCodeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::navigationEntry.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_http_status_code(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::NavigationEntryGetHttpStatusCodeResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::NavigationEntryGetSslStatusRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::NavigationEntryGetSslStatusRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::navigationEntry.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_sslstatus(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::NavigationEntryGetSslStatusResponse resp;
                    resp.result = insertOrRelease(&tables::sslstatus, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::CallbackContRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::CallbackContRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::callback.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->cont(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::CallbackCancelRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::CallbackCancelRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::callback.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->cancel(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::CookieManagerDeleteCookiesRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::CookieManagerDeleteCookiesRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::cookieManager.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string url = req.url;
            std::string cookieName = req.cookieName;
            std::int32_t callback = req.callback;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, url, cookieName, callback]() {
                ScopedCefString url_cef(url);
                ScopedCefString cookieName_cef(cookieName);
                cef_delete_cookies_callback_t* callback_ptr = callback != 0 ?
                        reinterpret_cast<cef_delete_cookies_callback_t*>(new genvisitors::JvmDeleteCookiesCallback(callback, ipc)) :
                        nullptr;
                auto rawResult = receiver->delete_cookies(receiver, url_cef.get(), cookieName_cef.get(), callback_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::CookieManagerDeleteCookiesResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::CookieManagerFlushStoreRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::CookieManagerFlushStoreRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::cookieManager.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t callback = req.callback;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, callback]() {
                cef_completion_callback_t* callback_ptr = callback != 0 ?
                        reinterpret_cast<cef_completion_callback_t*>(new genvisitors::JvmCompletionCallback(callback, ipc)) :
                        nullptr;
                auto rawResult = receiver->flush_store(receiver, callback_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::CookieManagerFlushStoreResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MediaRouterGetSourceRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MediaRouterGetSourceRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::mediaRouter.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string urn = req.urn;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, urn]() {
                ScopedCefString urn_cef(urn);
                auto rawResult = receiver->get_source(receiver, urn_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MediaRouterGetSourceResponse resp;
                    resp.result = insertOrRelease(&tables::mediaSource, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MediaRouterNotifyCurrentSinksRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MediaRouterNotifyCurrentSinksRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::mediaRouter.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->notify_current_sinks(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MediaRouterNotifyCurrentRoutesRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MediaRouterNotifyCurrentRoutesRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::mediaRouter.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->notify_current_routes(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MediaRouteGetIdRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MediaRouteGetIdRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::mediaRoute.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_id(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MediaRouteGetIdResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MediaRouteGetSourceRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MediaRouteGetSourceRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::mediaRoute.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_source(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MediaRouteGetSourceResponse resp;
                    resp.result = insertOrRelease(&tables::mediaSource, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MediaRouteGetSinkRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MediaRouteGetSinkRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::mediaRoute.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_sink(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MediaRouteGetSinkResponse resp;
                    resp.result = insertOrRelease(&tables::mediaSink, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MediaRouteSendRouteMessageRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MediaRouteSendRouteMessageRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::mediaRoute.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::vector<std::uint8_t> message = std::move(req.message);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, message]() {
                receiver->send_route_message(receiver, message.data(), static_cast<size_t>(message.size()));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MediaRouteTerminateRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MediaRouteTerminateRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::mediaRoute.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->terminate(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MediaSinkGetIdRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MediaSinkGetIdRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::mediaSink.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_id(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MediaSinkGetIdResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MediaSinkGetNameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MediaSinkGetNameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::mediaSink.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_name(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MediaSinkGetNameResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MediaSinkGetIconTypeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MediaSinkGetIconTypeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::mediaSink.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_icon_type(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MediaSinkGetIconTypeResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MediaSinkIsCastSinkRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MediaSinkIsCastSinkRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::mediaSink.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_cast_sink(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MediaSinkIsCastSinkResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MediaSinkIsDialSinkRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MediaSinkIsDialSinkRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::mediaSink.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_dial_sink(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MediaSinkIsDialSinkResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MediaSinkIsCompatibleWithRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MediaSinkIsCompatibleWithRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::mediaSink.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t source = req.source;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, source]() {
                cef_media_source_t* source_ptr = source != 0 ? tables::mediaSource.retain(source) : nullptr;
                auto rawResult = receiver->is_compatible_with(receiver, source_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (source_ptr) {
                    auto* source_base = reinterpret_cast<cef_base_ref_counted_t*>(source_ptr);
                    source_base->release(source_base);
                }
                if (ipc) {
                    gen::MediaSinkIsCompatibleWithResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MediaSourceGetIdRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MediaSourceGetIdRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::mediaSource.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_id(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MediaSourceGetIdResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MediaSourceIsCastSourceRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MediaSourceIsCastSourceRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::mediaSource.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_cast_source(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MediaSourceIsCastSourceResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MediaSourceIsDialSourceRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MediaSourceIsDialSourceRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::mediaSource.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_dial_source(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MediaSourceIsDialSourceResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PreferenceRegistrarAddPreferenceRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PreferenceRegistrarAddPreferenceRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::preferenceRegistrar.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string name = req.name;
            std::int32_t defaultValue = req.defaultValue;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, name, defaultValue]() {
                ScopedCefString name_cef(name);
                cef_value_t* defaultValue_ptr = defaultValue != 0 ? tables::value.retain(defaultValue) : nullptr;
                auto rawResult = receiver->add_preference(receiver, name_cef.get(), defaultValue_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (defaultValue_ptr) {
                    auto* defaultValue_base = reinterpret_cast<cef_base_ref_counted_t*>(defaultValue_ptr);
                    defaultValue_base->release(defaultValue_base);
                }
                if (ipc) {
                    gen::PreferenceRegistrarAddPreferenceResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PreferenceManagerHasPreferenceRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PreferenceManagerHasPreferenceRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::preferenceManager.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string name = req.name;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, name]() {
                ScopedCefString name_cef(name);
                auto rawResult = receiver->has_preference(receiver, name_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::PreferenceManagerHasPreferenceResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PreferenceManagerGetPreferenceRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PreferenceManagerGetPreferenceRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::preferenceManager.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string name = req.name;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, name]() {
                ScopedCefString name_cef(name);
                auto rawResult = receiver->get_preference(receiver, name_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::PreferenceManagerGetPreferenceResponse resp;
                    resp.result = insertOrRelease(&tables::value, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PreferenceManagerGetAllPreferencesRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PreferenceManagerGetAllPreferencesRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::preferenceManager.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t includeDefaults = req.includeDefaults;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, includeDefaults]() {
                auto rawResult = receiver->get_all_preferences(receiver, cefArg<decltype(receiver->get_all_preferences), 1>(includeDefaults));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::PreferenceManagerGetAllPreferencesResponse resp;
                    resp.result = insertOrRelease(&tables::dictionaryValue, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PreferenceManagerCanSetPreferenceRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PreferenceManagerCanSetPreferenceRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::preferenceManager.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string name = req.name;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, name]() {
                ScopedCefString name_cef(name);
                auto rawResult = receiver->can_set_preference(receiver, name_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::PreferenceManagerCanSetPreferenceResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PreferenceManagerSetPreferenceRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PreferenceManagerSetPreferenceRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::preferenceManager.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string name = req.name;
            std::int32_t value = req.value;
            std::string error = req.error;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, name, value, error]() {
                ScopedCefString name_cef(name);
                cef_value_t* value_ptr = value != 0 ? tables::value.retain(value) : nullptr;
                ScopedCefString error_cef(error);
                auto rawResult = receiver->set_preference(receiver, name_cef.get(), value_ptr, error_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (value_ptr) {
                    auto* value_base = reinterpret_cast<cef_base_ref_counted_t*>(value_ptr);
                    value_base->release(value_base);
                }
                if (ipc) {
                    gen::PreferenceManagerSetPreferenceResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestContextIsSameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestContextIsSameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::requestContext.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t other = req.other;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, other]() {
                cef_request_context_t* other_ptr = other != 0 ? tables::requestContext.retain(other) : nullptr;
                auto rawResult = receiver->is_same(receiver, other_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (other_ptr) {
                    auto* other_base = reinterpret_cast<cef_base_ref_counted_t*>(other_ptr);
                    other_base->release(other_base);
                }
                if (ipc) {
                    gen::RequestContextIsSameResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestContextIsSharingWithRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestContextIsSharingWithRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::requestContext.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t other = req.other;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, other]() {
                cef_request_context_t* other_ptr = other != 0 ? tables::requestContext.retain(other) : nullptr;
                auto rawResult = receiver->is_sharing_with(receiver, other_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (other_ptr) {
                    auto* other_base = reinterpret_cast<cef_base_ref_counted_t*>(other_ptr);
                    other_base->release(other_base);
                }
                if (ipc) {
                    gen::RequestContextIsSharingWithResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestContextIsGlobalRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestContextIsGlobalRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::requestContext.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_global(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::RequestContextIsGlobalResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestContextGetCachePathRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestContextGetCachePathRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::requestContext.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_cache_path(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::RequestContextGetCachePathResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestContextGetCookieManagerRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestContextGetCookieManagerRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::requestContext.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t callback = req.callback;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, callback]() {
                cef_completion_callback_t* callback_ptr = callback != 0 ?
                        reinterpret_cast<cef_completion_callback_t*>(new genvisitors::JvmCompletionCallback(callback, ipc)) :
                        nullptr;
                auto rawResult = receiver->get_cookie_manager(receiver, callback_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::RequestContextGetCookieManagerResponse resp;
                    resp.result = insertOrRelease(&tables::cookieManager, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestContextClearSchemeHandlerFactoriesRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestContextClearSchemeHandlerFactoriesRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::requestContext.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->clear_scheme_handler_factories(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::RequestContextClearSchemeHandlerFactoriesResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestContextClearCertificateExceptionsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestContextClearCertificateExceptionsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::requestContext.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t callback = req.callback;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, callback]() {
                cef_completion_callback_t* callback_ptr = callback != 0 ?
                        reinterpret_cast<cef_completion_callback_t*>(new genvisitors::JvmCompletionCallback(callback, ipc)) :
                        nullptr;
                receiver->clear_certificate_exceptions(receiver, callback_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestContextClearHttpAuthCredentialsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestContextClearHttpAuthCredentialsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::requestContext.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t callback = req.callback;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, callback]() {
                cef_completion_callback_t* callback_ptr = callback != 0 ?
                        reinterpret_cast<cef_completion_callback_t*>(new genvisitors::JvmCompletionCallback(callback, ipc)) :
                        nullptr;
                receiver->clear_http_auth_credentials(receiver, callback_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestContextCloseAllConnectionsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestContextCloseAllConnectionsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::requestContext.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t callback = req.callback;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, callback]() {
                cef_completion_callback_t* callback_ptr = callback != 0 ?
                        reinterpret_cast<cef_completion_callback_t*>(new genvisitors::JvmCompletionCallback(callback, ipc)) :
                        nullptr;
                receiver->close_all_connections(receiver, callback_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestContextGetMediaRouterRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestContextGetMediaRouterRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::requestContext.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t callback = req.callback;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, callback]() {
                cef_completion_callback_t* callback_ptr = callback != 0 ?
                        reinterpret_cast<cef_completion_callback_t*>(new genvisitors::JvmCompletionCallback(callback, ipc)) :
                        nullptr;
                auto rawResult = receiver->get_media_router(receiver, callback_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::RequestContextGetMediaRouterResponse resp;
                    resp.result = insertOrRelease(&tables::mediaRouter, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestContextGetWebsiteSettingRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestContextGetWebsiteSettingRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::requestContext.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string requestingUrl = req.requestingUrl;
            std::string topLevelUrl = req.topLevelUrl;
            std::int32_t contentType = req.contentType;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, requestingUrl, topLevelUrl, contentType]() {
                ScopedCefString requestingUrl_cef(requestingUrl);
                ScopedCefString topLevelUrl_cef(topLevelUrl);
                auto rawResult = receiver->get_website_setting(receiver, requestingUrl_cef.get(), topLevelUrl_cef.get(), cefArg<decltype(receiver->get_website_setting), 3>(contentType));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::RequestContextGetWebsiteSettingResponse resp;
                    resp.result = insertOrRelease(&tables::value, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestContextSetWebsiteSettingRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestContextSetWebsiteSettingRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::requestContext.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string requestingUrl = req.requestingUrl;
            std::string topLevelUrl = req.topLevelUrl;
            std::int32_t contentType = req.contentType;
            std::int32_t value = req.value;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, requestingUrl, topLevelUrl, contentType, value]() {
                ScopedCefString requestingUrl_cef(requestingUrl);
                ScopedCefString topLevelUrl_cef(topLevelUrl);
                cef_value_t* value_ptr = value != 0 ? tables::value.retain(value) : nullptr;
                receiver->set_website_setting(receiver, requestingUrl_cef.get(), topLevelUrl_cef.get(), cefArg<decltype(receiver->set_website_setting), 3>(contentType), value_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (value_ptr) {
                    auto* value_base = reinterpret_cast<cef_base_ref_counted_t*>(value_ptr);
                    value_base->release(value_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestContextGetContentSettingRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestContextGetContentSettingRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::requestContext.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string requestingUrl = req.requestingUrl;
            std::string topLevelUrl = req.topLevelUrl;
            std::int32_t contentType = req.contentType;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, requestingUrl, topLevelUrl, contentType]() {
                ScopedCefString requestingUrl_cef(requestingUrl);
                ScopedCefString topLevelUrl_cef(topLevelUrl);
                auto rawResult = receiver->get_content_setting(receiver, requestingUrl_cef.get(), topLevelUrl_cef.get(), cefArg<decltype(receiver->get_content_setting), 3>(contentType));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::RequestContextGetContentSettingResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestContextSetContentSettingRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestContextSetContentSettingRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::requestContext.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string requestingUrl = req.requestingUrl;
            std::string topLevelUrl = req.topLevelUrl;
            std::int32_t contentType = req.contentType;
            std::int32_t value = req.value;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, requestingUrl, topLevelUrl, contentType, value]() {
                ScopedCefString requestingUrl_cef(requestingUrl);
                ScopedCefString topLevelUrl_cef(topLevelUrl);
                receiver->set_content_setting(receiver, requestingUrl_cef.get(), topLevelUrl_cef.get(), cefArg<decltype(receiver->set_content_setting), 3>(contentType), cefArg<decltype(receiver->set_content_setting), 4>(value));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestContextSetChromeColorSchemeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestContextSetChromeColorSchemeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::requestContext.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t variant = req.variant;
            std::int32_t userColor = req.userColor;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, variant, userColor]() {
                receiver->set_chrome_color_scheme(receiver, cefArg<decltype(receiver->set_chrome_color_scheme), 1>(variant), cefArg<decltype(receiver->set_chrome_color_scheme), 2>(userColor));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestContextGetChromeColorSchemeModeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestContextGetChromeColorSchemeModeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::requestContext.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_chrome_color_scheme_mode(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::RequestContextGetChromeColorSchemeModeResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestContextGetChromeColorSchemeColorRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestContextGetChromeColorSchemeColorRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::requestContext.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_chrome_color_scheme_color(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::RequestContextGetChromeColorSchemeColorResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestContextGetChromeColorSchemeVariantRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestContextGetChromeColorSchemeVariantRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::requestContext.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_chrome_color_scheme_variant(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::RequestContextGetChromeColorSchemeVariantResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RequestContextClearHttpCacheRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RequestContextClearHttpCacheRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::requestContext.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t callback = req.callback;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, callback]() {
                cef_completion_callback_t* callback_ptr = callback != 0 ?
                        reinterpret_cast<cef_completion_callback_t*>(new genvisitors::JvmCompletionCallback(callback, ipc)) :
                        nullptr;
                receiver->clear_http_cache(receiver, callback_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserIsValidRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserIsValidRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browser.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_valid(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserIsValidResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserGetHostRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserGetHostRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browser.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_host(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserGetHostResponse resp;
                    resp.result = insertOrRelease(&tables::browserHost, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserCanGoBackRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserCanGoBackRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browser.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->can_go_back(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserCanGoBackResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserGoBackRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserGoBackRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browser.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->go_back(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserCanGoForwardRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserCanGoForwardRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browser.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->can_go_forward(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserCanGoForwardResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserGoForwardRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserGoForwardRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browser.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->go_forward(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserIsLoadingRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserIsLoadingRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browser.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_loading(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserIsLoadingResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserReloadRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserReloadRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browser.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->reload(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserReloadIgnoreCacheRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserReloadIgnoreCacheRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browser.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->reload_ignore_cache(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserStopLoadRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserStopLoadRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browser.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->stop_load(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserGetIdentifierRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserGetIdentifierRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browser.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_identifier(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserGetIdentifierResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserIsSameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserIsSameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browser.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t that = req.that;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, that]() {
                cef_browser_t* that_ptr = that != 0 ? tables::browser.retain(that) : nullptr;
                auto rawResult = receiver->is_same(receiver, that_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (that_ptr) {
                    auto* that_base = reinterpret_cast<cef_base_ref_counted_t*>(that_ptr);
                    that_base->release(that_base);
                }
                if (ipc) {
                    gen::BrowserIsSameResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserIsPopupRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserIsPopupRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browser.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_popup(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserIsPopupResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHasDocumentRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHasDocumentRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browser.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->has_document(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserHasDocumentResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserGetMainFrameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserGetMainFrameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browser.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_main_frame(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserGetMainFrameResponse resp;
                    resp.result = insertOrRelease(&tables::frame, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserGetFocusedFrameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserGetFocusedFrameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browser.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_focused_frame(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserGetFocusedFrameResponse resp;
                    resp.result = insertOrRelease(&tables::frame, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserGetFrameByIdentifierRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserGetFrameByIdentifierRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browser.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string identifier = req.identifier;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, identifier]() {
                ScopedCefString identifier_cef(identifier);
                auto rawResult = receiver->get_frame_by_identifier(receiver, identifier_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserGetFrameByIdentifierResponse resp;
                    resp.result = insertOrRelease(&tables::frame, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserGetFrameByNameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserGetFrameByNameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browser.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string name = req.name;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, name]() {
                ScopedCefString name_cef(name);
                auto rawResult = receiver->get_frame_by_name(receiver, name_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserGetFrameByNameResponse resp;
                    resp.result = insertOrRelease(&tables::frame, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserGetFrameCountRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserGetFrameCountRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browser.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_frame_count(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserGetFrameCountResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserGetFrameIdentifiersRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserGetFrameIdentifiersRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browser.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::vector<std::string> identifiers = std::move(req.identifiers);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, identifiers]() {
                cef_string_list_t identifiers_list = cef_string_list_alloc();
                for (const auto& __s : identifiers) {
                    cef_string_t __cs{};
                    if (!__s.empty()) cef_string_utf8_to_utf16(__s.data(), __s.size(), &__cs);
                    cef_string_list_append(identifiers_list, &__cs);
                    cef_string_clear(&__cs);
                }
                receiver->get_frame_identifiers(receiver, identifiers_list);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                cef_string_list_free(identifiers_list);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserGetFrameNamesRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserGetFrameNamesRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browser.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::vector<std::string> names = std::move(req.names);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, names]() {
                cef_string_list_t names_list = cef_string_list_alloc();
                for (const auto& __s : names) {
                    cef_string_t __cs{};
                    if (!__s.empty()) cef_string_utf8_to_utf16(__s.data(), __s.size(), &__cs);
                    cef_string_list_append(names_list, &__cs);
                    cef_string_clear(&__cs);
                }
                receiver->get_frame_names(receiver, names_list);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                cef_string_list_free(names_list);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostGetBrowserRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostGetBrowserRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_browser(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserHostGetBrowserResponse resp;
                    resp.result = insertOrRelease(&tables::browser, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostCloseBrowserRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostCloseBrowserRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t forceClose = req.forceClose;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, forceClose]() {
                receiver->close_browser(receiver, cefArg<decltype(receiver->close_browser), 1>(forceClose));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostTryCloseBrowserRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostTryCloseBrowserRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->try_close_browser(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserHostTryCloseBrowserResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostIsReadyToBeClosedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostIsReadyToBeClosedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_ready_to_be_closed(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserHostIsReadyToBeClosedResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostSetFocusRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostSetFocusRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t focus = req.focus;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, focus]() {
                receiver->set_focus(receiver, cefArg<decltype(receiver->set_focus), 1>(focus));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostGetWindowHandleRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostGetWindowHandleRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_window_handle(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserHostGetWindowHandleResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostGetOpenerWindowHandleRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostGetOpenerWindowHandleRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_opener_window_handle(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserHostGetOpenerWindowHandleResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostGetOpenerIdentifierRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostGetOpenerIdentifierRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_opener_identifier(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserHostGetOpenerIdentifierResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostHasViewRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostHasViewRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->has_view(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserHostHasViewResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostGetRequestContextRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostGetRequestContextRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_request_context(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserHostGetRequestContextResponse resp;
                    resp.result = insertOrRelease(&tables::requestContext, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostCanZoomRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostCanZoomRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t command = req.command;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, command]() {
                auto rawResult = receiver->can_zoom(receiver, cefArg<decltype(receiver->can_zoom), 1>(command));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserHostCanZoomResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostZoomRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostZoomRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t command = req.command;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, command]() {
                receiver->zoom(receiver, cefArg<decltype(receiver->zoom), 1>(command));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostStartDownloadRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostStartDownloadRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string url = req.url;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, url]() {
                ScopedCefString url_cef(url);
                receiver->start_download(receiver, url_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostPrintRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostPrintRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->print(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostFindRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostFindRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string searchText = req.searchText;
            std::int32_t forward = req.forward;
            std::int32_t matchCase = req.matchCase;
            std::int32_t findNext = req.findNext;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, searchText, forward, matchCase, findNext]() {
                ScopedCefString searchText_cef(searchText);
                receiver->find(receiver, searchText_cef.get(), cefArg<decltype(receiver->find), 2>(forward), cefArg<decltype(receiver->find), 3>(matchCase), cefArg<decltype(receiver->find), 4>(findNext));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostStopFindingRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostStopFindingRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t clearSelection = req.clearSelection;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, clearSelection]() {
                receiver->stop_finding(receiver, cefArg<decltype(receiver->stop_finding), 1>(clearSelection));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostCloseDevToolsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostCloseDevToolsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->close_dev_tools(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostHasDevToolsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostHasDevToolsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->has_dev_tools(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserHostHasDevToolsResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostSendDevToolsMessageRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostSendDevToolsMessageRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::vector<std::uint8_t> message = std::move(req.message);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, message]() {
                auto rawResult = receiver->send_dev_tools_message(receiver, message.data(), static_cast<size_t>(message.size()));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserHostSendDevToolsMessageResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostExecuteDevToolsMethodRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostExecuteDevToolsMethodRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t messageId_ = req.messageId_;
            std::string method = req.method;
            std::int32_t params = req.params;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, messageId_, method, params]() {
                ScopedCefString method_cef(method);
                cef_dictionary_value_t* params_ptr = params != 0 ? tables::dictionaryValue.retain(params) : nullptr;
                auto rawResult = receiver->execute_dev_tools_method(receiver, cefArg<decltype(receiver->execute_dev_tools_method), 1>(messageId_), method_cef.get(), params_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (params_ptr) {
                    auto* params_base = reinterpret_cast<cef_base_ref_counted_t*>(params_ptr);
                    params_base->release(params_base);
                }
                if (ipc) {
                    gen::BrowserHostExecuteDevToolsMethodResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostReplaceMisspellingRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostReplaceMisspellingRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string word = req.word;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, word]() {
                ScopedCefString word_cef(word);
                receiver->replace_misspelling(receiver, word_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostAddWordToDictionaryRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostAddWordToDictionaryRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string word = req.word;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, word]() {
                ScopedCefString word_cef(word);
                receiver->add_word_to_dictionary(receiver, word_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostIsWindowRenderingDisabledRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostIsWindowRenderingDisabledRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_window_rendering_disabled(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserHostIsWindowRenderingDisabledResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostWasResizedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostWasResizedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->was_resized(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostWasHiddenRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostWasHiddenRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t hidden = req.hidden;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, hidden]() {
                receiver->was_hidden(receiver, cefArg<decltype(receiver->was_hidden), 1>(hidden));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostNotifyScreenInfoChangedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostNotifyScreenInfoChangedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->notify_screen_info_changed(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostInvalidateRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostInvalidateRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t type = req.type;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, type]() {
                receiver->invalidate(receiver, cefArg<decltype(receiver->invalidate), 1>(type));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostSendExternalBeginFrameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostSendExternalBeginFrameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->send_external_begin_frame(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostSendKeyEventRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostSendKeyEventRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            gen::KeyEvent event = std::move(req.event);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, event]() {
                cef_key_event_t event_native{};
                event_native.size = sizeof(event_native);
                event_native.type = static_cast<decltype(event_native.type)>(event.type);
                event_native.modifiers = static_cast<decltype(event_native.modifiers)>(event.modifiers);
                event_native.windows_key_code = static_cast<decltype(event_native.windows_key_code)>(event.windowsKeyCode);
                event_native.native_key_code = static_cast<decltype(event_native.native_key_code)>(event.nativeKeyCode);
                event_native.is_system_key = static_cast<decltype(event_native.is_system_key)>(event.isSystemKey);
                event_native.character = static_cast<decltype(event_native.character)>(event.character);
                event_native.unmodified_character = static_cast<decltype(event_native.unmodified_character)>(event.unmodifiedCharacter);
                event_native.focus_on_editable_field = static_cast<decltype(event_native.focus_on_editable_field)>(event.focusOnEditableField);
                receiver->send_key_event(receiver, &event_native);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostSendMouseClickEventRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostSendMouseClickEventRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            gen::MouseEvent event = std::move(req.event);
            std::int32_t type = req.type;
            std::int32_t mouseUp = req.mouseUp;
            std::int32_t clickCount = req.clickCount;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, event, type, mouseUp, clickCount]() {
                cef_mouse_event_t event_native{};
                event_native.x = static_cast<decltype(event_native.x)>(event.x);
                event_native.y = static_cast<decltype(event_native.y)>(event.y);
                event_native.modifiers = static_cast<decltype(event_native.modifiers)>(event.modifiers);
                receiver->send_mouse_click_event(receiver, &event_native, cefArg<decltype(receiver->send_mouse_click_event), 2>(type), cefArg<decltype(receiver->send_mouse_click_event), 3>(mouseUp), cefArg<decltype(receiver->send_mouse_click_event), 4>(clickCount));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostSendMouseMoveEventRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostSendMouseMoveEventRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            gen::MouseEvent event = std::move(req.event);
            std::int32_t mouseLeave = req.mouseLeave;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, event, mouseLeave]() {
                cef_mouse_event_t event_native{};
                event_native.x = static_cast<decltype(event_native.x)>(event.x);
                event_native.y = static_cast<decltype(event_native.y)>(event.y);
                event_native.modifiers = static_cast<decltype(event_native.modifiers)>(event.modifiers);
                receiver->send_mouse_move_event(receiver, &event_native, cefArg<decltype(receiver->send_mouse_move_event), 2>(mouseLeave));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostSendMouseWheelEventRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostSendMouseWheelEventRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            gen::MouseEvent event = std::move(req.event);
            std::int32_t deltaX = req.deltaX;
            std::int32_t deltaY = req.deltaY;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, event, deltaX, deltaY]() {
                cef_mouse_event_t event_native{};
                event_native.x = static_cast<decltype(event_native.x)>(event.x);
                event_native.y = static_cast<decltype(event_native.y)>(event.y);
                event_native.modifiers = static_cast<decltype(event_native.modifiers)>(event.modifiers);
                receiver->send_mouse_wheel_event(receiver, &event_native, cefArg<decltype(receiver->send_mouse_wheel_event), 2>(deltaX), cefArg<decltype(receiver->send_mouse_wheel_event), 3>(deltaY));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostSendCaptureLostEventRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostSendCaptureLostEventRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->send_capture_lost_event(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostNotifyMoveOrResizeStartedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostNotifyMoveOrResizeStartedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->notify_move_or_resize_started(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostGetWindowlessFrameRateRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostGetWindowlessFrameRateRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_windowless_frame_rate(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserHostGetWindowlessFrameRateResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostSetWindowlessFrameRateRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostSetWindowlessFrameRateRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t frameRate = req.frameRate;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, frameRate]() {
                receiver->set_windowless_frame_rate(receiver, cefArg<decltype(receiver->set_windowless_frame_rate), 1>(frameRate));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostImeCommitTextRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostImeCommitTextRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string text = req.text;
            gen::Range replacementRange = std::move(req.replacementRange);
            std::int32_t relativeCursorPos = req.relativeCursorPos;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, text, replacementRange, relativeCursorPos]() {
                ScopedCefString text_cef(text);
                cef_range_t replacementRange_native{};
                replacementRange_native.from = static_cast<decltype(replacementRange_native.from)>(replacementRange.from);
                replacementRange_native.to = static_cast<decltype(replacementRange_native.to)>(replacementRange.to);
                receiver->ime_commit_text(receiver, text_cef.get(), &replacementRange_native, cefArg<decltype(receiver->ime_commit_text), 3>(relativeCursorPos));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostImeFinishComposingTextRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostImeFinishComposingTextRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t keepSelection = req.keepSelection;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, keepSelection]() {
                receiver->ime_finish_composing_text(receiver, cefArg<decltype(receiver->ime_finish_composing_text), 1>(keepSelection));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostImeCancelCompositionRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostImeCancelCompositionRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->ime_cancel_composition(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostDragTargetDragEnterRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostDragTargetDragEnterRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t dragData = req.dragData;
            gen::MouseEvent event = std::move(req.event);
            std::int32_t allowedOps = req.allowedOps;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, dragData, event, allowedOps]() {
                cef_drag_data_t* dragData_ptr = dragData != 0 ? tables::dragData.retain(dragData) : nullptr;
                cef_mouse_event_t event_native{};
                event_native.x = static_cast<decltype(event_native.x)>(event.x);
                event_native.y = static_cast<decltype(event_native.y)>(event.y);
                event_native.modifiers = static_cast<decltype(event_native.modifiers)>(event.modifiers);
                receiver->drag_target_drag_enter(receiver, dragData_ptr, &event_native, cefArg<decltype(receiver->drag_target_drag_enter), 3>(allowedOps));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (dragData_ptr) {
                    auto* dragData_base = reinterpret_cast<cef_base_ref_counted_t*>(dragData_ptr);
                    dragData_base->release(dragData_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostDragTargetDragOverRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostDragTargetDragOverRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            gen::MouseEvent event = std::move(req.event);
            std::int32_t allowedOps = req.allowedOps;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, event, allowedOps]() {
                cef_mouse_event_t event_native{};
                event_native.x = static_cast<decltype(event_native.x)>(event.x);
                event_native.y = static_cast<decltype(event_native.y)>(event.y);
                event_native.modifiers = static_cast<decltype(event_native.modifiers)>(event.modifiers);
                receiver->drag_target_drag_over(receiver, &event_native, cefArg<decltype(receiver->drag_target_drag_over), 2>(allowedOps));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostDragTargetDragLeaveRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostDragTargetDragLeaveRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->drag_target_drag_leave(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostDragTargetDropRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostDragTargetDropRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            gen::MouseEvent event = std::move(req.event);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, event]() {
                cef_mouse_event_t event_native{};
                event_native.x = static_cast<decltype(event_native.x)>(event.x);
                event_native.y = static_cast<decltype(event_native.y)>(event.y);
                event_native.modifiers = static_cast<decltype(event_native.modifiers)>(event.modifiers);
                receiver->drag_target_drop(receiver, &event_native);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostDragSourceEndedAtRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostDragSourceEndedAtRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t x = req.x;
            std::int32_t y = req.y;
            std::int32_t op = req.op;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, x, y, op]() {
                receiver->drag_source_ended_at(receiver, cefArg<decltype(receiver->drag_source_ended_at), 1>(x), cefArg<decltype(receiver->drag_source_ended_at), 2>(y), cefArg<decltype(receiver->drag_source_ended_at), 3>(op));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostDragSourceSystemDragEndedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostDragSourceSystemDragEndedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->drag_source_system_drag_ended(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostGetVisibleNavigationEntryRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostGetVisibleNavigationEntryRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_visible_navigation_entry(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserHostGetVisibleNavigationEntryResponse resp;
                    resp.result = insertOrRelease(&tables::navigationEntry, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostSetAccessibilityStateRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostSetAccessibilityStateRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t accessibilityState = req.accessibilityState;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, accessibilityState]() {
                receiver->set_accessibility_state(receiver, cefArg<decltype(receiver->set_accessibility_state), 1>(accessibilityState));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostSetAutoResizeEnabledRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostSetAutoResizeEnabledRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t enabled = req.enabled;
            gen::Size minSize = std::move(req.minSize);
            gen::Size maxSize = std::move(req.maxSize);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, enabled, minSize, maxSize]() {
                cef_size_t minSize_native{};
                minSize_native.width = static_cast<decltype(minSize_native.width)>(minSize.width);
                minSize_native.height = static_cast<decltype(minSize_native.height)>(minSize.height);
                cef_size_t maxSize_native{};
                maxSize_native.width = static_cast<decltype(maxSize_native.width)>(maxSize.width);
                maxSize_native.height = static_cast<decltype(maxSize_native.height)>(maxSize.height);
                receiver->set_auto_resize_enabled(receiver, cefArg<decltype(receiver->set_auto_resize_enabled), 1>(enabled), &minSize_native, &maxSize_native);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostSetAudioMutedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostSetAudioMutedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t mute = req.mute;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, mute]() {
                receiver->set_audio_muted(receiver, cefArg<decltype(receiver->set_audio_muted), 1>(mute));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostIsAudioMutedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostIsAudioMutedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_audio_muted(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserHostIsAudioMutedResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostIsFullscreenRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostIsFullscreenRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_fullscreen(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserHostIsFullscreenResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostExitFullscreenRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostExitFullscreenRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t willCauseResize = req.willCauseResize;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, willCauseResize]() {
                receiver->exit_fullscreen(receiver, cefArg<decltype(receiver->exit_fullscreen), 1>(willCauseResize));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostCanExecuteChromeCommandRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostCanExecuteChromeCommandRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t commandId = req.commandId;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, commandId]() {
                auto rawResult = receiver->can_execute_chrome_command(receiver, cefArg<decltype(receiver->can_execute_chrome_command), 1>(commandId));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserHostCanExecuteChromeCommandResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostExecuteChromeCommandRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostExecuteChromeCommandRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t commandId = req.commandId;
            std::int32_t disposition = req.disposition;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, commandId, disposition]() {
                receiver->execute_chrome_command(receiver, cefArg<decltype(receiver->execute_chrome_command), 1>(commandId), cefArg<decltype(receiver->execute_chrome_command), 2>(disposition));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostIsRenderProcessUnresponsiveRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostIsRenderProcessUnresponsiveRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_render_process_unresponsive(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserHostIsRenderProcessUnresponsiveResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserHostGetRuntimeStyleRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserHostGetRuntimeStyleRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserHost.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_runtime_style(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserHostGetRuntimeStyleResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelIsSubMenuRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelIsSubMenuRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_sub_menu(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelIsSubMenuResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelClearRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelClearRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->clear(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelClearResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelGetCountRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelGetCountRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_count(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelGetCountResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelAddSeparatorRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelAddSeparatorRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->add_separator(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelAddSeparatorResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelAddItemRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelAddItemRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t commandId = req.commandId;
            std::string label = req.label;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, commandId, label]() {
                ScopedCefString label_cef(label);
                auto rawResult = receiver->add_item(receiver, cefArg<decltype(receiver->add_item), 1>(commandId), label_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelAddItemResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelAddCheckItemRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelAddCheckItemRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t commandId = req.commandId;
            std::string label = req.label;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, commandId, label]() {
                ScopedCefString label_cef(label);
                auto rawResult = receiver->add_check_item(receiver, cefArg<decltype(receiver->add_check_item), 1>(commandId), label_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelAddCheckItemResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelAddRadioItemRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelAddRadioItemRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t commandId = req.commandId;
            std::string label = req.label;
            std::int32_t groupId = req.groupId;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, commandId, label, groupId]() {
                ScopedCefString label_cef(label);
                auto rawResult = receiver->add_radio_item(receiver, cefArg<decltype(receiver->add_radio_item), 1>(commandId), label_cef.get(), cefArg<decltype(receiver->add_radio_item), 3>(groupId));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelAddRadioItemResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelAddSubMenuRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelAddSubMenuRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t commandId = req.commandId;
            std::string label = req.label;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, commandId, label]() {
                ScopedCefString label_cef(label);
                auto rawResult = receiver->add_sub_menu(receiver, cefArg<decltype(receiver->add_sub_menu), 1>(commandId), label_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelAddSubMenuResponse resp;
                    resp.result = insertOrRelease(&tables::menuModel, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelInsertSeparatorAtRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelInsertSeparatorAtRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index]() {
                auto rawResult = receiver->insert_separator_at(receiver, cefArg<decltype(receiver->insert_separator_at), 1>(index));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelInsertSeparatorAtResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelInsertItemAtRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelInsertItemAtRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            std::int32_t commandId = req.commandId;
            std::string label = req.label;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index, commandId, label]() {
                ScopedCefString label_cef(label);
                auto rawResult = receiver->insert_item_at(receiver, cefArg<decltype(receiver->insert_item_at), 1>(index), cefArg<decltype(receiver->insert_item_at), 2>(commandId), label_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelInsertItemAtResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelInsertCheckItemAtRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelInsertCheckItemAtRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            std::int32_t commandId = req.commandId;
            std::string label = req.label;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index, commandId, label]() {
                ScopedCefString label_cef(label);
                auto rawResult = receiver->insert_check_item_at(receiver, cefArg<decltype(receiver->insert_check_item_at), 1>(index), cefArg<decltype(receiver->insert_check_item_at), 2>(commandId), label_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelInsertCheckItemAtResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelInsertRadioItemAtRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelInsertRadioItemAtRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            std::int32_t commandId = req.commandId;
            std::string label = req.label;
            std::int32_t groupId = req.groupId;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index, commandId, label, groupId]() {
                ScopedCefString label_cef(label);
                auto rawResult = receiver->insert_radio_item_at(receiver, cefArg<decltype(receiver->insert_radio_item_at), 1>(index), cefArg<decltype(receiver->insert_radio_item_at), 2>(commandId), label_cef.get(), cefArg<decltype(receiver->insert_radio_item_at), 4>(groupId));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelInsertRadioItemAtResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelInsertSubMenuAtRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelInsertSubMenuAtRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            std::int32_t commandId = req.commandId;
            std::string label = req.label;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index, commandId, label]() {
                ScopedCefString label_cef(label);
                auto rawResult = receiver->insert_sub_menu_at(receiver, cefArg<decltype(receiver->insert_sub_menu_at), 1>(index), cefArg<decltype(receiver->insert_sub_menu_at), 2>(commandId), label_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelInsertSubMenuAtResponse resp;
                    resp.result = insertOrRelease(&tables::menuModel, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelRemoveRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelRemoveRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t commandId = req.commandId;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, commandId]() {
                auto rawResult = receiver->remove(receiver, cefArg<decltype(receiver->remove), 1>(commandId));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelRemoveResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelRemoveAtRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelRemoveAtRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index]() {
                auto rawResult = receiver->remove_at(receiver, cefArg<decltype(receiver->remove_at), 1>(index));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelRemoveAtResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelGetIndexOfRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelGetIndexOfRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t commandId = req.commandId;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, commandId]() {
                auto rawResult = receiver->get_index_of(receiver, cefArg<decltype(receiver->get_index_of), 1>(commandId));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelGetIndexOfResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelGetCommandIdAtRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelGetCommandIdAtRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index]() {
                auto rawResult = receiver->get_command_id_at(receiver, cefArg<decltype(receiver->get_command_id_at), 1>(index));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelGetCommandIdAtResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelSetCommandIdAtRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelSetCommandIdAtRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            std::int32_t commandId = req.commandId;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index, commandId]() {
                auto rawResult = receiver->set_command_id_at(receiver, cefArg<decltype(receiver->set_command_id_at), 1>(index), cefArg<decltype(receiver->set_command_id_at), 2>(commandId));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelSetCommandIdAtResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelGetLabelRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelGetLabelRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t commandId = req.commandId;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, commandId]() {
                auto rawResult = receiver->get_label(receiver, cefArg<decltype(receiver->get_label), 1>(commandId));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelGetLabelResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelSetLabelRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelSetLabelRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t commandId = req.commandId;
            std::string label = req.label;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, commandId, label]() {
                ScopedCefString label_cef(label);
                auto rawResult = receiver->set_label(receiver, cefArg<decltype(receiver->set_label), 1>(commandId), label_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelSetLabelResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelSetLabelAtRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelSetLabelAtRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            std::string label = req.label;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index, label]() {
                ScopedCefString label_cef(label);
                auto rawResult = receiver->set_label_at(receiver, cefArg<decltype(receiver->set_label_at), 1>(index), label_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelSetLabelAtResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelGetTypeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelGetTypeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t commandId = req.commandId;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, commandId]() {
                auto rawResult = receiver->get_type(receiver, cefArg<decltype(receiver->get_type), 1>(commandId));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelGetTypeResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelGetGroupIdRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelGetGroupIdRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t commandId = req.commandId;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, commandId]() {
                auto rawResult = receiver->get_group_id(receiver, cefArg<decltype(receiver->get_group_id), 1>(commandId));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelGetGroupIdResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelGetGroupIdAtRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelGetGroupIdAtRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index]() {
                auto rawResult = receiver->get_group_id_at(receiver, cefArg<decltype(receiver->get_group_id_at), 1>(index));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelGetGroupIdAtResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelSetGroupIdRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelSetGroupIdRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t commandId = req.commandId;
            std::int32_t groupId = req.groupId;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, commandId, groupId]() {
                auto rawResult = receiver->set_group_id(receiver, cefArg<decltype(receiver->set_group_id), 1>(commandId), cefArg<decltype(receiver->set_group_id), 2>(groupId));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelSetGroupIdResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelSetGroupIdAtRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelSetGroupIdAtRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            std::int32_t groupId = req.groupId;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index, groupId]() {
                auto rawResult = receiver->set_group_id_at(receiver, cefArg<decltype(receiver->set_group_id_at), 1>(index), cefArg<decltype(receiver->set_group_id_at), 2>(groupId));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelSetGroupIdAtResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelGetSubMenuRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelGetSubMenuRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t commandId = req.commandId;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, commandId]() {
                auto rawResult = receiver->get_sub_menu(receiver, cefArg<decltype(receiver->get_sub_menu), 1>(commandId));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelGetSubMenuResponse resp;
                    resp.result = insertOrRelease(&tables::menuModel, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelGetSubMenuAtRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelGetSubMenuAtRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index]() {
                auto rawResult = receiver->get_sub_menu_at(receiver, cefArg<decltype(receiver->get_sub_menu_at), 1>(index));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelGetSubMenuAtResponse resp;
                    resp.result = insertOrRelease(&tables::menuModel, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelIsVisibleRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelIsVisibleRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t commandId = req.commandId;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, commandId]() {
                auto rawResult = receiver->is_visible(receiver, cefArg<decltype(receiver->is_visible), 1>(commandId));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelIsVisibleResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelIsVisibleAtRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelIsVisibleAtRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index]() {
                auto rawResult = receiver->is_visible_at(receiver, cefArg<decltype(receiver->is_visible_at), 1>(index));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelIsVisibleAtResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelSetVisibleRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelSetVisibleRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t commandId = req.commandId;
            std::int32_t visible = req.visible;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, commandId, visible]() {
                auto rawResult = receiver->set_visible(receiver, cefArg<decltype(receiver->set_visible), 1>(commandId), cefArg<decltype(receiver->set_visible), 2>(visible));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelSetVisibleResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelSetVisibleAtRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelSetVisibleAtRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            std::int32_t visible = req.visible;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index, visible]() {
                auto rawResult = receiver->set_visible_at(receiver, cefArg<decltype(receiver->set_visible_at), 1>(index), cefArg<decltype(receiver->set_visible_at), 2>(visible));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelSetVisibleAtResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelIsEnabledRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelIsEnabledRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t commandId = req.commandId;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, commandId]() {
                auto rawResult = receiver->is_enabled(receiver, cefArg<decltype(receiver->is_enabled), 1>(commandId));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelIsEnabledResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelIsEnabledAtRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelIsEnabledAtRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index]() {
                auto rawResult = receiver->is_enabled_at(receiver, cefArg<decltype(receiver->is_enabled_at), 1>(index));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelIsEnabledAtResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelSetEnabledRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelSetEnabledRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t commandId = req.commandId;
            std::int32_t enabled = req.enabled;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, commandId, enabled]() {
                auto rawResult = receiver->set_enabled(receiver, cefArg<decltype(receiver->set_enabled), 1>(commandId), cefArg<decltype(receiver->set_enabled), 2>(enabled));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelSetEnabledResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelSetEnabledAtRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelSetEnabledAtRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            std::int32_t enabled = req.enabled;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index, enabled]() {
                auto rawResult = receiver->set_enabled_at(receiver, cefArg<decltype(receiver->set_enabled_at), 1>(index), cefArg<decltype(receiver->set_enabled_at), 2>(enabled));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelSetEnabledAtResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelIsCheckedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelIsCheckedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t commandId = req.commandId;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, commandId]() {
                auto rawResult = receiver->is_checked(receiver, cefArg<decltype(receiver->is_checked), 1>(commandId));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelIsCheckedResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelIsCheckedAtRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelIsCheckedAtRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index]() {
                auto rawResult = receiver->is_checked_at(receiver, cefArg<decltype(receiver->is_checked_at), 1>(index));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelIsCheckedAtResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelSetCheckedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelSetCheckedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t commandId = req.commandId;
            std::int32_t checked = req.checked;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, commandId, checked]() {
                auto rawResult = receiver->set_checked(receiver, cefArg<decltype(receiver->set_checked), 1>(commandId), cefArg<decltype(receiver->set_checked), 2>(checked));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelSetCheckedResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelSetCheckedAtRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelSetCheckedAtRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            std::int32_t checked = req.checked;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index, checked]() {
                auto rawResult = receiver->set_checked_at(receiver, cefArg<decltype(receiver->set_checked_at), 1>(index), cefArg<decltype(receiver->set_checked_at), 2>(checked));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelSetCheckedAtResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelHasAcceleratorRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelHasAcceleratorRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t commandId = req.commandId;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, commandId]() {
                auto rawResult = receiver->has_accelerator(receiver, cefArg<decltype(receiver->has_accelerator), 1>(commandId));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelHasAcceleratorResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelHasAcceleratorAtRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelHasAcceleratorAtRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index]() {
                auto rawResult = receiver->has_accelerator_at(receiver, cefArg<decltype(receiver->has_accelerator_at), 1>(index));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelHasAcceleratorAtResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelSetAcceleratorRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelSetAcceleratorRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t commandId = req.commandId;
            std::int32_t keyCode = req.keyCode;
            std::int32_t shiftPressed = req.shiftPressed;
            std::int32_t ctrlPressed = req.ctrlPressed;
            std::int32_t altPressed = req.altPressed;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, commandId, keyCode, shiftPressed, ctrlPressed, altPressed]() {
                auto rawResult = receiver->set_accelerator(receiver, cefArg<decltype(receiver->set_accelerator), 1>(commandId), cefArg<decltype(receiver->set_accelerator), 2>(keyCode), cefArg<decltype(receiver->set_accelerator), 3>(shiftPressed), cefArg<decltype(receiver->set_accelerator), 4>(ctrlPressed), cefArg<decltype(receiver->set_accelerator), 5>(altPressed));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelSetAcceleratorResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelSetAcceleratorAtRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelSetAcceleratorAtRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            std::int32_t keyCode = req.keyCode;
            std::int32_t shiftPressed = req.shiftPressed;
            std::int32_t ctrlPressed = req.ctrlPressed;
            std::int32_t altPressed = req.altPressed;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index, keyCode, shiftPressed, ctrlPressed, altPressed]() {
                auto rawResult = receiver->set_accelerator_at(receiver, cefArg<decltype(receiver->set_accelerator_at), 1>(index), cefArg<decltype(receiver->set_accelerator_at), 2>(keyCode), cefArg<decltype(receiver->set_accelerator_at), 3>(shiftPressed), cefArg<decltype(receiver->set_accelerator_at), 4>(ctrlPressed), cefArg<decltype(receiver->set_accelerator_at), 5>(altPressed));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelSetAcceleratorAtResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelRemoveAcceleratorRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelRemoveAcceleratorRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t commandId = req.commandId;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, commandId]() {
                auto rawResult = receiver->remove_accelerator(receiver, cefArg<decltype(receiver->remove_accelerator), 1>(commandId));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelRemoveAcceleratorResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelRemoveAcceleratorAtRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelRemoveAcceleratorAtRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t index = req.index;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index]() {
                auto rawResult = receiver->remove_accelerator_at(receiver, cefArg<decltype(receiver->remove_accelerator_at), 1>(index));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelRemoveAcceleratorAtResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelSetColorRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelSetColorRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t commandId = req.commandId;
            std::int32_t colorType = req.colorType;
            std::int32_t color = req.color;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, commandId, colorType, color]() {
                auto rawResult = receiver->set_color(receiver, cefArg<decltype(receiver->set_color), 1>(commandId), cefArg<decltype(receiver->set_color), 2>(colorType), cefArg<decltype(receiver->set_color), 3>(color));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelSetColorResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelSetColorAtRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelSetColorAtRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t index = req.index;
            std::int32_t colorType = req.colorType;
            std::int32_t color = req.color;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index, colorType, color]() {
                auto rawResult = receiver->set_color_at(receiver, cefArg<decltype(receiver->set_color_at), 1>(index), cefArg<decltype(receiver->set_color_at), 2>(colorType), cefArg<decltype(receiver->set_color_at), 3>(color));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelSetColorAtResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelSetFontListRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelSetFontListRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t commandId = req.commandId;
            std::string fontList = req.fontList;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, commandId, fontList]() {
                ScopedCefString fontList_cef(fontList);
                auto rawResult = receiver->set_font_list(receiver, cefArg<decltype(receiver->set_font_list), 1>(commandId), fontList_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelSetFontListResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuModelSetFontListAtRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuModelSetFontListAtRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuModel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t index = req.index;
            std::string fontList = req.fontList;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index, fontList]() {
                ScopedCefString fontList_cef(fontList);
                auto rawResult = receiver->set_font_list_at(receiver, cefArg<decltype(receiver->set_font_list_at), 1>(index), fontList_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::MenuModelSetFontListAtResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RunContextMenuCallbackContRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RunContextMenuCallbackContRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::runContextMenuCallback.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t commandId = req.commandId;
            std::int32_t eventFlags = req.eventFlags;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, commandId, eventFlags]() {
                receiver->cont(receiver, cefArg<decltype(receiver->cont), 1>(commandId), cefArg<decltype(receiver->cont), 2>(eventFlags));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RunContextMenuCallbackCancelRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RunContextMenuCallbackCancelRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::runContextMenuCallback.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->cancel(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RunQuickMenuCallbackContRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RunQuickMenuCallbackContRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::runQuickMenuCallback.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t commandId = req.commandId;
            std::int32_t eventFlags = req.eventFlags;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, commandId, eventFlags]() {
                receiver->cont(receiver, cefArg<decltype(receiver->cont), 1>(commandId), cefArg<decltype(receiver->cont), 2>(eventFlags));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::RunQuickMenuCallbackCancelRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::RunQuickMenuCallbackCancelRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::runQuickMenuCallback.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->cancel(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ContextMenuParamsGetXcoordRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ContextMenuParamsGetXcoordRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::contextMenuParams.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_xcoord(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ContextMenuParamsGetXcoordResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ContextMenuParamsGetYcoordRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ContextMenuParamsGetYcoordRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::contextMenuParams.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_ycoord(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ContextMenuParamsGetYcoordResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ContextMenuParamsGetTypeFlagsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ContextMenuParamsGetTypeFlagsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::contextMenuParams.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_type_flags(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ContextMenuParamsGetTypeFlagsResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ContextMenuParamsGetLinkUrlRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ContextMenuParamsGetLinkUrlRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::contextMenuParams.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_link_url(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ContextMenuParamsGetLinkUrlResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ContextMenuParamsGetUnfilteredLinkUrlRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ContextMenuParamsGetUnfilteredLinkUrlRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::contextMenuParams.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_unfiltered_link_url(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ContextMenuParamsGetUnfilteredLinkUrlResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ContextMenuParamsGetSourceUrlRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ContextMenuParamsGetSourceUrlRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::contextMenuParams.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_source_url(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ContextMenuParamsGetSourceUrlResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ContextMenuParamsHasImageContentsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ContextMenuParamsHasImageContentsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::contextMenuParams.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->has_image_contents(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ContextMenuParamsHasImageContentsResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ContextMenuParamsGetTitleTextRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ContextMenuParamsGetTitleTextRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::contextMenuParams.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_title_text(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ContextMenuParamsGetTitleTextResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ContextMenuParamsGetPageUrlRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ContextMenuParamsGetPageUrlRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::contextMenuParams.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_page_url(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ContextMenuParamsGetPageUrlResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ContextMenuParamsGetFrameUrlRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ContextMenuParamsGetFrameUrlRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::contextMenuParams.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_frame_url(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ContextMenuParamsGetFrameUrlResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ContextMenuParamsGetFrameCharsetRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ContextMenuParamsGetFrameCharsetRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::contextMenuParams.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_frame_charset(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ContextMenuParamsGetFrameCharsetResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ContextMenuParamsGetMediaTypeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ContextMenuParamsGetMediaTypeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::contextMenuParams.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_media_type(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ContextMenuParamsGetMediaTypeResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ContextMenuParamsGetMediaStateFlagsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ContextMenuParamsGetMediaStateFlagsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::contextMenuParams.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_media_state_flags(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ContextMenuParamsGetMediaStateFlagsResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ContextMenuParamsGetSelectionTextRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ContextMenuParamsGetSelectionTextRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::contextMenuParams.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_selection_text(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ContextMenuParamsGetSelectionTextResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ContextMenuParamsGetMisspelledWordRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ContextMenuParamsGetMisspelledWordRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::contextMenuParams.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_misspelled_word(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ContextMenuParamsGetMisspelledWordResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ContextMenuParamsGetDictionarySuggestionsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ContextMenuParamsGetDictionarySuggestionsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::contextMenuParams.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::vector<std::string> suggestions = std::move(req.suggestions);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, suggestions]() {
                cef_string_list_t suggestions_list = cef_string_list_alloc();
                for (const auto& __s : suggestions) {
                    cef_string_t __cs{};
                    if (!__s.empty()) cef_string_utf8_to_utf16(__s.data(), __s.size(), &__cs);
                    cef_string_list_append(suggestions_list, &__cs);
                    cef_string_clear(&__cs);
                }
                auto rawResult = receiver->get_dictionary_suggestions(receiver, suggestions_list);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                cef_string_list_free(suggestions_list);
                if (ipc) {
                    gen::ContextMenuParamsGetDictionarySuggestionsResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ContextMenuParamsIsEditableRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ContextMenuParamsIsEditableRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::contextMenuParams.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_editable(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ContextMenuParamsIsEditableResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ContextMenuParamsIsSpellCheckEnabledRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ContextMenuParamsIsSpellCheckEnabledRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::contextMenuParams.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_spell_check_enabled(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ContextMenuParamsIsSpellCheckEnabledResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ContextMenuParamsGetEditStateFlagsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ContextMenuParamsGetEditStateFlagsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::contextMenuParams.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_edit_state_flags(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ContextMenuParamsGetEditStateFlagsResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ContextMenuParamsIsCustomMenuRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ContextMenuParamsIsCustomMenuRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::contextMenuParams.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_custom_menu(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ContextMenuParamsIsCustomMenuResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::FileDialogCallbackContRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::FileDialogCallbackContRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::fileDialogCallback.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::vector<std::string> filePaths = std::move(req.filePaths);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, filePaths]() {
                cef_string_list_t filePaths_list = cef_string_list_alloc();
                for (const auto& __s : filePaths) {
                    cef_string_t __cs{};
                    if (!__s.empty()) cef_string_utf8_to_utf16(__s.data(), __s.size(), &__cs);
                    cef_string_list_append(filePaths_list, &__cs);
                    cef_string_clear(&__cs);
                }
                receiver->cont(receiver, filePaths_list);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                cef_string_list_free(filePaths_list);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::FileDialogCallbackCancelRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::FileDialogCallbackCancelRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::fileDialogCallback.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->cancel(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DownloadItemIsValidRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DownloadItemIsValidRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::downloadItem.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_valid(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DownloadItemIsValidResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DownloadItemIsInProgressRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DownloadItemIsInProgressRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::downloadItem.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_in_progress(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DownloadItemIsInProgressResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DownloadItemIsCompleteRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DownloadItemIsCompleteRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::downloadItem.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_complete(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DownloadItemIsCompleteResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DownloadItemIsCanceledRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DownloadItemIsCanceledRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::downloadItem.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_canceled(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DownloadItemIsCanceledResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DownloadItemIsInterruptedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DownloadItemIsInterruptedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::downloadItem.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_interrupted(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DownloadItemIsInterruptedResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DownloadItemGetInterruptReasonRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DownloadItemGetInterruptReasonRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::downloadItem.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_interrupt_reason(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DownloadItemGetInterruptReasonResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DownloadItemGetCurrentSpeedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DownloadItemGetCurrentSpeedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::downloadItem.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_current_speed(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DownloadItemGetCurrentSpeedResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DownloadItemGetPercentCompleteRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DownloadItemGetPercentCompleteRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::downloadItem.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_percent_complete(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DownloadItemGetPercentCompleteResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DownloadItemGetTotalBytesRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DownloadItemGetTotalBytesRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::downloadItem.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_total_bytes(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DownloadItemGetTotalBytesResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DownloadItemGetReceivedBytesRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DownloadItemGetReceivedBytesRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::downloadItem.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_received_bytes(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DownloadItemGetReceivedBytesResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DownloadItemGetStartTimeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DownloadItemGetStartTimeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::downloadItem.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                cef_basetime_t rawResult = receiver->get_start_time(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DownloadItemGetStartTimeResponse resp;
                resp.result.val = static_cast<decltype(resp.result.val)>(rawResult.val);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DownloadItemGetEndTimeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DownloadItemGetEndTimeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::downloadItem.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                cef_basetime_t rawResult = receiver->get_end_time(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DownloadItemGetEndTimeResponse resp;
                resp.result.val = static_cast<decltype(resp.result.val)>(rawResult.val);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DownloadItemGetFullPathRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DownloadItemGetFullPathRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::downloadItem.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_full_path(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DownloadItemGetFullPathResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DownloadItemGetIdRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DownloadItemGetIdRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::downloadItem.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_id(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DownloadItemGetIdResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DownloadItemGetUrlRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DownloadItemGetUrlRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::downloadItem.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_url(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DownloadItemGetUrlResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DownloadItemGetOriginalUrlRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DownloadItemGetOriginalUrlRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::downloadItem.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_original_url(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DownloadItemGetOriginalUrlResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DownloadItemGetSuggestedFileNameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DownloadItemGetSuggestedFileNameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::downloadItem.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_suggested_file_name(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DownloadItemGetSuggestedFileNameResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DownloadItemGetContentDispositionRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DownloadItemGetContentDispositionRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::downloadItem.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_content_disposition(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DownloadItemGetContentDispositionResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DownloadItemGetMimeTypeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DownloadItemGetMimeTypeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::downloadItem.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_mime_type(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DownloadItemGetMimeTypeResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DownloadItemIsPausedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DownloadItemIsPausedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::downloadItem.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_paused(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DownloadItemIsPausedResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BeforeDownloadCallbackContRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BeforeDownloadCallbackContRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::beforeDownloadCallback.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string downloadPath = req.downloadPath;
            std::int32_t showDialog = req.showDialog;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, downloadPath, showDialog]() {
                ScopedCefString downloadPath_cef(downloadPath);
                receiver->cont(receiver, downloadPath_cef.get(), cefArg<decltype(receiver->cont), 2>(showDialog));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DownloadItemCallbackCancelRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DownloadItemCallbackCancelRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::downloadItemCallback.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->cancel(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DownloadItemCallbackPauseRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DownloadItemCallbackPauseRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::downloadItemCallback.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->pause(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DownloadItemCallbackResumeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DownloadItemCallbackResumeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::downloadItemCallback.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->resume(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::JsDialogCallbackContRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::JsDialogCallbackContRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::jsDialogCallback.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t success = req.success;
            std::string userInput = req.userInput;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, success, userInput]() {
                ScopedCefString userInput_cef(userInput);
                receiver->cont(receiver, cefArg<decltype(receiver->cont), 1>(success), userInput_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MediaAccessCallbackContRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MediaAccessCallbackContRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::mediaAccessCallback.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t allowedPermissions = req.allowedPermissions;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, allowedPermissions]() {
                receiver->cont(receiver, cefArg<decltype(receiver->cont), 1>(allowedPermissions));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MediaAccessCallbackCancelRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MediaAccessCallbackCancelRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::mediaAccessCallback.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->cancel(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PermissionPromptCallbackContRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PermissionPromptCallbackContRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::permissionPromptCallback.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t result = req.result;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, result]() {
                receiver->cont(receiver, cefArg<decltype(receiver->cont), 1>(result));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PrintSettingsIsValidRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PrintSettingsIsValidRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::printSettings.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_valid(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::PrintSettingsIsValidResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PrintSettingsIsReadOnlyRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PrintSettingsIsReadOnlyRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::printSettings.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_read_only(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::PrintSettingsIsReadOnlyResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PrintSettingsSetOrientationRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PrintSettingsSetOrientationRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::printSettings.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t landscape = req.landscape;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, landscape]() {
                receiver->set_orientation(receiver, cefArg<decltype(receiver->set_orientation), 1>(landscape));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PrintSettingsIsLandscapeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PrintSettingsIsLandscapeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::printSettings.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_landscape(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::PrintSettingsIsLandscapeResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PrintSettingsSetPrinterPrintableAreaRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PrintSettingsSetPrinterPrintableAreaRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::printSettings.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            gen::Size physicalSizeDeviceUnits = std::move(req.physicalSizeDeviceUnits);
            gen::Rect printableAreaDeviceUnits = std::move(req.printableAreaDeviceUnits);
            std::int32_t landscapeNeedsFlip = req.landscapeNeedsFlip;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, physicalSizeDeviceUnits, printableAreaDeviceUnits, landscapeNeedsFlip]() {
                cef_size_t physicalSizeDeviceUnits_native{};
                physicalSizeDeviceUnits_native.width = static_cast<decltype(physicalSizeDeviceUnits_native.width)>(physicalSizeDeviceUnits.width);
                physicalSizeDeviceUnits_native.height = static_cast<decltype(physicalSizeDeviceUnits_native.height)>(physicalSizeDeviceUnits.height);
                cef_rect_t printableAreaDeviceUnits_native{};
                printableAreaDeviceUnits_native.x = static_cast<decltype(printableAreaDeviceUnits_native.x)>(printableAreaDeviceUnits.x);
                printableAreaDeviceUnits_native.y = static_cast<decltype(printableAreaDeviceUnits_native.y)>(printableAreaDeviceUnits.y);
                printableAreaDeviceUnits_native.width = static_cast<decltype(printableAreaDeviceUnits_native.width)>(printableAreaDeviceUnits.width);
                printableAreaDeviceUnits_native.height = static_cast<decltype(printableAreaDeviceUnits_native.height)>(printableAreaDeviceUnits.height);
                receiver->set_printer_printable_area(receiver, &physicalSizeDeviceUnits_native, &printableAreaDeviceUnits_native, cefArg<decltype(receiver->set_printer_printable_area), 3>(landscapeNeedsFlip));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PrintSettingsSetDeviceNameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PrintSettingsSetDeviceNameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::printSettings.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string name = req.name;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, name]() {
                ScopedCefString name_cef(name);
                receiver->set_device_name(receiver, name_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PrintSettingsGetDeviceNameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PrintSettingsGetDeviceNameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::printSettings.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_device_name(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::PrintSettingsGetDeviceNameResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PrintSettingsSetDpiRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PrintSettingsSetDpiRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::printSettings.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t dpi = req.dpi;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, dpi]() {
                receiver->set_dpi(receiver, cefArg<decltype(receiver->set_dpi), 1>(dpi));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PrintSettingsGetDpiRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PrintSettingsGetDpiRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::printSettings.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_dpi(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::PrintSettingsGetDpiResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PrintSettingsGetPageRangesCountRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PrintSettingsGetPageRangesCountRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::printSettings.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_page_ranges_count(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::PrintSettingsGetPageRangesCountResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PrintSettingsSetSelectionOnlyRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PrintSettingsSetSelectionOnlyRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::printSettings.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t selectionOnly = req.selectionOnly;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, selectionOnly]() {
                receiver->set_selection_only(receiver, cefArg<decltype(receiver->set_selection_only), 1>(selectionOnly));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PrintSettingsIsSelectionOnlyRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PrintSettingsIsSelectionOnlyRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::printSettings.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_selection_only(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::PrintSettingsIsSelectionOnlyResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PrintSettingsSetCollateRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PrintSettingsSetCollateRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::printSettings.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t collate = req.collate;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, collate]() {
                receiver->set_collate(receiver, cefArg<decltype(receiver->set_collate), 1>(collate));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PrintSettingsWillCollateRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PrintSettingsWillCollateRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::printSettings.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->will_collate(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::PrintSettingsWillCollateResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PrintSettingsSetColorModelRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PrintSettingsSetColorModelRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::printSettings.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t model = req.model;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, model]() {
                receiver->set_color_model(receiver, cefArg<decltype(receiver->set_color_model), 1>(model));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PrintSettingsGetColorModelRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PrintSettingsGetColorModelRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::printSettings.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_color_model(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::PrintSettingsGetColorModelResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PrintSettingsSetCopiesRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PrintSettingsSetCopiesRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::printSettings.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t copies = req.copies;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, copies]() {
                receiver->set_copies(receiver, cefArg<decltype(receiver->set_copies), 1>(copies));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PrintSettingsGetCopiesRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PrintSettingsGetCopiesRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::printSettings.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_copies(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::PrintSettingsGetCopiesResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PrintSettingsSetDuplexModeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PrintSettingsSetDuplexModeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::printSettings.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t mode = req.mode;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, mode]() {
                receiver->set_duplex_mode(receiver, cefArg<decltype(receiver->set_duplex_mode), 1>(mode));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PrintSettingsGetDuplexModeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PrintSettingsGetDuplexModeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::printSettings.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_duplex_mode(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::PrintSettingsGetDuplexModeResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PrintDialogCallbackContRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PrintDialogCallbackContRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::printDialogCallback.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t settings = req.settings;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, settings]() {
                cef_print_settings_t* settings_ptr = settings != 0 ? tables::printSettings.retain(settings) : nullptr;
                receiver->cont(receiver, settings_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (settings_ptr) {
                    auto* settings_base = reinterpret_cast<cef_base_ref_counted_t*>(settings_ptr);
                    settings_base->release(settings_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PrintDialogCallbackCancelRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PrintDialogCallbackCancelRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::printDialogCallback.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->cancel(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PrintJobCallbackContRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PrintJobCallbackContRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::printJobCallback.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->cont(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::AuthCallbackContRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::AuthCallbackContRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::authCallback.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string username = req.username;
            std::string password = req.password;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, username, password]() {
                ScopedCefString username_cef(username);
                ScopedCefString password_cef(password);
                receiver->cont(receiver, username_cef.get(), password_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::AuthCallbackCancelRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::AuthCallbackCancelRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::authCallback.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->cancel(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ResponseIsReadOnlyRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ResponseIsReadOnlyRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::response.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_read_only(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ResponseIsReadOnlyResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ResponseGetErrorRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ResponseGetErrorRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::response.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_error(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ResponseGetErrorResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ResponseSetErrorRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ResponseSetErrorRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::response.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t error = req.error;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, error]() {
                receiver->set_error(receiver, cefArg<decltype(receiver->set_error), 1>(error));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ResponseGetStatusRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ResponseGetStatusRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::response.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_status(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ResponseGetStatusResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ResponseSetStatusRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ResponseSetStatusRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::response.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t status = req.status;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, status]() {
                receiver->set_status(receiver, cefArg<decltype(receiver->set_status), 1>(status));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ResponseGetStatusTextRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ResponseGetStatusTextRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::response.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_status_text(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ResponseGetStatusTextResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ResponseSetStatusTextRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ResponseSetStatusTextRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::response.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string statusText = req.statusText;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, statusText]() {
                ScopedCefString statusText_cef(statusText);
                receiver->set_status_text(receiver, statusText_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ResponseGetMimeTypeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ResponseGetMimeTypeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::response.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_mime_type(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ResponseGetMimeTypeResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ResponseSetMimeTypeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ResponseSetMimeTypeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::response.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string mimeType = req.mimeType;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, mimeType]() {
                ScopedCefString mimeType_cef(mimeType);
                receiver->set_mime_type(receiver, mimeType_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ResponseGetCharsetRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ResponseGetCharsetRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::response.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_charset(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ResponseGetCharsetResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ResponseSetCharsetRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ResponseSetCharsetRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::response.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string charset = req.charset;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, charset]() {
                ScopedCefString charset_cef(charset);
                receiver->set_charset(receiver, charset_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ResponseGetHeaderByNameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ResponseGetHeaderByNameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::response.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string name = req.name;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, name]() {
                ScopedCefString name_cef(name);
                auto rawResult = receiver->get_header_by_name(receiver, name_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ResponseGetHeaderByNameResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ResponseSetHeaderByNameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ResponseSetHeaderByNameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::response.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string name = req.name;
            std::string value = req.value;
            std::int32_t overwrite = req.overwrite;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, name, value, overwrite]() {
                ScopedCefString name_cef(name);
                ScopedCefString value_cef(value);
                receiver->set_header_by_name(receiver, name_cef.get(), value_cef.get(), cefArg<decltype(receiver->set_header_by_name), 3>(overwrite));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ResponseGetUrlRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ResponseGetUrlRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::response.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_url(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ResponseGetUrlResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ResponseSetUrlRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ResponseSetUrlRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::response.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string url = req.url;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, url]() {
                ScopedCefString url_cef(url);
                receiver->set_url(receiver, url_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ResourceSkipCallbackContRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ResourceSkipCallbackContRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::resourceSkipCallback.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t bytesSkipped = req.bytesSkipped;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, bytesSkipped]() {
                receiver->cont(receiver, cefArg<decltype(receiver->cont), 1>(bytesSkipped));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ResourceReadCallbackContRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ResourceReadCallbackContRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::resourceReadCallback.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t bytesRead = req.bytesRead;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, bytesRead]() {
                receiver->cont(receiver, cefArg<decltype(receiver->cont), 1>(bytesRead));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::SslInfoGetCertStatusRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::SslInfoGetCertStatusRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::sslinfo.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_cert_status(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::SslInfoGetCertStatusResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::SslInfoGetX509CertificateRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::SslInfoGetX509CertificateRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::sslinfo.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_x509_certificate(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::SslInfoGetX509CertificateResponse resp;
                    resp.result = insertOrRelease(&tables::x509Certificate, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::UnresponsiveProcessCallbackWaitRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::UnresponsiveProcessCallbackWaitRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::unresponsiveProcessCallback.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->wait(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::UnresponsiveProcessCallbackTerminateRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::UnresponsiveProcessCallbackTerminateRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::unresponsiveProcessCallback.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->terminate(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::SelectClientCertificateCallbackSelectRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::SelectClientCertificateCallbackSelectRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::selectClientCertificateCallback.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t cert = req.cert;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, cert]() {
                cef_x509_certificate_t* cert_ptr = cert != 0 ? tables::x509Certificate.retain(cert) : nullptr;
                receiver->select(receiver, cert_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (cert_ptr) {
                    auto* cert_base = reinterpret_cast<cef_base_ref_counted_t*>(cert_ptr);
                    cert_base->release(cert_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::CommandLineIsValidRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::CommandLineIsValidRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::commandLine.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_valid(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::CommandLineIsValidResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::CommandLineIsReadOnlyRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::CommandLineIsReadOnlyRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::commandLine.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_read_only(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::CommandLineIsReadOnlyResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::CommandLineCopyRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::CommandLineCopyRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::commandLine.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->copy(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::CommandLineCopyResponse resp;
                    resp.result = insertOrRelease(&tables::commandLine, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::CommandLineInitFromStringRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::CommandLineInitFromStringRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::commandLine.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string commandLine = req.commandLine;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, commandLine]() {
                ScopedCefString commandLine_cef(commandLine);
                receiver->init_from_string(receiver, commandLine_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::CommandLineResetRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::CommandLineResetRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::commandLine.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->reset(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::CommandLineGetArgvRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::CommandLineGetArgvRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::commandLine.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::vector<std::string> argv = std::move(req.argv);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, argv]() {
                cef_string_list_t argv_list = cef_string_list_alloc();
                for (const auto& __s : argv) {
                    cef_string_t __cs{};
                    if (!__s.empty()) cef_string_utf8_to_utf16(__s.data(), __s.size(), &__cs);
                    cef_string_list_append(argv_list, &__cs);
                    cef_string_clear(&__cs);
                }
                receiver->get_argv(receiver, argv_list);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                cef_string_list_free(argv_list);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::CommandLineGetCommandLineStringRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::CommandLineGetCommandLineStringRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::commandLine.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_command_line_string(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::CommandLineGetCommandLineStringResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::CommandLineGetProgramRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::CommandLineGetProgramRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::commandLine.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_program(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::CommandLineGetProgramResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::CommandLineSetProgramRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::CommandLineSetProgramRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::commandLine.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string program = req.program;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, program]() {
                ScopedCefString program_cef(program);
                receiver->set_program(receiver, program_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::CommandLineHasSwitchesRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::CommandLineHasSwitchesRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::commandLine.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->has_switches(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::CommandLineHasSwitchesResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::CommandLineHasSwitchRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::CommandLineHasSwitchRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::commandLine.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string name = req.name;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, name]() {
                ScopedCefString name_cef(name);
                auto rawResult = receiver->has_switch(receiver, name_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::CommandLineHasSwitchResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::CommandLineGetSwitchValueRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::CommandLineGetSwitchValueRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::commandLine.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string name = req.name;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, name]() {
                ScopedCefString name_cef(name);
                auto rawResult = receiver->get_switch_value(receiver, name_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::CommandLineGetSwitchValueResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::CommandLineAppendSwitchRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::CommandLineAppendSwitchRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::commandLine.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string name = req.name;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, name]() {
                ScopedCefString name_cef(name);
                receiver->append_switch(receiver, name_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::CommandLineAppendSwitchWithValueRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::CommandLineAppendSwitchWithValueRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::commandLine.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string name = req.name;
            std::string value = req.value;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, name, value]() {
                ScopedCefString name_cef(name);
                ScopedCefString value_cef(value);
                receiver->append_switch_with_value(receiver, name_cef.get(), value_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::CommandLineHasArgumentsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::CommandLineHasArgumentsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::commandLine.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->has_arguments(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::CommandLineHasArgumentsResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::CommandLineGetArgumentsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::CommandLineGetArgumentsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::commandLine.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::vector<std::string> arguments = std::move(req.arguments);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, arguments]() {
                cef_string_list_t arguments_list = cef_string_list_alloc();
                for (const auto& __s : arguments) {
                    cef_string_t __cs{};
                    if (!__s.empty()) cef_string_utf8_to_utf16(__s.data(), __s.size(), &__cs);
                    cef_string_list_append(arguments_list, &__cs);
                    cef_string_clear(&__cs);
                }
                receiver->get_arguments(receiver, arguments_list);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                cef_string_list_free(arguments_list);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::CommandLineAppendArgumentRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::CommandLineAppendArgumentRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::commandLine.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string argument = req.argument;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, argument]() {
                ScopedCefString argument_cef(argument);
                receiver->append_argument(receiver, argument_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::CommandLinePrependWrapperRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::CommandLinePrependWrapperRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::commandLine.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string wrapper = req.wrapper;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, wrapper]() {
                ScopedCefString wrapper_cef(wrapper);
                receiver->prepend_wrapper(receiver, wrapper_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::CommandLineRemoveSwitchRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::CommandLineRemoveSwitchRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::commandLine.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string name = req.name;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, name]() {
                ScopedCefString name_cef(name);
                receiver->remove_switch(receiver, name_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TaskRunnerIsSameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TaskRunnerIsSameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::taskRunner.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t that = req.that;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, that]() {
                cef_task_runner_t* that_ptr = that != 0 ? tables::taskRunner.retain(that) : nullptr;
                auto rawResult = receiver->is_same(receiver, that_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (that_ptr) {
                    auto* that_base = reinterpret_cast<cef_base_ref_counted_t*>(that_ptr);
                    that_base->release(that_base);
                }
                if (ipc) {
                    gen::TaskRunnerIsSameResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TaskRunnerBelongsToCurrentThreadRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TaskRunnerBelongsToCurrentThreadRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::taskRunner.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->belongs_to_current_thread(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::TaskRunnerBelongsToCurrentThreadResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TaskRunnerBelongsToThreadRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TaskRunnerBelongsToThreadRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::taskRunner.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t threadId = req.threadId;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, threadId]() {
                auto rawResult = receiver->belongs_to_thread(receiver, cefArg<decltype(receiver->belongs_to_thread), 1>(threadId));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::TaskRunnerBelongsToThreadResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TaskRunnerPostTaskRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TaskRunnerPostTaskRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::taskRunner.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t task = req.task;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, task]() {
                cef_task_t* task_ptr = task != 0 ?
                        reinterpret_cast<cef_task_t*>(new genvisitors::JvmTask(task, ipc)) :
                        nullptr;
                auto rawResult = receiver->post_task(receiver, task_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::TaskRunnerPostTaskResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TaskRunnerPostDelayedTaskRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TaskRunnerPostDelayedTaskRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::taskRunner.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t task = req.task;
            std::int64_t delayMs = req.delayMs;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, task, delayMs]() {
                cef_task_t* task_ptr = task != 0 ?
                        reinterpret_cast<cef_task_t*>(new genvisitors::JvmTask(task, ipc)) :
                        nullptr;
                auto rawResult = receiver->post_delayed_task(receiver, task_ptr, cefArg<decltype(receiver->post_delayed_task), 2>(delayMs));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::TaskRunnerPostDelayedTaskResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::SchemeRegistrarAddCustomSchemeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::SchemeRegistrarAddCustomSchemeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::schemeRegistrar.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string schemeName = req.schemeName;
            std::int32_t options = req.options;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, schemeName, options]() {
                ScopedCefString schemeName_cef(schemeName);
                auto rawResult = receiver->add_custom_scheme(receiver, schemeName_cef.get(), cefArg<decltype(receiver->add_custom_scheme), 2>(options));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::SchemeRegistrarAddCustomSchemeResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ComponentGetIdRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ComponentGetIdRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::component.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_id(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ComponentGetIdResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ComponentGetNameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ComponentGetNameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::component.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_name(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ComponentGetNameResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ComponentGetVersionRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ComponentGetVersionRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::component.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_version(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ComponentGetVersionResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ComponentGetStateRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ComponentGetStateRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::component.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_state(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ComponentGetStateResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ComponentUpdaterGetComponentCountRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ComponentUpdaterGetComponentCountRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::componentUpdater.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_component_count(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ComponentUpdaterGetComponentCountResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ComponentUpdaterGetComponentByIdRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ComponentUpdaterGetComponentByIdRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::componentUpdater.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string componentId = req.componentId;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, componentId]() {
                ScopedCefString componentId_cef(componentId);
                auto rawResult = receiver->get_component_by_id(receiver, componentId_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ComponentUpdaterGetComponentByIdResponse resp;
                    resp.result = insertOrRelease(&tables::component, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ComponentUpdaterUpdateRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ComponentUpdaterUpdateRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::componentUpdater.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string componentId = req.componentId;
            std::int32_t priority = req.priority;
            std::int32_t callback = req.callback;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, componentId, priority, callback]() {
                ScopedCefString componentId_cef(componentId);
                cef_component_update_callback_t* callback_ptr = callback != 0 ?
                        reinterpret_cast<cef_component_update_callback_t*>(new genvisitors::JvmComponentUpdateCallback(callback, ipc)) :
                        nullptr;
                receiver->update(receiver, componentId_cef.get(), cefArg<decltype(receiver->update), 2>(priority), callback_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ResourceBundleGetLocalizedStringRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ResourceBundleGetLocalizedStringRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::resourceBundle.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t stringId = req.stringId;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, stringId]() {
                auto rawResult = receiver->get_localized_string(receiver, cefArg<decltype(receiver->get_localized_string), 1>(stringId));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ResourceBundleGetLocalizedStringResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ResourceBundleGetDataResourceRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ResourceBundleGetDataResourceRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::resourceBundle.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t resourceId = req.resourceId;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, resourceId]() {
                auto rawResult = receiver->get_data_resource(receiver, cefArg<decltype(receiver->get_data_resource), 1>(resourceId));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ResourceBundleGetDataResourceResponse resp;
                    resp.result = insertOrRelease(&tables::binaryValue, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ResourceBundleGetDataResourceForScaleRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ResourceBundleGetDataResourceForScaleRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::resourceBundle.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t resourceId = req.resourceId;
            std::int32_t scaleFactor = req.scaleFactor;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, resourceId, scaleFactor]() {
                auto rawResult = receiver->get_data_resource_for_scale(receiver, cefArg<decltype(receiver->get_data_resource_for_scale), 1>(resourceId), cefArg<decltype(receiver->get_data_resource_for_scale), 2>(scaleFactor));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ResourceBundleGetDataResourceForScaleResponse resp;
                    resp.result = insertOrRelease(&tables::binaryValue, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ServerGetTaskRunnerRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ServerGetTaskRunnerRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::server.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_task_runner(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ServerGetTaskRunnerResponse resp;
                    resp.result = insertOrRelease(&tables::taskRunner, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ServerShutdownRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ServerShutdownRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::server.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->shutdown(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ServerIsRunningRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ServerIsRunningRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::server.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_running(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ServerIsRunningResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ServerGetAddressRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ServerGetAddressRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::server.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_address(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ServerGetAddressResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ServerHasConnectionRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ServerHasConnectionRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::server.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->has_connection(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ServerHasConnectionResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ServerIsValidConnectionRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ServerIsValidConnectionRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::server.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t connectionId = req.connectionId;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, connectionId]() {
                auto rawResult = receiver->is_valid_connection(receiver, cefArg<decltype(receiver->is_valid_connection), 1>(connectionId));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ServerIsValidConnectionResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ServerSendHttp200ResponseRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ServerSendHttp200ResponseRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::server.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t connectionId = req.connectionId;
            std::string contentType = req.contentType;
            std::vector<std::uint8_t> data = std::move(req.data);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, connectionId, contentType, data]() {
                ScopedCefString contentType_cef(contentType);
                receiver->send_http200_response(receiver, cefArg<decltype(receiver->send_http200_response), 1>(connectionId), contentType_cef.get(), data.data(), static_cast<size_t>(data.size()));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ServerSendHttp404ResponseRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ServerSendHttp404ResponseRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::server.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t connectionId = req.connectionId;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, connectionId]() {
                receiver->send_http404_response(receiver, cefArg<decltype(receiver->send_http404_response), 1>(connectionId));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ServerSendHttp500ResponseRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ServerSendHttp500ResponseRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::server.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t connectionId = req.connectionId;
            std::string errorMessage = req.errorMessage;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, connectionId, errorMessage]() {
                ScopedCefString errorMessage_cef(errorMessage);
                receiver->send_http500_response(receiver, cefArg<decltype(receiver->send_http500_response), 1>(connectionId), errorMessage_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ServerSendRawDataRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ServerSendRawDataRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::server.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t connectionId = req.connectionId;
            std::vector<std::uint8_t> data = std::move(req.data);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, connectionId, data]() {
                receiver->send_raw_data(receiver, cefArg<decltype(receiver->send_raw_data), 1>(connectionId), data.data(), static_cast<size_t>(data.size()));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ServerCloseConnectionRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ServerCloseConnectionRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::server.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t connectionId = req.connectionId;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, connectionId]() {
                receiver->close_connection(receiver, cefArg<decltype(receiver->close_connection), 1>(connectionId));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ServerSendWebSocketMessageRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ServerSendWebSocketMessageRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::server.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t connectionId = req.connectionId;
            std::vector<std::uint8_t> data = std::move(req.data);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, connectionId, data]() {
                receiver->send_web_socket_message(receiver, cefArg<decltype(receiver->send_web_socket_message), 1>(connectionId), data.data(), static_cast<size_t>(data.size()));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::SharedProcessMessageBuilderIsValidRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::SharedProcessMessageBuilderIsValidRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::sharedProcessMessageBuilder.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_valid(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::SharedProcessMessageBuilderIsValidResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::SharedProcessMessageBuilderSizeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::SharedProcessMessageBuilderSizeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::sharedProcessMessageBuilder.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->size(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::SharedProcessMessageBuilderSizeResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::SharedProcessMessageBuilderBuildRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::SharedProcessMessageBuilderBuildRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::sharedProcessMessageBuilder.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->build(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::SharedProcessMessageBuilderBuildResponse resp;
                    resp.result = insertOrRelease(&tables::processMessage, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TaskManagerGetTasksCountRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TaskManagerGetTasksCountRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::taskManager.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_tasks_count(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::TaskManagerGetTasksCountResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TaskManagerKillTaskRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TaskManagerKillTaskRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::taskManager.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t taskId = req.taskId;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, taskId]() {
                auto rawResult = receiver->kill_task(receiver, cefArg<decltype(receiver->kill_task), 1>(taskId));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::TaskManagerKillTaskResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TaskManagerGetTaskIdForBrowserIdRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TaskManagerGetTaskIdForBrowserIdRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::taskManager.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t browserId = req.browserId;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, browserId]() {
                auto rawResult = receiver->get_task_id_for_browser_id(receiver, cefArg<decltype(receiver->get_task_id_for_browser_id), 1>(browserId));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::TaskManagerGetTaskIdForBrowserIdResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ThreadGetTaskRunnerRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ThreadGetTaskRunnerRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::thread.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_task_runner(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ThreadGetTaskRunnerResponse resp;
                    resp.result = insertOrRelease(&tables::taskRunner, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ThreadGetPlatformThreadIdRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ThreadGetPlatformThreadIdRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::thread.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_platform_thread_id(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ThreadGetPlatformThreadIdResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ThreadStopRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ThreadStopRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::thread.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->stop(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ThreadIsRunningRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ThreadIsRunningRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::thread.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_running(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ThreadIsRunningResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::UrlRequestGetRequestRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::UrlRequestGetRequestRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::urlrequest.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_request(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::UrlRequestGetRequestResponse resp;
                    resp.result = insertOrRelease(&tables::request, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::UrlRequestGetRequestStatusRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::UrlRequestGetRequestStatusRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::urlrequest.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_request_status(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::UrlRequestGetRequestStatusResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::UrlRequestGetRequestErrorRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::UrlRequestGetRequestErrorRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::urlrequest.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_request_error(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::UrlRequestGetRequestErrorResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::UrlRequestGetResponseRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::UrlRequestGetResponseRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::urlrequest.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_response(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::UrlRequestGetResponseResponse resp;
                    resp.result = insertOrRelease(&tables::response, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::UrlRequestResponseWasCachedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::UrlRequestResponseWasCachedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::urlrequest.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->response_was_cached(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::UrlRequestResponseWasCachedResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::UrlRequestCancelRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::UrlRequestCancelRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::urlrequest.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->cancel(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WaitableEventResetRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WaitableEventResetRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::waitableEvent.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->reset(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WaitableEventSignalRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WaitableEventSignalRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::waitableEvent.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->signal(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WaitableEventIsSignaledRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WaitableEventIsSignaledRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::waitableEvent.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_signaled(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::WaitableEventIsSignaledResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WaitableEventWaitRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WaitableEventWaitRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::waitableEvent.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->wait(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WaitableEventTimedWaitRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WaitableEventTimedWaitRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::waitableEvent.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int64_t maxMs = req.maxMs;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, maxMs]() {
                auto rawResult = receiver->timed_wait(receiver, cefArg<decltype(receiver->timed_wait), 1>(maxMs));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::WaitableEventTimedWaitResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::XmlReaderMoveToNextNodeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::XmlReaderMoveToNextNodeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::xmlReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->move_to_next_node(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::XmlReaderMoveToNextNodeResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::XmlReaderCloseRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::XmlReaderCloseRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::xmlReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->close(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::XmlReaderCloseResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::XmlReaderHasErrorRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::XmlReaderHasErrorRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::xmlReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->has_error(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::XmlReaderHasErrorResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::XmlReaderGetErrorRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::XmlReaderGetErrorRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::xmlReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_error(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::XmlReaderGetErrorResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::XmlReaderGetTypeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::XmlReaderGetTypeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::xmlReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_type(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::XmlReaderGetTypeResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::XmlReaderGetDepthRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::XmlReaderGetDepthRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::xmlReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_depth(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::XmlReaderGetDepthResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::XmlReaderGetLocalNameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::XmlReaderGetLocalNameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::xmlReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_local_name(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::XmlReaderGetLocalNameResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::XmlReaderGetPrefixRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::XmlReaderGetPrefixRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::xmlReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_prefix(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::XmlReaderGetPrefixResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::XmlReaderGetQualifiedNameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::XmlReaderGetQualifiedNameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::xmlReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_qualified_name(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::XmlReaderGetQualifiedNameResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::XmlReaderGetNamespaceUriRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::XmlReaderGetNamespaceUriRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::xmlReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_namespace_uri(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::XmlReaderGetNamespaceUriResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::XmlReaderGetBaseUriRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::XmlReaderGetBaseUriRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::xmlReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_base_uri(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::XmlReaderGetBaseUriResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::XmlReaderGetXmlLangRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::XmlReaderGetXmlLangRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::xmlReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_xml_lang(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::XmlReaderGetXmlLangResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::XmlReaderIsEmptyElementRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::XmlReaderIsEmptyElementRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::xmlReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_empty_element(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::XmlReaderIsEmptyElementResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::XmlReaderHasValueRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::XmlReaderHasValueRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::xmlReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->has_value(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::XmlReaderHasValueResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::XmlReaderGetValueRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::XmlReaderGetValueRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::xmlReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_value(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::XmlReaderGetValueResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::XmlReaderHasAttributesRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::XmlReaderHasAttributesRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::xmlReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->has_attributes(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::XmlReaderHasAttributesResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::XmlReaderGetAttributeCountRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::XmlReaderGetAttributeCountRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::xmlReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_attribute_count(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::XmlReaderGetAttributeCountResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::XmlReaderGetAttributeByindexRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::XmlReaderGetAttributeByindexRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::xmlReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t index = req.index;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index]() {
                auto rawResult = receiver->get_attribute_byindex(receiver, cefArg<decltype(receiver->get_attribute_byindex), 1>(index));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::XmlReaderGetAttributeByindexResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::XmlReaderGetAttributeByqnameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::XmlReaderGetAttributeByqnameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::xmlReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string qualifiedName = req.qualifiedName;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, qualifiedName]() {
                ScopedCefString qualifiedName_cef(qualifiedName);
                auto rawResult = receiver->get_attribute_byqname(receiver, qualifiedName_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::XmlReaderGetAttributeByqnameResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::XmlReaderGetAttributeBylnameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::XmlReaderGetAttributeBylnameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::xmlReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string localName = req.localName;
            std::string namespaceURI = req.namespaceURI;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, localName, namespaceURI]() {
                ScopedCefString localName_cef(localName);
                ScopedCefString namespaceURI_cef(namespaceURI);
                auto rawResult = receiver->get_attribute_bylname(receiver, localName_cef.get(), namespaceURI_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::XmlReaderGetAttributeBylnameResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::XmlReaderGetInnerXmlRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::XmlReaderGetInnerXmlRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::xmlReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_inner_xml(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::XmlReaderGetInnerXmlResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::XmlReaderGetOuterXmlRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::XmlReaderGetOuterXmlRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::xmlReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_outer_xml(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::XmlReaderGetOuterXmlResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::XmlReaderGetLineNumberRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::XmlReaderGetLineNumberRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::xmlReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_line_number(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::XmlReaderGetLineNumberResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::XmlReaderMoveToAttributeByindexRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::XmlReaderMoveToAttributeByindexRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::xmlReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t index = req.index;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index]() {
                auto rawResult = receiver->move_to_attribute_byindex(receiver, cefArg<decltype(receiver->move_to_attribute_byindex), 1>(index));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::XmlReaderMoveToAttributeByindexResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::XmlReaderMoveToAttributeByqnameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::XmlReaderMoveToAttributeByqnameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::xmlReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string qualifiedName = req.qualifiedName;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, qualifiedName]() {
                ScopedCefString qualifiedName_cef(qualifiedName);
                auto rawResult = receiver->move_to_attribute_byqname(receiver, qualifiedName_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::XmlReaderMoveToAttributeByqnameResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::XmlReaderMoveToAttributeBylnameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::XmlReaderMoveToAttributeBylnameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::xmlReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string localName = req.localName;
            std::string namespaceURI = req.namespaceURI;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, localName, namespaceURI]() {
                ScopedCefString localName_cef(localName);
                ScopedCefString namespaceURI_cef(namespaceURI);
                auto rawResult = receiver->move_to_attribute_bylname(receiver, localName_cef.get(), namespaceURI_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::XmlReaderMoveToAttributeBylnameResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::XmlReaderMoveToFirstAttributeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::XmlReaderMoveToFirstAttributeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::xmlReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->move_to_first_attribute(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::XmlReaderMoveToFirstAttributeResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::XmlReaderMoveToNextAttributeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::XmlReaderMoveToNextAttributeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::xmlReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->move_to_next_attribute(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::XmlReaderMoveToNextAttributeResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::XmlReaderMoveToCarryingElementRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::XmlReaderMoveToCarryingElementRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::xmlReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->move_to_carrying_element(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::XmlReaderMoveToCarryingElementResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ZipReaderMoveToFirstFileRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ZipReaderMoveToFirstFileRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::zipReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->move_to_first_file(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ZipReaderMoveToFirstFileResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ZipReaderMoveToNextFileRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ZipReaderMoveToNextFileRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::zipReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->move_to_next_file(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ZipReaderMoveToNextFileResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ZipReaderMoveToFileRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ZipReaderMoveToFileRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::zipReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string fileName = req.fileName;
            std::int32_t caseSensitive = req.caseSensitive;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, fileName, caseSensitive]() {
                ScopedCefString fileName_cef(fileName);
                auto rawResult = receiver->move_to_file(receiver, fileName_cef.get(), cefArg<decltype(receiver->move_to_file), 2>(caseSensitive));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ZipReaderMoveToFileResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ZipReaderCloseRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ZipReaderCloseRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::zipReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->close(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ZipReaderCloseResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ZipReaderGetFileNameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ZipReaderGetFileNameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::zipReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_file_name(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ZipReaderGetFileNameResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ZipReaderGetFileSizeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ZipReaderGetFileSizeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::zipReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_file_size(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ZipReaderGetFileSizeResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ZipReaderGetFileLastModifiedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ZipReaderGetFileLastModifiedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::zipReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                cef_basetime_t rawResult = receiver->get_file_last_modified(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ZipReaderGetFileLastModifiedResponse resp;
                resp.result.val = static_cast<decltype(resp.result.val)>(rawResult.val);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ZipReaderOpenFileRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ZipReaderOpenFileRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::zipReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string password = req.password;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, password]() {
                ScopedCefString password_cef(password);
                auto rawResult = receiver->open_file(receiver, password_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ZipReaderOpenFileResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ZipReaderCloseFileRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ZipReaderCloseFileRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::zipReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->close_file(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ZipReaderCloseFileResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ZipReaderTellRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ZipReaderTellRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::zipReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->tell(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ZipReaderTellResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ZipReaderEofRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ZipReaderEofRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::zipReader.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->eof(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ZipReaderEofResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::LayoutAsBoxLayoutRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::LayoutAsBoxLayoutRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::layout.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->as_box_layout(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::LayoutAsBoxLayoutResponse resp;
                    resp.result = insertOrRelease(&tables::boxLayout, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::LayoutIsValidRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::LayoutIsValidRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::layout.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_valid(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::LayoutIsValidResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BoxLayoutSetFlexForViewRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BoxLayoutSetFlexForViewRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::boxLayout.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t view = req.view;
            std::int32_t flex = req.flex;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, view, flex]() {
                cef_view_t* view_ptr = view != 0 ? tables::view.retain(view) : nullptr;
                receiver->set_flex_for_view(receiver, view_ptr, cefArg<decltype(receiver->set_flex_for_view), 2>(flex));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (view_ptr) {
                    auto* view_base = reinterpret_cast<cef_base_ref_counted_t*>(view_ptr);
                    view_base->release(view_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BoxLayoutClearFlexForViewRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BoxLayoutClearFlexForViewRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::boxLayout.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t view = req.view;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, view]() {
                cef_view_t* view_ptr = view != 0 ? tables::view.retain(view) : nullptr;
                receiver->clear_flex_for_view(receiver, view_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (view_ptr) {
                    auto* view_base = reinterpret_cast<cef_base_ref_counted_t*>(view_ptr);
                    view_base->release(view_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewDelegateGetPreferredSizeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewDelegateGetPreferredSizeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::viewDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t view = req.view;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, view]() {
                cef_view_t* view_ptr = view != 0 ? tables::view.retain(view) : nullptr;
                cef_size_t rawResult = receiver->get_preferred_size(receiver, view_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (view_ptr) {
                    auto* view_base = reinterpret_cast<cef_base_ref_counted_t*>(view_ptr);
                    view_base->release(view_base);
                }
                if (ipc) {
                    gen::ViewDelegateGetPreferredSizeResponse resp;
                resp.result.width = static_cast<decltype(resp.result.width)>(rawResult.width);
                resp.result.height = static_cast<decltype(resp.result.height)>(rawResult.height);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewDelegateGetMinimumSizeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewDelegateGetMinimumSizeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::viewDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t view = req.view;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, view]() {
                cef_view_t* view_ptr = view != 0 ? tables::view.retain(view) : nullptr;
                cef_size_t rawResult = receiver->get_minimum_size(receiver, view_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (view_ptr) {
                    auto* view_base = reinterpret_cast<cef_base_ref_counted_t*>(view_ptr);
                    view_base->release(view_base);
                }
                if (ipc) {
                    gen::ViewDelegateGetMinimumSizeResponse resp;
                resp.result.width = static_cast<decltype(resp.result.width)>(rawResult.width);
                resp.result.height = static_cast<decltype(resp.result.height)>(rawResult.height);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewDelegateGetMaximumSizeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewDelegateGetMaximumSizeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::viewDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t view = req.view;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, view]() {
                cef_view_t* view_ptr = view != 0 ? tables::view.retain(view) : nullptr;
                cef_size_t rawResult = receiver->get_maximum_size(receiver, view_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (view_ptr) {
                    auto* view_base = reinterpret_cast<cef_base_ref_counted_t*>(view_ptr);
                    view_base->release(view_base);
                }
                if (ipc) {
                    gen::ViewDelegateGetMaximumSizeResponse resp;
                resp.result.width = static_cast<decltype(resp.result.width)>(rawResult.width);
                resp.result.height = static_cast<decltype(resp.result.height)>(rawResult.height);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewDelegateGetHeightForWidthRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewDelegateGetHeightForWidthRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::viewDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t view = req.view;
            std::int32_t width = req.width;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, view, width]() {
                cef_view_t* view_ptr = view != 0 ? tables::view.retain(view) : nullptr;
                auto rawResult = receiver->get_height_for_width(receiver, view_ptr, cefArg<decltype(receiver->get_height_for_width), 2>(width));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (view_ptr) {
                    auto* view_base = reinterpret_cast<cef_base_ref_counted_t*>(view_ptr);
                    view_base->release(view_base);
                }
                if (ipc) {
                    gen::ViewDelegateGetHeightForWidthResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewDelegateOnParentViewChangedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewDelegateOnParentViewChangedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::viewDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t view = req.view;
            std::int32_t added = req.added;
            std::int32_t parent = req.parent;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, view, added, parent]() {
                cef_view_t* view_ptr = view != 0 ? tables::view.retain(view) : nullptr;
                cef_view_t* parent_ptr = parent != 0 ? tables::view.retain(parent) : nullptr;
                receiver->on_parent_view_changed(receiver, view_ptr, cefArg<decltype(receiver->on_parent_view_changed), 2>(added), parent_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (view_ptr) {
                    auto* view_base = reinterpret_cast<cef_base_ref_counted_t*>(view_ptr);
                    view_base->release(view_base);
                }
                if (parent_ptr) {
                    auto* parent_base = reinterpret_cast<cef_base_ref_counted_t*>(parent_ptr);
                    parent_base->release(parent_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewDelegateOnChildViewChangedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewDelegateOnChildViewChangedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::viewDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t view = req.view;
            std::int32_t added = req.added;
            std::int32_t child = req.child;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, view, added, child]() {
                cef_view_t* view_ptr = view != 0 ? tables::view.retain(view) : nullptr;
                cef_view_t* child_ptr = child != 0 ? tables::view.retain(child) : nullptr;
                receiver->on_child_view_changed(receiver, view_ptr, cefArg<decltype(receiver->on_child_view_changed), 2>(added), child_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (view_ptr) {
                    auto* view_base = reinterpret_cast<cef_base_ref_counted_t*>(view_ptr);
                    view_base->release(view_base);
                }
                if (child_ptr) {
                    auto* child_base = reinterpret_cast<cef_base_ref_counted_t*>(child_ptr);
                    child_base->release(child_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewDelegateOnWindowChangedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewDelegateOnWindowChangedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::viewDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t view = req.view;
            std::int32_t added = req.added;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, view, added]() {
                cef_view_t* view_ptr = view != 0 ? tables::view.retain(view) : nullptr;
                receiver->on_window_changed(receiver, view_ptr, cefArg<decltype(receiver->on_window_changed), 2>(added));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (view_ptr) {
                    auto* view_base = reinterpret_cast<cef_base_ref_counted_t*>(view_ptr);
                    view_base->release(view_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewDelegateOnLayoutChangedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewDelegateOnLayoutChangedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::viewDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t view = req.view;
            gen::Rect newBounds = std::move(req.newBounds);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, view, newBounds]() {
                cef_view_t* view_ptr = view != 0 ? tables::view.retain(view) : nullptr;
                cef_rect_t newBounds_native{};
                newBounds_native.x = static_cast<decltype(newBounds_native.x)>(newBounds.x);
                newBounds_native.y = static_cast<decltype(newBounds_native.y)>(newBounds.y);
                newBounds_native.width = static_cast<decltype(newBounds_native.width)>(newBounds.width);
                newBounds_native.height = static_cast<decltype(newBounds_native.height)>(newBounds.height);
                receiver->on_layout_changed(receiver, view_ptr, &newBounds_native);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (view_ptr) {
                    auto* view_base = reinterpret_cast<cef_base_ref_counted_t*>(view_ptr);
                    view_base->release(view_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewDelegateOnFocusRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewDelegateOnFocusRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::viewDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t view = req.view;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, view]() {
                cef_view_t* view_ptr = view != 0 ? tables::view.retain(view) : nullptr;
                receiver->on_focus(receiver, view_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (view_ptr) {
                    auto* view_base = reinterpret_cast<cef_base_ref_counted_t*>(view_ptr);
                    view_base->release(view_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewDelegateOnBlurRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewDelegateOnBlurRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::viewDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t view = req.view;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, view]() {
                cef_view_t* view_ptr = view != 0 ? tables::view.retain(view) : nullptr;
                receiver->on_blur(receiver, view_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (view_ptr) {
                    auto* view_base = reinterpret_cast<cef_base_ref_counted_t*>(view_ptr);
                    view_base->release(view_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewDelegateOnThemeChangedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewDelegateOnThemeChangedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::viewDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t view = req.view;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, view]() {
                cef_view_t* view_ptr = view != 0 ? tables::view.retain(view) : nullptr;
                receiver->on_theme_changed(receiver, view_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (view_ptr) {
                    auto* view_base = reinterpret_cast<cef_base_ref_counted_t*>(view_ptr);
                    view_base->release(view_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserViewDelegateOnBrowserCreatedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserViewDelegateOnBrowserCreatedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserViewDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t browserView = req.browserView;
            std::int32_t browser = req.browser;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, browserView, browser]() {
                cef_browser_view_t* browserView_ptr = browserView != 0 ? tables::browserView.retain(browserView) : nullptr;
                cef_browser_t* browser_ptr = browser != 0 ? tables::browser.retain(browser) : nullptr;
                receiver->on_browser_created(receiver, browserView_ptr, browser_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (browserView_ptr) {
                    auto* browserView_base = reinterpret_cast<cef_base_ref_counted_t*>(browserView_ptr);
                    browserView_base->release(browserView_base);
                }
                if (browser_ptr) {
                    auto* browser_base = reinterpret_cast<cef_base_ref_counted_t*>(browser_ptr);
                    browser_base->release(browser_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserViewDelegateOnBrowserDestroyedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserViewDelegateOnBrowserDestroyedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserViewDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t browserView = req.browserView;
            std::int32_t browser = req.browser;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, browserView, browser]() {
                cef_browser_view_t* browserView_ptr = browserView != 0 ? tables::browserView.retain(browserView) : nullptr;
                cef_browser_t* browser_ptr = browser != 0 ? tables::browser.retain(browser) : nullptr;
                receiver->on_browser_destroyed(receiver, browserView_ptr, browser_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (browserView_ptr) {
                    auto* browserView_base = reinterpret_cast<cef_base_ref_counted_t*>(browserView_ptr);
                    browserView_base->release(browserView_base);
                }
                if (browser_ptr) {
                    auto* browser_base = reinterpret_cast<cef_base_ref_counted_t*>(browser_ptr);
                    browser_base->release(browser_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserViewDelegateOnPopupBrowserViewCreatedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserViewDelegateOnPopupBrowserViewCreatedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserViewDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t browserView = req.browserView;
            std::int32_t popupBrowserView = req.popupBrowserView;
            std::int32_t isDevtools = req.isDevtools;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, browserView, popupBrowserView, isDevtools]() {
                cef_browser_view_t* browserView_ptr = browserView != 0 ? tables::browserView.retain(browserView) : nullptr;
                cef_browser_view_t* popupBrowserView_ptr = popupBrowserView != 0 ? tables::browserView.retain(popupBrowserView) : nullptr;
                auto rawResult = receiver->on_popup_browser_view_created(receiver, browserView_ptr, popupBrowserView_ptr, cefArg<decltype(receiver->on_popup_browser_view_created), 3>(isDevtools));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (browserView_ptr) {
                    auto* browserView_base = reinterpret_cast<cef_base_ref_counted_t*>(browserView_ptr);
                    browserView_base->release(browserView_base);
                }
                if (popupBrowserView_ptr) {
                    auto* popupBrowserView_base = reinterpret_cast<cef_base_ref_counted_t*>(popupBrowserView_ptr);
                    popupBrowserView_base->release(popupBrowserView_base);
                }
                if (ipc) {
                    gen::BrowserViewDelegateOnPopupBrowserViewCreatedResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserViewDelegateGetChromeToolbarTypeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserViewDelegateGetChromeToolbarTypeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserViewDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t browserView = req.browserView;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, browserView]() {
                cef_browser_view_t* browserView_ptr = browserView != 0 ? tables::browserView.retain(browserView) : nullptr;
                auto rawResult = receiver->get_chrome_toolbar_type(receiver, browserView_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (browserView_ptr) {
                    auto* browserView_base = reinterpret_cast<cef_base_ref_counted_t*>(browserView_ptr);
                    browserView_base->release(browserView_base);
                }
                if (ipc) {
                    gen::BrowserViewDelegateGetChromeToolbarTypeResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserViewDelegateUseFramelessWindowForPictureInPictureRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserViewDelegateUseFramelessWindowForPictureInPictureRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserViewDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t browserView = req.browserView;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, browserView]() {
                cef_browser_view_t* browserView_ptr = browserView != 0 ? tables::browserView.retain(browserView) : nullptr;
                auto rawResult = receiver->use_frameless_window_for_picture_in_picture(receiver, browserView_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (browserView_ptr) {
                    auto* browserView_base = reinterpret_cast<cef_base_ref_counted_t*>(browserView_ptr);
                    browserView_base->release(browserView_base);
                }
                if (ipc) {
                    gen::BrowserViewDelegateUseFramelessWindowForPictureInPictureResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserViewDelegateOnGestureCommandRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserViewDelegateOnGestureCommandRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserViewDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t browserView = req.browserView;
            std::int32_t gestureCommand = req.gestureCommand;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, browserView, gestureCommand]() {
                cef_browser_view_t* browserView_ptr = browserView != 0 ? tables::browserView.retain(browserView) : nullptr;
                auto rawResult = receiver->on_gesture_command(receiver, browserView_ptr, cefArg<decltype(receiver->on_gesture_command), 2>(gestureCommand));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (browserView_ptr) {
                    auto* browserView_base = reinterpret_cast<cef_base_ref_counted_t*>(browserView_ptr);
                    browserView_base->release(browserView_base);
                }
                if (ipc) {
                    gen::BrowserViewDelegateOnGestureCommandResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserViewDelegateGetBrowserRuntimeStyleRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserViewDelegateGetBrowserRuntimeStyleRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserViewDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_browser_runtime_style(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserViewDelegateGetBrowserRuntimeStyleResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserViewDelegateAllowMoveForPictureInPictureRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserViewDelegateAllowMoveForPictureInPictureRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserViewDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t browserView = req.browserView;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, browserView]() {
                cef_browser_view_t* browserView_ptr = browserView != 0 ? tables::browserView.retain(browserView) : nullptr;
                auto rawResult = receiver->allow_move_for_picture_in_picture(receiver, browserView_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (browserView_ptr) {
                    auto* browserView_base = reinterpret_cast<cef_base_ref_counted_t*>(browserView_ptr);
                    browserView_base->release(browserView_base);
                }
                if (ipc) {
                    gen::BrowserViewDelegateAllowMoveForPictureInPictureResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserViewDelegateAllowPictureInPictureWithoutUserActivationRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserViewDelegateAllowPictureInPictureWithoutUserActivationRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserViewDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t browserView = req.browserView;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, browserView]() {
                cef_browser_view_t* browserView_ptr = browserView != 0 ? tables::browserView.retain(browserView) : nullptr;
                auto rawResult = receiver->allow_picture_in_picture_without_user_activation(receiver, browserView_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (browserView_ptr) {
                    auto* browserView_base = reinterpret_cast<cef_base_ref_counted_t*>(browserView_ptr);
                    browserView_base->release(browserView_base);
                }
                if (ipc) {
                    gen::BrowserViewDelegateAllowPictureInPictureWithoutUserActivationResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewAsBrowserViewRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewAsBrowserViewRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->as_browser_view(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ViewAsBrowserViewResponse resp;
                    resp.result = insertOrRelease(&tables::browserView, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewAsButtonRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewAsButtonRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->as_button(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ViewAsButtonResponse resp;
                    resp.result = insertOrRelease(&tables::button, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewAsPanelRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewAsPanelRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->as_panel(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ViewAsPanelResponse resp;
                    resp.result = insertOrRelease(&tables::panel, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewAsScrollViewRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewAsScrollViewRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->as_scroll_view(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ViewAsScrollViewResponse resp;
                    resp.result = insertOrRelease(&tables::scrollView, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewAsTextFieldRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewAsTextFieldRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->as_textfield(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ViewAsTextFieldResponse resp;
                    resp.result = insertOrRelease(&tables::textfield, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewGetTypeStringRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewGetTypeStringRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_type_string(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ViewGetTypeStringResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewToStringRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewToStringRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t includeChildren = req.includeChildren;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, includeChildren]() {
                auto rawResult = receiver->to_string(receiver, cefArg<decltype(receiver->to_string), 1>(includeChildren));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ViewToStringResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewIsValidRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewIsValidRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_valid(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ViewIsValidResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewIsAttachedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewIsAttachedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_attached(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ViewIsAttachedResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewIsSameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewIsSameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t that = req.that;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, that]() {
                cef_view_t* that_ptr = that != 0 ? tables::view.retain(that) : nullptr;
                auto rawResult = receiver->is_same(receiver, that_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (that_ptr) {
                    auto* that_base = reinterpret_cast<cef_base_ref_counted_t*>(that_ptr);
                    that_base->release(that_base);
                }
                if (ipc) {
                    gen::ViewIsSameResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewGetDelegateRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewGetDelegateRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_delegate(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ViewGetDelegateResponse resp;
                    resp.result = insertOrRelease(&tables::viewDelegate, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewGetWindowRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewGetWindowRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_window(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ViewGetWindowResponse resp;
                    resp.result = insertOrRelease(&tables::window, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewGetIdRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewGetIdRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_id(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ViewGetIdResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewSetIdRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewSetIdRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t id = req.id;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, id]() {
                receiver->set_id(receiver, cefArg<decltype(receiver->set_id), 1>(id));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewGetGroupIdRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewGetGroupIdRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_group_id(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ViewGetGroupIdResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewSetGroupIdRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewSetGroupIdRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t groupId = req.groupId;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, groupId]() {
                receiver->set_group_id(receiver, cefArg<decltype(receiver->set_group_id), 1>(groupId));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewGetParentViewRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewGetParentViewRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_parent_view(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ViewGetParentViewResponse resp;
                    resp.result = insertOrRelease(&tables::view, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewGetViewForIdRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewGetViewForIdRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t id = req.id;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, id]() {
                auto rawResult = receiver->get_view_for_id(receiver, cefArg<decltype(receiver->get_view_for_id), 1>(id));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ViewGetViewForIdResponse resp;
                    resp.result = insertOrRelease(&tables::view, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewSetBoundsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewSetBoundsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            gen::Rect bounds = std::move(req.bounds);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, bounds]() {
                cef_rect_t bounds_native{};
                bounds_native.x = static_cast<decltype(bounds_native.x)>(bounds.x);
                bounds_native.y = static_cast<decltype(bounds_native.y)>(bounds.y);
                bounds_native.width = static_cast<decltype(bounds_native.width)>(bounds.width);
                bounds_native.height = static_cast<decltype(bounds_native.height)>(bounds.height);
                receiver->set_bounds(receiver, &bounds_native);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewGetBoundsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewGetBoundsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                cef_rect_t rawResult = receiver->get_bounds(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ViewGetBoundsResponse resp;
                resp.result.x = static_cast<decltype(resp.result.x)>(rawResult.x);
                resp.result.y = static_cast<decltype(resp.result.y)>(rawResult.y);
                resp.result.width = static_cast<decltype(resp.result.width)>(rawResult.width);
                resp.result.height = static_cast<decltype(resp.result.height)>(rawResult.height);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewGetBoundsInScreenRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewGetBoundsInScreenRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                cef_rect_t rawResult = receiver->get_bounds_in_screen(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ViewGetBoundsInScreenResponse resp;
                resp.result.x = static_cast<decltype(resp.result.x)>(rawResult.x);
                resp.result.y = static_cast<decltype(resp.result.y)>(rawResult.y);
                resp.result.width = static_cast<decltype(resp.result.width)>(rawResult.width);
                resp.result.height = static_cast<decltype(resp.result.height)>(rawResult.height);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewSetSizeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewSetSizeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            gen::Size size = std::move(req.size);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, size]() {
                cef_size_t size_native{};
                size_native.width = static_cast<decltype(size_native.width)>(size.width);
                size_native.height = static_cast<decltype(size_native.height)>(size.height);
                receiver->set_size(receiver, &size_native);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewGetSizeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewGetSizeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                cef_size_t rawResult = receiver->get_size(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ViewGetSizeResponse resp;
                resp.result.width = static_cast<decltype(resp.result.width)>(rawResult.width);
                resp.result.height = static_cast<decltype(resp.result.height)>(rawResult.height);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewSetPositionRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewSetPositionRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            gen::Point position = std::move(req.position);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, position]() {
                cef_point_t position_native{};
                position_native.x = static_cast<decltype(position_native.x)>(position.x);
                position_native.y = static_cast<decltype(position_native.y)>(position.y);
                receiver->set_position(receiver, &position_native);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewGetPositionRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewGetPositionRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                cef_point_t rawResult = receiver->get_position(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ViewGetPositionResponse resp;
                resp.result.x = static_cast<decltype(resp.result.x)>(rawResult.x);
                resp.result.y = static_cast<decltype(resp.result.y)>(rawResult.y);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewSetInsetsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewSetInsetsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            gen::Insets insets = std::move(req.insets);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, insets]() {
                cef_insets_t insets_native{};
                insets_native.top = static_cast<decltype(insets_native.top)>(insets.top);
                insets_native.left = static_cast<decltype(insets_native.left)>(insets.left);
                insets_native.bottom = static_cast<decltype(insets_native.bottom)>(insets.bottom);
                insets_native.right = static_cast<decltype(insets_native.right)>(insets.right);
                receiver->set_insets(receiver, &insets_native);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewGetInsetsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewGetInsetsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                cef_insets_t rawResult = receiver->get_insets(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ViewGetInsetsResponse resp;
                resp.result.top = static_cast<decltype(resp.result.top)>(rawResult.top);
                resp.result.left = static_cast<decltype(resp.result.left)>(rawResult.left);
                resp.result.bottom = static_cast<decltype(resp.result.bottom)>(rawResult.bottom);
                resp.result.right = static_cast<decltype(resp.result.right)>(rawResult.right);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewGetPreferredSizeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewGetPreferredSizeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                cef_size_t rawResult = receiver->get_preferred_size(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ViewGetPreferredSizeResponse resp;
                resp.result.width = static_cast<decltype(resp.result.width)>(rawResult.width);
                resp.result.height = static_cast<decltype(resp.result.height)>(rawResult.height);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewSizeToPreferredSizeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewSizeToPreferredSizeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->size_to_preferred_size(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewGetMinimumSizeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewGetMinimumSizeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                cef_size_t rawResult = receiver->get_minimum_size(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ViewGetMinimumSizeResponse resp;
                resp.result.width = static_cast<decltype(resp.result.width)>(rawResult.width);
                resp.result.height = static_cast<decltype(resp.result.height)>(rawResult.height);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewGetMaximumSizeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewGetMaximumSizeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                cef_size_t rawResult = receiver->get_maximum_size(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ViewGetMaximumSizeResponse resp;
                resp.result.width = static_cast<decltype(resp.result.width)>(rawResult.width);
                resp.result.height = static_cast<decltype(resp.result.height)>(rawResult.height);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewGetHeightForWidthRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewGetHeightForWidthRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t width = req.width;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, width]() {
                auto rawResult = receiver->get_height_for_width(receiver, cefArg<decltype(receiver->get_height_for_width), 1>(width));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ViewGetHeightForWidthResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewInvalidateLayoutRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewInvalidateLayoutRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->invalidate_layout(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewSetVisibleRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewSetVisibleRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t visible = req.visible;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, visible]() {
                receiver->set_visible(receiver, cefArg<decltype(receiver->set_visible), 1>(visible));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewIsVisibleRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewIsVisibleRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_visible(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ViewIsVisibleResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewIsDrawnRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewIsDrawnRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_drawn(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ViewIsDrawnResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewSetEnabledRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewSetEnabledRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t enabled = req.enabled;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, enabled]() {
                receiver->set_enabled(receiver, cefArg<decltype(receiver->set_enabled), 1>(enabled));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewIsEnabledRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewIsEnabledRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_enabled(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ViewIsEnabledResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewSetFocusableRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewSetFocusableRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t focusable = req.focusable;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, focusable]() {
                receiver->set_focusable(receiver, cefArg<decltype(receiver->set_focusable), 1>(focusable));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewIsFocusableRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewIsFocusableRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_focusable(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ViewIsFocusableResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewIsAccessibilityFocusableRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewIsAccessibilityFocusableRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_accessibility_focusable(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ViewIsAccessibilityFocusableResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewHasFocusRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewHasFocusRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->has_focus(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ViewHasFocusResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewRequestFocusRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewRequestFocusRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->request_focus(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewSetBackgroundColorRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewSetBackgroundColorRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t color = req.color;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, color]() {
                receiver->set_background_color(receiver, cefArg<decltype(receiver->set_background_color), 1>(color));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewGetBackgroundColorRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewGetBackgroundColorRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_background_color(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ViewGetBackgroundColorResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ViewGetThemeColorRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ViewGetThemeColorRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::view.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t colorId = req.colorId;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, colorId]() {
                auto rawResult = receiver->get_theme_color(receiver, cefArg<decltype(receiver->get_theme_color), 1>(colorId));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ViewGetThemeColorResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserViewGetBrowserRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserViewGetBrowserRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserView.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_browser(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserViewGetBrowserResponse resp;
                    resp.result = insertOrRelease(&tables::browser, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserViewGetChromeToolbarRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserViewGetChromeToolbarRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserView.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_chrome_toolbar(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserViewGetChromeToolbarResponse resp;
                    resp.result = insertOrRelease(&tables::view, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserViewSetPreferAcceleratorsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserViewSetPreferAcceleratorsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserView.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t preferAccelerators = req.preferAccelerators;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, preferAccelerators]() {
                receiver->set_prefer_accelerators(receiver, cefArg<decltype(receiver->set_prefer_accelerators), 1>(preferAccelerators));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::BrowserViewGetRuntimeStyleRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::BrowserViewGetRuntimeStyleRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::browserView.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_runtime_style(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::BrowserViewGetRuntimeStyleResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ButtonAsLabelButtonRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ButtonAsLabelButtonRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::button.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->as_label_button(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ButtonAsLabelButtonResponse resp;
                    resp.result = insertOrRelease(&tables::labelButton, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ButtonSetStateRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ButtonSetStateRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::button.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t state = req.state;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, state]() {
                receiver->set_state(receiver, cefArg<decltype(receiver->set_state), 1>(state));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ButtonGetStateRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ButtonGetStateRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::button.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_state(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ButtonGetStateResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ButtonSetInkDropEnabledRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ButtonSetInkDropEnabledRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::button.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t enabled = req.enabled;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, enabled]() {
                receiver->set_ink_drop_enabled(receiver, cefArg<decltype(receiver->set_ink_drop_enabled), 1>(enabled));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ButtonSetTooltipTextRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ButtonSetTooltipTextRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::button.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string tooltipText = req.tooltipText;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, tooltipText]() {
                ScopedCefString tooltipText_cef(tooltipText);
                receiver->set_tooltip_text(receiver, tooltipText_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ButtonSetAccessibleNameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ButtonSetAccessibleNameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::button.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string name = req.name;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, name]() {
                ScopedCefString name_cef(name);
                receiver->set_accessible_name(receiver, name_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ButtonDelegateOnButtonPressedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ButtonDelegateOnButtonPressedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::buttonDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t button = req.button;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, button]() {
                cef_button_t* button_ptr = button != 0 ? tables::button.retain(button) : nullptr;
                receiver->on_button_pressed(receiver, button_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (button_ptr) {
                    auto* button_base = reinterpret_cast<cef_base_ref_counted_t*>(button_ptr);
                    button_base->release(button_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ButtonDelegateOnButtonStateChangedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ButtonDelegateOnButtonStateChangedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::buttonDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t button = req.button;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, button]() {
                cef_button_t* button_ptr = button != 0 ? tables::button.retain(button) : nullptr;
                receiver->on_button_state_changed(receiver, button_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (button_ptr) {
                    auto* button_base = reinterpret_cast<cef_base_ref_counted_t*>(button_ptr);
                    button_base->release(button_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DisplayGetIdRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DisplayGetIdRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::display.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_id(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DisplayGetIdResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DisplayGetBoundsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DisplayGetBoundsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::display.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                cef_rect_t rawResult = receiver->get_bounds(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DisplayGetBoundsResponse resp;
                resp.result.x = static_cast<decltype(resp.result.x)>(rawResult.x);
                resp.result.y = static_cast<decltype(resp.result.y)>(rawResult.y);
                resp.result.width = static_cast<decltype(resp.result.width)>(rawResult.width);
                resp.result.height = static_cast<decltype(resp.result.height)>(rawResult.height);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DisplayGetWorkAreaRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DisplayGetWorkAreaRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::display.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                cef_rect_t rawResult = receiver->get_work_area(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DisplayGetWorkAreaResponse resp;
                resp.result.x = static_cast<decltype(resp.result.x)>(rawResult.x);
                resp.result.y = static_cast<decltype(resp.result.y)>(rawResult.y);
                resp.result.width = static_cast<decltype(resp.result.width)>(rawResult.width);
                resp.result.height = static_cast<decltype(resp.result.height)>(rawResult.height);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::DisplayGetRotationRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::DisplayGetRotationRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::display.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_rotation(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::DisplayGetRotationResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::LabelButtonAsMenuButtonRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::LabelButtonAsMenuButtonRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::labelButton.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->as_menu_button(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::LabelButtonAsMenuButtonResponse resp;
                    resp.result = insertOrRelease(&tables::menuButton, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::LabelButtonSetTextRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::LabelButtonSetTextRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::labelButton.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string text = req.text;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, text]() {
                ScopedCefString text_cef(text);
                receiver->set_text(receiver, text_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::LabelButtonGetTextRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::LabelButtonGetTextRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::labelButton.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_text(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::LabelButtonGetTextResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::LabelButtonSetImageRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::LabelButtonSetImageRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::labelButton.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t buttonState = req.buttonState;
            std::int32_t image = req.image;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, buttonState, image]() {
                cef_image_t* image_ptr = image != 0 ? tables::image.retain(image) : nullptr;
                receiver->set_image(receiver, cefArg<decltype(receiver->set_image), 1>(buttonState), image_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (image_ptr) {
                    auto* image_base = reinterpret_cast<cef_base_ref_counted_t*>(image_ptr);
                    image_base->release(image_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::LabelButtonGetImageRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::LabelButtonGetImageRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::labelButton.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t buttonState = req.buttonState;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, buttonState]() {
                auto rawResult = receiver->get_image(receiver, cefArg<decltype(receiver->get_image), 1>(buttonState));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::LabelButtonGetImageResponse resp;
                    resp.result = insertOrRelease(&tables::image, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::LabelButtonSetTextColorRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::LabelButtonSetTextColorRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::labelButton.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t forState = req.forState;
            std::int32_t color = req.color;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, forState, color]() {
                receiver->set_text_color(receiver, cefArg<decltype(receiver->set_text_color), 1>(forState), cefArg<decltype(receiver->set_text_color), 2>(color));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::LabelButtonSetEnabledTextColorsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::LabelButtonSetEnabledTextColorsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::labelButton.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t color = req.color;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, color]() {
                receiver->set_enabled_text_colors(receiver, cefArg<decltype(receiver->set_enabled_text_colors), 1>(color));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::LabelButtonSetFontListRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::LabelButtonSetFontListRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::labelButton.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string fontList = req.fontList;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, fontList]() {
                ScopedCefString fontList_cef(fontList);
                receiver->set_font_list(receiver, fontList_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::LabelButtonSetHorizontalAlignmentRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::LabelButtonSetHorizontalAlignmentRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::labelButton.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t alignment = req.alignment;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, alignment]() {
                receiver->set_horizontal_alignment(receiver, cefArg<decltype(receiver->set_horizontal_alignment), 1>(alignment));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::LabelButtonSetMinimumSizeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::LabelButtonSetMinimumSizeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::labelButton.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            gen::Size size = std::move(req.size);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, size]() {
                cef_size_t size_native{};
                size_native.width = static_cast<decltype(size_native.width)>(size.width);
                size_native.height = static_cast<decltype(size_native.height)>(size.height);
                receiver->set_minimum_size(receiver, &size_native);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::LabelButtonSetMaximumSizeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::LabelButtonSetMaximumSizeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::labelButton.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            gen::Size size = std::move(req.size);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, size]() {
                cef_size_t size_native{};
                size_native.width = static_cast<decltype(size_native.width)>(size.width);
                size_native.height = static_cast<decltype(size_native.height)>(size.height);
                receiver->set_maximum_size(receiver, &size_native);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuButtonShowMenuRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuButtonShowMenuRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuButton.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t menuModel = req.menuModel;
            gen::Point screenPoint = std::move(req.screenPoint);
            std::int32_t anchorPosition = req.anchorPosition;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, menuModel, screenPoint, anchorPosition]() {
                cef_menu_model_t* menuModel_ptr = menuModel != 0 ? tables::menuModel.retain(menuModel) : nullptr;
                cef_point_t screenPoint_native{};
                screenPoint_native.x = static_cast<decltype(screenPoint_native.x)>(screenPoint.x);
                screenPoint_native.y = static_cast<decltype(screenPoint_native.y)>(screenPoint.y);
                receiver->show_menu(receiver, menuModel_ptr, &screenPoint_native, cefArg<decltype(receiver->show_menu), 3>(anchorPosition));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (menuModel_ptr) {
                    auto* menuModel_base = reinterpret_cast<cef_base_ref_counted_t*>(menuModel_ptr);
                    menuModel_base->release(menuModel_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::MenuButtonTriggerMenuRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::MenuButtonTriggerMenuRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::menuButton.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->trigger_menu(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::OverlayControllerIsValidRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::OverlayControllerIsValidRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::overlayController.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_valid(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::OverlayControllerIsValidResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::OverlayControllerIsSameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::OverlayControllerIsSameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::overlayController.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t that = req.that;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, that]() {
                cef_overlay_controller_t* that_ptr = that != 0 ? tables::overlayController.retain(that) : nullptr;
                auto rawResult = receiver->is_same(receiver, that_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (that_ptr) {
                    auto* that_base = reinterpret_cast<cef_base_ref_counted_t*>(that_ptr);
                    that_base->release(that_base);
                }
                if (ipc) {
                    gen::OverlayControllerIsSameResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::OverlayControllerGetContentsViewRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::OverlayControllerGetContentsViewRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::overlayController.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_contents_view(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::OverlayControllerGetContentsViewResponse resp;
                    resp.result = insertOrRelease(&tables::view, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::OverlayControllerGetWindowRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::OverlayControllerGetWindowRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::overlayController.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_window(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::OverlayControllerGetWindowResponse resp;
                    resp.result = insertOrRelease(&tables::window, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::OverlayControllerGetDockingModeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::OverlayControllerGetDockingModeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::overlayController.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_docking_mode(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::OverlayControllerGetDockingModeResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::OverlayControllerDestroyRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::OverlayControllerDestroyRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::overlayController.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->destroy(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::OverlayControllerSetBoundsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::OverlayControllerSetBoundsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::overlayController.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            gen::Rect bounds = std::move(req.bounds);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, bounds]() {
                cef_rect_t bounds_native{};
                bounds_native.x = static_cast<decltype(bounds_native.x)>(bounds.x);
                bounds_native.y = static_cast<decltype(bounds_native.y)>(bounds.y);
                bounds_native.width = static_cast<decltype(bounds_native.width)>(bounds.width);
                bounds_native.height = static_cast<decltype(bounds_native.height)>(bounds.height);
                receiver->set_bounds(receiver, &bounds_native);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::OverlayControllerGetBoundsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::OverlayControllerGetBoundsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::overlayController.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                cef_rect_t rawResult = receiver->get_bounds(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::OverlayControllerGetBoundsResponse resp;
                resp.result.x = static_cast<decltype(resp.result.x)>(rawResult.x);
                resp.result.y = static_cast<decltype(resp.result.y)>(rawResult.y);
                resp.result.width = static_cast<decltype(resp.result.width)>(rawResult.width);
                resp.result.height = static_cast<decltype(resp.result.height)>(rawResult.height);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::OverlayControllerGetBoundsInScreenRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::OverlayControllerGetBoundsInScreenRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::overlayController.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                cef_rect_t rawResult = receiver->get_bounds_in_screen(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::OverlayControllerGetBoundsInScreenResponse resp;
                resp.result.x = static_cast<decltype(resp.result.x)>(rawResult.x);
                resp.result.y = static_cast<decltype(resp.result.y)>(rawResult.y);
                resp.result.width = static_cast<decltype(resp.result.width)>(rawResult.width);
                resp.result.height = static_cast<decltype(resp.result.height)>(rawResult.height);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::OverlayControllerSetSizeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::OverlayControllerSetSizeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::overlayController.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            gen::Size size = std::move(req.size);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, size]() {
                cef_size_t size_native{};
                size_native.width = static_cast<decltype(size_native.width)>(size.width);
                size_native.height = static_cast<decltype(size_native.height)>(size.height);
                receiver->set_size(receiver, &size_native);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::OverlayControllerGetSizeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::OverlayControllerGetSizeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::overlayController.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                cef_size_t rawResult = receiver->get_size(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::OverlayControllerGetSizeResponse resp;
                resp.result.width = static_cast<decltype(resp.result.width)>(rawResult.width);
                resp.result.height = static_cast<decltype(resp.result.height)>(rawResult.height);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::OverlayControllerSetPositionRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::OverlayControllerSetPositionRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::overlayController.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            gen::Point position = std::move(req.position);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, position]() {
                cef_point_t position_native{};
                position_native.x = static_cast<decltype(position_native.x)>(position.x);
                position_native.y = static_cast<decltype(position_native.y)>(position.y);
                receiver->set_position(receiver, &position_native);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::OverlayControllerGetPositionRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::OverlayControllerGetPositionRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::overlayController.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                cef_point_t rawResult = receiver->get_position(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::OverlayControllerGetPositionResponse resp;
                resp.result.x = static_cast<decltype(resp.result.x)>(rawResult.x);
                resp.result.y = static_cast<decltype(resp.result.y)>(rawResult.y);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::OverlayControllerSetInsetsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::OverlayControllerSetInsetsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::overlayController.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            gen::Insets insets = std::move(req.insets);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, insets]() {
                cef_insets_t insets_native{};
                insets_native.top = static_cast<decltype(insets_native.top)>(insets.top);
                insets_native.left = static_cast<decltype(insets_native.left)>(insets.left);
                insets_native.bottom = static_cast<decltype(insets_native.bottom)>(insets.bottom);
                insets_native.right = static_cast<decltype(insets_native.right)>(insets.right);
                receiver->set_insets(receiver, &insets_native);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::OverlayControllerGetInsetsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::OverlayControllerGetInsetsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::overlayController.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                cef_insets_t rawResult = receiver->get_insets(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::OverlayControllerGetInsetsResponse resp;
                resp.result.top = static_cast<decltype(resp.result.top)>(rawResult.top);
                resp.result.left = static_cast<decltype(resp.result.left)>(rawResult.left);
                resp.result.bottom = static_cast<decltype(resp.result.bottom)>(rawResult.bottom);
                resp.result.right = static_cast<decltype(resp.result.right)>(rawResult.right);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::OverlayControllerSizeToPreferredSizeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::OverlayControllerSizeToPreferredSizeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::overlayController.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->size_to_preferred_size(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::OverlayControllerSetVisibleRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::OverlayControllerSetVisibleRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::overlayController.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t visible = req.visible;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, visible]() {
                receiver->set_visible(receiver, cefArg<decltype(receiver->set_visible), 1>(visible));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::OverlayControllerIsVisibleRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::OverlayControllerIsVisibleRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::overlayController.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_visible(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::OverlayControllerIsVisibleResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::OverlayControllerIsDrawnRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::OverlayControllerIsDrawnRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::overlayController.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_drawn(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::OverlayControllerIsDrawnResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PanelAsWindowRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PanelAsWindowRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::panel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->as_window(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::PanelAsWindowResponse resp;
                    resp.result = insertOrRelease(&tables::window, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PanelGetLayoutRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PanelGetLayoutRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::panel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_layout(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::PanelGetLayoutResponse resp;
                    resp.result = insertOrRelease(&tables::layout, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PanelLayoutRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PanelLayoutRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::panel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->layout(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PanelAddChildViewRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PanelAddChildViewRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::panel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t view = req.view;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, view]() {
                cef_view_t* view_ptr = view != 0 ? tables::view.retain(view) : nullptr;
                receiver->add_child_view(receiver, view_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (view_ptr) {
                    auto* view_base = reinterpret_cast<cef_base_ref_counted_t*>(view_ptr);
                    view_base->release(view_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PanelAddChildViewAtRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PanelAddChildViewAtRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::panel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t view = req.view;
            std::int32_t index = req.index;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, view, index]() {
                cef_view_t* view_ptr = view != 0 ? tables::view.retain(view) : nullptr;
                receiver->add_child_view_at(receiver, view_ptr, cefArg<decltype(receiver->add_child_view_at), 2>(index));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (view_ptr) {
                    auto* view_base = reinterpret_cast<cef_base_ref_counted_t*>(view_ptr);
                    view_base->release(view_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PanelReorderChildViewRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PanelReorderChildViewRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::panel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t view = req.view;
            std::int32_t index = req.index;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, view, index]() {
                cef_view_t* view_ptr = view != 0 ? tables::view.retain(view) : nullptr;
                receiver->reorder_child_view(receiver, view_ptr, cefArg<decltype(receiver->reorder_child_view), 2>(index));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (view_ptr) {
                    auto* view_base = reinterpret_cast<cef_base_ref_counted_t*>(view_ptr);
                    view_base->release(view_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PanelRemoveChildViewRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PanelRemoveChildViewRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::panel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t view = req.view;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, view]() {
                cef_view_t* view_ptr = view != 0 ? tables::view.retain(view) : nullptr;
                receiver->remove_child_view(receiver, view_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (view_ptr) {
                    auto* view_base = reinterpret_cast<cef_base_ref_counted_t*>(view_ptr);
                    view_base->release(view_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PanelRemoveAllChildViewsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PanelRemoveAllChildViewsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::panel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->remove_all_child_views(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PanelGetChildViewCountRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PanelGetChildViewCountRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::panel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_child_view_count(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::PanelGetChildViewCountResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::PanelGetChildViewAtRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::PanelGetChildViewAtRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::panel.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t index = req.index;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, index]() {
                auto rawResult = receiver->get_child_view_at(receiver, cefArg<decltype(receiver->get_child_view_at), 1>(index));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::PanelGetChildViewAtResponse resp;
                    resp.result = insertOrRelease(&tables::view, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ScrollViewSetContentViewRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ScrollViewSetContentViewRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::scrollView.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t view = req.view;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, view]() {
                cef_view_t* view_ptr = view != 0 ? tables::view.retain(view) : nullptr;
                receiver->set_content_view(receiver, view_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (view_ptr) {
                    auto* view_base = reinterpret_cast<cef_base_ref_counted_t*>(view_ptr);
                    view_base->release(view_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ScrollViewGetContentViewRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ScrollViewGetContentViewRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::scrollView.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_content_view(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ScrollViewGetContentViewResponse resp;
                    resp.result = insertOrRelease(&tables::view, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ScrollViewGetVisibleContentRectRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ScrollViewGetVisibleContentRectRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::scrollView.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                cef_rect_t rawResult = receiver->get_visible_content_rect(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ScrollViewGetVisibleContentRectResponse resp;
                resp.result.x = static_cast<decltype(resp.result.x)>(rawResult.x);
                resp.result.y = static_cast<decltype(resp.result.y)>(rawResult.y);
                resp.result.width = static_cast<decltype(resp.result.width)>(rawResult.width);
                resp.result.height = static_cast<decltype(resp.result.height)>(rawResult.height);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ScrollViewHasHorizontalScrollbarRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ScrollViewHasHorizontalScrollbarRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::scrollView.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->has_horizontal_scrollbar(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ScrollViewHasHorizontalScrollbarResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ScrollViewGetHorizontalScrollbarHeightRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ScrollViewGetHorizontalScrollbarHeightRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::scrollView.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_horizontal_scrollbar_height(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ScrollViewGetHorizontalScrollbarHeightResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ScrollViewHasVerticalScrollbarRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ScrollViewHasVerticalScrollbarRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::scrollView.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->has_vertical_scrollbar(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ScrollViewHasVerticalScrollbarResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::ScrollViewGetVerticalScrollbarWidthRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::ScrollViewGetVerticalScrollbarWidthRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::scrollView.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_vertical_scrollbar_width(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::ScrollViewGetVerticalScrollbarWidthResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TextFieldDelegateOnKeyEventRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TextFieldDelegateOnKeyEventRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::textFieldDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t textfield = req.textfield;
            gen::KeyEvent event = std::move(req.event);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, textfield, event]() {
                cef_textfield_t* textfield_ptr = textfield != 0 ? tables::textfield.retain(textfield) : nullptr;
                cef_key_event_t event_native{};
                event_native.size = sizeof(event_native);
                event_native.type = static_cast<decltype(event_native.type)>(event.type);
                event_native.modifiers = static_cast<decltype(event_native.modifiers)>(event.modifiers);
                event_native.windows_key_code = static_cast<decltype(event_native.windows_key_code)>(event.windowsKeyCode);
                event_native.native_key_code = static_cast<decltype(event_native.native_key_code)>(event.nativeKeyCode);
                event_native.is_system_key = static_cast<decltype(event_native.is_system_key)>(event.isSystemKey);
                event_native.character = static_cast<decltype(event_native.character)>(event.character);
                event_native.unmodified_character = static_cast<decltype(event_native.unmodified_character)>(event.unmodifiedCharacter);
                event_native.focus_on_editable_field = static_cast<decltype(event_native.focus_on_editable_field)>(event.focusOnEditableField);
                auto rawResult = receiver->on_key_event(receiver, textfield_ptr, &event_native);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (textfield_ptr) {
                    auto* textfield_base = reinterpret_cast<cef_base_ref_counted_t*>(textfield_ptr);
                    textfield_base->release(textfield_base);
                }
                if (ipc) {
                    gen::TextFieldDelegateOnKeyEventResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TextFieldDelegateOnAfterUserActionRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TextFieldDelegateOnAfterUserActionRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::textFieldDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t textfield = req.textfield;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, textfield]() {
                cef_textfield_t* textfield_ptr = textfield != 0 ? tables::textfield.retain(textfield) : nullptr;
                receiver->on_after_user_action(receiver, textfield_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (textfield_ptr) {
                    auto* textfield_base = reinterpret_cast<cef_base_ref_counted_t*>(textfield_ptr);
                    textfield_base->release(textfield_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TextFieldSetPasswordInputRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TextFieldSetPasswordInputRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::textfield.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t passwordInput = req.passwordInput;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, passwordInput]() {
                receiver->set_password_input(receiver, cefArg<decltype(receiver->set_password_input), 1>(passwordInput));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TextFieldIsPasswordInputRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TextFieldIsPasswordInputRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::textfield.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_password_input(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::TextFieldIsPasswordInputResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TextFieldSetReadOnlyRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TextFieldSetReadOnlyRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::textfield.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t readOnly = req.readOnly;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, readOnly]() {
                receiver->set_read_only(receiver, cefArg<decltype(receiver->set_read_only), 1>(readOnly));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TextFieldIsReadOnlyRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TextFieldIsReadOnlyRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::textfield.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_read_only(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::TextFieldIsReadOnlyResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TextFieldGetTextRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TextFieldGetTextRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::textfield.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_text(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::TextFieldGetTextResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TextFieldSetTextRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TextFieldSetTextRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::textfield.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string text = req.text;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, text]() {
                ScopedCefString text_cef(text);
                receiver->set_text(receiver, text_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TextFieldAppendTextRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TextFieldAppendTextRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::textfield.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string text = req.text;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, text]() {
                ScopedCefString text_cef(text);
                receiver->append_text(receiver, text_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TextFieldInsertOrReplaceTextRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TextFieldInsertOrReplaceTextRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::textfield.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string text = req.text;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, text]() {
                ScopedCefString text_cef(text);
                receiver->insert_or_replace_text(receiver, text_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TextFieldHasSelectionRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TextFieldHasSelectionRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::textfield.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->has_selection(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::TextFieldHasSelectionResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TextFieldGetSelectedTextRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TextFieldGetSelectedTextRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::textfield.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_selected_text(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::TextFieldGetSelectedTextResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TextFieldSelectAllRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TextFieldSelectAllRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::textfield.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t reversed = req.reversed;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, reversed]() {
                receiver->select_all(receiver, cefArg<decltype(receiver->select_all), 1>(reversed));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TextFieldClearSelectionRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TextFieldClearSelectionRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::textfield.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->clear_selection(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TextFieldGetSelectedRangeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TextFieldGetSelectedRangeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::textfield.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                cef_range_t rawResult = receiver->get_selected_range(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::TextFieldGetSelectedRangeResponse resp;
                resp.result.from = static_cast<decltype(resp.result.from)>(rawResult.from);
                resp.result.to = static_cast<decltype(resp.result.to)>(rawResult.to);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TextFieldSelectRangeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TextFieldSelectRangeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::textfield.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            gen::Range range = std::move(req.range);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, range]() {
                cef_range_t range_native{};
                range_native.from = static_cast<decltype(range_native.from)>(range.from);
                range_native.to = static_cast<decltype(range_native.to)>(range.to);
                receiver->select_range(receiver, &range_native);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TextFieldGetCursorPositionRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TextFieldGetCursorPositionRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::textfield.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_cursor_position(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::TextFieldGetCursorPositionResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TextFieldSetFontListRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TextFieldSetFontListRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::textfield.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string fontList = req.fontList;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, fontList]() {
                ScopedCefString fontList_cef(fontList);
                receiver->set_font_list(receiver, fontList_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TextFieldApplyTextColorRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TextFieldApplyTextColorRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::textfield.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t color = req.color;
            gen::Range range = std::move(req.range);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, color, range]() {
                cef_range_t range_native{};
                range_native.from = static_cast<decltype(range_native.from)>(range.from);
                range_native.to = static_cast<decltype(range_native.to)>(range.to);
                receiver->apply_text_color(receiver, cefArg<decltype(receiver->apply_text_color), 1>(color), &range_native);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TextFieldApplyTextStyleRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TextFieldApplyTextStyleRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::textfield.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t style = req.style;
            std::int32_t add = req.add;
            gen::Range range = std::move(req.range);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, style, add, range]() {
                cef_range_t range_native{};
                range_native.from = static_cast<decltype(range_native.from)>(range.from);
                range_native.to = static_cast<decltype(range_native.to)>(range.to);
                receiver->apply_text_style(receiver, cefArg<decltype(receiver->apply_text_style), 1>(style), cefArg<decltype(receiver->apply_text_style), 2>(add), &range_native);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TextFieldIsCommandEnabledRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TextFieldIsCommandEnabledRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::textfield.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t commandId = req.commandId;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, commandId]() {
                auto rawResult = receiver->is_command_enabled(receiver, cefArg<decltype(receiver->is_command_enabled), 1>(commandId));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::TextFieldIsCommandEnabledResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TextFieldExecuteCommandRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TextFieldExecuteCommandRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::textfield.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t commandId = req.commandId;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, commandId]() {
                receiver->execute_command(receiver, cefArg<decltype(receiver->execute_command), 1>(commandId));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TextFieldClearEditHistoryRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TextFieldClearEditHistoryRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::textfield.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->clear_edit_history(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TextFieldSetPlaceholderTextRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TextFieldSetPlaceholderTextRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::textfield.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string text = req.text;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, text]() {
                ScopedCefString text_cef(text);
                receiver->set_placeholder_text(receiver, text_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TextFieldGetPlaceholderTextRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TextFieldGetPlaceholderTextRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::textfield.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_placeholder_text(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::TextFieldGetPlaceholderTextResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::TextFieldSetAccessibleNameRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::TextFieldSetAccessibleNameRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::textfield.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string name = req.name;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, name]() {
                ScopedCefString name_cef(name);
                receiver->set_accessible_name(receiver, name_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowDelegateOnWindowCreatedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowDelegateOnWindowCreatedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::windowDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t window = req.window;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, window]() {
                cef_window_t* window_ptr = window != 0 ? tables::window.retain(window) : nullptr;
                receiver->on_window_created(receiver, window_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (window_ptr) {
                    auto* window_base = reinterpret_cast<cef_base_ref_counted_t*>(window_ptr);
                    window_base->release(window_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowDelegateOnWindowClosingRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowDelegateOnWindowClosingRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::windowDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t window = req.window;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, window]() {
                cef_window_t* window_ptr = window != 0 ? tables::window.retain(window) : nullptr;
                receiver->on_window_closing(receiver, window_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (window_ptr) {
                    auto* window_base = reinterpret_cast<cef_base_ref_counted_t*>(window_ptr);
                    window_base->release(window_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowDelegateOnWindowDestroyedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowDelegateOnWindowDestroyedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::windowDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t window = req.window;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, window]() {
                cef_window_t* window_ptr = window != 0 ? tables::window.retain(window) : nullptr;
                receiver->on_window_destroyed(receiver, window_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (window_ptr) {
                    auto* window_base = reinterpret_cast<cef_base_ref_counted_t*>(window_ptr);
                    window_base->release(window_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowDelegateOnWindowActivationChangedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowDelegateOnWindowActivationChangedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::windowDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t window = req.window;
            std::int32_t active = req.active;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, window, active]() {
                cef_window_t* window_ptr = window != 0 ? tables::window.retain(window) : nullptr;
                receiver->on_window_activation_changed(receiver, window_ptr, cefArg<decltype(receiver->on_window_activation_changed), 2>(active));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (window_ptr) {
                    auto* window_base = reinterpret_cast<cef_base_ref_counted_t*>(window_ptr);
                    window_base->release(window_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowDelegateOnWindowBoundsChangedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowDelegateOnWindowBoundsChangedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::windowDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t window = req.window;
            gen::Rect newBounds = std::move(req.newBounds);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, window, newBounds]() {
                cef_window_t* window_ptr = window != 0 ? tables::window.retain(window) : nullptr;
                cef_rect_t newBounds_native{};
                newBounds_native.x = static_cast<decltype(newBounds_native.x)>(newBounds.x);
                newBounds_native.y = static_cast<decltype(newBounds_native.y)>(newBounds.y);
                newBounds_native.width = static_cast<decltype(newBounds_native.width)>(newBounds.width);
                newBounds_native.height = static_cast<decltype(newBounds_native.height)>(newBounds.height);
                receiver->on_window_bounds_changed(receiver, window_ptr, &newBounds_native);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (window_ptr) {
                    auto* window_base = reinterpret_cast<cef_base_ref_counted_t*>(window_ptr);
                    window_base->release(window_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowDelegateOnWindowFullscreenTransitionRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowDelegateOnWindowFullscreenTransitionRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::windowDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t window = req.window;
            std::int32_t isCompleted = req.isCompleted;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, window, isCompleted]() {
                cef_window_t* window_ptr = window != 0 ? tables::window.retain(window) : nullptr;
                receiver->on_window_fullscreen_transition(receiver, window_ptr, cefArg<decltype(receiver->on_window_fullscreen_transition), 2>(isCompleted));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (window_ptr) {
                    auto* window_base = reinterpret_cast<cef_base_ref_counted_t*>(window_ptr);
                    window_base->release(window_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowDelegateIsWindowModalDialogRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowDelegateIsWindowModalDialogRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::windowDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t window = req.window;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, window]() {
                cef_window_t* window_ptr = window != 0 ? tables::window.retain(window) : nullptr;
                auto rawResult = receiver->is_window_modal_dialog(receiver, window_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (window_ptr) {
                    auto* window_base = reinterpret_cast<cef_base_ref_counted_t*>(window_ptr);
                    window_base->release(window_base);
                }
                if (ipc) {
                    gen::WindowDelegateIsWindowModalDialogResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowDelegateGetInitialBoundsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowDelegateGetInitialBoundsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::windowDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t window = req.window;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, window]() {
                cef_window_t* window_ptr = window != 0 ? tables::window.retain(window) : nullptr;
                cef_rect_t rawResult = receiver->get_initial_bounds(receiver, window_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (window_ptr) {
                    auto* window_base = reinterpret_cast<cef_base_ref_counted_t*>(window_ptr);
                    window_base->release(window_base);
                }
                if (ipc) {
                    gen::WindowDelegateGetInitialBoundsResponse resp;
                resp.result.x = static_cast<decltype(resp.result.x)>(rawResult.x);
                resp.result.y = static_cast<decltype(resp.result.y)>(rawResult.y);
                resp.result.width = static_cast<decltype(resp.result.width)>(rawResult.width);
                resp.result.height = static_cast<decltype(resp.result.height)>(rawResult.height);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowDelegateGetInitialShowStateRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowDelegateGetInitialShowStateRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::windowDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t window = req.window;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, window]() {
                cef_window_t* window_ptr = window != 0 ? tables::window.retain(window) : nullptr;
                auto rawResult = receiver->get_initial_show_state(receiver, window_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (window_ptr) {
                    auto* window_base = reinterpret_cast<cef_base_ref_counted_t*>(window_ptr);
                    window_base->release(window_base);
                }
                if (ipc) {
                    gen::WindowDelegateGetInitialShowStateResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowDelegateIsFramelessRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowDelegateIsFramelessRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::windowDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t window = req.window;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, window]() {
                cef_window_t* window_ptr = window != 0 ? tables::window.retain(window) : nullptr;
                auto rawResult = receiver->is_frameless(receiver, window_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (window_ptr) {
                    auto* window_base = reinterpret_cast<cef_base_ref_counted_t*>(window_ptr);
                    window_base->release(window_base);
                }
                if (ipc) {
                    gen::WindowDelegateIsFramelessResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowDelegateWithStandardWindowButtonsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowDelegateWithStandardWindowButtonsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::windowDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t window = req.window;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, window]() {
                cef_window_t* window_ptr = window != 0 ? tables::window.retain(window) : nullptr;
                auto rawResult = receiver->with_standard_window_buttons(receiver, window_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (window_ptr) {
                    auto* window_base = reinterpret_cast<cef_base_ref_counted_t*>(window_ptr);
                    window_base->release(window_base);
                }
                if (ipc) {
                    gen::WindowDelegateWithStandardWindowButtonsResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowDelegateAcceptsFirstMouseRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowDelegateAcceptsFirstMouseRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::windowDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t window = req.window;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, window]() {
                cef_window_t* window_ptr = window != 0 ? tables::window.retain(window) : nullptr;
                auto rawResult = receiver->accepts_first_mouse(receiver, window_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (window_ptr) {
                    auto* window_base = reinterpret_cast<cef_base_ref_counted_t*>(window_ptr);
                    window_base->release(window_base);
                }
                if (ipc) {
                    gen::WindowDelegateAcceptsFirstMouseResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowDelegateCanResizeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowDelegateCanResizeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::windowDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t window = req.window;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, window]() {
                cef_window_t* window_ptr = window != 0 ? tables::window.retain(window) : nullptr;
                auto rawResult = receiver->can_resize(receiver, window_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (window_ptr) {
                    auto* window_base = reinterpret_cast<cef_base_ref_counted_t*>(window_ptr);
                    window_base->release(window_base);
                }
                if (ipc) {
                    gen::WindowDelegateCanResizeResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowDelegateCanMaximizeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowDelegateCanMaximizeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::windowDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t window = req.window;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, window]() {
                cef_window_t* window_ptr = window != 0 ? tables::window.retain(window) : nullptr;
                auto rawResult = receiver->can_maximize(receiver, window_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (window_ptr) {
                    auto* window_base = reinterpret_cast<cef_base_ref_counted_t*>(window_ptr);
                    window_base->release(window_base);
                }
                if (ipc) {
                    gen::WindowDelegateCanMaximizeResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowDelegateCanMinimizeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowDelegateCanMinimizeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::windowDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t window = req.window;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, window]() {
                cef_window_t* window_ptr = window != 0 ? tables::window.retain(window) : nullptr;
                auto rawResult = receiver->can_minimize(receiver, window_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (window_ptr) {
                    auto* window_base = reinterpret_cast<cef_base_ref_counted_t*>(window_ptr);
                    window_base->release(window_base);
                }
                if (ipc) {
                    gen::WindowDelegateCanMinimizeResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowDelegateCanCloseRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowDelegateCanCloseRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::windowDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t window = req.window;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, window]() {
                cef_window_t* window_ptr = window != 0 ? tables::window.retain(window) : nullptr;
                auto rawResult = receiver->can_close(receiver, window_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (window_ptr) {
                    auto* window_base = reinterpret_cast<cef_base_ref_counted_t*>(window_ptr);
                    window_base->release(window_base);
                }
                if (ipc) {
                    gen::WindowDelegateCanCloseResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowDelegateOnAcceleratorRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowDelegateOnAcceleratorRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::windowDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t window = req.window;
            std::int32_t commandId = req.commandId;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, window, commandId]() {
                cef_window_t* window_ptr = window != 0 ? tables::window.retain(window) : nullptr;
                auto rawResult = receiver->on_accelerator(receiver, window_ptr, cefArg<decltype(receiver->on_accelerator), 2>(commandId));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (window_ptr) {
                    auto* window_base = reinterpret_cast<cef_base_ref_counted_t*>(window_ptr);
                    window_base->release(window_base);
                }
                if (ipc) {
                    gen::WindowDelegateOnAcceleratorResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowDelegateOnKeyEventRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowDelegateOnKeyEventRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::windowDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t window = req.window;
            gen::KeyEvent event = std::move(req.event);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, window, event]() {
                cef_window_t* window_ptr = window != 0 ? tables::window.retain(window) : nullptr;
                cef_key_event_t event_native{};
                event_native.size = sizeof(event_native);
                event_native.type = static_cast<decltype(event_native.type)>(event.type);
                event_native.modifiers = static_cast<decltype(event_native.modifiers)>(event.modifiers);
                event_native.windows_key_code = static_cast<decltype(event_native.windows_key_code)>(event.windowsKeyCode);
                event_native.native_key_code = static_cast<decltype(event_native.native_key_code)>(event.nativeKeyCode);
                event_native.is_system_key = static_cast<decltype(event_native.is_system_key)>(event.isSystemKey);
                event_native.character = static_cast<decltype(event_native.character)>(event.character);
                event_native.unmodified_character = static_cast<decltype(event_native.unmodified_character)>(event.unmodifiedCharacter);
                event_native.focus_on_editable_field = static_cast<decltype(event_native.focus_on_editable_field)>(event.focusOnEditableField);
                auto rawResult = receiver->on_key_event(receiver, window_ptr, &event_native);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (window_ptr) {
                    auto* window_base = reinterpret_cast<cef_base_ref_counted_t*>(window_ptr);
                    window_base->release(window_base);
                }
                if (ipc) {
                    gen::WindowDelegateOnKeyEventResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowDelegateOnThemeColorsChangedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowDelegateOnThemeColorsChangedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::windowDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t window = req.window;
            std::int32_t chromeTheme = req.chromeTheme;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, window, chromeTheme]() {
                cef_window_t* window_ptr = window != 0 ? tables::window.retain(window) : nullptr;
                receiver->on_theme_colors_changed(receiver, window_ptr, cefArg<decltype(receiver->on_theme_colors_changed), 2>(chromeTheme));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (window_ptr) {
                    auto* window_base = reinterpret_cast<cef_base_ref_counted_t*>(window_ptr);
                    window_base->release(window_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowDelegateGetWindowRuntimeStyleRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowDelegateGetWindowRuntimeStyleRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::windowDelegate.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_window_runtime_style(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::WindowDelegateGetWindowRuntimeStyleResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowShowRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowShowRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->show(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowShowAsBrowserModalDialogRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowShowAsBrowserModalDialogRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t browserView = req.browserView;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, browserView]() {
                cef_browser_view_t* browserView_ptr = browserView != 0 ? tables::browserView.retain(browserView) : nullptr;
                receiver->show_as_browser_modal_dialog(receiver, browserView_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (browserView_ptr) {
                    auto* browserView_base = reinterpret_cast<cef_base_ref_counted_t*>(browserView_ptr);
                    browserView_base->release(browserView_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowHideRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowHideRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->hide(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowCenterWindowRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowCenterWindowRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            gen::Size size = std::move(req.size);
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, size]() {
                cef_size_t size_native{};
                size_native.width = static_cast<decltype(size_native.width)>(size.width);
                size_native.height = static_cast<decltype(size_native.height)>(size.height);
                receiver->center_window(receiver, &size_native);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowCloseRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowCloseRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->close(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowIsClosedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowIsClosedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_closed(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::WindowIsClosedResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowActivateRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowActivateRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->activate(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowDeactivateRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowDeactivateRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->deactivate(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowIsActiveRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowIsActiveRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_active(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::WindowIsActiveResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowBringToTopRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowBringToTopRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->bring_to_top(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowSetAlwaysOnTopRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowSetAlwaysOnTopRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t onTop = req.onTop;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, onTop]() {
                receiver->set_always_on_top(receiver, cefArg<decltype(receiver->set_always_on_top), 1>(onTop));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowIsAlwaysOnTopRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowIsAlwaysOnTopRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_always_on_top(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::WindowIsAlwaysOnTopResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowMaximizeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowMaximizeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->maximize(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowMinimizeRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowMinimizeRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->minimize(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowRestoreRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowRestoreRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->restore(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowSetFullscreenRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowSetFullscreenRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t fullscreen = req.fullscreen;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, fullscreen]() {
                receiver->set_fullscreen(receiver, cefArg<decltype(receiver->set_fullscreen), 1>(fullscreen));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowIsMaximizedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowIsMaximizedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_maximized(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::WindowIsMaximizedResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowIsMinimizedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowIsMinimizedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_minimized(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::WindowIsMinimizedResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowIsFullscreenRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowIsFullscreenRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->is_fullscreen(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::WindowIsFullscreenResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowGetFocusedViewRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowGetFocusedViewRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_focused_view(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::WindowGetFocusedViewResponse resp;
                    resp.result = insertOrRelease(&tables::view, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowSetTitleRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowSetTitleRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::string title = req.title;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, title]() {
                ScopedCefString title_cef(title);
                receiver->set_title(receiver, title_cef.get());
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowGetTitleRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowGetTitleRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_title(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::WindowGetTitleResponse resp;
                    resp.result = ScopedCefString::take(rawResult).toUtf8();
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowSetWindowIconRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowSetWindowIconRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t image = req.image;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, image]() {
                cef_image_t* image_ptr = image != 0 ? tables::image.retain(image) : nullptr;
                receiver->set_window_icon(receiver, image_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (image_ptr) {
                    auto* image_base = reinterpret_cast<cef_base_ref_counted_t*>(image_ptr);
                    image_base->release(image_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowGetWindowIconRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowGetWindowIconRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_window_icon(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::WindowGetWindowIconResponse resp;
                    resp.result = insertOrRelease(&tables::image, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowSetWindowAppIconRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowSetWindowAppIconRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t image = req.image;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, image]() {
                cef_image_t* image_ptr = image != 0 ? tables::image.retain(image) : nullptr;
                receiver->set_window_app_icon(receiver, image_ptr);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (image_ptr) {
                    auto* image_base = reinterpret_cast<cef_base_ref_counted_t*>(image_ptr);
                    image_base->release(image_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowGetWindowAppIconRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowGetWindowAppIconRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_window_app_icon(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::WindowGetWindowAppIconResponse resp;
                    resp.result = insertOrRelease(&tables::image, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowAddOverlayViewRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowAddOverlayViewRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t view = req.view;
            std::int32_t dockingMode = req.dockingMode;
            std::int32_t canActivate = req.canActivate;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, view, dockingMode, canActivate]() {
                cef_view_t* view_ptr = view != 0 ? tables::view.retain(view) : nullptr;
                auto rawResult = receiver->add_overlay_view(receiver, view_ptr, cefArg<decltype(receiver->add_overlay_view), 2>(dockingMode), cefArg<decltype(receiver->add_overlay_view), 3>(canActivate));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (view_ptr) {
                    auto* view_base = reinterpret_cast<cef_base_ref_counted_t*>(view_ptr);
                    view_base->release(view_base);
                }
                if (ipc) {
                    gen::WindowAddOverlayViewResponse resp;
                    resp.result = insertOrRelease(&tables::overlayController, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowShowMenuRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowShowMenuRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t menuModel = req.menuModel;
            gen::Point screenPoint = std::move(req.screenPoint);
            std::int32_t anchorPosition = req.anchorPosition;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, menuModel, screenPoint, anchorPosition]() {
                cef_menu_model_t* menuModel_ptr = menuModel != 0 ? tables::menuModel.retain(menuModel) : nullptr;
                cef_point_t screenPoint_native{};
                screenPoint_native.x = static_cast<decltype(screenPoint_native.x)>(screenPoint.x);
                screenPoint_native.y = static_cast<decltype(screenPoint_native.y)>(screenPoint.y);
                receiver->show_menu(receiver, menuModel_ptr, &screenPoint_native, cefArg<decltype(receiver->show_menu), 3>(anchorPosition));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (menuModel_ptr) {
                    auto* menuModel_base = reinterpret_cast<cef_base_ref_counted_t*>(menuModel_ptr);
                    menuModel_base->release(menuModel_base);
                }
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowCancelMenuRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowCancelMenuRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->cancel_menu(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowGetDisplayRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowGetDisplayRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_display(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::WindowGetDisplayResponse resp;
                    resp.result = insertOrRelease(&tables::display, rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowGetClientAreaBoundsInScreenRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowGetClientAreaBoundsInScreenRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                cef_rect_t rawResult = receiver->get_client_area_bounds_in_screen(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::WindowGetClientAreaBoundsInScreenResponse resp;
                resp.result.x = static_cast<decltype(resp.result.x)>(rawResult.x);
                resp.result.y = static_cast<decltype(resp.result.y)>(rawResult.y);
                resp.result.width = static_cast<decltype(resp.result.width)>(rawResult.width);
                resp.result.height = static_cast<decltype(resp.result.height)>(rawResult.height);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowGetWindowHandleRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowGetWindowHandleRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_window_handle(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::WindowGetWindowHandleResponse resp;
                    resp.result = static_cast<std::int64_t>((std::uintptr_t) rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowSendKeyPressRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowSendKeyPressRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t keyCode = req.keyCode;
            std::int32_t eventFlags = req.eventFlags;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, keyCode, eventFlags]() {
                receiver->send_key_press(receiver, cefArg<decltype(receiver->send_key_press), 1>(keyCode), cefArg<decltype(receiver->send_key_press), 2>(eventFlags));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowSendMouseMoveRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowSendMouseMoveRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t screenX = req.screenX;
            std::int32_t screenY = req.screenY;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, screenX, screenY]() {
                receiver->send_mouse_move(receiver, cefArg<decltype(receiver->send_mouse_move), 1>(screenX), cefArg<decltype(receiver->send_mouse_move), 2>(screenY));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowSendMouseEventsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowSendMouseEventsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t button = req.button;
            std::int32_t mouseDown = req.mouseDown;
            std::int32_t mouseUp = req.mouseUp;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, button, mouseDown, mouseUp]() {
                receiver->send_mouse_events(receiver, cefArg<decltype(receiver->send_mouse_events), 1>(button), cefArg<decltype(receiver->send_mouse_events), 2>(mouseDown), cefArg<decltype(receiver->send_mouse_events), 3>(mouseUp));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowSetAcceleratorRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowSetAcceleratorRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t commandId = req.commandId;
            std::int32_t keyCode = req.keyCode;
            std::int32_t shiftPressed = req.shiftPressed;
            std::int32_t ctrlPressed = req.ctrlPressed;
            std::int32_t altPressed = req.altPressed;
            std::int32_t highPriority = req.highPriority;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, commandId, keyCode, shiftPressed, ctrlPressed, altPressed, highPriority]() {
                receiver->set_accelerator(receiver, cefArg<decltype(receiver->set_accelerator), 1>(commandId), cefArg<decltype(receiver->set_accelerator), 2>(keyCode), cefArg<decltype(receiver->set_accelerator), 3>(shiftPressed), cefArg<decltype(receiver->set_accelerator), 4>(ctrlPressed), cefArg<decltype(receiver->set_accelerator), 5>(altPressed), cefArg<decltype(receiver->set_accelerator), 6>(highPriority));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowRemoveAcceleratorRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowRemoveAcceleratorRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t commandId = req.commandId;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, commandId]() {
                receiver->remove_accelerator(receiver, cefArg<decltype(receiver->remove_accelerator), 1>(commandId));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowRemoveAllAcceleratorsRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowRemoveAllAcceleratorsRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->remove_all_accelerators(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowSetThemeColorRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowSetThemeColorRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            std::int32_t colorId = req.colorId;
            std::int32_t color = req.color;
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId, colorId, color]() {
                receiver->set_theme_color(receiver, cefArg<decltype(receiver->set_theme_color), 1>(colorId), cefArg<decltype(receiver->set_theme_color), 2>(color));
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowThemeChangedRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowThemeChangedRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                receiver->theme_changed(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId, nullptr, 0);
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case gen::WindowGetRuntimeStyleRequest::kMessageId: {
            auto* ipc = ctx.ipc;
            const std::int32_t corrId = h.corrId;
            const std::int32_t msgId = h.messageId;
            auto req = gen::WindowGetRuntimeStyleRequest::decode(payload.data(), payload.size());
            auto* receiver = tables::window.retain(req.self);
            if (!receiver) {
                static const std::uint8_t kReceiverGonePayload[8] = {0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00};
                if (ipc) ipc->send(cef4j::ipc::Kind::Error, 0, corrId, msgId,
                                   kReceiverGonePayload, sizeof(kReceiverGonePayload));
                return true;
            }
            auto* uiTask = new LambdaTask([receiver, ipc, corrId, msgId]() {
                auto rawResult = receiver->get_runtime_style(receiver);
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                if (ipc) {
                    gen::WindowGetRuntimeStyleResponse resp;
                    resp.result = static_cast<std::int32_t>(rawResult);
                    std::vector<std::uint8_t> respPayload(resp.encodedSize());
                    resp.encodeInto(respPayload.data());
                    ipc->send(cef4j::ipc::Kind::Response, 0, corrId, msgId,
                              respPayload.data(), respPayload.size());
                }
            });
            if (!postUiTask(uiTask)) {
                auto* base = reinterpret_cast<cef_base_ref_counted_t*>(receiver);
                base->release(base);
                sendTaskRejected(ipc, corrId, msgId);
            }
            return true;
        }
        case 1668885411: // DomDocumentGetTypeRequest (cef_domdocument_t::get_type)
        case 1605205650: // DomDocumentGetDocumentRequest (cef_domdocument_t::get_document)
        case 1104414347: // DomDocumentGetBodyRequest (cef_domdocument_t::get_body)
        case 880981426: // DomDocumentGetHeadRequest (cef_domdocument_t::get_head)
        case 742525445: // DomDocumentGetTitleRequest (cef_domdocument_t::get_title)
        case 308286242: // DomDocumentGetElementByIdRequest (cef_domdocument_t::get_element_by_id)
        case 742182637: // DomDocumentGetFocusedNodeRequest (cef_domdocument_t::get_focused_node)
        case 1858159100: // DomDocumentHasSelectionRequest (cef_domdocument_t::has_selection)
        case 977002428: // DomDocumentGetSelectionStartOffsetRequest (cef_domdocument_t::get_selection_start_offset)
        case 1869821225: // DomDocumentGetSelectionEndOffsetRequest (cef_domdocument_t::get_selection_end_offset)
        case 1913336881: // DomDocumentGetSelectionAsMarkupRequest (cef_domdocument_t::get_selection_as_markup)
        case 270581756: // DomDocumentGetSelectionAsTextRequest (cef_domdocument_t::get_selection_as_text)
        case 315403025: // DomDocumentGetBaseUrlRequest (cef_domdocument_t::get_base_url)
        case 2269451: // DomDocumentGetCompleteUrlRequest (cef_domdocument_t::get_complete_url)
        case 824869216: // DomNodeGetTypeRequest (cef_domnode_t::get_type)
        case 222961493: // DomNodeIsTextRequest (cef_domnode_t::is_text)
        case 1451283750: // DomNodeIsElementRequest (cef_domnode_t::is_element)
        case 1936990561: // DomNodeIsEditableRequest (cef_domnode_t::is_editable)
        case 1750785873: // DomNodeIsFormControlElementRequest (cef_domnode_t::is_form_control_element)
        case 1515749719: // DomNodeGetFormControlElementTypeRequest (cef_domnode_t::get_form_control_element_type)
        case 118020072: // DomNodeIsSameRequest (cef_domnode_t::is_same)
        case 990592577: // DomNodeGetNameRequest (cef_domnode_t::get_name)
        case 954965703: // DomNodeGetValueRequest (cef_domnode_t::get_value)
        case 1052167400: // DomNodeSetValueRequest (cef_domnode_t::set_value)
        case 1631466775: // DomNodeGetAsMarkupRequest (cef_domnode_t::get_as_markup)
        case 560089365: // DomNodeGetDocumentRequest (cef_domnode_t::get_document)
        case 135714567: // DomNodeGetParentRequest (cef_domnode_t::get_parent)
        case 1708689931: // DomNodeGetPreviousSiblingRequest (cef_domnode_t::get_previous_sibling)
        case 1732474249: // DomNodeGetNextSiblingRequest (cef_domnode_t::get_next_sibling)
        case 563216232: // DomNodeHasChildrenRequest (cef_domnode_t::has_children)
        case 1300862319: // DomNodeGetFirstChildRequest (cef_domnode_t::get_first_child)
        case 2086051350: // DomNodeGetLastChildRequest (cef_domnode_t::get_last_child)
        case 2082915339: // DomNodeGetElementTagNameRequest (cef_domnode_t::get_element_tag_name)
        case 1534633747: // DomNodeHasElementAttributesRequest (cef_domnode_t::has_element_attributes)
        case 191218072: // DomNodeHasElementAttributeRequest (cef_domnode_t::has_element_attribute)
        case 1684904725: // DomNodeGetElementAttributeRequest (cef_domnode_t::get_element_attribute)
        case 1658979847: // DomNodeSetElementAttributeRequest (cef_domnode_t::set_element_attribute)
        case 1394658863: // DomNodeGetElementInnerTextRequest (cef_domnode_t::get_element_inner_text)
        case 2082725108: // DomNodeGetElementBoundsRequest (cef_domnode_t::get_element_bounds)
        case 1377621369: // V8ContextGetTaskRunnerRequest (cef_v8_context_t::get_task_runner)
        case 862541885: // V8ContextIsValidRequest (cef_v8_context_t::is_valid)
        case 1141297699: // V8ContextGetBrowserRequest (cef_v8_context_t::get_browser)
        case 1021611346: // V8ContextGetFrameRequest (cef_v8_context_t::get_frame)
        case 1021233512: // V8ContextGetGlobalRequest (cef_v8_context_t::get_global)
        case 785348964: // V8ContextEnterRequest (cef_v8_context_t::enter)
        case 332503754: // V8ContextExitRequest (cef_v8_context_t::exit)
        case 1638294176: // V8ContextIsSameRequest (cef_v8_context_t::is_same)
        case 1059909765: // V8ExceptionGetMessageRequest (cef_v8_exception_t::get_message)
        case 1746462116: // V8ExceptionGetSourceLineRequest (cef_v8_exception_t::get_source_line)
        case 454395428: // V8ExceptionGetScriptResourceNameRequest (cef_v8_exception_t::get_script_resource_name)
        case 1571065208: // V8ExceptionGetLineNumberRequest (cef_v8_exception_t::get_line_number)
        case 628810830: // V8ExceptionGetStartPositionRequest (cef_v8_exception_t::get_start_position)
        case 294421112: // V8ExceptionGetEndPositionRequest (cef_v8_exception_t::get_end_position)
        case 887414417: // V8ExceptionGetStartColumnRequest (cef_v8_exception_t::get_start_column)
        case 1297367184: // V8ExceptionGetEndColumnRequest (cef_v8_exception_t::get_end_column)
        case 1819148377: // V8BackingStoreByteLengthRequest (cef_v8_backing_store_t::byte_length)
        case 1093023320: // V8BackingStoreIsValidRequest (cef_v8_backing_store_t::is_valid)
        case 1442154556: // V8ValueIsValidRequest (cef_v8_value_t::is_valid)
        case 481259852: // V8ValueIsUndefinedRequest (cef_v8_value_t::is_undefined)
        case 1074977225: // V8ValueIsNullRequest (cef_v8_value_t::is_null)
        case 883354597: // V8ValueIsBoolRequest (cef_v8_value_t::is_bool)
        case 2005424993: // V8ValueIsIntRequest (cef_v8_value_t::is_int)
        case 1398910508: // V8ValueIsUintRequest (cef_v8_value_t::is_uint)
        case 1991209794: // V8ValueIsDoubleRequest (cef_v8_value_t::is_double)
        case 204997686: // V8ValueIsDateRequest (cef_v8_value_t::is_date)
        case 1051897356: // V8ValueIsStringRequest (cef_v8_value_t::is_string)
        case 1023806905: // V8ValueIsObjectRequest (cef_v8_value_t::is_object)
        case 1984793671: // V8ValueIsArrayRequest (cef_v8_value_t::is_array)
        case 1214867833: // V8ValueIsArrayBufferRequest (cef_v8_value_t::is_array_buffer)
        case 299879852: // V8ValueIsFunctionRequest (cef_v8_value_t::is_function)
        case 1941079555: // V8ValueIsPromiseRequest (cef_v8_value_t::is_promise)
        case 2130374950: // V8ValueIsSameRequest (cef_v8_value_t::is_same)
        case 162718459: // V8ValueGetBoolValueRequest (cef_v8_value_t::get_bool_value)
        case 682208989: // V8ValueGetIntValueRequest (cef_v8_value_t::get_int_value)
        case 798005445: // V8ValueGetUintValueRequest (cef_v8_value_t::get_uint_value)
        case 1318956872: // V8ValueGetDateValueRequest (cef_v8_value_t::get_date_value)
        case 1945199507: // V8ValueGetStringValueRequest (cef_v8_value_t::get_string_value)
        case 452332057: // V8ValueIsUserCreatedRequest (cef_v8_value_t::is_user_created)
        case 1827705764: // V8ValueHasExceptionRequest (cef_v8_value_t::has_exception)
        case 1443485932: // V8ValueGetExceptionRequest (cef_v8_value_t::get_exception)
        case 2100602782: // V8ValueClearExceptionRequest (cef_v8_value_t::clear_exception)
        case 1229557317: // V8ValueWillRethrowExceptionsRequest (cef_v8_value_t::will_rethrow_exceptions)
        case 412331163: // V8ValueSetRethrowExceptionsRequest (cef_v8_value_t::set_rethrow_exceptions)
        case 62758253: // V8ValueHasValueBykeyRequest (cef_v8_value_t::has_value_bykey)
        case 1991034654: // V8ValueHasValueByindexRequest (cef_v8_value_t::has_value_byindex)
        case 197502653: // V8ValueDeleteValueBykeyRequest (cef_v8_value_t::delete_value_bykey)
        case 928355063: // V8ValueDeleteValueByindexRequest (cef_v8_value_t::delete_value_byindex)
        case 633651366: // V8ValueGetValueBykeyRequest (cef_v8_value_t::get_value_bykey)
        case 844101139: // V8ValueGetValueByindexRequest (cef_v8_value_t::get_value_byindex)
        case 714554987: // V8ValueSetValueBykeyRequest (cef_v8_value_t::set_value_bykey)
        case 1448266441: // V8ValueSetValueByindexRequest (cef_v8_value_t::set_value_byindex)
        case 1660624: // V8ValueSetValueByaccessorRequest (cef_v8_value_t::set_value_byaccessor)
        case 341739378: // V8ValueGetKeysRequest (cef_v8_value_t::get_keys)
        case 1106946116: // V8ValueSetUserDataRequest (cef_v8_value_t::set_user_data)
        case 1299029892: // V8ValueGetUserDataRequest (cef_v8_value_t::get_user_data)
        case 407986733: // V8ValueGetExternallyAllocatedMemoryRequest (cef_v8_value_t::get_externally_allocated_memory)
        case 1723692752: // V8ValueAdjustExternallyAllocatedMemoryRequest (cef_v8_value_t::adjust_externally_allocated_memory)
        case 689813683: // V8ValueGetArrayLengthRequest (cef_v8_value_t::get_array_length)
        case 676233885: // V8ValueGetArrayBufferReleaseCallbackRequest (cef_v8_value_t::get_array_buffer_release_callback)
        case 1554879041: // V8ValueNeuterArrayBufferRequest (cef_v8_value_t::neuter_array_buffer)
        case 379623011: // V8ValueGetArrayBufferByteLengthRequest (cef_v8_value_t::get_array_buffer_byte_length)
        case 458533964: // V8ValueGetFunctionNameRequest (cef_v8_value_t::get_function_name)
        case 1220020744: // V8ValueGetFunctionHandlerRequest (cef_v8_value_t::get_function_handler)
        case 433109875: // V8ValueResolvePromiseRequest (cef_v8_value_t::resolve_promise)
        case 2005927269: // V8ValueRejectPromiseRequest (cef_v8_value_t::reject_promise)
        case 677250727: // V8StackTraceIsValidRequest (cef_v8_stack_trace_t::is_valid)
        case 1897498147: // V8StackTraceGetFrameCountRequest (cef_v8_stack_trace_t::get_frame_count)
        case 2121783969: // V8StackTraceGetFrameRequest (cef_v8_stack_trace_t::get_frame)
        case 1911959162: // V8StackFrameIsValidRequest (cef_v8_stack_frame_t::is_valid)
        case 842305600: // V8StackFrameGetScriptNameRequest (cef_v8_stack_frame_t::get_script_name)
        case 1519099453: // V8StackFrameGetScriptNameOrSourceUrlRequest (cef_v8_stack_frame_t::get_script_name_or_source_url)
        case 1164021933: // V8StackFrameGetFunctionNameRequest (cef_v8_stack_frame_t::get_function_name)
        case 1857499267: // V8StackFrameGetLineNumberRequest (cef_v8_stack_frame_t::get_line_number)
        case 652036581: // V8StackFrameGetColumnRequest (cef_v8_stack_frame_t::get_column)
        case 918708096: // V8StackFrameIsEvalRequest (cef_v8_stack_frame_t::is_eval)
        case 475740060: // V8StackFrameIsConstructorRequest (cef_v8_stack_frame_t::is_constructor)
        case 24: // hand-written renderer-relay message
            return relayToRenderer(ctx, h, payload);
        default:
            return false;
    }
}

} // namespace net_kurobako_cef4j_ipc_protocol_gen_dispatcher

#endif // CEF4J_IPC_GEN_DISPATCHER_H_
