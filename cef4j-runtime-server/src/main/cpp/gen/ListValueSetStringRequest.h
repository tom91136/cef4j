// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
#ifndef NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_LIST_VALUE_SET_STRING_REQUEST_H_
#define NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_LIST_VALUE_SET_STRING_REQUEST_H_

#include <cstdint>
#include <cstring>
#include <stdexcept>
#include <string>
#include <vector>


namespace net_kurobako_cef4j_ipc_protocol_gen {

struct ListValueSetStringRequest {
    static constexpr int32_t kMessageId = 942448174;
    static constexpr std::size_t kMaxFieldBytes = 64U * 1024U * 1024U;
    static constexpr std::size_t kMaxCollectionItems = 1000000U;

    std::int32_t self = 0;
    std::int64_t index = 0;
    std::string value;

    /** Number of payload bytes that {@link encodeInto} writes for this instance. */
    std::size_t encodedSize() const noexcept {
        return 16 + value.size();
    }

    /** Writes exactly {@link encodedSize} bytes starting at {@code dst}. */
    void encodeInto(std::uint8_t* dst) const noexcept {
        std::size_t pos = 0;
        {
            std::uint32_t v = static_cast<std::uint32_t>(self);
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
        {
            std::uint64_t v = static_cast<std::uint64_t>(index);
            for (int i = 0; i < 8; ++i) dst[pos + i] = static_cast<std::uint8_t>((v >> (i * 8)) & 0xFF);
            pos += 8;
        }
        {
            std::uint32_t v = static_cast<std::uint32_t>(value.size());
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
        std::memcpy(dst + pos, value.data(), value.size());
        pos += value.size();
    }

    /** Reads a ListValueSetStringRequest from a payload starting at {@code src}; {@code len} bounds the buffer. */
    static ListValueSetStringRequest decode(const std::uint8_t* src, std::size_t len) {
        ListValueSetStringRequest out;
        std::size_t pos = 0;
        [[maybe_unused]] auto requireAvailable = [&](std::size_t count) {
            if (pos > len || count > len - pos)
                throw std::invalid_argument("truncated ListValueSetStringRequest payload");
        };
        requireAvailable(4);
        out.self = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
        pos += 4;
        requireAvailable(8);
        out.index = static_cast<std::int64_t>(static_cast<std::uint64_t>(src[pos]) | (static_cast<std::uint64_t>(src[pos + 1]) << 8) | (static_cast<std::uint64_t>(src[pos + 2]) << 16) | (static_cast<std::uint64_t>(src[pos + 3]) << 24) | (static_cast<std::uint64_t>(src[pos + 4]) << 32) | (static_cast<std::uint64_t>(src[pos + 5]) << 40) | (static_cast<std::uint64_t>(src[pos + 6]) << 48) | (static_cast<std::uint64_t>(src[pos + 7]) << 56));
        pos += 8;
        {
            requireAvailable(4);
            std::int32_t n = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
            pos += 4;
            if (n < 0) throw std::invalid_argument("negative length for value");
            if (static_cast<std::size_t>(n) > kMaxFieldBytes) throw std::invalid_argument("oversized length for value");
            requireAvailable(static_cast<std::size_t>(n));
            out.value.assign(reinterpret_cast<const char*>(src + pos), static_cast<std::size_t>(n));
            pos += static_cast<std::size_t>(n);
        }
        if (pos != len)
            throw std::invalid_argument("trailing ListValueSetStringRequest payload");
        return out;
    }
};

} // namespace net_kurobako_cef4j_ipc_protocol_gen

#endif // NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_LIST_VALUE_SET_STRING_REQUEST_H_
