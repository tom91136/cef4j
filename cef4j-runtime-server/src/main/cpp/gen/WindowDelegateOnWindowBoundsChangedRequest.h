// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
#ifndef NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_WINDOW_DELEGATE_ON_WINDOW_BOUNDS_CHANGED_REQUEST_H_
#define NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_WINDOW_DELEGATE_ON_WINDOW_BOUNDS_CHANGED_REQUEST_H_

#include <cstdint>
#include <cstring>
#include <stdexcept>
#include <string>
#include <vector>
#include "Rect.h"

namespace net_kurobako_cef4j_ipc_protocol_gen {

struct WindowDelegateOnWindowBoundsChangedRequest {
    static constexpr int32_t kMessageId = 1577236236;

    std::int32_t self = 0;
    std::int32_t window = 0;
    Rect newBounds;

    /** Number of payload bytes that {@link encodeInto} writes for this instance. */
    std::size_t encodedSize() const noexcept {
        return 8 + newBounds.encodedSize();
    }

    /** Writes exactly {@link encodedSize} bytes starting at {@code dst}. */
    void encodeInto(std::uint8_t* dst) const noexcept {
        std::size_t pos = 0;
        {
            std::uint32_t v = static_cast<std::uint32_t>(self);
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
        {
            std::uint32_t v = static_cast<std::uint32_t>(window);
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
        newBounds.encodeInto(dst + pos);
        pos += newBounds.encodedSize();
    }

    /** Reads a WindowDelegateOnWindowBoundsChangedRequest from a payload starting at {@code src}; {@code len} bounds the buffer. */
    static WindowDelegateOnWindowBoundsChangedRequest decode(const std::uint8_t* src, std::size_t len) {
        WindowDelegateOnWindowBoundsChangedRequest out;
        std::size_t pos = 0;
        [[maybe_unused]] auto requireAvailable = [&](std::size_t count) {
            if (pos > len || count > len - pos)
                throw std::invalid_argument("truncated WindowDelegateOnWindowBoundsChangedRequest payload");
        };
        requireAvailable(4);
        out.self = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
        pos += 4;
        requireAvailable(4);
        out.window = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
        pos += 4;
        out.newBounds = Rect::decode(src + pos, len - pos);
        requireAvailable(out.newBounds.encodedSize());
        pos += out.newBounds.encodedSize();
        return out;
    }
};

} // namespace net_kurobako_cef4j_ipc_protocol_gen

#endif // NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_WINDOW_DELEGATE_ON_WINDOW_BOUNDS_CHANGED_REQUEST_H_
