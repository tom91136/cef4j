// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
#ifndef NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_BROWSER_PROCESS_HANDLER_ON_ALREADY_RUNNING_APP_RELAUNCH_EVENT_H_
#define NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_BROWSER_PROCESS_HANDLER_ON_ALREADY_RUNNING_APP_RELAUNCH_EVENT_H_

#include <cstdint>
#include <cstring>
#include <stdexcept>
#include <string>
#include <vector>


namespace net_kurobako_cef4j_ipc_protocol_gen {

struct BrowserProcessHandlerOnAlreadyRunningAppRelaunchEvent {
    static constexpr int32_t kMessageId = 1259220936;

    std::int32_t commandLine = 0;
    std::string currentDirectory;

    /** Number of payload bytes that {@link encodeInto} writes for this instance. */
    std::size_t encodedSize() const noexcept {
        return 8 + currentDirectory.size();
    }

    /** Writes exactly {@link encodedSize} bytes starting at {@code dst}. */
    void encodeInto(std::uint8_t* dst) const noexcept {
        std::size_t pos = 0;
        {
            std::uint32_t v = static_cast<std::uint32_t>(commandLine);
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
        {
            std::uint32_t v = static_cast<std::uint32_t>(currentDirectory.size());
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
        std::memcpy(dst + pos, currentDirectory.data(), currentDirectory.size());
        pos += currentDirectory.size();
    }

    /** Reads a BrowserProcessHandlerOnAlreadyRunningAppRelaunchEvent from a payload starting at {@code src}; {@code len} bounds the buffer. */
    static BrowserProcessHandlerOnAlreadyRunningAppRelaunchEvent decode(const std::uint8_t* src, std::size_t len) {
        BrowserProcessHandlerOnAlreadyRunningAppRelaunchEvent out;
        std::size_t pos = 0;
        [[maybe_unused]] auto requireAvailable = [&](std::size_t count) {
            if (pos > len || count > len - pos)
                throw std::invalid_argument("truncated BrowserProcessHandlerOnAlreadyRunningAppRelaunchEvent payload");
        };
        requireAvailable(4);
        out.commandLine = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
        pos += 4;
        {
            requireAvailable(4);
            std::int32_t n = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
            pos += 4;
            if (n < 0) throw std::invalid_argument("negative length for currentDirectory");
            requireAvailable(static_cast<std::size_t>(n));
            out.currentDirectory.assign(reinterpret_cast<const char*>(src + pos), static_cast<std::size_t>(n));
            pos += static_cast<std::size_t>(n);
        }
        return out;
    }
};

} // namespace net_kurobako_cef4j_ipc_protocol_gen

#endif // NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_BROWSER_PROCESS_HANDLER_ON_ALREADY_RUNNING_APP_RELAUNCH_EVENT_H_
