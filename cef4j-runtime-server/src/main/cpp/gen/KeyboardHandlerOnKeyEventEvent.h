// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
#ifndef NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_KEYBOARD_HANDLER_ON_KEY_EVENT_EVENT_H_
#define NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_KEYBOARD_HANDLER_ON_KEY_EVENT_EVENT_H_

#include <cstdint>
#include <cstring>
#include <stdexcept>
#include <string>
#include <vector>
#include "KeyEvent.h"

namespace net_kurobako_cef4j_ipc_protocol_gen {

struct KeyboardHandlerOnKeyEventEvent {
    static constexpr int32_t kMessageId = 1887878341;
    static constexpr std::size_t kMaxFieldBytes = 64U * 1024U * 1024U;
    static constexpr std::size_t kMaxCollectionItems = 1000000U;

    std::int32_t browser = 0;
    KeyEvent event;
    std::int64_t osEvent = 0;

    /** Number of payload bytes that {@link encodeInto} writes for this instance. */
    std::size_t encodedSize() const noexcept {
        return 12 + event.encodedSize();
    }

    /** Writes exactly {@link encodedSize} bytes starting at {@code dst}. */
    void encodeInto(std::uint8_t* dst) const noexcept {
        std::size_t pos = 0;
        {
            std::uint32_t v = static_cast<std::uint32_t>(browser);
            dst[pos + 0] = static_cast<std::uint8_t>(v & 0xFF);
            dst[pos + 1] = static_cast<std::uint8_t>((v >> 8) & 0xFF);
            dst[pos + 2] = static_cast<std::uint8_t>((v >> 16) & 0xFF);
            dst[pos + 3] = static_cast<std::uint8_t>((v >> 24) & 0xFF);
            pos += 4;
        }
        event.encodeInto(dst + pos);
        pos += event.encodedSize();
        {
            std::uint64_t v = static_cast<std::uint64_t>(osEvent);
            for (int i = 0; i < 8; ++i) dst[pos + i] = static_cast<std::uint8_t>((v >> (i * 8)) & 0xFF);
            pos += 8;
        }
    }

    /** Reads a KeyboardHandlerOnKeyEventEvent from a payload starting at {@code src}; {@code len} bounds the buffer. */
    static KeyboardHandlerOnKeyEventEvent decode(const std::uint8_t* src, std::size_t len) {
        KeyboardHandlerOnKeyEventEvent out;
        std::size_t pos = 0;
        [[maybe_unused]] auto requireAvailable = [&](std::size_t count) {
            if (pos > len || count > len - pos)
                throw std::invalid_argument("truncated KeyboardHandlerOnKeyEventEvent payload");
        };
        requireAvailable(4);
        out.browser = static_cast<std::int32_t>(static_cast<std::uint32_t>(src[pos]) | (static_cast<std::uint32_t>(src[pos + 1]) << 8) | (static_cast<std::uint32_t>(src[pos + 2]) << 16) | (static_cast<std::uint32_t>(src[pos + 3]) << 24));
        pos += 4;
        out.event = KeyEvent::decode(src + pos, len - pos);
        requireAvailable(out.event.encodedSize());
        pos += out.event.encodedSize();
        requireAvailable(8);
        out.osEvent = static_cast<std::int64_t>(static_cast<std::uint64_t>(src[pos]) | (static_cast<std::uint64_t>(src[pos + 1]) << 8) | (static_cast<std::uint64_t>(src[pos + 2]) << 16) | (static_cast<std::uint64_t>(src[pos + 3]) << 24) | (static_cast<std::uint64_t>(src[pos + 4]) << 32) | (static_cast<std::uint64_t>(src[pos + 5]) << 40) | (static_cast<std::uint64_t>(src[pos + 6]) << 48) | (static_cast<std::uint64_t>(src[pos + 7]) << 56));
        pos += 8;
        if (pos != len)
            throw std::invalid_argument("trailing KeyboardHandlerOnKeyEventEvent payload");
        return out;
    }
};

} // namespace net_kurobako_cef4j_ipc_protocol_gen

#endif // NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_KEYBOARD_HANDLER_ON_KEY_EVENT_EVENT_H_
