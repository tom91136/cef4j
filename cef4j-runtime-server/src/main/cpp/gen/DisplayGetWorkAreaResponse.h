// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
#ifndef NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_DISPLAY_GET_WORK_AREA_RESPONSE_H_
#define NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_DISPLAY_GET_WORK_AREA_RESPONSE_H_

#include <cstdint>
#include <cstring>
#include <stdexcept>
#include <string>
#include <vector>
#include "Rect.h"

namespace net_kurobako_cef4j_ipc_protocol_gen {

struct DisplayGetWorkAreaResponse {
    static constexpr int32_t kMessageId = 583322782;

    Rect result;

    /** Number of payload bytes that {@link encodeInto} writes for this instance. */
    std::size_t encodedSize() const noexcept {
        return 0 + result.encodedSize();
    }

    /** Writes exactly {@link encodedSize} bytes starting at {@code dst}. */
    void encodeInto(std::uint8_t* dst) const noexcept {
        std::size_t pos = 0;
        result.encodeInto(dst + pos);
        pos += result.encodedSize();
    }

    /** Reads a DisplayGetWorkAreaResponse from a payload starting at {@code src}; {@code len} bounds the buffer. */
    static DisplayGetWorkAreaResponse decode(const std::uint8_t* src, std::size_t len) {
        DisplayGetWorkAreaResponse out;
        std::size_t pos = 0;
        [[maybe_unused]] auto requireAvailable = [&](std::size_t count) {
            if (pos > len || count > len - pos)
                throw std::invalid_argument("truncated DisplayGetWorkAreaResponse payload");
        };
        out.result = Rect::decode(src + pos, len - pos);
        requireAvailable(out.result.encodedSize());
        pos += out.result.encodedSize();
        return out;
    }
};

} // namespace net_kurobako_cef4j_ipc_protocol_gen

#endif // NET_KUROBAKO_CEF4J_IPC_PROTOCOL_GEN_DISPLAY_GET_WORK_AREA_RESPONSE_H_
