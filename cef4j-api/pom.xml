<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>net.kurobako.cef4j</groupId>
        <artifactId>cef4j-parent</artifactId>
        <version>146.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>cef4j-api</artifactId>
    <packaging>jar</packaging>
    <name>cef4j-api</name>
    <description>Java 8+ API for CEF OSR bindings</description>

    <dependencies>
        <dependency>
            <groupId>com.google.code.findbugs</groupId>
            <artifactId>jsr305</artifactId>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-simple</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>net.kurobako.cef4j</groupId>
            <artifactId>cef4j-platform</artifactId>
            <version>${project.version}</version>
            <classifier>${cef.platform}</classifier>
            <optional>true</optional>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <systemPropertyVariables>
                        <cef4j.libcef.dir>${cef.dist.dir}/Release</cef4j.libcef.dir>
                    </systemPropertyVariables>
                    <!-- Exclude multi-threaded-message-loop tests from the default fork;
                         they initialise CEF with multiThreadedMessageLoop=1 which is
                         incompatible with the external-message-pump mode used by the
                         other test classes (CEF cannot be re-initialised). -->
                    <excludes>
                        <exclude>**/CefScriptEngineMultiThreadedTest.java</exclude>
                        <exclude>**/CefSchemeHandlerTest.java</exclude>
                    </excludes>
                </configuration>
                <executions>
                    <execution>
                        <id>scheme-handler-tests</id>
                        <goals><goal>test</goal></goals>
                        <configuration>
                            <excludes combine.self="override"/>
                            <includes>
                                <include>**/CefSchemeHandlerTest.java</include>
                            </includes>
                        </configuration>
                    </execution>
                    <execution>
                        <id>multi-threaded-message-loop-tests</id>
                        <goals><goal>test</goal></goals>
                        <configuration>
                            <excludes combine.self="override"/>
                            <includes>
                                <include>**/CefScriptEngineMultiThreadedTest.java</include>
                            </includes>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <executions>
                    <execution>
                        <id>verify-javadocs</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
