<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.2.0 https://maven.apache.org/xsd/assembly-2.2.0.xsd">
    <id>${cef.classifier}</id>
    <formats>
        <format>jar</format>
    </formats>
    <includeBaseDirectory>false</includeBaseDirectory>

    <fileSets>
        <!-- CEF itself is intentionally not published to Maven Central. Consumers
             supply a complete CEF runtime externally or package one as resources
             with cef4j-runtime-packager. -->
        <fileSet>
            <directory>${project.build.directory}/cmake-build</directory>
            <outputDirectory>/native/${cef.platform}</outputDirectory>
            <includes>
                <include>libcef4j.so</include>
                <include>cef4j.dll</include>
                <include>libcef4j.dylib</include>
                <include>cef4j_launcher</include>
                <include>cef4j_launcher.exe</include>
            </includes>
        </fileSet>
    </fileSets>
</assembly>
