// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
package net.kurobako.cef4j.ipc.protocol.gen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.nio.charset.StandardCharsets;
import javax.annotation.Nonnull;
import net.kurobako.cef4j.ipc.session.CefMessageDecoder;
import net.kurobako.cef4j.ipc.session.CefMessageEncoder;
import net.kurobako.cef4j.ipc.session.CefMessageView;
import net.kurobako.cef4j.ipc.session.RemoteHandle;

public final class DisplayHandlerOnConsoleMessageEvent implements CefMessageView, CefMessageEncoder {

    public static final int MESSAGE_ID = 1245975857;

    private final RemoteHandle browser;
    private final int level;
    private final String message;
    private final byte[] messageBytes;
    private final String source;
    private final byte[] sourceBytes;
    private final int line;

    public DisplayHandlerOnConsoleMessageEvent(@Nonnull RemoteHandle browser, int level, @Nonnull String message, @Nonnull String source, int line) {
        this.browser = browser;
        this.level = level;
        this.message = message;
        this.messageBytes = message.getBytes(StandardCharsets.UTF_8);
        this.source = source;
        this.sourceBytes = source.getBytes(StandardCharsets.UTF_8);
        this.line = line;
    }

    @Nonnull
    public RemoteHandle browser() {
        return browser;
    }

    public int level() {
        return level;
    }

    @Nonnull
    public String message() {
        return message;
    }

    @Nonnull
    public String source() {
        return source;
    }

    public int line() {
        return line;
    }

    @Override
    public int messageId() {
        return MESSAGE_ID;
    }

    @Override
    public int encodedSize() {
        return 20 + messageBytes.length + sourceBytes.length;
    }

    @Override
    public void encodeInto(@Nonnull ByteBuffer __dst) {
        ByteOrder originalOrder = __dst.order();
        try {
            __dst.order(ByteOrder.LITTLE_ENDIAN);
            __dst.putInt(browser.id());
            __dst.putInt(level);
            __dst.putInt(messageBytes.length);
            __dst.put(messageBytes);
            __dst.putInt(sourceBytes.length);
            __dst.put(sourceBytes);
            __dst.putInt(line);
        } finally {
            __dst.order(originalOrder);
        }
    }

    public static final CefMessageDecoder<DisplayHandlerOnConsoleMessageEvent> DECODER = payload -> {
        ByteBuffer __buf = payload.duplicate();
        __buf.order(ByteOrder.LITTLE_ENDIAN);
        RemoteHandle browser = new RemoteHandle(__buf.getInt());
        int level = __buf.getInt();
        int messageLen = __buf.getInt();
        byte[] messageBuf = new byte[messageLen];
        __buf.get(messageBuf);
        String message = new String(messageBuf, StandardCharsets.UTF_8);
        int sourceLen = __buf.getInt();
        byte[] sourceBuf = new byte[sourceLen];
        __buf.get(sourceBuf);
        String source = new String(sourceBuf, StandardCharsets.UTF_8);
        int line = __buf.getInt();
        return new DisplayHandlerOnConsoleMessageEvent(browser, level, message, source, line);
    };
}
