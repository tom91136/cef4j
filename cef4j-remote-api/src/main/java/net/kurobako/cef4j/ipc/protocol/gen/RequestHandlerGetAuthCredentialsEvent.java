// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
package net.kurobako.cef4j.ipc.protocol.gen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.nio.charset.StandardCharsets;
import javax.annotation.Nonnull;
import net.kurobako.cef4j.ipc.session.CefMessageDecoder;
import net.kurobako.cef4j.ipc.session.CefMessageEncoder;
import net.kurobako.cef4j.ipc.session.CefMessageView;
import net.kurobako.cef4j.ipc.session.RemoteHandle;

public final class RequestHandlerGetAuthCredentialsEvent implements CefMessageView, CefMessageEncoder {

    public static final int MESSAGE_ID = 326882809;

    private final RemoteHandle browser;
    private final String originUrl;
    private final byte[] originUrlBytes;
    private final int isProxy;
    private final String host;
    private final byte[] hostBytes;
    private final int port;
    private final String realm;
    private final byte[] realmBytes;
    private final String scheme;
    private final byte[] schemeBytes;
    private final RemoteHandle callback;

    public RequestHandlerGetAuthCredentialsEvent(@Nonnull RemoteHandle browser, @Nonnull String originUrl, int isProxy, @Nonnull String host, int port, @Nonnull String realm, @Nonnull String scheme, @Nonnull RemoteHandle callback) {
        this.browser = browser;
        this.originUrl = originUrl;
        this.originUrlBytes = originUrl.getBytes(StandardCharsets.UTF_8);
        this.isProxy = isProxy;
        this.host = host;
        this.hostBytes = host.getBytes(StandardCharsets.UTF_8);
        this.port = port;
        this.realm = realm;
        this.realmBytes = realm.getBytes(StandardCharsets.UTF_8);
        this.scheme = scheme;
        this.schemeBytes = scheme.getBytes(StandardCharsets.UTF_8);
        this.callback = callback;
    }

    @Nonnull
    public RemoteHandle browser() {
        return browser;
    }

    @Nonnull
    public String originUrl() {
        return originUrl;
    }

    public int isProxy() {
        return isProxy;
    }

    @Nonnull
    public String host() {
        return host;
    }

    public int port() {
        return port;
    }

    @Nonnull
    public String realm() {
        return realm;
    }

    @Nonnull
    public String scheme() {
        return scheme;
    }

    @Nonnull
    public RemoteHandle callback() {
        return callback;
    }

    @Override
    public int messageId() {
        return MESSAGE_ID;
    }

    @Override
    public int encodedSize() {
        return 32 + originUrlBytes.length + hostBytes.length + realmBytes.length + schemeBytes.length;
    }

    @Override
    public void encodeInto(@Nonnull ByteBuffer __dst) {
        ByteOrder originalOrder = __dst.order();
        try {
            __dst.order(ByteOrder.LITTLE_ENDIAN);
            __dst.putInt(browser.id());
            __dst.putInt(originUrlBytes.length);
            __dst.put(originUrlBytes);
            __dst.putInt(isProxy);
            __dst.putInt(hostBytes.length);
            __dst.put(hostBytes);
            __dst.putInt(port);
            __dst.putInt(realmBytes.length);
            __dst.put(realmBytes);
            __dst.putInt(schemeBytes.length);
            __dst.put(schemeBytes);
            __dst.putInt(callback.id());
        } finally {
            __dst.order(originalOrder);
        }
    }

    public static final CefMessageDecoder<RequestHandlerGetAuthCredentialsEvent> DECODER = payload -> {
        ByteBuffer __buf = payload.duplicate();
        __buf.order(ByteOrder.LITTLE_ENDIAN);
        RemoteHandle browser = new RemoteHandle(__buf.getInt());
        int originUrlLen = __buf.getInt();
        byte[] originUrlBuf = new byte[originUrlLen];
        __buf.get(originUrlBuf);
        String originUrl = new String(originUrlBuf, StandardCharsets.UTF_8);
        int isProxy = __buf.getInt();
        int hostLen = __buf.getInt();
        byte[] hostBuf = new byte[hostLen];
        __buf.get(hostBuf);
        String host = new String(hostBuf, StandardCharsets.UTF_8);
        int port = __buf.getInt();
        int realmLen = __buf.getInt();
        byte[] realmBuf = new byte[realmLen];
        __buf.get(realmBuf);
        String realm = new String(realmBuf, StandardCharsets.UTF_8);
        int schemeLen = __buf.getInt();
        byte[] schemeBuf = new byte[schemeLen];
        __buf.get(schemeBuf);
        String scheme = new String(schemeBuf, StandardCharsets.UTF_8);
        RemoteHandle callback = new RemoteHandle(__buf.getInt());
        return new RequestHandlerGetAuthCredentialsEvent(browser, originUrl, isProxy, host, port, realm, scheme, callback);
    };
}
