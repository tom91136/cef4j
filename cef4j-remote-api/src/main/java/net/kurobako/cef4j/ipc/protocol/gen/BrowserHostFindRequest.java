// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
package net.kurobako.cef4j.ipc.protocol.gen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.nio.charset.StandardCharsets;
import javax.annotation.Nonnull;
import net.kurobako.cef4j.ipc.session.CefMessageDecoder;
import net.kurobako.cef4j.ipc.session.CefMessageEncoder;
import net.kurobako.cef4j.ipc.session.CefMessageView;
import net.kurobako.cef4j.ipc.session.RemoteHandle;

public final class BrowserHostFindRequest implements CefMessageView, CefMessageEncoder {

    public static final int MESSAGE_ID = 861131453;

    private final RemoteHandle self;
    private final String searchText;
    private final byte[] searchTextBytes;
    private final int forward;
    private final int matchCase;
    private final int findNext;

    public BrowserHostFindRequest(@Nonnull RemoteHandle self, @Nonnull String searchText, int forward, int matchCase, int findNext) {
        this.self = self;
        this.searchText = searchText;
        this.searchTextBytes = searchText.getBytes(StandardCharsets.UTF_8);
        this.forward = forward;
        this.matchCase = matchCase;
        this.findNext = findNext;
    }

    @Nonnull
    public RemoteHandle self() {
        return self;
    }

    @Nonnull
    public String searchText() {
        return searchText;
    }

    public int forward() {
        return forward;
    }

    public int matchCase() {
        return matchCase;
    }

    public int findNext() {
        return findNext;
    }

    @Override
    public int messageId() {
        return MESSAGE_ID;
    }

    @Override
    public int encodedSize() {
        return 20 + searchTextBytes.length;
    }

    @Override
    public void encodeInto(@Nonnull ByteBuffer __dst) {
        ByteOrder originalOrder = __dst.order();
        try {
            __dst.order(ByteOrder.LITTLE_ENDIAN);
            __dst.putInt(self.id());
            __dst.putInt(searchTextBytes.length);
            __dst.put(searchTextBytes);
            __dst.putInt(forward);
            __dst.putInt(matchCase);
            __dst.putInt(findNext);
        } finally {
            __dst.order(originalOrder);
        }
    }

    public static final CefMessageDecoder<BrowserHostFindRequest> DECODER = payload -> {
        ByteBuffer __buf = payload.duplicate();
        __buf.order(ByteOrder.LITTLE_ENDIAN);
        RemoteHandle self = new RemoteHandle(__buf.getInt());
        int searchTextLen = __buf.getInt();
        byte[] searchTextBuf = new byte[searchTextLen];
        __buf.get(searchTextBuf);
        String searchText = new String(searchTextBuf, StandardCharsets.UTF_8);
        int forward = __buf.getInt();
        int matchCase = __buf.getInt();
        int findNext = __buf.getInt();
        return new BrowserHostFindRequest(self, searchText, forward, matchCase, findNext);
    };
}
