// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
package net.kurobako.cef4j.ipc.protocol.gen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import javax.annotation.Nonnull;
import net.kurobako.cef4j.ipc.session.CefMessageDecoder;
import net.kurobako.cef4j.ipc.session.CefMessageEncoder;
import net.kurobako.cef4j.ipc.session.CefMessageView;
import net.kurobako.cef4j.ipc.session.WireDecoder;
import net.kurobako.cef4j.ipc.session.RemoteHandle;

public final class RenderHandlerStartDraggingEvent implements CefMessageView, CefMessageEncoder {

    public static final int MESSAGE_ID = 1000679350;

    private final RemoteHandle browser;
    private final RemoteHandle dragData;
    private final int allowedOps;
    private final int x;
    private final int y;

    public RenderHandlerStartDraggingEvent(@Nonnull RemoteHandle browser, @Nonnull RemoteHandle dragData, int allowedOps, int x, int y) {
        this.browser = browser;
        this.dragData = dragData;
        this.allowedOps = allowedOps;
        this.x = x;
        this.y = y;
    }

    @Nonnull
    public RemoteHandle browser() {
        return browser;
    }

    @Nonnull
    public RemoteHandle dragData() {
        return dragData;
    }

    public int allowedOps() {
        return allowedOps;
    }

    public int x() {
        return x;
    }

    public int y() {
        return y;
    }

    @Override
    public int messageId() {
        return MESSAGE_ID;
    }

    @Override
    public int encodedSize() {
        return 20;
    }

    @Override
    public void encodeInto(@Nonnull ByteBuffer __dst) {
        ByteOrder originalOrder = __dst.order();
        try {
            __dst.order(ByteOrder.LITTLE_ENDIAN);
            __dst.putInt(browser.id());
            __dst.putInt(dragData.id());
            __dst.putInt(allowedOps);
            __dst.putInt(x);
            __dst.putInt(y);
        } finally {
            __dst.order(originalOrder);
        }
    }

    public static final CefMessageDecoder<RenderHandlerStartDraggingEvent> DECODER = payload -> {
        ByteBuffer __buf = payload.duplicate();
        __buf.order(ByteOrder.LITTLE_ENDIAN);
        WireDecoder.requireRemaining(__buf, Integer.BYTES, "browser");
        RemoteHandle browser = new RemoteHandle(__buf.getInt());
        WireDecoder.requireRemaining(__buf, Integer.BYTES, "dragData");
        RemoteHandle dragData = new RemoteHandle(__buf.getInt());
        WireDecoder.requireRemaining(__buf, Integer.BYTES, "allowedOps");
        int allowedOps = __buf.getInt();
        WireDecoder.requireRemaining(__buf, Integer.BYTES, "x");
        int x = __buf.getInt();
        WireDecoder.requireRemaining(__buf, Integer.BYTES, "y");
        int y = __buf.getInt();
        WireDecoder.requireFullyConsumed(__buf, "RenderHandlerStartDraggingEvent");
        return new RenderHandlerStartDraggingEvent(browser, dragData, allowedOps, x, y);
    };
}
