// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
package net.kurobako.cef4j.ipc.protocol.gen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.nio.charset.StandardCharsets;
import javax.annotation.Nonnull;
import net.kurobako.cef4j.ipc.session.CefMessageDecoder;
import net.kurobako.cef4j.ipc.session.CefMessageEncoder;
import net.kurobako.cef4j.ipc.session.CefMessageView;
import net.kurobako.cef4j.ipc.session.RemoteHandle;

public final class BrowserHostDownloadImageRequest implements CefMessageView, CefMessageEncoder {

    public static final int MESSAGE_ID = 38156526;

    private final RemoteHandle self;
    private final String imageUrl;
    private final byte[] imageUrlBytes;
    private final int isFavicon;
    private final int maxImageSize;
    private final int bypassCache;
    private final RemoteHandle callback;

    public BrowserHostDownloadImageRequest(@Nonnull RemoteHandle self, @Nonnull String imageUrl, int isFavicon, int maxImageSize, int bypassCache, @Nonnull RemoteHandle callback) {
        this.self = self;
        this.imageUrl = imageUrl;
        this.imageUrlBytes = imageUrl.getBytes(StandardCharsets.UTF_8);
        this.isFavicon = isFavicon;
        this.maxImageSize = maxImageSize;
        this.bypassCache = bypassCache;
        this.callback = callback;
    }

    @Nonnull
    public RemoteHandle self() {
        return self;
    }

    @Nonnull
    public String imageUrl() {
        return imageUrl;
    }

    public int isFavicon() {
        return isFavicon;
    }

    public int maxImageSize() {
        return maxImageSize;
    }

    public int bypassCache() {
        return bypassCache;
    }

    @Nonnull
    public RemoteHandle callback() {
        return callback;
    }

    @Override
    public int messageId() {
        return MESSAGE_ID;
    }

    @Override
    public int encodedSize() {
        return 24 + imageUrlBytes.length;
    }

    @Override
    public void encodeInto(@Nonnull ByteBuffer __dst) {
        ByteOrder originalOrder = __dst.order();
        try {
            __dst.order(ByteOrder.LITTLE_ENDIAN);
            __dst.putInt(self.id());
            __dst.putInt(imageUrlBytes.length);
            __dst.put(imageUrlBytes);
            __dst.putInt(isFavicon);
            __dst.putInt(maxImageSize);
            __dst.putInt(bypassCache);
            __dst.putInt(callback.id());
        } finally {
            __dst.order(originalOrder);
        }
    }

    public static final CefMessageDecoder<BrowserHostDownloadImageRequest> DECODER = payload -> {
        ByteBuffer __buf = payload.duplicate();
        __buf.order(ByteOrder.LITTLE_ENDIAN);
        RemoteHandle self = new RemoteHandle(__buf.getInt());
        int imageUrlLen = __buf.getInt();
        byte[] imageUrlBuf = new byte[imageUrlLen];
        __buf.get(imageUrlBuf);
        String imageUrl = new String(imageUrlBuf, StandardCharsets.UTF_8);
        int isFavicon = __buf.getInt();
        int maxImageSize = __buf.getInt();
        int bypassCache = __buf.getInt();
        RemoteHandle callback = new RemoteHandle(__buf.getInt());
        return new BrowserHostDownloadImageRequest(self, imageUrl, isFavicon, maxImageSize, bypassCache, callback);
    };
}
