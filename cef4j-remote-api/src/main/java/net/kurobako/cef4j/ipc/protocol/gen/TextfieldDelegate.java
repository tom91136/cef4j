// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
package net.kurobako.cef4j.ipc.protocol.gen;

import java.util.concurrent.CompletableFuture;
import javax.annotation.Nonnull;
import javax.annotation.Nullable;
import net.kurobako.cef4j.ipc.session.CefFutures;
import net.kurobako.cef4j.ipc.session.CefSession;
import net.kurobako.cef4j.ipc.session.RemoteHandle;

/**
 * Typed facade for {@code cef_textfield_delegate_t}, dispatching through a {@link CefSession}.
 * Each instance is a thin wrapper around a {@link RemoteHandle} that points at a runtime-server-side
 * ref-counted CEF object.
 */
public final class TextfieldDelegate implements AutoCloseable {

    private final CefSession session;
    private final RemoteHandle handle;
    @Nullable private CompletableFuture<Void> closeFuture;

    public TextfieldDelegate(@Nonnull CefSession session, @Nonnull RemoteHandle handle) {
        this.session = session;
        this.handle = handle;
    }

    @Nonnull
    public RemoteHandle handle() {
        return handle;
    }

    public synchronized CompletableFuture<Void> closeAsync() {
        if (closeFuture == null) {
            closeFuture = CefFutures.map(
                session.request(
                    new ReleaseHandleRequest(handle, "cef_textfield_delegate_t"), ReleaseHandleResponse.DECODER),
                r -> null);
        }
        return closeFuture;
    }

    public CompletableFuture<Void> releaseHandle() {
        return closeAsync();
    }

    @Override
    @SuppressWarnings("FutureReturnValueIgnored")
    public void close() {
        closeAsync();
    }

    /**
     * Called after the renderer and JavaScript in the page has had a chance to handle the event. {@code event} contains information about the keyboard event. {@code os_event} is the operating system event message, if any. Return {@code true} if the keyboard event was handled or {@code false} otherwise.
     * <p>Definition generated from cef_keyboard_handler.h
     *
     * @see <a href="https://cef-builds.spotifycdn.com/docs/150.0/cef__keyboard__handler_8h.html">cef_keyboard_handler.h:66</a>
     */
    public CompletableFuture<Integer> onKeyEvent(@Nonnull RemoteHandle textfield, KeyEvent event) {
        return CefFutures.map(
            session.request(new TextfieldDelegateOnKeyEventRequest(handle, textfield, event), TextfieldDelegateOnKeyEventResponse.DECODER),
            TextfieldDelegateOnKeyEventResponse::result);
    }

    /** Dispatches {@code on_after_user_action} to the runtime server. */
    public CompletableFuture<Void> onAfterUserAction(@Nonnull RemoteHandle textfield) {
        return CefFutures.map(
            session.request(new TextfieldDelegateOnAfterUserActionRequest(handle, textfield), TextfieldDelegateOnAfterUserActionResponse.DECODER),
            r -> null);
    }
}
