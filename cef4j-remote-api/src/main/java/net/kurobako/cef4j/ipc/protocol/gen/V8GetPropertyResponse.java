// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
package net.kurobako.cef4j.ipc.protocol.gen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.nio.charset.StandardCharsets;
import javax.annotation.Nonnull;
import net.kurobako.cef4j.ipc.session.CefMessageDecoder;
import net.kurobako.cef4j.ipc.session.CefMessageEncoder;
import net.kurobako.cef4j.ipc.session.CefMessageView;

public final class V8GetPropertyResponse implements CefMessageView, CefMessageEncoder {

    public static final int MESSAGE_ID = 14;

    private final int valueKind;
    private final boolean boolValue;
    private final int intValue;
    private final long doubleValue;
    private final String stringValue;
    private final byte[] stringValueBytes;
    private final String errorMessage;
    private final byte[] errorMessageBytes;
    private final int valueHandle;

    public V8GetPropertyResponse(int valueKind, boolean boolValue, int intValue, long doubleValue, @Nonnull String stringValue, @Nonnull String errorMessage, int valueHandle) {
        this.valueKind = valueKind;
        this.boolValue = boolValue;
        this.intValue = intValue;
        this.doubleValue = doubleValue;
        this.stringValue = stringValue;
        this.stringValueBytes = stringValue.getBytes(StandardCharsets.UTF_8);
        this.errorMessage = errorMessage;
        this.errorMessageBytes = errorMessage.getBytes(StandardCharsets.UTF_8);
        this.valueHandle = valueHandle;
    }

    public int valueKind() {
        return valueKind;
    }

    public boolean boolValue() {
        return boolValue;
    }

    public int intValue() {
        return intValue;
    }

    public long doubleValue() {
        return doubleValue;
    }

    @Nonnull
    public String stringValue() {
        return stringValue;
    }

    @Nonnull
    public String errorMessage() {
        return errorMessage;
    }

    public int valueHandle() {
        return valueHandle;
    }

    @Override
    public int messageId() {
        return MESSAGE_ID;
    }

    @Override
    public int encodedSize() {
        return 29 + stringValueBytes.length + errorMessageBytes.length;
    }

    @Override
    public void encodeInto(@Nonnull ByteBuffer __dst) {
        ByteOrder originalOrder = __dst.order();
        try {
            __dst.order(ByteOrder.LITTLE_ENDIAN);
            __dst.putInt(valueKind);
            __dst.put((byte) (boolValue ? 1 : 0));
            __dst.putInt(intValue);
            __dst.putLong(doubleValue);
            __dst.putInt(stringValueBytes.length);
            __dst.put(stringValueBytes);
            __dst.putInt(errorMessageBytes.length);
            __dst.put(errorMessageBytes);
            __dst.putInt(valueHandle);
        } finally {
            __dst.order(originalOrder);
        }
    }

    public static final CefMessageDecoder<V8GetPropertyResponse> DECODER = payload -> {
        ByteBuffer __buf = payload.duplicate();
        __buf.order(ByteOrder.LITTLE_ENDIAN);
        int valueKind = __buf.getInt();
        boolean boolValue = __buf.get() != 0;
        int intValue = __buf.getInt();
        long doubleValue = __buf.getLong();
        int stringValueLen = __buf.getInt();
        byte[] stringValueBuf = new byte[stringValueLen];
        __buf.get(stringValueBuf);
        String stringValue = new String(stringValueBuf, StandardCharsets.UTF_8);
        int errorMessageLen = __buf.getInt();
        byte[] errorMessageBuf = new byte[errorMessageLen];
        __buf.get(errorMessageBuf);
        String errorMessage = new String(errorMessageBuf, StandardCharsets.UTF_8);
        int valueHandle = __buf.getInt();
        return new V8GetPropertyResponse(valueKind, boolValue, intValue, doubleValue, stringValue, errorMessage, valueHandle);
    };
}
