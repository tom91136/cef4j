// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
package net.kurobako.cef4j.ipc.protocol.gen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.nio.charset.StandardCharsets;
import javax.annotation.Nonnull;
import net.kurobako.cef4j.ipc.session.CefMessageDecoder;
import net.kurobako.cef4j.ipc.session.CefMessageEncoder;
import net.kurobako.cef4j.ipc.session.CefMessageView;
import net.kurobako.cef4j.ipc.session.WireDecoder;
import net.kurobako.cef4j.ipc.session.RemoteHandle;

public final class PermissionHandlerOnRequestMediaAccessPermissionEvent implements CefMessageView, CefMessageEncoder {

    public static final int MESSAGE_ID = 1073236129;

    private final RemoteHandle browser;
    private final RemoteHandle frame;
    private final String requestingOrigin;
    private final byte[] requestingOriginBytes;
    private final int requestedPermissions;
    private final RemoteHandle callback;

    public PermissionHandlerOnRequestMediaAccessPermissionEvent(@Nonnull RemoteHandle browser, @Nonnull RemoteHandle frame, @Nonnull String requestingOrigin, int requestedPermissions, @Nonnull RemoteHandle callback) {
        this.browser = browser;
        this.frame = frame;
        this.requestingOrigin = requestingOrigin;
        this.requestingOriginBytes = requestingOrigin.getBytes(StandardCharsets.UTF_8);
        this.requestedPermissions = requestedPermissions;
        this.callback = callback;
    }

    @Nonnull
    public RemoteHandle browser() {
        return browser;
    }

    @Nonnull
    public RemoteHandle frame() {
        return frame;
    }

    @Nonnull
    public String requestingOrigin() {
        return requestingOrigin;
    }

    public int requestedPermissions() {
        return requestedPermissions;
    }

    @Nonnull
    public RemoteHandle callback() {
        return callback;
    }

    @Override
    public int messageId() {
        return MESSAGE_ID;
    }

    @Override
    public int encodedSize() {
        return 20 + requestingOriginBytes.length;
    }

    @Override
    public void encodeInto(@Nonnull ByteBuffer __dst) {
        ByteOrder originalOrder = __dst.order();
        try {
            __dst.order(ByteOrder.LITTLE_ENDIAN);
            __dst.putInt(browser.id());
            __dst.putInt(frame.id());
            __dst.putInt(requestingOriginBytes.length);
            __dst.put(requestingOriginBytes);
            __dst.putInt(requestedPermissions);
            __dst.putInt(callback.id());
        } finally {
            __dst.order(originalOrder);
        }
    }

    public static final CefMessageDecoder<PermissionHandlerOnRequestMediaAccessPermissionEvent> DECODER = payload -> {
        ByteBuffer __buf = payload.duplicate();
        __buf.order(ByteOrder.LITTLE_ENDIAN);
        WireDecoder.requireRemaining(__buf, Integer.BYTES, "browser");
        RemoteHandle browser = new RemoteHandle(__buf.getInt());
        WireDecoder.requireRemaining(__buf, Integer.BYTES, "frame");
        RemoteHandle frame = new RemoteHandle(__buf.getInt());
        int requestingOriginLen = WireDecoder.length(__buf, "requestingOrigin");
        byte[] requestingOriginBuf = new byte[requestingOriginLen];
        __buf.get(requestingOriginBuf);
        String requestingOrigin = new String(requestingOriginBuf, StandardCharsets.UTF_8);
        WireDecoder.requireRemaining(__buf, Integer.BYTES, "requestedPermissions");
        int requestedPermissions = __buf.getInt();
        WireDecoder.requireRemaining(__buf, Integer.BYTES, "callback");
        RemoteHandle callback = new RemoteHandle(__buf.getInt());
        WireDecoder.requireFullyConsumed(__buf, "PermissionHandlerOnRequestMediaAccessPermissionEvent");
        return new PermissionHandlerOnRequestMediaAccessPermissionEvent(browser, frame, requestingOrigin, requestedPermissions, callback);
    };
}
