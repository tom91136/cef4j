// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
package net.kurobako.cef4j.ipc.protocol.gen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.nio.charset.StandardCharsets;
import javax.annotation.Nonnull;
import net.kurobako.cef4j.ipc.session.CefMessageDecoder;
import net.kurobako.cef4j.ipc.session.CefMessageEncoder;
import net.kurobako.cef4j.ipc.session.CefMessageView;
import net.kurobako.cef4j.ipc.session.WireDecoder;
import net.kurobako.cef4j.ipc.session.RemoteHandle;

public final class RequestHandlerOnOpenUrlfromTabEvent implements CefMessageView, CefMessageEncoder {

    public static final int MESSAGE_ID = 686384909;

    private final RemoteHandle browser;
    private final RemoteHandle frame;
    private final String targetUrl;
    private final byte[] targetUrlBytes;
    private final int targetDisposition;
    private final int userGesture;

    public RequestHandlerOnOpenUrlfromTabEvent(@Nonnull RemoteHandle browser, @Nonnull RemoteHandle frame, @Nonnull String targetUrl, int targetDisposition, int userGesture) {
        this.browser = browser;
        this.frame = frame;
        this.targetUrl = targetUrl;
        this.targetUrlBytes = targetUrl.getBytes(StandardCharsets.UTF_8);
        this.targetDisposition = targetDisposition;
        this.userGesture = userGesture;
    }

    @Nonnull
    public RemoteHandle browser() {
        return browser;
    }

    @Nonnull
    public RemoteHandle frame() {
        return frame;
    }

    @Nonnull
    public String targetUrl() {
        return targetUrl;
    }

    public int targetDisposition() {
        return targetDisposition;
    }

    public int userGesture() {
        return userGesture;
    }

    @Override
    public int messageId() {
        return MESSAGE_ID;
    }

    @Override
    public int encodedSize() {
        return 20 + targetUrlBytes.length;
    }

    @Override
    public void encodeInto(@Nonnull ByteBuffer __dst) {
        ByteOrder originalOrder = __dst.order();
        try {
            __dst.order(ByteOrder.LITTLE_ENDIAN);
            __dst.putInt(browser.id());
            __dst.putInt(frame.id());
            __dst.putInt(targetUrlBytes.length);
            __dst.put(targetUrlBytes);
            __dst.putInt(targetDisposition);
            __dst.putInt(userGesture);
        } finally {
            __dst.order(originalOrder);
        }
    }

    public static final CefMessageDecoder<RequestHandlerOnOpenUrlfromTabEvent> DECODER = payload -> {
        ByteBuffer __buf = payload.duplicate();
        __buf.order(ByteOrder.LITTLE_ENDIAN);
        WireDecoder.requireRemaining(__buf, Integer.BYTES, "browser");
        RemoteHandle browser = new RemoteHandle(__buf.getInt());
        WireDecoder.requireRemaining(__buf, Integer.BYTES, "frame");
        RemoteHandle frame = new RemoteHandle(__buf.getInt());
        int targetUrlLen = WireDecoder.length(__buf, "targetUrl");
        byte[] targetUrlBuf = new byte[targetUrlLen];
        __buf.get(targetUrlBuf);
        String targetUrl = new String(targetUrlBuf, StandardCharsets.UTF_8);
        WireDecoder.requireRemaining(__buf, Integer.BYTES, "targetDisposition");
        int targetDisposition = __buf.getInt();
        WireDecoder.requireRemaining(__buf, Integer.BYTES, "userGesture");
        int userGesture = __buf.getInt();
        WireDecoder.requireFullyConsumed(__buf, "RequestHandlerOnOpenUrlfromTabEvent");
        return new RequestHandlerOnOpenUrlfromTabEvent(browser, frame, targetUrl, targetDisposition, userGesture);
    };
}
