// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
package net.kurobako.cef4j.ipc.protocol.gen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.nio.charset.StandardCharsets;
import javax.annotation.Nonnull;
import net.kurobako.cef4j.ipc.session.CefMessageDecoder;
import net.kurobako.cef4j.ipc.session.CefMessageEncoder;
import net.kurobako.cef4j.ipc.session.CefMessageView;

public final class NavigateRequest implements CefMessageView, CefMessageEncoder {

    public static final int MESSAGE_ID = 100;

    private final String url;
    private final byte[] urlBytes;
    private final String referrer;
    private final byte[] referrerBytes;
    private final int transitionType;
    private final boolean preserveSession;

    public NavigateRequest(@Nonnull String url, @Nonnull String referrer, int transitionType, boolean preserveSession) {
        this.url = url;
        this.urlBytes = url.getBytes(StandardCharsets.UTF_8);
        this.referrer = referrer;
        this.referrerBytes = referrer.getBytes(StandardCharsets.UTF_8);
        this.transitionType = transitionType;
        this.preserveSession = preserveSession;
    }

    @Nonnull
    public String url() {
        return url;
    }

    @Nonnull
    public String referrer() {
        return referrer;
    }

    public int transitionType() {
        return transitionType;
    }

    public boolean preserveSession() {
        return preserveSession;
    }

    @Override
    public int messageId() {
        return MESSAGE_ID;
    }

    @Override
    public int encodedSize() {
        return 13 + urlBytes.length + referrerBytes.length;
    }

    @Override
    public void encodeInto(@Nonnull ByteBuffer __dst) {
        ByteOrder originalOrder = __dst.order();
        try {
            __dst.order(ByteOrder.LITTLE_ENDIAN);
            __dst.putInt(urlBytes.length);
            __dst.put(urlBytes);
            __dst.putInt(referrerBytes.length);
            __dst.put(referrerBytes);
            __dst.putInt(transitionType);
            __dst.put((byte) (preserveSession ? 1 : 0));
        } finally {
            __dst.order(originalOrder);
        }
    }

    public static final CefMessageDecoder<NavigateRequest> DECODER = payload -> {
        ByteBuffer __buf = payload.duplicate();
        __buf.order(ByteOrder.LITTLE_ENDIAN);
        int urlLen = __buf.getInt();
        byte[] urlBuf = new byte[urlLen];
        __buf.get(urlBuf);
        String url = new String(urlBuf, StandardCharsets.UTF_8);
        int referrerLen = __buf.getInt();
        byte[] referrerBuf = new byte[referrerLen];
        __buf.get(referrerBuf);
        String referrer = new String(referrerBuf, StandardCharsets.UTF_8);
        int transitionType = __buf.getInt();
        boolean preserveSession = __buf.get() != 0;
        return new NavigateRequest(url, referrer, transitionType, preserveSession);
    };
}
