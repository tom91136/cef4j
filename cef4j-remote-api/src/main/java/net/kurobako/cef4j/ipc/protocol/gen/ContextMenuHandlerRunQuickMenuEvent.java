// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
package net.kurobako.cef4j.ipc.protocol.gen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import javax.annotation.Nonnull;
import net.kurobako.cef4j.ipc.session.CefMessageDecoder;
import net.kurobako.cef4j.ipc.session.CefMessageEncoder;
import net.kurobako.cef4j.ipc.session.CefMessageView;
import net.kurobako.cef4j.ipc.session.WireDecoder;
import net.kurobako.cef4j.ipc.session.RemoteHandle;

public final class ContextMenuHandlerRunQuickMenuEvent implements CefMessageView, CefMessageEncoder {

    public static final int MESSAGE_ID = 2062326684;

    private final RemoteHandle browser;
    private final RemoteHandle frame;
    private final Point location;
    private final Size size;
    private final int editStateFlags;
    private final RemoteHandle callback;

    public ContextMenuHandlerRunQuickMenuEvent(@Nonnull RemoteHandle browser, @Nonnull RemoteHandle frame, @Nonnull Point location, @Nonnull Size size, int editStateFlags, @Nonnull RemoteHandle callback) {
        this.browser = browser;
        this.frame = frame;
        this.location = location;
        this.size = size;
        this.editStateFlags = editStateFlags;
        this.callback = callback;
    }

    @Nonnull
    public RemoteHandle browser() {
        return browser;
    }

    @Nonnull
    public RemoteHandle frame() {
        return frame;
    }

    @Nonnull
    public Point location() {
        return location;
    }

    @Nonnull
    public Size size() {
        return size;
    }

    public int editStateFlags() {
        return editStateFlags;
    }

    @Nonnull
    public RemoteHandle callback() {
        return callback;
    }

    @Override
    public int messageId() {
        return MESSAGE_ID;
    }

    @Override
    public int encodedSize() {
        return 16 + location.encodedSize() + size.encodedSize();
    }

    @Override
    public void encodeInto(@Nonnull ByteBuffer __dst) {
        ByteOrder originalOrder = __dst.order();
        try {
            __dst.order(ByteOrder.LITTLE_ENDIAN);
            __dst.putInt(browser.id());
            __dst.putInt(frame.id());
            location.encodeInto(__dst);
            size.encodeInto(__dst);
            __dst.putInt(editStateFlags);
            __dst.putInt(callback.id());
        } finally {
            __dst.order(originalOrder);
        }
    }

    public static final CefMessageDecoder<ContextMenuHandlerRunQuickMenuEvent> DECODER = payload -> {
        ByteBuffer __buf = payload.duplicate();
        __buf.order(ByteOrder.LITTLE_ENDIAN);
        WireDecoder.requireRemaining(__buf, Integer.BYTES, "browser");
        RemoteHandle browser = new RemoteHandle(__buf.getInt());
        WireDecoder.requireRemaining(__buf, Integer.BYTES, "frame");
        RemoteHandle frame = new RemoteHandle(__buf.getInt());
        Point location = Point.decode(__buf);
        Size size = Size.decode(__buf);
        WireDecoder.requireRemaining(__buf, Integer.BYTES, "editStateFlags");
        int editStateFlags = __buf.getInt();
        WireDecoder.requireRemaining(__buf, Integer.BYTES, "callback");
        RemoteHandle callback = new RemoteHandle(__buf.getInt());
        WireDecoder.requireFullyConsumed(__buf, "ContextMenuHandlerRunQuickMenuEvent");
        return new ContextMenuHandlerRunQuickMenuEvent(browser, frame, location, size, editStateFlags, callback);
    };
}
