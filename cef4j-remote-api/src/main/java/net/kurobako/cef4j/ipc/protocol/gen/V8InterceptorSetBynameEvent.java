// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
package net.kurobako.cef4j.ipc.protocol.gen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.nio.charset.StandardCharsets;
import javax.annotation.Nonnull;
import net.kurobako.cef4j.ipc.session.CefMessageDecoder;
import net.kurobako.cef4j.ipc.session.CefMessageEncoder;
import net.kurobako.cef4j.ipc.session.CefMessageView;
import net.kurobako.cef4j.ipc.session.WireDecoder;
import net.kurobako.cef4j.ipc.session.RemoteHandle;

public final class V8InterceptorSetBynameEvent implements CefMessageView, CefMessageEncoder {

    public static final int MESSAGE_ID = 296677527;

    private final String name;
    private final byte[] nameBytes;
    private final RemoteHandle object;
    private final RemoteHandle value;
    private final String exception;
    private final byte[] exceptionBytes;

    public V8InterceptorSetBynameEvent(@Nonnull String name, @Nonnull RemoteHandle object, @Nonnull RemoteHandle value, @Nonnull String exception) {
        this.name = name;
        this.nameBytes = name.getBytes(StandardCharsets.UTF_8);
        this.object = object;
        this.value = value;
        this.exception = exception;
        this.exceptionBytes = exception.getBytes(StandardCharsets.UTF_8);
    }

    @Nonnull
    public String name() {
        return name;
    }

    @Nonnull
    public RemoteHandle object() {
        return object;
    }

    @Nonnull
    public RemoteHandle value() {
        return value;
    }

    @Nonnull
    public String exception() {
        return exception;
    }

    @Override
    public int messageId() {
        return MESSAGE_ID;
    }

    @Override
    public int encodedSize() {
        return 16 + nameBytes.length + exceptionBytes.length;
    }

    @Override
    public void encodeInto(@Nonnull ByteBuffer __dst) {
        ByteOrder originalOrder = __dst.order();
        try {
            __dst.order(ByteOrder.LITTLE_ENDIAN);
            __dst.putInt(nameBytes.length);
            __dst.put(nameBytes);
            __dst.putInt(object.id());
            __dst.putInt(value.id());
            __dst.putInt(exceptionBytes.length);
            __dst.put(exceptionBytes);
        } finally {
            __dst.order(originalOrder);
        }
    }

    public static final CefMessageDecoder<V8InterceptorSetBynameEvent> DECODER = payload -> {
        ByteBuffer __buf = payload.duplicate();
        __buf.order(ByteOrder.LITTLE_ENDIAN);
        int nameLen = WireDecoder.length(__buf, "name");
        byte[] nameBuf = new byte[nameLen];
        __buf.get(nameBuf);
        String name = new String(nameBuf, StandardCharsets.UTF_8);
        WireDecoder.requireRemaining(__buf, Integer.BYTES, "object");
        RemoteHandle object = new RemoteHandle(__buf.getInt());
        WireDecoder.requireRemaining(__buf, Integer.BYTES, "value");
        RemoteHandle value = new RemoteHandle(__buf.getInt());
        int exceptionLen = WireDecoder.length(__buf, "exception");
        byte[] exceptionBuf = new byte[exceptionLen];
        __buf.get(exceptionBuf);
        String exception = new String(exceptionBuf, StandardCharsets.UTF_8);
        WireDecoder.requireFullyConsumed(__buf, "V8InterceptorSetBynameEvent");
        return new V8InterceptorSetBynameEvent(name, object, value, exception);
    };
}
