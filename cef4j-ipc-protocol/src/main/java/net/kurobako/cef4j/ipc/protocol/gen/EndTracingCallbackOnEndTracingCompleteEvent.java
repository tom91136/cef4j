// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
package net.kurobako.cef4j.ipc.protocol.gen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.nio.charset.StandardCharsets;
import javax.annotation.Nonnull;
import net.kurobako.cef4j.ipc.session.CefMessageDecoder;
import net.kurobako.cef4j.ipc.session.CefMessageEncoder;
import net.kurobako.cef4j.ipc.session.CefMessageView;

public final class EndTracingCallbackOnEndTracingCompleteEvent implements CefMessageView, CefMessageEncoder {

    public static final int MESSAGE_ID = 1608024082;

    private final String tracingFile;
    private final byte[] tracingFileBytes;

    public EndTracingCallbackOnEndTracingCompleteEvent(@Nonnull String tracingFile) {
        this.tracingFile = tracingFile;
        this.tracingFileBytes = tracingFile.getBytes(StandardCharsets.UTF_8);
    }

    @Nonnull
    public String tracingFile() {
        return tracingFile;
    }

    @Override
    public int messageId() {
        return MESSAGE_ID;
    }

    @Override
    public int encodedSize() {
        return 4 + tracingFileBytes.length;
    }

    @Override
    public void encodeInto(@Nonnull ByteBuffer __dst) {
        ByteOrder originalOrder = __dst.order();
        try {
            __dst.order(ByteOrder.LITTLE_ENDIAN);
            __dst.putInt(tracingFileBytes.length);
            __dst.put(tracingFileBytes);
        } finally {
            __dst.order(originalOrder);
        }
    }

    public static final CefMessageDecoder<EndTracingCallbackOnEndTracingCompleteEvent> DECODER = payload -> {
        ByteBuffer __buf = payload.duplicate();
        __buf.order(ByteOrder.LITTLE_ENDIAN);
        int tracingFileLen = __buf.getInt();
        byte[] tracingFileBuf = new byte[tracingFileLen];
        __buf.get(tracingFileBuf);
        String tracingFile = new String(tracingFileBuf, StandardCharsets.UTF_8);
        return new EndTracingCallbackOnEndTracingCompleteEvent(tracingFile);
    };
}
