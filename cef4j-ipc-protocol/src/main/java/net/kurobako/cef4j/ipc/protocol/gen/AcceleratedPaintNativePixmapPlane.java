// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
package net.kurobako.cef4j.ipc.protocol.gen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.nio.charset.StandardCharsets;
import javax.annotation.Nonnull;

/**
 * Wire-format value type mirroring {@code cef_accelerated_paint_native_pixmap_plane_t}. Fields are immutable; pass instances
 * to facade methods (or factory requests) that take this struct. The dispatcher decodes them on the
 * helper side and fills the matching {@code cef_accelerated_paint_native_pixmap_plane_t} before invoking CEF.
 */
public final class AcceleratedPaintNativePixmapPlane {

    private final int stride;
    private final long offset;
    private final long size;
    private final int fd;

    public AcceleratedPaintNativePixmapPlane(int stride, long offset, long size, int fd) {
        this.stride = stride;
        this.offset = offset;
        this.size = size;
        this.fd = fd;
    }

    public int stride() {
        return stride;
    }

    public long offset() {
        return offset;
    }

    public long size() {
        return size;
    }

    public int fd() {
        return fd;
    }

    public int encodedSize() {
        return 24;
    }

    public void encodeInto(@Nonnull ByteBuffer dst) {
        ByteOrder originalOrder = dst.order();
        try {
            dst.order(ByteOrder.LITTLE_ENDIAN);
            dst.putInt(stride);
            dst.putLong(offset);
            dst.putLong(size);
            dst.putInt(fd);
        } finally {
            dst.order(originalOrder);
        }
    }

    /** Reads in place from {@code __buf}, advancing its position. Caller must have set
      * {@link ByteOrder#LITTLE_ENDIAN} on the buffer (the standalone {@code decode(payload)} entry point
      * below does that for you). For nested use inside a parent message's decoder the parent has already
      * configured the buffer.
      */
    public static AcceleratedPaintNativePixmapPlane decode(@Nonnull ByteBuffer __buf) {
        int stride = __buf.getInt();
        long offset = __buf.getLong();
        long size = __buf.getLong();
        int fd = __buf.getInt();
        return new AcceleratedPaintNativePixmapPlane(stride, offset, size, fd);
    }

    public static Builder builder() {
        return new Builder();
    }

    /** Lombok-style builder. */
    public static final class Builder {

        private int stride = 0;
        private long offset = 0L;
        private long size = 0L;
        private int fd = 0;

        public Builder stride(int stride) {
            this.stride = stride;
            return this;
        }

        public Builder offset(long offset) {
            this.offset = offset;
            return this;
        }

        public Builder size(long size) {
            this.size = size;
            return this;
        }

        public Builder fd(int fd) {
            this.fd = fd;
            return this;
        }

        public AcceleratedPaintNativePixmapPlane build() {
            return new AcceleratedPaintNativePixmapPlane(stride, offset, size, fd);
        }
    }
}
