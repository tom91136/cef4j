// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
package net.kurobako.cef4j.ipc.protocol.gen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.nio.charset.StandardCharsets;
import javax.annotation.Nonnull;
import net.kurobako.cef4j.ipc.session.CefMessageDecoder;
import net.kurobako.cef4j.ipc.session.CefMessageEncoder;
import net.kurobako.cef4j.ipc.session.CefMessageView;
import net.kurobako.cef4j.ipc.session.RemoteHandle;

public final class OsrPaintEvent implements CefMessageView, CefMessageEncoder {

    public static final int MESSAGE_ID = 9;

    private final RemoteHandle browser;
    private final String shmName;
    private final byte[] shmNameBytes;
    private final long frameSequence;
    private final int width;
    private final int height;
    private final int byteCount;
    private final int paintType;
    private final int dirtyX;
    private final int dirtyY;
    private final int dirtyWidth;
    private final int dirtyHeight;

    public OsrPaintEvent(@Nonnull RemoteHandle browser, @Nonnull String shmName, long frameSequence, int width, int height, int byteCount, int paintType, int dirtyX, int dirtyY, int dirtyWidth, int dirtyHeight) {
        this.browser = browser;
        this.shmName = shmName;
        this.shmNameBytes = shmName.getBytes(StandardCharsets.UTF_8);
        this.frameSequence = frameSequence;
        this.width = width;
        this.height = height;
        this.byteCount = byteCount;
        this.paintType = paintType;
        this.dirtyX = dirtyX;
        this.dirtyY = dirtyY;
        this.dirtyWidth = dirtyWidth;
        this.dirtyHeight = dirtyHeight;
    }

    @Nonnull
    public RemoteHandle browser() {
        return browser;
    }

    @Nonnull
    public String shmName() {
        return shmName;
    }

    public long frameSequence() {
        return frameSequence;
    }

    public int width() {
        return width;
    }

    public int height() {
        return height;
    }

    public int byteCount() {
        return byteCount;
    }

    public int paintType() {
        return paintType;
    }

    public int dirtyX() {
        return dirtyX;
    }

    public int dirtyY() {
        return dirtyY;
    }

    public int dirtyWidth() {
        return dirtyWidth;
    }

    public int dirtyHeight() {
        return dirtyHeight;
    }

    @Override
    public int messageId() {
        return MESSAGE_ID;
    }

    @Override
    public int encodedSize() {
        return 48 + shmNameBytes.length;
    }

    @Override
    public void encodeInto(@Nonnull ByteBuffer __dst) {
        ByteOrder originalOrder = __dst.order();
        try {
            __dst.order(ByteOrder.LITTLE_ENDIAN);
            __dst.putInt(browser.id());
            __dst.putInt(shmNameBytes.length);
            __dst.put(shmNameBytes);
            __dst.putLong(frameSequence);
            __dst.putInt(width);
            __dst.putInt(height);
            __dst.putInt(byteCount);
            __dst.putInt(paintType);
            __dst.putInt(dirtyX);
            __dst.putInt(dirtyY);
            __dst.putInt(dirtyWidth);
            __dst.putInt(dirtyHeight);
        } finally {
            __dst.order(originalOrder);
        }
    }

    public static final CefMessageDecoder<OsrPaintEvent> DECODER = payload -> {
        ByteBuffer __buf = payload.duplicate();
        __buf.order(ByteOrder.LITTLE_ENDIAN);
        RemoteHandle browser = new RemoteHandle(__buf.getInt());
        int shmNameLen = __buf.getInt();
        byte[] shmNameBuf = new byte[shmNameLen];
        __buf.get(shmNameBuf);
        String shmName = new String(shmNameBuf, StandardCharsets.UTF_8);
        long frameSequence = __buf.getLong();
        int width = __buf.getInt();
        int height = __buf.getInt();
        int byteCount = __buf.getInt();
        int paintType = __buf.getInt();
        int dirtyX = __buf.getInt();
        int dirtyY = __buf.getInt();
        int dirtyWidth = __buf.getInt();
        int dirtyHeight = __buf.getInt();
        return new OsrPaintEvent(browser, shmName, frameSequence, width, height, byteCount, paintType, dirtyX, dirtyY, dirtyWidth, dirtyHeight);
    };
}
