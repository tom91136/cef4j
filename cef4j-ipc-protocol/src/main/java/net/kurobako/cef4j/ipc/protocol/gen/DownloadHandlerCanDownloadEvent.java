// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
package net.kurobako.cef4j.ipc.protocol.gen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.nio.charset.StandardCharsets;
import javax.annotation.Nonnull;
import net.kurobako.cef4j.ipc.session.CefMessageDecoder;
import net.kurobako.cef4j.ipc.session.CefMessageEncoder;
import net.kurobako.cef4j.ipc.session.CefMessageView;
import net.kurobako.cef4j.ipc.session.RemoteHandle;

public final class DownloadHandlerCanDownloadEvent implements CefMessageView, CefMessageEncoder {

    public static final int MESSAGE_ID = 1646418617;

    private final RemoteHandle browser;
    private final String url;
    private final byte[] urlBytes;
    private final String requestMethod;
    private final byte[] requestMethodBytes;

    public DownloadHandlerCanDownloadEvent(@Nonnull RemoteHandle browser, @Nonnull String url, @Nonnull String requestMethod) {
        this.browser = browser;
        this.url = url;
        this.urlBytes = url.getBytes(StandardCharsets.UTF_8);
        this.requestMethod = requestMethod;
        this.requestMethodBytes = requestMethod.getBytes(StandardCharsets.UTF_8);
    }

    @Nonnull
    public RemoteHandle browser() {
        return browser;
    }

    @Nonnull
    public String url() {
        return url;
    }

    @Nonnull
    public String requestMethod() {
        return requestMethod;
    }

    @Override
    public int messageId() {
        return MESSAGE_ID;
    }

    @Override
    public int encodedSize() {
        return 12 + urlBytes.length + requestMethodBytes.length;
    }

    @Override
    public void encodeInto(@Nonnull ByteBuffer __dst) {
        ByteOrder originalOrder = __dst.order();
        try {
            __dst.order(ByteOrder.LITTLE_ENDIAN);
            __dst.putInt(browser.id());
            __dst.putInt(urlBytes.length);
            __dst.put(urlBytes);
            __dst.putInt(requestMethodBytes.length);
            __dst.put(requestMethodBytes);
        } finally {
            __dst.order(originalOrder);
        }
    }

    public static final CefMessageDecoder<DownloadHandlerCanDownloadEvent> DECODER = payload -> {
        ByteBuffer __buf = payload.duplicate();
        __buf.order(ByteOrder.LITTLE_ENDIAN);
        RemoteHandle browser = new RemoteHandle(__buf.getInt());
        int urlLen = __buf.getInt();
        byte[] urlBuf = new byte[urlLen];
        __buf.get(urlBuf);
        String url = new String(urlBuf, StandardCharsets.UTF_8);
        int requestMethodLen = __buf.getInt();
        byte[] requestMethodBuf = new byte[requestMethodLen];
        __buf.get(requestMethodBuf);
        String requestMethod = new String(requestMethodBuf, StandardCharsets.UTF_8);
        return new DownloadHandlerCanDownloadEvent(browser, url, requestMethod);
    };
}
