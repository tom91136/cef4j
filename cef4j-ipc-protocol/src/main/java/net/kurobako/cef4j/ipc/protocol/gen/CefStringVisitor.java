// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
package net.kurobako.cef4j.ipc.protocol.gen;

import net.kurobako.cef4j.ipc.session.CefSession;
import net.kurobako.cef4j.ipc.session.JvmCallbackTable;

/**
 * Typed interface for {@code cef_string_visitor_t}, a JVM-owned CEF visitor. JVM provides the implementation,
 * passes a callbackId minted from a {@link JvmCallbackTable} as the visitor handle to a CEF method, and the
 * helper synthesises a real {@code cef_string_visitor_t} that routes the callback back to the JVM.
 */
@FunctionalInterface
public interface CefStringVisitor {

    /** Mirrors the {@code visit} callback on the CEF struct. */
    void visit(String string);

    /**
     * Wires inbound {@link StringVisitorVisitCallbackEvent} frames into the supplied {@code table} so each visitor invocation
     * dispatches to the {@code CefStringVisitor} registered under that callbackId. Call once per session.
     */
    static void route(CefSession session, JvmCallbackTable<CefStringVisitor> table) {
        session.on(StringVisitorVisitCallbackEvent.MESSAGE_ID, StringVisitorVisitCallbackEvent.DECODER, ev -> {
            CefStringVisitor v = table.lookup(ev.callbackId());
            if (v != null) v.visit(ev.string());
        });
    }
}
