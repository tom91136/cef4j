// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
package net.kurobako.cef4j.ipc.protocol.gen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.nio.charset.StandardCharsets;
import javax.annotation.Nonnull;
import net.kurobako.cef4j.ipc.session.CefMessageDecoder;
import net.kurobako.cef4j.ipc.session.CefMessageEncoder;
import net.kurobako.cef4j.ipc.session.CefMessageView;
import net.kurobako.cef4j.ipc.session.RemoteHandle;

public final class AppOnBeforeCommandLineProcessingEvent implements CefMessageView, CefMessageEncoder {

    public static final int MESSAGE_ID = 34351359;

    private final String processType;
    private final byte[] processTypeBytes;
    private final RemoteHandle commandLine;

    public AppOnBeforeCommandLineProcessingEvent(@Nonnull String processType, @Nonnull RemoteHandle commandLine) {
        this.processType = processType;
        this.processTypeBytes = processType.getBytes(StandardCharsets.UTF_8);
        this.commandLine = commandLine;
    }

    @Nonnull
    public String processType() {
        return processType;
    }

    @Nonnull
    public RemoteHandle commandLine() {
        return commandLine;
    }

    @Override
    public int messageId() {
        return MESSAGE_ID;
    }

    @Override
    public int encodedSize() {
        return 8 + processTypeBytes.length;
    }

    @Override
    public void encodeInto(@Nonnull ByteBuffer __dst) {
        ByteOrder originalOrder = __dst.order();
        try {
            __dst.order(ByteOrder.LITTLE_ENDIAN);
            __dst.putInt(processTypeBytes.length);
            __dst.put(processTypeBytes);
            __dst.putInt(commandLine.id());
        } finally {
            __dst.order(originalOrder);
        }
    }

    public static final CefMessageDecoder<AppOnBeforeCommandLineProcessingEvent> DECODER = payload -> {
        ByteBuffer __buf = payload.duplicate();
        __buf.order(ByteOrder.LITTLE_ENDIAN);
        int processTypeLen = __buf.getInt();
        byte[] processTypeBuf = new byte[processTypeLen];
        __buf.get(processTypeBuf);
        String processType = new String(processTypeBuf, StandardCharsets.UTF_8);
        RemoteHandle commandLine = new RemoteHandle(__buf.getInt());
        return new AppOnBeforeCommandLineProcessingEvent(processType, commandLine);
    };
}
