// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
package net.kurobako.cef4j.ipc.protocol.gen;

import java.util.concurrent.CompletableFuture;
import javax.annotation.Nonnull;
import net.kurobako.cef4j.ipc.session.CefSession;
import net.kurobako.cef4j.ipc.session.RemoteHandle;

/**
 * Typed facade for {@code cef_run_quick_menu_callback_t}, dispatching through a {@link CefSession}.
 * Each instance is a thin wrapper around a {@link RemoteHandle} that points at a helper-side
 * ref-counted CEF object.
 */
public final class RunQuickMenuCallback {

    private final CefSession session;
    private final RemoteHandle handle;

    public RunQuickMenuCallback(@Nonnull CefSession session, @Nonnull RemoteHandle handle) {
        this.session = session;
        this.handle = handle;
    }

    @Nonnull
    public RemoteHandle handle() {
        return handle;
    }


    /** Releases the helper-side handle this facade points at. Subsequent method calls on this instance
      * will fail with an empty / null-receiver response. */
    public java.util.concurrent.CompletableFuture<Void> releaseHandle() {
        return session
            .request(new ReleaseHandleRequest(handle, "cef_run_quick_menu_callback_t"), ReleaseHandleResponse.DECODER)
            .thenApply(r -> null);
    }

    public CompletableFuture<Void> cont(int commandId, int eventFlags) {
        return session
            .request(new RunQuickMenuCallbackContRequest(handle, commandId, eventFlags), RunQuickMenuCallbackContResponse.DECODER)
            .thenApply(r -> null);
    }

    public CompletableFuture<Void> cancel() {
        return session
            .request(new RunQuickMenuCallbackCancelRequest(handle), RunQuickMenuCallbackCancelResponse.DECODER)
            .thenApply(r -> null);
    }
}
