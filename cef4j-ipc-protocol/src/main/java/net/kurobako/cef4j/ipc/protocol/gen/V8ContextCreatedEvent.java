// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
package net.kurobako.cef4j.ipc.protocol.gen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.nio.charset.StandardCharsets;
import javax.annotation.Nonnull;
import net.kurobako.cef4j.ipc.session.CefMessageDecoder;
import net.kurobako.cef4j.ipc.session.CefMessageEncoder;
import net.kurobako.cef4j.ipc.session.CefMessageView;
import net.kurobako.cef4j.ipc.session.RemoteHandle;

public final class V8ContextCreatedEvent implements CefMessageView, CefMessageEncoder {

    public static final int MESSAGE_ID = 10;

    private final RemoteHandle browser;
    private final String frameUrl;
    private final byte[] frameUrlBytes;

    public V8ContextCreatedEvent(@Nonnull RemoteHandle browser, @Nonnull String frameUrl) {
        this.browser = browser;
        this.frameUrl = frameUrl;
        this.frameUrlBytes = frameUrl.getBytes(StandardCharsets.UTF_8);
    }

    @Nonnull
    public RemoteHandle browser() {
        return browser;
    }

    @Nonnull
    public String frameUrl() {
        return frameUrl;
    }

    @Override
    public int messageId() {
        return MESSAGE_ID;
    }

    @Override
    public int encodedSize() {
        return 8 + frameUrlBytes.length;
    }

    @Override
    public void encodeInto(@Nonnull ByteBuffer __dst) {
        ByteOrder originalOrder = __dst.order();
        try {
            __dst.order(ByteOrder.LITTLE_ENDIAN);
            __dst.putInt(browser.id());
            __dst.putInt(frameUrlBytes.length);
            __dst.put(frameUrlBytes);
        } finally {
            __dst.order(originalOrder);
        }
    }

    public static final CefMessageDecoder<V8ContextCreatedEvent> DECODER = payload -> {
        ByteBuffer __buf = payload.duplicate();
        __buf.order(ByteOrder.LITTLE_ENDIAN);
        RemoteHandle browser = new RemoteHandle(__buf.getInt());
        int frameUrlLen = __buf.getInt();
        byte[] frameUrlBuf = new byte[frameUrlLen];
        __buf.get(frameUrlBuf);
        String frameUrl = new String(frameUrlBuf, StandardCharsets.UTF_8);
        return new V8ContextCreatedEvent(browser, frameUrl);
    };
}
