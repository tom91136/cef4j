// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
package net.kurobako.cef4j.ipc.protocol.gen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.nio.charset.StandardCharsets;
import javax.annotation.Nonnull;
import net.kurobako.cef4j.ipc.session.CefMessageDecoder;
import net.kurobako.cef4j.ipc.session.CefMessageEncoder;
import net.kurobako.cef4j.ipc.session.CefMessageView;
import net.kurobako.cef4j.ipc.session.RemoteHandle;

public final class CookieManagerDeleteCookiesRequest implements CefMessageView, CefMessageEncoder {

    public static final int MESSAGE_ID = 927990966;

    private final RemoteHandle self;
    private final String url;
    private final byte[] urlBytes;
    private final String cookieName;
    private final byte[] cookieNameBytes;
    private final RemoteHandle callback;

    public CookieManagerDeleteCookiesRequest(@Nonnull RemoteHandle self, @Nonnull String url, @Nonnull String cookieName, @Nonnull RemoteHandle callback) {
        this.self = self;
        this.url = url;
        this.urlBytes = url.getBytes(StandardCharsets.UTF_8);
        this.cookieName = cookieName;
        this.cookieNameBytes = cookieName.getBytes(StandardCharsets.UTF_8);
        this.callback = callback;
    }

    @Nonnull
    public RemoteHandle self() {
        return self;
    }

    @Nonnull
    public String url() {
        return url;
    }

    @Nonnull
    public String cookieName() {
        return cookieName;
    }

    @Nonnull
    public RemoteHandle callback() {
        return callback;
    }

    @Override
    public int messageId() {
        return MESSAGE_ID;
    }

    @Override
    public int encodedSize() {
        return 16 + urlBytes.length + cookieNameBytes.length;
    }

    @Override
    public void encodeInto(@Nonnull ByteBuffer __dst) {
        ByteOrder originalOrder = __dst.order();
        try {
            __dst.order(ByteOrder.LITTLE_ENDIAN);
            __dst.putInt(self.id());
            __dst.putInt(urlBytes.length);
            __dst.put(urlBytes);
            __dst.putInt(cookieNameBytes.length);
            __dst.put(cookieNameBytes);
            __dst.putInt(callback.id());
        } finally {
            __dst.order(originalOrder);
        }
    }

    public static final CefMessageDecoder<CookieManagerDeleteCookiesRequest> DECODER = payload -> {
        ByteBuffer __buf = payload.duplicate();
        __buf.order(ByteOrder.LITTLE_ENDIAN);
        RemoteHandle self = new RemoteHandle(__buf.getInt());
        int urlLen = __buf.getInt();
        byte[] urlBuf = new byte[urlLen];
        __buf.get(urlBuf);
        String url = new String(urlBuf, StandardCharsets.UTF_8);
        int cookieNameLen = __buf.getInt();
        byte[] cookieNameBuf = new byte[cookieNameLen];
        __buf.get(cookieNameBuf);
        String cookieName = new String(cookieNameBuf, StandardCharsets.UTF_8);
        RemoteHandle callback = new RemoteHandle(__buf.getInt());
        return new CookieManagerDeleteCookiesRequest(self, url, cookieName, callback);
    };
}
