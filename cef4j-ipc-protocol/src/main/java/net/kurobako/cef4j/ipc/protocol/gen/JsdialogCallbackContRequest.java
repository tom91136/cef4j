// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
package net.kurobako.cef4j.ipc.protocol.gen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.nio.charset.StandardCharsets;
import javax.annotation.Nonnull;
import net.kurobako.cef4j.ipc.session.CefMessageDecoder;
import net.kurobako.cef4j.ipc.session.CefMessageEncoder;
import net.kurobako.cef4j.ipc.session.CefMessageView;
import net.kurobako.cef4j.ipc.session.RemoteHandle;

public final class JsdialogCallbackContRequest implements CefMessageView, CefMessageEncoder {

    public static final int MESSAGE_ID = 369730572;

    private final RemoteHandle self;
    private final int success;
    private final String userInput;
    private final byte[] userInputBytes;

    public JsdialogCallbackContRequest(@Nonnull RemoteHandle self, int success, @Nonnull String userInput) {
        this.self = self;
        this.success = success;
        this.userInput = userInput;
        this.userInputBytes = userInput.getBytes(StandardCharsets.UTF_8);
    }

    @Nonnull
    public RemoteHandle self() {
        return self;
    }

    public int success() {
        return success;
    }

    @Nonnull
    public String userInput() {
        return userInput;
    }

    @Override
    public int messageId() {
        return MESSAGE_ID;
    }

    @Override
    public int encodedSize() {
        return 12 + userInputBytes.length;
    }

    @Override
    public void encodeInto(@Nonnull ByteBuffer __dst) {
        ByteOrder originalOrder = __dst.order();
        try {
            __dst.order(ByteOrder.LITTLE_ENDIAN);
            __dst.putInt(self.id());
            __dst.putInt(success);
            __dst.putInt(userInputBytes.length);
            __dst.put(userInputBytes);
        } finally {
            __dst.order(originalOrder);
        }
    }

    public static final CefMessageDecoder<JsdialogCallbackContRequest> DECODER = payload -> {
        ByteBuffer __buf = payload.duplicate();
        __buf.order(ByteOrder.LITTLE_ENDIAN);
        RemoteHandle self = new RemoteHandle(__buf.getInt());
        int success = __buf.getInt();
        int userInputLen = __buf.getInt();
        byte[] userInputBuf = new byte[userInputLen];
        __buf.get(userInputBuf);
        String userInput = new String(userInputBuf, StandardCharsets.UTF_8);
        return new JsdialogCallbackContRequest(self, success, userInput);
    };
}
