// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
package net.kurobako.cef4j.ipc.protocol.gen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.nio.charset.StandardCharsets;
import javax.annotation.Nonnull;
import net.kurobako.cef4j.ipc.session.CefMessageDecoder;
import net.kurobako.cef4j.ipc.session.CefMessageEncoder;
import net.kurobako.cef4j.ipc.session.CefMessageView;
import net.kurobako.cef4j.ipc.session.RemoteHandle;

public final class AuthCallbackContRequest implements CefMessageView, CefMessageEncoder {

    public static final int MESSAGE_ID = 608486756;

    private final RemoteHandle self;
    private final String username;
    private final byte[] usernameBytes;
    private final String password;
    private final byte[] passwordBytes;

    public AuthCallbackContRequest(@Nonnull RemoteHandle self, @Nonnull String username, @Nonnull String password) {
        this.self = self;
        this.username = username;
        this.usernameBytes = username.getBytes(StandardCharsets.UTF_8);
        this.password = password;
        this.passwordBytes = password.getBytes(StandardCharsets.UTF_8);
    }

    @Nonnull
    public RemoteHandle self() {
        return self;
    }

    @Nonnull
    public String username() {
        return username;
    }

    @Nonnull
    public String password() {
        return password;
    }

    @Override
    public int messageId() {
        return MESSAGE_ID;
    }

    @Override
    public int encodedSize() {
        return 12 + usernameBytes.length + passwordBytes.length;
    }

    @Override
    public void encodeInto(@Nonnull ByteBuffer __dst) {
        ByteOrder originalOrder = __dst.order();
        try {
            __dst.order(ByteOrder.LITTLE_ENDIAN);
            __dst.putInt(self.id());
            __dst.putInt(usernameBytes.length);
            __dst.put(usernameBytes);
            __dst.putInt(passwordBytes.length);
            __dst.put(passwordBytes);
        } finally {
            __dst.order(originalOrder);
        }
    }

    public static final CefMessageDecoder<AuthCallbackContRequest> DECODER = payload -> {
        ByteBuffer __buf = payload.duplicate();
        __buf.order(ByteOrder.LITTLE_ENDIAN);
        RemoteHandle self = new RemoteHandle(__buf.getInt());
        int usernameLen = __buf.getInt();
        byte[] usernameBuf = new byte[usernameLen];
        __buf.get(usernameBuf);
        String username = new String(usernameBuf, StandardCharsets.UTF_8);
        int passwordLen = __buf.getInt();
        byte[] passwordBuf = new byte[passwordLen];
        __buf.get(passwordBuf);
        String password = new String(passwordBuf, StandardCharsets.UTF_8);
        return new AuthCallbackContRequest(self, username, password);
    };
}
