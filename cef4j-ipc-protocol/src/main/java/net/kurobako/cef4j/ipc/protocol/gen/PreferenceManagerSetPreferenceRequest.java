// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
package net.kurobako.cef4j.ipc.protocol.gen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.nio.charset.StandardCharsets;
import javax.annotation.Nonnull;
import net.kurobako.cef4j.ipc.session.CefMessageDecoder;
import net.kurobako.cef4j.ipc.session.CefMessageEncoder;
import net.kurobako.cef4j.ipc.session.CefMessageView;
import net.kurobako.cef4j.ipc.session.RemoteHandle;

public final class PreferenceManagerSetPreferenceRequest implements CefMessageView, CefMessageEncoder {

    public static final int MESSAGE_ID = 997745305;

    private final RemoteHandle self;
    private final String name;
    private final byte[] nameBytes;
    private final RemoteHandle value;
    private final String error;
    private final byte[] errorBytes;

    public PreferenceManagerSetPreferenceRequest(@Nonnull RemoteHandle self, @Nonnull String name, @Nonnull RemoteHandle value, @Nonnull String error) {
        this.self = self;
        this.name = name;
        this.nameBytes = name.getBytes(StandardCharsets.UTF_8);
        this.value = value;
        this.error = error;
        this.errorBytes = error.getBytes(StandardCharsets.UTF_8);
    }

    @Nonnull
    public RemoteHandle self() {
        return self;
    }

    @Nonnull
    public String name() {
        return name;
    }

    @Nonnull
    public RemoteHandle value() {
        return value;
    }

    @Nonnull
    public String error() {
        return error;
    }

    @Override
    public int messageId() {
        return MESSAGE_ID;
    }

    @Override
    public int encodedSize() {
        return 16 + nameBytes.length + errorBytes.length;
    }

    @Override
    public void encodeInto(@Nonnull ByteBuffer __dst) {
        ByteOrder originalOrder = __dst.order();
        try {
            __dst.order(ByteOrder.LITTLE_ENDIAN);
            __dst.putInt(self.id());
            __dst.putInt(nameBytes.length);
            __dst.put(nameBytes);
            __dst.putInt(value.id());
            __dst.putInt(errorBytes.length);
            __dst.put(errorBytes);
        } finally {
            __dst.order(originalOrder);
        }
    }

    public static final CefMessageDecoder<PreferenceManagerSetPreferenceRequest> DECODER = payload -> {
        ByteBuffer __buf = payload.duplicate();
        __buf.order(ByteOrder.LITTLE_ENDIAN);
        RemoteHandle self = new RemoteHandle(__buf.getInt());
        int nameLen = __buf.getInt();
        byte[] nameBuf = new byte[nameLen];
        __buf.get(nameBuf);
        String name = new String(nameBuf, StandardCharsets.UTF_8);
        RemoteHandle value = new RemoteHandle(__buf.getInt());
        int errorLen = __buf.getInt();
        byte[] errorBuf = new byte[errorLen];
        __buf.get(errorBuf);
        String error = new String(errorBuf, StandardCharsets.UTF_8);
        return new PreferenceManagerSetPreferenceRequest(self, name, value, error);
    };
}
