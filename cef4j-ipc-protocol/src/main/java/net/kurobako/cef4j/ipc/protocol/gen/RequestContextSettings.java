// Generated by net.kurobako.cef4j.codegen.ipc — do not edit by hand.
package net.kurobako.cef4j.ipc.protocol.gen;

import java.nio.ByteBuffer;
import java.nio.ByteOrder;
import java.nio.charset.StandardCharsets;
import javax.annotation.Nonnull;

/**
 * Wire-format value type mirroring {@code cef_request_context_settings_t}. Fields are immutable; pass instances
 * to facade methods (or factory requests) that take this struct. The dispatcher decodes them on the
 * helper side and fills the matching {@code cef_request_context_settings_t} before invoking CEF.
 */
public final class RequestContextSettings {

    private final long size;
    private final String cachePath;
    private final int persistSessionCookies;
    private final String acceptLanguageList;
    private final String cookieableSchemesList;
    private final int cookieableSchemesExcludeDefaults;

    public RequestContextSettings(long size, @Nonnull String cachePath, int persistSessionCookies, @Nonnull String acceptLanguageList, @Nonnull String cookieableSchemesList, int cookieableSchemesExcludeDefaults) {
        this.size = size;
        this.cachePath = cachePath;
        this.persistSessionCookies = persistSessionCookies;
        this.acceptLanguageList = acceptLanguageList;
        this.cookieableSchemesList = cookieableSchemesList;
        this.cookieableSchemesExcludeDefaults = cookieableSchemesExcludeDefaults;
    }

    public long size() {
        return size;
    }

    @Nonnull
    public String cachePath() {
        return cachePath;
    }

    public int persistSessionCookies() {
        return persistSessionCookies;
    }

    @Nonnull
    public String acceptLanguageList() {
        return acceptLanguageList;
    }

    @Nonnull
    public String cookieableSchemesList() {
        return cookieableSchemesList;
    }

    public int cookieableSchemesExcludeDefaults() {
        return cookieableSchemesExcludeDefaults;
    }

    public int encodedSize() {
        return 28 + cachePath.getBytes(StandardCharsets.UTF_8).length + acceptLanguageList.getBytes(StandardCharsets.UTF_8).length + cookieableSchemesList.getBytes(StandardCharsets.UTF_8).length;
    }

    public void encodeInto(@Nonnull ByteBuffer dst) {
        ByteOrder originalOrder = dst.order();
        try {
            dst.order(ByteOrder.LITTLE_ENDIAN);
            dst.putLong(size);
            byte[] cachePathBytes = cachePath.getBytes(StandardCharsets.UTF_8);
            dst.putInt(cachePathBytes.length);
            dst.put(cachePathBytes);
            dst.putInt(persistSessionCookies);
            byte[] acceptLanguageListBytes = acceptLanguageList.getBytes(StandardCharsets.UTF_8);
            dst.putInt(acceptLanguageListBytes.length);
            dst.put(acceptLanguageListBytes);
            byte[] cookieableSchemesListBytes = cookieableSchemesList.getBytes(StandardCharsets.UTF_8);
            dst.putInt(cookieableSchemesListBytes.length);
            dst.put(cookieableSchemesListBytes);
            dst.putInt(cookieableSchemesExcludeDefaults);
        } finally {
            dst.order(originalOrder);
        }
    }

    /** Reads in place from {@code __buf}, advancing its position. Caller must have set
      * {@link ByteOrder#LITTLE_ENDIAN} on the buffer (the standalone {@code decode(payload)} entry point
      * below does that for you). For nested use inside a parent message's decoder the parent has already
      * configured the buffer.
      */
    public static RequestContextSettings decode(@Nonnull ByteBuffer __buf) {
        long size = __buf.getLong();
        int cachePathLen = __buf.getInt();
        byte[] cachePathBuf = new byte[cachePathLen];
        __buf.get(cachePathBuf);
        String cachePath = new String(cachePathBuf, StandardCharsets.UTF_8);
        int persistSessionCookies = __buf.getInt();
        int acceptLanguageListLen = __buf.getInt();
        byte[] acceptLanguageListBuf = new byte[acceptLanguageListLen];
        __buf.get(acceptLanguageListBuf);
        String acceptLanguageList = new String(acceptLanguageListBuf, StandardCharsets.UTF_8);
        int cookieableSchemesListLen = __buf.getInt();
        byte[] cookieableSchemesListBuf = new byte[cookieableSchemesListLen];
        __buf.get(cookieableSchemesListBuf);
        String cookieableSchemesList = new String(cookieableSchemesListBuf, StandardCharsets.UTF_8);
        int cookieableSchemesExcludeDefaults = __buf.getInt();
        return new RequestContextSettings(size, cachePath, persistSessionCookies, acceptLanguageList, cookieableSchemesList, cookieableSchemesExcludeDefaults);
    }

    public static Builder builder() {
        return new Builder();
    }

    /** Lombok-style builder. */
    public static final class Builder {

        private long size = 0L;
        private String cachePath = "";
        private int persistSessionCookies = 0;
        private String acceptLanguageList = "";
        private String cookieableSchemesList = "";
        private int cookieableSchemesExcludeDefaults = 0;

        public Builder size(long size) {
            this.size = size;
            return this;
        }

        public Builder cachePath(@Nonnull String cachePath) {
            this.cachePath = cachePath;
            return this;
        }

        public Builder persistSessionCookies(int persistSessionCookies) {
            this.persistSessionCookies = persistSessionCookies;
            return this;
        }

        public Builder acceptLanguageList(@Nonnull String acceptLanguageList) {
            this.acceptLanguageList = acceptLanguageList;
            return this;
        }

        public Builder cookieableSchemesList(@Nonnull String cookieableSchemesList) {
            this.cookieableSchemesList = cookieableSchemesList;
            return this;
        }

        public Builder cookieableSchemesExcludeDefaults(int cookieableSchemesExcludeDefaults) {
            this.cookieableSchemesExcludeDefaults = cookieableSchemesExcludeDefaults;
            return this;
        }

        public RequestContextSettings build() {
            return new RequestContextSettings(size, cachePath, persistSessionCookies, acceptLanguageList, cookieableSchemesList, cookieableSchemesExcludeDefaults);
        }
    }
}
